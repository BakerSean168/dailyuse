---
tags:
  - audit
  - w4
  - goal
  - task
  - transaction
description: W4 Goal/Task 边界与持久化原子性第一轮实施审查
created: 2026-08-11T00:00:00Z
---

# W4 第一轮审查：Goal/Task 边界与持久化原子性

本次审查对照 [`2026-08-09-business-p0-elegant-fix-plan.md`](../plan/active/2026-08-09-business-p0-elegant-fix-plan.md) W4，复核当前工作区未提交实现。审查只新增本文档；未修改业务代码，未 commit/push。

## 终审结论

**W4 第一轮不可放行：不要提交 W4，不进入 W5。**

当前没有发现 P0。Goal completed/archived 的重复调用已经做到不推进 version、不调用 repository save；Task create 已强制经过 transaction runner，Prisma/PowerSync runner 也把模板、实例和 eligible Goal outbox 放在同一事务回调内，生产 module 缺 runner 会 fail-fast。相关单测、集成测试、typecheck、build 和 governance 均通过。

但核心跨模块边界没有按 W0/W4 契约闭合：Goal 的 canonical Prisma/PowerSync factory 仍在 Goal 包内直接实例化查询 `taskTemplate/task_templates` 的 adapter，Task 包新增 adapter 没有被任何组合根消费；本轮还绕开 W0 已冻结的 `GoalDependencyReadPort`，另建了一套签名不同且不执行输入 schema 校验的 port。依赖检查也没有把 task binding count 作为结构化字段返回。终态测试只证明 aggregate 不再保存，未实现或验证 W0 `GoalReliableOperationPort` 的 durable completion receipt，因此“receipt 不再次写入”没有持久化证据。最后，`task:lint` 因遗留 unused inline-runner import 实际失败。

## 分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现立即数据破坏或启动中断。 |
| P1 | 4 | 跨模块 read-port/组合根边界错误；binding count 未结构化返回；durable receipt 幂等未落地；Task lint 门禁失败。 |
| P2 | 2 | 真实 adapter 的跨 identity 测试缺失；outbox 写失败的事务故障注入缺失。 |

## Findings

### P1-1：Goal 仍直接拥有 Task ORM/SQL adapter，Task read port 是未接线的重复实现

W0 已冻结 `GoalDependencyReadPort.checkActiveTaskBindings({ identityId, goalId })`，输出包含 `hasActiveBindings/activeCount`（[`ports.ts:175`](../../packages/contracts/src/modules/reliable-messaging/ports.ts:175)）。本轮却在 Goal application 内另建 `ITaskBindingReadPort.findActiveBindingsForGoal(identityId, goalId)`（[`task-binding-read-port.ts:6`](../../packages/goal/src/server/application/ports/task-binding-read-port.ts:6)），既没有复用 frozen contract，也没有在边界执行 `GoalTaskBindingQueryInputSchema`。

更关键的是，Goal Prisma factory 直接构造 Goal 包内的 `PrismaTaskBindingReadPort`（[`prisma.ts:27`](../../packages/goal/src/server/infrastructure/prisma.ts:27)、[`prisma.ts:39`](../../packages/goal/src/server/infrastructure/prisma.ts:39)），该 adapter 直接访问 `db.taskTemplate.count`（[`prisma-task-binding-read-port.ts:4`](../../packages/goal/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.ts:4)）。PowerSync factory 同样构造 Goal 包内 adapter（[`powersync.ts:27`](../../packages/goal/src/server/infrastructure/powersync.ts:27)、[`powersync.ts:39`](../../packages/goal/src/server/infrastructure/powersync.ts:39)），后者直接查询 `task_templates`。这仍是 Goal infrastructure 知道 Task persistence model，并不满足“不让 Goal 直接依赖 Task ORM”。

Task 包新增的 Prisma adapter 同时暴露两套方法且没有显式 `implements GoalDependencyReadPort`（[`prisma-task-binding-read-port.ts:3`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.ts:3)）；全仓调用点搜索显示它和 PowerSync 对应实现只被 index export，没有生产消费者。结果是双轨重复 adapter，而不是 Task-owned read capability 被 host composition root 注入 Goal。

**要求**：保留一套 W0 frozen port；由 Task 提供明确实现并在 API/Desktop 的外层组合根注入 Goal，Goal 包删除所有 `taskTemplate/task_templates` ORM/SQL 知识。生产 factory 必须对缺失 capability fail-fast，不能在 Goal 内重新构造跨域 adapter。

### P1-2：删除依赖检查未结构化返回 task binding count

`checkDependencies()` 内部取得 `taskBindingCount`，但返回类型只有 `hasTaskLinks/canDelete/warnings`，没有 `taskBindingCount`（[`delete-goal.use-case.ts:33`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:33)、[`delete-goal.use-case.ts:76`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:76)）。计数只存在于中文 warning 字符串；execute conflict 也只把计数拼进英文 message（[`delete-goal.use-case.ts:105`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:105)）。调用方若要显示数量、埋点或做策略判断，只能解析文案。

删除阻断本身是 fail-closed 的：查询异常不会继续删除，count 大于 0 返回 `CONFLICT`，count 为 0 才执行带 expectedVersion 的 soft delete。Prisma 和 PowerSync 查询也都带 `identityId + goalId + deletedAt is null`。但 W4 的“返回计数/警告”要求尚未形成稳定结构化 contract。

**要求**：在正式 application/transport contract 中返回明确的 `taskBindingCount`（或直接复用 W0 `activeCount`），warning 仅用于展示；补 controller/client contract 断言。

### P1-3：只证明了 aggregate 幂等，未实现 durable completion receipt 幂等

Complete/Archive 在检查 expectedVersion 前识别终态并直接 materialize `GoalMutationReceipt`（[`complete-goal.use-case.ts:36`](../../packages/goal/src/server/application/use-cases/commands/complete-goal.use-case.ts:36)、[`archive-goal.use-case.ts:36`](../../packages/goal/src/server/application/use-cases/commands/archive-goal.use-case.ts:36)）。对应测试证明重复 complete 不增加 version/event 且不调用 save（[`complete-goal.test.ts:76`](../../packages/goal/src/server/application/use-cases/commands/__tests__/complete-goal.test.ts:76)），archive 测试证明不增加 version 且不调用 save（[`archive-goal.test.ts:60`](../../packages/goal/src/server/application/use-cases/commands/__tests__/archive-goal.test.ts:60)、[`archive-goal.test.ts:87`](../../packages/goal/src/server/application/use-cases/commands/__tests__/archive-goal.test.ts:87)）。这一部分成立。

但 W0 已冻结的 `GoalReliableOperationPort.recordGoalCompletionReceipt()`（[`ports.ts:200`](../../packages/contracts/src/modules/reliable-messaging/ports.ts:200)）在 Goal/Task/apps 中没有实现、注入或调用；当前 `GoalMutationReceipt` 只是每次从 aggregate 重新构造的返回 DTO。因而没有 durable receipt write，也无法验证首次终态提交与 receipt 的原子关系、重复请求不再次写 receipt、进程重启后返回同一 operation receipt。

**要求**：明确 W4 所称 receipt 的 canonical 类型。若按计划/W0 是 `BusinessOperationReceipt`，应接入 `GoalReliableOperationPort`，以 identity + source + occurrence 的 canonical idempotency key 持久化，并补首次写、重复不写、重启重放和失败回滚测试；不能用“本来没有 receipt write”宣称“receipt 不再次写入”。

### P1-4：Task lint 门禁实际失败

`CreateTaskTemplateUseCase` 已不再使用 inline fallback，但 import 仍保留 `createInlineTaskWriteTransactionRunner`（[`create-task-template.use-case.ts:19`](../../packages/task/src/server/application/use-cases/commands/create-task-template.use-case.ts:19)）。本轮实跑 `task:lint` 报 `@typescript-eslint/no-unused-vars` error，导致 goal/task 的 `test,typecheck,build,lint` run-many 总体失败。

这不改变“生产 create 强制 runner”的行为判断，但发布门禁不绿，不能提交放行。

### P2-1：跨 identity 只测了 mock，没有验证真实 adapter

DeleteGoal unit test 通过 mock 根据传入 identity 返回不同 count，证明 use case 会转发 identity；没有 Prisma/PowerSync read adapter 测试写入两个 identity 的同 goalId/binding 后验证隔离，也没有空 identity/schema 负向测试。由于本轮恰好新增跨域 SQL/ORM adapter，这属于 W4 明列验证项的证据缺口。

### P2-2：outbox 同事务的代码结构成立，但缺 outbox 故障注入

Prisma runner 在同一 `$transaction` callback 中创建事务绑定 repositories，并在 callback 返回前 append eligible `taskGoalOutbox`（[`prisma-task-write-transaction-runner.ts:25`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-write-transaction-runner.ts:25)、[`prisma-task-write-transaction-runner.ts:49`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-write-transaction-runner.ts:49)）；PowerSync runner 对应逻辑也位于同一 `writeTransaction` callback（[`powersync-task-write-transaction-runner.ts:25`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.ts:25)、[`powersync-task-write-transaction-runner.ts:32`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.ts:32)）。Create use case 的 template save 与 instance saveMany 均在 runner work 内（[`create-task-template.use-case.ts:51`](../../packages/task/src/server/application/use-cases/commands/create-task-template.use-case.ts:51)、[`create-task-template.use-case.ts:112`](../../packages/task/src/server/application/use-cases/commands/create-task-template.use-case.ts:112)）。静态上同 commit 成立。

现有 Prisma/PowerSync 测试注入的是 `saveMany` 失败，能证明 template/instances 回滚且 event 不发布（[`prisma-task-write-transaction-runner.integration.test.ts:119`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-write-transaction-runner.integration.test.ts:119)、[`powersync-task-write-transaction-runner.test.ts:205`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:205)）；没有注入 outbox append/SQL 失败并断言 template、instances、outbox 全部回滚，也没有成功路径断言三者同 commit。应补这一故障矩阵以锁定 W4 不变量。

## 已满足项

- Goal/Task module dependency 都将 write transaction runner 设为必填，并在 runtime 对 falsy dependency 明确抛错（[`goal.module.ts:204`](../../packages/goal/src/server/infrastructure/goal.module.ts:204)、[`task.module.ts:201`](../../packages/task/src/server/infrastructure/task.module.ts:201)）；module 内没有恢复 inline fallback。
- `CreateTaskTemplateUseCase` constructor 对缺失 runner fail-fast，所有模板与首批实例写均使用 runner 提供的 transaction-bound repositories。
- Goal terminal path 位于 expectedVersion 校验之前，允许携带旧 expectedVersion 的真实重试返回当前终态 read model；重复 complete 不增加 version/event，重复 archive 不增加 version，二者均不 save aggregate。
- 删除执行路径查询 binding count 大于 0 时返回 conflict，且不 soft-delete、不 save；查询带 identityId。
- Prisma/PowerSync transaction runner 均在 commit 后才 flush 非 outbox events；事务失败不发布 buffered events。

## W0--W3 回归判断

- **W0：有回退。** W0 frozen `GoalDependencyReadPort` 被旁路，新增了签名不同的 Goal-local port；W0 `GoalReliableOperationPort` 也没有进入完成/归档路径。因此不能宣称 W4 复用了 W0 transaction/identity/receipt port。
- **W1--W3：未发现直接代码回退。** 当前工作树业务变更只在 Goal/Task；Reminder、Notification、Account/Cloud Auth 文件未改。governance 全量通过。但本次没有重跑 W1--W3 各自全套产品/故障矩阵，故结论限于变更范围和治理门禁，不扩张为端到端再认证。

## 验证记录

当前环境没有 `pnpm` 可执行文件，使用仓库本地 Nx CLI 等价执行并统一加 `--skip-nx-cache`：

- `goal:test`：84 files，445/445 tests passed。
- `goal:test:integration`：2 files，3/3 tests passed，真实 PostgreSQL test database。
- `task:test`：68 files，784/784 tests passed。
- `task:test:integration`：5 files，26/26 tests passed，真实 PostgreSQL test database。
- `goal:typecheck`、`task:typecheck`：passed。
- `goal:build`、`task:build`：passed。
- `goal:lint`：passed（174 warnings，0 errors）。
- `task:lint`：**failed**（1 error，86 warnings）；error 为 create use case 的 unused inline-runner import。
- `memoflow:governance-check`：passed；inventory 972 files（unit 842、integration 17、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；package-internal boundary 649 files / 0 known violations。

绿测证明已断言路径，不覆盖 P1-1/P1-3 的生产边界与 durable receipt，也不替代 P2 的真实 adapter/outbox 故障注入。

## 放行前最低修复条件

1. 删除 Goal-local Task ORM/SQL adapter 和重复 port，复用 W0 `GoalDependencyReadPort`，由 Task capability 经 API/Desktop 外层组合根注入 Goal，并证明缺 capability fail-fast。
2. 将 binding count 纳入结构化 delete-dependency contract，补真实 Prisma/PowerSync 跨 identity 测试。
3. 明确并实现 canonical completion receipt：若为 W0 reliable receipt，完成首次写、重复不写、重启可重放及原子性验证。
4. 清除 unused inline-runner import，使 Task lint 通过。
5. 补 outbox append 失败的 Prisma/PowerSync 故障注入，断言模板、实例、outbox 和事件发布的一致性。

上述 P1 清零且近邻门禁全绿后，再进行 W4 复审；当前结论仍是 **不放行，不进入 W5**。

---

## 第二轮审查

本轮对照第一轮 4 个 P1 逐项复核当前工作区，并检查 API/Desktop 的真实注册入口、W0 frozen ports、Prisma receipt 集成测试与近邻门禁。审查只更新本文档；未修改业务代码，未 commit/push。

### 终审结论

**W4 第二轮仍不可放行：不要提交 W4，不进入 W5。**

第二轮修复关闭了原 P1-4，并完成了 P1-1 的大部分内部重构：Task-owned Prisma/PowerSync adapter 已显式实现 W0 `GoalDependencyReadPort`，输入经过 frozen schema 校验；Goal-local Task ORM/SQL adapter 已删除，Goal module 的两个 port 也改为必填且 fail-fast。Prisma lane 还新增了真实数据库集成测试，证明相同 canonical idempotency key 最终只有一条 outbox 记录。

但生产主路径存在新的启动阻断：API 与 Desktop 实际注册的 `GoalApiModule` / `GoalElectronModule` 都没有注入已经变成 fail-closed 的 Task capability，注册时必然抛错。原 P1-2 仍未修复，`taskBindingCount` 依旧只进入 warning。原 P1-3 也只完成了“成功后去重”的一半：Goal aggregate 保存与 Prisma receipt 写入是两个独立提交，PowerSync lane 使用进程内 Map，均不满足第一轮要求的 durable/atomic/restart receipt 闭环。

### 第二轮分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 1 | API/Desktop canonical Goal module 未注入必需 Task read capability，生产注册路径启动即抛错。 |
| P1 | 2 | binding count 仍未结构化返回；completion/archive receipt 未与 aggregate 原子提交且 PowerSync receipt 不 durable。 |
| P2 | 3 | factory 参数在类型上仍可选；真实 binding adapter 缺跨 identity/schema 测试；receipt/outbox 失败故障矩阵仍缺。 |

### P0-1：API/Desktop 主模块入口未接线，fail-closed factory 使应用无法启动

Prisma factory 在缺少 `options.taskBindingReadPort` 时立即抛出 `FAIL-CLOSED`（[`prisma.ts:27`](../../packages/goal/src/server/infrastructure/prisma.ts:27)）；PowerSync factory 同样经 `requirePort()` 抛错（[`powersync.ts:29`](../../packages/goal/src/server/infrastructure/powersync.ts:29)）。但 API 真正注册到 `apps/api/src/main.ts` 的 `GoalApiModule` 仍只传 `runtimeContributions`，没有构造或传入 `PrismaTaskBindingReadPort`（[`module.ts:42`](../../packages/goal/src/api/module.ts:42)、[`module.ts:49`](../../packages/goal/src/api/module.ts:49)）。Desktop 真正注册到 `apps/desktop/src/main/main.ts` 的 `GoalElectronModule` 也只传 runtime contribution，没有传 `PowerSyncTaskBindingReadPort`（[`index.ts:61`](../../packages/goal/src/electron/index.ts:61)、[`index.ts:68`](../../packages/goal/src/electron/index.ts:68)）。

本轮确实给三个 AI adapter 和一个 host-restart integration test 补了注入，但这些不是 API/Desktop 的 canonical Goal module 注册路径。由于 factory 的 `options` 与 `taskBindingReadPort` 在 TypeScript 签名上仍标成可选，API/Desktop typecheck 也不能在编译期发现漏接线，错误被推迟到启动期。

**要求**：在 API/Desktop 实际 composition root 构造 Task-owned adapter 并注入 Goal；新增覆盖 `GoalApiModule.register()` 与 `GoalElectronModule.register()` 的启动/注册测试。factory 类型签名应表达必填 capability，运行时 fail-fast 作为第二道保护。

### P1-1（原 P1-2）：binding count 仍没有进入结构化 contract

`checkDependencies()` 已调用 W0 `checkActiveTaskBindings({ identityId, goalId })` 并读取 `activeCount`，但公开返回类型仍只有 `hasTaskLinks/canDelete/warnings`，没有 `taskBindingCount` 或 `activeCount`（[`delete-goal.use-case.ts:33`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:33)、[`delete-goal.use-case.ts:76`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:76)）。新增测试也只断言 boolean 与中文 warning，没有断言结构化 count（[`delete-goal.test.ts:182`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:182)）。因此第一轮 P1-2 的最低条件没有完成。

此外测试文件还残留一个指向已删除 Goal-local port 的 type-only import（[`delete-goal.test.ts:7`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:7)）；当前 Vitest 因 type erase 仍通过，ESLint 将其报告为 warning。这不是本项阻断根因，但说明旧双轨清理并不完整。

**要求**：把 `taskBindingCount`（或 `activeCount`）加入正式 application/transport 返回契约，并补精确数值断言；warning 保持纯展示用途。

### P1-2（原 P1-3）：Prisma 只证明成功后去重，receipt 仍非原子；PowerSync 仍非 durable

正向进展成立：Complete/Archive 都强制注入 `GoalReliableOperationPort`，使用 canonical idempotency key；Prisma adapter 在 `reliable_outbox_messages.idempotency_key` 唯一约束上做 find/create/race refetch；新增集成测试真实驱动 Prisma，重复 complete 后以 `findMany` 和 raw SQL 双重断言一行，且本轮实跑 4/4 通过（[`goal-prisma.repository.integration.test.ts:211`](../../packages/goal/src/server/infrastructure/adapters/prisma/goal-prisma.repository.integration.test.ts:211)）。

但首次完成仍先独立调用 `saveRootWithExpectedVersion()`，成功后才调用 receipt port（[`complete-goal.use-case.ts:64`](../../packages/goal/src/server/application/use-cases/commands/complete-goal.use-case.ts:64)、[`complete-goal.use-case.ts:74`](../../packages/goal/src/server/application/use-cases/commands/complete-goal.use-case.ts:74)）；Archive 同构。若 receipt create 失败，请求会报错但 Goal 已进入终态，首次终态提交与 receipt 并非原子。现有集成测试只覆盖两个调用都成功，没有注入 receipt 写失败并断言回滚，也没有创建新 Prisma client/module 来证明重启重放返回同一 operation receipt。

PowerSync factory 更直接注入 `InMemoryGoalReliableOperationAdapter`（[`powersync.ts:47`](../../packages/goal/src/server/infrastructure/powersync.ts:47)）；其状态只是实例内 `Map`（[`in-memory-goal-reliable-operation.adapter.ts:10`](../../packages/goal/src/server/infrastructure/adapters/in-memory/in-memory-goal-reliable-operation.adapter.ts:10)），模块重建或进程重启即丢失，并会为相同 operation 生成新的 `operationId`。它是单进程幂等 fake，不是 durable receipt 实现。

**要求**：让 Goal 终态 CAS 保存与 Prisma receipt/outbox 在同一 transaction-bound operation 中提交；补 receipt 写失败回滚、重启后同 receipt/operationId、archive 对称路径测试。PowerSync lane 必须使用本地持久化实现并覆盖模块重建/进程重启语义，不能以进程内 Map 作为生产可靠性 adapter。

### 已关闭与部分关闭项

- **原 P1-1 部分关闭**：Goal application 已复用 W0 frozen `GoalDependencyReadPort`；Task 包的 Prisma/PowerSync adapter 显式 `implements` frozen port 并执行 `GoalTaskBindingQueryInputSchema.parse()`；Goal-local ORM/SQL adapter 与 local port 文件已删除。未关闭部分由 P0-1（真实 host 漏注入）和 P2（真实 adapter 验证缺口）承接。
- **原 P1-4 已关闭**：本轮实跑 `task:lint` 为 86 warnings、0 errors，退出码 0；`goal:lint` 为 176 warnings、0 errors，退出码 0。Task 的 unused inline-runner error 已消失。
- **Prisma receipt 去重证据成立但不足以关闭 P1-3**：真实 PostgreSQL 测试证明成功路径只有一行；它不证明 aggregate/receipt 原子性，也不覆盖 PowerSync durable/restart。

### W0--W3 回归判断

- **W0：仍有回退，且影响生产启动。** Frozen read-port 本身已复用，但 canonical API/Desktop host 未完成 capability 注入；`GoalReliableOperationPort` 的 Prisma 调用未进入 aggregate 同事务，PowerSync 又落到非 durable 内存实现。不能宣称 W0 transaction/receipt 约束已闭环。
- **W1--W3：未发现直接业务代码回退。** Reminder、Notification、Account 核心实现未在本轮变更范围内；但 API/Desktop Goal 注册阻断会使承载它们的整体应用不可用，因此系统级回归判定不能为绿。本轮不重新认证 W1--W3 各自完整故障矩阵。
- **W4 既有 Task 原子性 P2 仍在。** 第一轮指出的真实 adapter 跨 identity/schema 负向测试、Task outbox append 失败回滚矩阵均未补；第二轮新增 receipt 后又增加了 Goal receipt 写失败与 PowerSync restart 的故障矩阵缺口。

### 第二轮验证记录

当前环境仍没有 `pnpm`，使用仓库本地 Nx CLI 并加 `--skip-nx-cache`：

- `goal:test`：84 files，445/445 tests passed。
- `goal:test:integration`：2 files，4/4 tests passed；真实 PostgreSQL test database，包含新增 durable outbox 单行断言。
- `goal:lint`：passed（176 warnings，0 errors）。
- `task:lint`：passed（86 warnings，0 errors）。
- `memoflow:governance-check`：passed；inventory 972 files（unit 842、integration 17、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。
- API/Desktop typecheck 尝试未形成有效结论：仓库 target 内部调用 `pnpm` 而当前环境无该命令；并行依赖构建还触发一次 Prisma generate 产物竞争，故不把该次基础设施失败计为本轮业务 finding，也不采信为绿测证据。

绿测覆盖了 package 单测、Prisma receipt 成功去重和 lint，但没有调用 API/Desktop 的真实 module register 路径，也没有覆盖 receipt 写失败/重启，所以不能抵消上述 P0/P1。

### 第二轮放行前最低修复条件

1. 给 `GoalApiModule` / `GoalElectronModule` 的真实注册路径注入 Task-owned read adapter，factory 在类型与运行时都必填，并补两条启动接线测试。
2. 在 delete dependency 正式返回契约中加入精确 `taskBindingCount`/`activeCount`，补 contract 数值断言并清理旧 port 残留 import。
3. 将 Goal terminal CAS 与 Prisma durable receipt 纳入同一事务，补 receipt failure rollback、restart replay、archive 对称测试；PowerSync 改用持久化 adapter。
4. 补第一轮遗留的真实 Prisma/PowerSync binding identity/schema 测试，以及 Task outbox append 失败的全事务回滚矩阵。
5. 修复后重跑 goal/task test、integration、typecheck、build、lint、API/Desktop typecheck、governance 与 inventory，再进行第三轮审查。

在 P0/P1 清零且近邻门禁完整有效之前，结论保持：**W4 不可放行，不得提交，不进入 W5。**

---

## 第三轮审查

本轮对照第二轮的 P0/P1/P2 关闭条件，复核当前工作区的 API/Desktop 主组合根、Goal 事务 runner、Prisma/PowerSync receipt、delete dependency contract、Task use-case factory 类型及新增测试，并实跑近邻门禁。审查只追加本文档；未修改业务代码，未 commit/push。

### 终审结论

**W4 第三轮仍不可放行：不要提交 W4，不进入 W5。**

第二轮 P0 的生产接线已经修复：API/Desktop 都从 Task 包构造 canonical adapter 并注入 Goal，Goal factory 的 capability 在类型层为必填。Prisma 终态 CAS 与 receipt 也已进入同一个 `$transaction(tx)`，真实数据库故障注入证明 receipt 抛错会回滚 Goal；`taskBindingCount` 已进入结构化返回。

但本轮宣称的关闭证据与实际代码不符。`api:test` 由本轮 adapter 接线直接新增 3 个失败；PowerSync 所谓“重启持久化”测试仍以共享 `Map` 模拟数据库，且生产 adapter 在 INSERT 后查不到持久化行时会构造并返回未落盘的成功 receipt；明确要求的 API/Desktop `register()` 启动测试、`taskBindingCount` 数值断言和“use-case 层 inline fallback 全移除”也均未完成。第一/二轮遗留的真实 binding adapter 隔离测试和 Task outbox 故障矩阵仍不存在。因此 P1/P2 未清零，完整回归门禁也不是全绿。

### 第三轮分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | API/Desktop canonical host 已注入 Task-owned read capability，未发现新的启动必现阻断。 |
| P1 | 2 | 本轮接线导致 API 单测回归；PowerSync durable/restart receipt 仍可无落盘成功且没有真实持久化重启证据。 |
| P2 | 5 | 主模块 register 启动测试缺失；binding count 数值断言缺失；7 个 Task use case 仍有 inline fallback；真实 binding adapter 测试缺失；Task/PowerSync 故障矩阵未补。 |

### 已关闭：原 P0-1 主组合根接线

API 主入口从 `@memoflow/task` 构造 `PrismaTaskBindingReadPort` 并传给 `createGoalApiModule()`（[`main.ts:44`](../../apps/api/src/main.ts:44)、[`main.ts:232`](../../apps/api/src/main.ts:232)）；Desktop 对称构造 `PowerSyncTaskBindingReadPort` 并传给 `createGoalElectronModule()`（[`main.ts:25`](../../apps/desktop/src/main/main.ts:25)、[`main.ts:281`](../../apps/desktop/src/main/main.ts:281)）。两个 factory 都把必填 port 继续传给 Prisma/PowerSync Goal module（[`module.ts:42`](../../packages/goal/src/api/module.ts:42)、[`index.ts:61`](../../packages/goal/src/electron/index.ts:61)）。Goal 包内没有恢复 Task ORM/SQL adapter，governance 的 package-internal boundary audit 也为 0 violation。

因此第二轮的“生产注册时必然因缺 capability 抛错”已经消失，原 P0-1 的实现部分关闭。factory 参数在类型层也已必填，运行时底层 factory 仍有 fail-closed 防线。

### P1-1：本轮跨模块 adapter 接线使 `api:test` 直接回归

`BackendAutomationToolExecutorAdapter` 本轮新增 `new PrismaTaskBindingReadPort(db)`（[`backend-automation-tool-executor.adapter.ts:42`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.ts:42)），但对应测试完整 mock `@memoflow/task` 时只导出 `createTaskPrismaModule`，没有导出新类（[`backend-automation-tool-executor.adapter.test.ts:23`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.test.ts:23)）。本轮实跑 `api:test`，该文件 3/3 测试均以 `No "PrismaTaskBindingReadPort" export is defined on the "@memoflow/task" mock` 失败。

同一 target 另有 `bootstrap-module-names.surface.spec.ts` 失败：测试仍要求 `.register(AccountApiModule)`，而当前 W3 主入口使用 `createAccountApiModule(...)`。该项不是本轮 Goal diff 引入，但说明“全绿”和 W0--W3 完整回归均不能成立。总计 `api:test` 为 30 files 中 2 files failed、85 tests 中 4 tests failed。

**要求**：修正受 W4 接线影响的测试 mock，并使 API 全量 test target 通过；同时回收失真的 Account surface 断言，不能只运行 6 条 integration test 后宣称 API 绿。

### P1-2：PowerSync 使用了持久化表，但 durable/restart 闭环仍未被真实证明且存在 fail-open 返回

正向实现成立：PowerSync schema 新增 local-only `goal_operation_receipts` 表（[`index.ts:657`](../../packages/powersync-schema/src/index.ts:657)），PowerSync transaction runner 在同一个 `db.writeTransaction(tx)` 内同时提供 tx-bound Goal repository 与 receipt adapter（[`powersync-goal-write-transaction-runner.ts:21`](../../packages/goal/src/server/infrastructure/adapters/powersync/powersync-goal-write-transaction-runner.ts:21)）。生产 lane 已不再注入 `InMemoryGoalReliableOperationAdapter`。

但新增“restart / module reconstruction”测试的 `MockPowerSyncDatabase` 本身就是进程内 `Map`（[`powersync-goal-reliable-operation.adapter.test.ts:5`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:5)）；所谓 restart 只是用同一个 mock 对象创建第二个 adapter（[`powersync-goal-reliable-operation.adapter.test.ts:48`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:48)）。它没有创建、关闭并重新打开真实 SQLite/PowerSync database，无法证明 schema 表、磁盘落盘或进程重启语义。

生产 adapter 还有可靠性语义缺口：INSERT 后二次读取不到行时，不抛错，而是直接构造 `status: 'succeeded'` receipt 返回（[`powersync-goal-reliable-operation.adapter.ts:87`](../../packages/goal/src/server/infrastructure/adapters/powersync/powersync-goal-reliable-operation.adapter.ts:87)、[`powersync-goal-reliable-operation.adapter.ts:98`](../../packages/goal/src/server/infrastructure/adapters/powersync/powersync-goal-reliable-operation.adapter.ts:98)）。这允许调用方在没有任何 durable row 的情况下收到成功结果，与 W0 reliable receipt 的 fail-closed 语义相反。

**要求**：删除未落盘成功 fallback，写后无法读取 canonical row 必须失败并使外层 transaction 回滚；使用真实临时 SQLite/PowerSync database 覆盖建表、首次写、关闭/重开、相同 operationId、complete/archive 对称与 receipt 写失败全事务回滚。

### 已关闭：原 P1-1 `taskBindingCount` 结构化 contract 的实现部分

`checkDependencies()` 返回类型已包含 `taskBindingCount: number`，并把 frozen port 的 `activeCount` 原样返回（[`delete-goal.use-case.ts:33`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:33)、[`delete-goal.use-case.ts:60`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:60)、[`delete-goal.use-case.ts:77`](../../packages/goal/src/server/application/use-cases/commands/delete-goal.use-case.ts:77)）。`hasTaskLinks/canDelete` 也由数值计算，warning 仅承担展示用途。原 P1 的生产 contract 缺口关闭。

但声称存在的“数值断言”并不存在：无 binding 测试只断言 boolean/canDelete/warnings，有 binding 测试也只断言 boolean、canDelete 和中文 warning（[`delete-goal.test.ts:165`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:165)、[`delete-goal.test.ts:182`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:182)）；全仓只有 use case 实现出现 `taskBindingCount`。该明确验收项降级为 P2 测试缺口，仍须补 `expect(result.data.taskBindingCount).toBe(n)` 的 0 与非零精确断言，最好同时锁定 transport contract。

### 已关闭：Prisma 保存 + receipt 原子提交

`PrismaGoalWriteTransactionRunner.run()` 在单个 `prisma.$transaction(async tx => ...)` 内构造 tx-bound `GoalPrismaRepository` 和 `PrismaGoalReliableOperationAdapter`（[`prisma-goal-write-transaction-runner.ts:21`](../../packages/goal/src/server/infrastructure/adapters/prisma/prisma-goal-write-transaction-runner.ts:21)）。Complete/Archive 在同一个 runner callback 内先 CAS save、再 record receipt；callback 任一步抛错都不会 commit，事件仍在 commit 后 flush。

新增真实 PostgreSQL 集成测试证明 complete 重试只有一行、archive 对称重试只有一行，并通过替换 transaction context 的 receipt 写入函数注入失败，断言数据库 Goal 仍为 Active 且 `completedAt` 为 null（[`goal-prisma.repository.integration.test.ts:245`](../../packages/goal/src/server/infrastructure/adapters/prisma/goal-prisma.repository.integration.test.ts:245)、[`goal-prisma.repository.integration.test.ts:264`](../../packages/goal/src/server/infrastructure/adapters/prisma/goal-prisma.repository.integration.test.ts:264)）。本轮实跑 6/6 通过。第二轮关于 Prisma 双提交的 P1 已关闭。

### P2-1：第二轮要求的 API/Desktop `register()` 启动测试没有新增

全仓没有测试调用 `createGoalApiModule(...).register(context)` 或 `createGoalElectronModule(...).register(context)`。现有 `goal-electron.surface.spec.ts` 仍只是读取 `index.ts` 源码并检查 channel 字符串；API host restart integration test 直接调用 `createGoalPrismaModule()`，也没有走 `GoalApiModule.register()`。因此主入口静态接线虽然正确，但第二轮明确要求的两条启动/注册证据没有交付。

### P2-2：Task use-case 层仍残留 7 个 inline transaction fallback

Goal module、Task module、`CreateTaskTemplateUseCase` 和 `createGoalTaskProgressHandler` 已改为 runner 类型必填并 fail-fast，这部分成立。但“use-case 层 inline runner 默认值全移除”不成立：以下 7 个生产 use case 的 runner 仍为可选或带 `createInlineTaskWriteTransactionRunner(...)` 默认值：

- `UpdateTaskTemplateUseCase`（[`update-task-template.use-case.ts:68`](../../packages/task/src/server/application/use-cases/commands/update-task-template.use-case.ts:68)）
- `CompleteTaskInstanceUseCase`（[`complete-task-instance.use-case.ts:31`](../../packages/task/src/server/application/use-cases/commands/complete-task-instance.use-case.ts:31)）
- `UncompleteTaskInstanceUseCase`（[`uncomplete-task-instance.use-case.ts:21`](../../packages/task/src/server/application/use-cases/commands/uncomplete-task-instance.use-case.ts:21)）
- `GenerateTaskInstancesUseCase`（[`generate-task-instances.use-case.ts:28`](../../packages/task/src/server/application/use-cases/commands/generate-task-instances.use-case.ts:28)）
- `DeleteTaskTemplateUseCase`（[`delete-task-template.use-case.ts:28`](../../packages/task/src/server/application/use-cases/commands/delete-task-template.use-case.ts:28)）
- `ActivateTaskTemplateUseCase`（[`activate-task-template.use-case.ts:34`](../../packages/task/src/server/application/use-cases/commands/activate-task-template.use-case.ts:34)）
- `PauseTaskTemplateUseCase`（[`pause-task-template.use-case.ts:32`](../../packages/task/src/server/application/use-cases/commands/pause-task-template.use-case.ts:32)）

生产 module 当前会传入真实 runner，但直接构造这些 use case 时类型系统仍允许绕过事务/outbox 不变量。按本轮 P2 验收口径，必须全部改为类型必填；inline runner 仅可作为测试 double 显式传入，不能作为生产 constructor fallback。

### P2-3：第二轮其余 P2 故障矩阵仍未关闭

- Task-owned `PrismaTaskBindingReadPort` / `PowerSyncTaskBindingReadPort` 没有自己的真实 adapter 测试；现有 identity 测试仍只 mock frozen port，未验证两个 identity、同 goalId、deleted binding 与 schema 空输入。
- 第一轮要求的 Task outbox append/SQL 失败故障注入仍未新增，现有 Task transaction 测试未证明 outbox 写失败时 template、instances、outbox 和 buffered event 一并回滚。
- PowerSync Goal receipt 没有真实事务故障注入；Map adapter 测试只覆盖顺序重复调用，不覆盖 receipt SQL 失败时 Goal CAS 回滚。

这些均是前两轮已记录的放行条件，不能因新增成功路径测试而视为自动关闭。

### W0--W3 回归判断

- **W0：结构性回退已修复。** Goal 复用 frozen `GoalDependencyReadPort` / `GoalReliableOperationPort`，Task-owned adapter 由 host 注入；Prisma transaction/receipt 原子性成立。PowerSync 的 fail-open receipt 与假重启测试仍使 W0 durable receipt 不能最终认证。
- **W1--W2：未发现直接代码回退。** Reminder/Notification 核心文件不在本轮变更范围，governance 全量通过；本轮没有重跑各自完整故障矩阵。
- **W3：未发现 Account 核心实现被 W4 修改，但回归门禁不是绿。** `bootstrap-module-names.surface.spec.ts` 仍要求旧注册形式并失败，因此不能给出“W0--W3 全部回归通过”的系统级结论。
- **W4：存在本轮直接引入的 API test 回归，且 P1/P2 未清零。** 不满足提交和进入 W5 的条件。

### 第三轮验证记录

当前环境没有 `pnpm` 可执行文件，使用仓库 `./node_modules/.bin/nx` 并加 `--skip-nx-cache`；API/Desktop typecheck 直接调用仓库 TypeScript CLI以避开 target 内部的 `pnpm` wrapper：

- `goal:test`：85 files，447/447 tests passed。
- `goal:test:integration`：2 files，6/6 tests passed，真实 PostgreSQL test database。
- `task:test`：68 files，784/784 tests passed。
- `api:test`：**failed**；30 files 中 2 failed / 28 passed，85 tests 中 4 failed / 81 passed。3 个失败由本轮 Task adapter mock 漏导出直接触发，1 个为既存 Account surface 断言失真。
- `goal:lint`：passed（179 warnings，0 errors）；`task:lint`：passed（86 warnings，0 errors）。
- `goal:typecheck`、`task:typecheck`、API direct typecheck、Desktop direct typecheck：passed。
- `goal:build`、`task:build`：passed。并行首次尝试曾因多个 target 同时清理 `contracts/dist` 出现 ENOENT；改为串行后通过，判定为验证并发竞争而非业务 finding。
- `memoflow:governance-check`：passed；inventory 973 files（unit 843、integration 17、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；package-internal boundary 648 files / 0 known violations。

### 第三轮放行前最低修复条件

1. 修复 W4 引入的 API mock 回归并使 `api:test` 全量通过；同步回收既存 Account surface 失真断言。
2. PowerSync receipt 写后必须读到 durable canonical row，否则抛错回滚；补真实 SQLite/PowerSync close/reopen restart、complete/archive 与 receipt failure rollback 测试。
3. 新增 API/Desktop factory module 的 `register()` 启动测试；不能用源码字符串检查或直接 infrastructure factory 测试替代。
4. 给 `taskBindingCount` 补 0/非零精确数值及 transport contract 断言。
5. 移除 7 个 Task production use-case inline runner 默认值，使 runner 在所有相关 constructor/factory 类型层必填。
6. 补真实 Prisma/PowerSync binding identity/schema 测试和 Task outbox append 失败全事务回滚矩阵。

在上述 P1/P2 全部关闭并重跑完整近邻门禁前，最终判定保持：**W4 不可放行，不得提交，不进入 W5。**

---

## 第四轮审查

本轮对照第三轮的 2 个 P1、5 个 P2 及其明确关闭条件，复核当前工作区的 API 回归、PowerSync receipt 持久化测试、API/Desktop 组合根门禁、Task use-case constructor、Task outbox 故障注入，以及第三轮未关闭的 binding count / Task-owned adapter 测试。审查只追加本文档；未修改业务代码，未 commit/push。

### 终审结论

**W4 第四轮仍不可放行：不要提交 W4，不进入 W5。**

本轮确实关闭了 API 全量测试回归和 7 个 Task use-case 的 inline fallback；PowerSync receipt 生产 adapter 也已删除未落盘成功 fallback，Prisma outbox 故障注入能够证明实例更新、outbox 和事件的一致回滚。全量 Goal/Task/API 测试、近邻 lint/typecheck/build 与 governance 均为绿。

但第三轮要求的 durable restart 闭环仍没有交付：新增测试虽然从 `Map` 升级为真实 `better-sqlite3`，却使用 `:memory:` 且始终复用同一连接，只重建 adapter，没有关闭并重新打开数据库；PowerSync receipt 写失败也只测试“表不存在时 adapter 抛错”，没有经过 Goal write transaction 证明 Goal CAS 同时回滚。另有 4 个第三轮 P2 仍未关闭：所谓 API/Desktop `register()` 测试没有调用任何 `register()`；`taskBindingCount` 仍无 0/非零精确断言；Task-owned Prisma/PowerSync binding adapter 仍无真实 identity/schema 测试；PowerSync outbox 故障测试没有断言被修改实例的状态回滚。

### 第四轮分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现新的立即数据破坏或生产启动必现阻断。 |
| P1 | 1 | PowerSync receipt 已 fail-closed，但仍无真实数据库关闭/重开和 receipt 失败时 Goal CAS 同事务回滚证据。 |
| P2 | 4 | API/Desktop `register()` 未实测；binding count 精确断言缺失；真实 binding adapter 测试缺失；PowerSync outbox 测试未断言实例状态回滚。 |

### 已关闭：第三轮 P1-1 API 全量回归

`backend-automation-tool-executor.adapter.test.ts` 的 `@memoflow/task` mock 已补 `PrismaTaskBindingReadPort`（[`backend-automation-tool-executor.adapter.test.ts:23`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.test.ts:23)），Account bootstrap surface 也已改为检查生产入口的 `createAccountApiModule({ cloudAuth })` 注册形式（[`bootstrap-module-names.surface.spec.ts:19`](../../apps/api/src/bootstrap-module-names.surface.spec.ts:19)）。本轮实跑 `api:test` 为 30/30 files、85/85 tests passed，第三轮 P1-1 关闭。

### P1-1：PowerSync receipt 生产 fail-open 已删除，但 durable restart / 原子回滚证据仍不成立

生产修复本身成立：PowerSync adapter 在 INSERT 后读取不到 canonical row 时明确抛错，不再构造未落盘的成功 receipt（[`powersync-goal-reliable-operation.adapter.ts:87`](../../packages/goal/src/server/infrastructure/adapters/powersync/powersync-goal-reliable-operation.adapter.ts:87)、[`powersync-goal-reliable-operation.adapter.ts:98`](../../packages/goal/src/server/infrastructure/adapters/powersync/powersync-goal-reliable-operation.adapter.ts:98)）。新增测试也确实使用真实 `better-sqlite3`，不再是第三轮的 `Map` mock，并覆盖建表、首次写、同 key 返回同 operationId、archive 对称与缺表抛错。

但测试数据库是 `new Database(':memory:')`（[`powersync-goal-reliable-operation.adapter.test.ts:10`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:10)），helper 没有暴露或调用 `close()`；所谓 restart 只是在同一个 `db`/同一个 SQLite connection 上从 `adapter1` 换成 `adapter2`（[`powersync-goal-reliable-operation.adapter.test.ts:74`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:74)、[`powersync-goal-reliable-operation.adapter.test.ts:85`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:85)）。这证明 adapter reconstruction，不证明 receipt 在数据库关闭/重开或进程重启后仍 durable。

负向测试也只是不建 `goal_operation_receipts` 表，直接调用 receipt adapter 并期待抛错（[`powersync-goal-reliable-operation.adapter.test.ts:109`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:109)）；它没有通过 `PowerSyncGoalWriteTransactionRunner` 执行 complete/archive，也没有断言 receipt SQL 失败后 Goal 状态/version/CAS 写回滚。第三轮明确要求的“关闭/重开 + receipt failure rollback”仍未满足，因此 W0 durable receipt 不能最终认证。

**要求**：使用临时文件 SQLite（或真实 PowerSync database）首次写后关闭连接，再以同一路径重开并重建 module/adapter，断言 complete/archive 的 operationId 和单行唯一性；另在 `PowerSyncGoalWriteTransactionRunner` 内注入 receipt SQL 失败，断言 Goal 状态、version、receipt row 与事件全部回滚/不发布。

### P2-1：新增测试名为 `register()` startup，实际仍未调用 API/Desktop module 的 `register()`

Electron 测试的 describe 虽命名为 `GoalElectronModule.register() startup`，实际导入并调用的是底层 `createGoalPowerSyncModule()`（[`goal-electron.surface.spec.ts:28`](../../packages/goal/src/electron/goal-electron.surface.spec.ts:28)、[`goal-electron.surface.spec.ts:60`](../../packages/goal/src/electron/goal-electron.surface.spec.ts:60)），没有构造 `createGoalElectronModule()`，也没有调用其 `register(ctx)`。API 新测试只断言无参数调用 `createGoalApiModule()` 和 `createGoalPrismaModule()` 会抛错（[`goal-api-module-gate.surface.spec.ts:3`](../../packages/goal/src/api/__tests__/goal-api-module-gate.surface.spec.ts:3)），同样没有调用 `createGoalApiModule(options).register(context)`。

这些测试能证明底层 factory fail-closed，但不能覆盖第三轮要求的 host module 注册生命周期、context 传递、route/IPC 注册和 listener 启动。该 P2 仍未关闭。

### P2-2：`taskBindingCount` 仍没有精确数值断言

生产 contract 已在第三轮关闭，但测试仍只断言 `hasTaskLinks/canDelete/warnings`：无 binding 分支没有 `taskBindingCount === 0`，有 binding 分支也只检查中文 warning 中的 `1`（[`delete-goal.test.ts:164`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:164)、[`delete-goal.test.ts:182`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:182)）。全仓测试搜索仍无 `taskBindingCount` 命中。第三轮要求的 0/非零精确值和 transport contract 证据没有新增。

### P2-3：Task-owned binding adapter 仍无真实 identity/schema 测试

两个生产 adapter 都正确调用 `GoalTaskBindingQueryInputSchema.parse()`，并以 `identityId + goalId + deletedAt/deleted_at` 过滤（[`prisma-task-binding-read-port.ts:8`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.ts:8)、[`powersync-task-binding-read-port.ts:8`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.ts:8)）。但 Task 测试中仍没有实例化 `PrismaTaskBindingReadPort` 或 `PowerSyncTaskBindingReadPort`；现有 API integration 只使用 Prisma adapter 走 host restart 成功路径，不覆盖两个 identity 使用同 goalId、软删除 binding 排除和空输入 schema 拒绝。该第三轮 P2 仍未关闭。

### P2-4：Prisma outbox 回滚证据成立，PowerSync 测试漏断言核心实例状态

Prisma 集成测试在 transaction-bound `taskGoalOutbox.createMany` 注入失败，随后断言 complete 返回 `INTERNAL_ERROR`、实例仍为 `Pending`、outbox 为 0 且不发布事件（[`prisma-task-write-transaction-runner.integration.test.ts:300`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-write-transaction-runner.integration.test.ts:300)、[`prisma-task-write-transaction-runner.integration.test.ts:330`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-write-transaction-runner.integration.test.ts:330)）。这部分关闭。

PowerSync fake DB 也使用 staged state，outbox INSERT 抛错后不会把 staged state赋回 committed state，代码结构支持整体回滚。但测试在失败后只断言 template count 为 1、outbox count 为 0、事件未发布（[`powersync-task-write-transaction-runner.test.ts:359`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:359)）；它没有重新读取被 complete 的 instance 并断言状态仍为 `Pending`，也没有任何失败后的 instance count/status 断言。由于这个场景真正发生的业务写是 instance completion，当前测试没有以可观察结果证明核心实例更新被回滚。该 P2 只关闭 Prisma 半边。

### 已关闭：第三轮 P2-2 Task use-case inline fallback

7 个生产 use case 的 `TaskWriteTransactionRunner` 均已改为 constructor 必填，并对 falsy runner 运行时抛错：activate、complete、delete、generate、pause、uncomplete、update。生产 use-case 文件搜索 `= createInlineTaskWriteTransactionRunner` 为 0；`createInlineTaskWriteTransactionRunner(...)` 只剩 test double 的显式使用及 helper 定义。`CompleteTaskInstanceUseCase.execute()` 也已捕获 transaction/outbox 异常并映射为 `INTERNAL_ERROR`（[`complete-task-instance.use-case.ts:32`](../../packages/task/src/server/application/use-cases/commands/complete-task-instance.use-case.ts:32)、[`complete-task-instance.use-case.ts:48`](../../packages/task/src/server/application/use-cases/commands/complete-task-instance.use-case.ts:48)）。该 P2 关闭。

### W0--W3 回归判断

- **W0：结构实现未见新回退，但 durable receipt 证据仍未闭合。** Frozen ports、host 注入、Prisma 原子事务与 PowerSync fail-closed 生产逻辑保持；PowerSync 关闭/重开和 receipt failure transaction rollback 缺失使 W0 receipt 不能最终认证。
- **W1--W2：未发现直接代码回退。** Reminder/Notification 核心实现不在第四轮 diff 范围；governance 全量通过。本轮仍未重跑 W1--W2 各自完整产品/故障矩阵。
- **W3：本轮已恢复 API 回归门禁。** `api:test` 85/85 通过，API direct typecheck 通过；正式 `desktop:typecheck` target 也通过。未发现 Account/Cloud Auth 新回退。
- **W4：P1/P2 未清零。** 近邻门禁全绿不替代上述持久化重启、注册生命周期和故障断言缺口，仍不满足提交和进入 W5 的条件。

### 第四轮验证记录

当前环境没有 `pnpm` 可执行文件，使用仓库 `./node_modules/.bin/nx` 并加 `--skip-nx-cache`；Task/API direct typecheck 使用仓库 TypeScript CLI：

- `api:test`：30 files，85/85 tests passed。
- `goal:test`：86 files，452/452 tests passed。
- `goal:test:integration`：2 files，6/6 tests passed，真实 PostgreSQL test database。
- `task:test`：68 files，792/792 tests passed。
- `task:test:integration`：5 files，27/27 tests passed，真实 PostgreSQL test database。
- `goal:lint`：passed（177 warnings，0 errors）；`task:lint`：passed（96 warnings，0 errors）。
- `goal:typecheck`、Task direct typecheck、API direct typecheck、正式 `desktop:typecheck` target：passed。
- `goal:build`、`task:build`：作为正式 typecheck target 的依赖实际执行并 passed。
- `memoflow:governance-check`：passed；inventory 974 files（unit 844、integration 17、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；package-internal boundary 648 files / 0 known violations。

曾用 `apps/desktop/tsconfig.json` 而非 target 指定的 `tsconfig.typecheck.json` 做一次额外 direct typecheck，因 source/dist 私有类型双实例失败；随后按仓库正式 `desktop:typecheck` target（含依赖 build 和 dist-only type paths）复核通过，因此不记为产品 finding。

### 第四轮放行前最低修复条件

1. PowerSync receipt 改用临时文件数据库完成 close/reopen restart；同 key 在重开后返回同 operationId，complete/archive 对称且仅一行。
2. 经 `PowerSyncGoalWriteTransactionRunner` 注入 receipt SQL 失败，断言 Goal 状态/version、receipt 与事件全部回滚/不发布。
3. API/Desktop 测试实际调用 `createGoalApiModule(...).register(context)` 与 `createGoalElectronModule(...).register(context)`，覆盖有 capability 成功、缺 capability fail-closed 及必要注册副作用。
4. 给 `taskBindingCount` 增加 0/非零精确值和 transport contract 断言；给 Prisma/PowerSync Task binding adapter 增加真实 identity、软删除与 schema 负向测试。
5. PowerSync outbox 故障测试在失败后重新读取目标 instance，明确断言 status/version 未改变，并继续断言 outbox 为 0、事件不发布。

在上述 1 个 P1 和 4 个 P2 全部关闭前，最终判定保持：**W4 不可放行，不得提交，不进入 W5。**

---

## 第五轮审查

本轮对照第四轮的 1 个 P1、4 个 P2 及其逐项关闭条件，复核 PowerSync receipt 的真实关闭/重开与事务回滚、API/Desktop 模块注册生命周期、delete dependency 数值契约、Task-owned binding adapter，以及 PowerSync Task outbox 故障测试。审查只追加本文档；未修改业务代码，未 commit/push。

### 终审结论

**W4 第五轮仍不可放行：不要提交 W4，不进入 W5。**

第四轮唯一 P1 已关闭：PowerSync receipt 测试现在使用真实临时文件 SQLite，首次写入后关闭连接，再从同一路径重开并构造新 adapter，能够证明同一 idempotency key 返回同一 operationId 且数据库中只有一行；另一个真实 SQLite 测试通过 `PowerSyncGoalWriteTransactionRunner` 执行 complete，并用 `RAISE(ABORT)` 触发 receipt INSERT 失败，证明 Goal 的状态与 version CAS 一并回滚。API 和 Desktop 也都实际调用了公开 module 的 `register()`；`taskBindingCount` 的 0/1 精确断言已经补齐。

但第四轮的两个 P2 仍未形成有效关闭证据。Task-owned adapter 只新增了 Prisma 集成测试，没有覆盖生产 Desktop 使用的 `PowerSyncTaskBindingReadPort`，也没有验证软删除 binding 排除。PowerSync outbox 测试虽然新增 `status === 'Pending'` 断言，但 fake DB 的 `UPDATE task_instances` 分支根本不把传入的新 status/version 写进 staged state，因此实例在成功提交、失败回滚甚至没有事务隔离时都会保持 `Pending`；该断言不能证明实例更新被回滚。P2 未清零，故不满足 W4 放行条件。

### 第五轮分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现新的立即数据破坏或生产启动必现阻断。 |
| P1 | 0 | PowerSync durable close/reopen 与 receipt 失败时 Goal CAS 回滚证据成立。 |
| P2 | 2 | PowerSync binding adapter/软删除测试仍缺；PowerSync outbox 的实例状态断言为空证明。 |

### 已关闭：第四轮 P1-1 PowerSync durable receipt 与 Goal CAS 回滚

真实重启证据成立。测试创建文件路径 SQLite，写入 receipt 后明确 `close()`，再对同一路径创建第二个数据库连接和新 adapter（[`powersync-goal-reliable-operation.adapter.test.ts:77`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:77)）；重开后断言 operationId/createdAt 与首次一致，并直接查询 `goal_operation_receipts` 断言只有一行（[`powersync-goal-reliable-operation.adapter.test.ts:90`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:90)、[`powersync-goal-reliable-operation.adapter.test.ts:98`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:98)）。这不再是 `:memory:` 同连接上的 adapter reconstruction。

事务失败证据也成立。测试建立真实 `goals`/`goal_operation_receipts` 表并种入 Active、version 1 的 Goal，随后用 SQLite trigger 在 receipt INSERT 时 `RAISE(ABORT)`（[`powersync-goal-reliable-operation.adapter.test.ts:157`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:157)、[`powersync-goal-reliable-operation.adapter.test.ts:179`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:179)）。Complete 经真实 `PowerSyncGoalWriteTransactionRunner` 抛错后，重新从 repository 读取仍为 version 1 / active（[`powersync-goal-reliable-operation.adapter.test.ts:186`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:186)、[`powersync-goal-reliable-operation.adapter.test.ts:194`](../../packages/goal/src/server/infrastructure/adapters/powersync/__tests__/powersync-goal-reliable-operation.adapter.test.ts:194)）。结合 runner 仅在 `writeTransaction` 成功返回后 flush buffered events 的结构，第四轮 P1 关闭。

### 已关闭：第四轮 P2-1 API/Desktop 实际 register 生命周期

API 的真实 PostgreSQL 集成测试构造 Express `Router`、host-injected Task binding port 和真实 Prisma client，实际调用 `createGoalApiModule(...).register(context)`，并以 `router.stack.length > 0` 锁定路由副作用，最后执行 destroy（[`goal-prisma.repository.integration.test.ts:299`](../../packages/goal/src/server/infrastructure/adapters/prisma/goal-prisma.repository.integration.test.ts:299)）。这已经穿过公开 API module 的组合与注册生命周期，而不是只调用 infrastructure factory。

Electron 测试对称构造 `createGoalElectronModule()` 并实际调用 `module.register(ctx)`，mock 的 `ipcMain.handle` 至少被调用一次，随后 destroy 正常完成（[`goal-electron.surface.spec.ts:86`](../../packages/goal/src/electron/goal-electron.surface.spec.ts:86)）。第四轮 P2-1 关闭。

### 已关闭：第四轮 P2-2 `taskBindingCount` 精确数值

DeleteGoal dependency 测试现在对无 binding 分支明确断言 `taskBindingCount === 0`，对一个 active binding 分支明确断言 `taskBindingCount === 1`，并同时锁定 `hasTaskLinks/canDelete/warnings`（[`delete-goal.test.ts:161`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:161)、[`delete-goal.test.ts:183`](../../packages/goal/src/server/application/use-cases/commands/__tests__/delete-goal.test.ts:183)）。生产结构化 contract 已在第三轮确认，本轮补足数值证据，第四轮 P2-2 关闭。

### P2-1：Task binding 只测 Prisma，PowerSync lane 与软删除过滤仍没有验证

新增 `PrismaTaskBindingReadPort` 集成测试使用真实 PostgreSQL，identity A 对 goal A 的两个 binding 与 identity B 对同 goal A 的一个 binding 并存；A 的结果仍精确为 2，因此 identity 隔离证据有效。它也覆盖缺失 goal 返回 0 和空 identity 被 frozen schema 拒绝（[`prisma-task-binding-read-port.integration.test.ts:23`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.integration.test.ts:23)、[`prisma-task-binding-read-port.integration.test.ts:59`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.integration.test.ts:59)）。Prisma 半边关闭。

但第四轮要求的是 Prisma/PowerSync 两个 Task-owned adapter。生产 Desktop 注入的 `PowerSyncTaskBindingReadPort` 仍没有任何测试实例化；全仓测试搜索只有 Prisma adapter 命中。其 SQL 自身包含 `identity_id + goal_id + deleted_at IS NULL`（[`powersync-task-binding-read-port.ts:8`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.ts:8)），但没有真实 SQLite 数据证明 identity 隔离、count 类型映射、缺失行及 schema 负向行为。此外，新 Prisma fixture 的四行 `deletedAt` 都为空，没有插入同 identity/goal 的软删除 binding，因此第四轮明确要求的软删除排除也未被锁定。

**要求**：给 `PowerSyncTaskBindingReadPort` 增加真实 SQLite/PowerSync adapter 测试，覆盖两个 identity 指向同 goalId、精确计数、缺失 goal、软删除排除和 frozen schema 负向输入；Prisma 测试同时加入软删除 fixture 并证明 count 不包含该行。

### P2-2：PowerSync outbox 的 Pending 断言不能观察到被测更新

测试新增了失败后查询实例并断言 `status === 'Pending'`（[`powersync-task-write-transaction-runner.test.ts:325`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:325)）。问题在于 fake DB 的 `UPDATE task_instances` 分支只取出 existing record 后原样写回，完全没有应用 SQL 参数中的 `status`、version 或时间字段（[`powersync-task-write-transaction-runner.test.ts:161`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:161)）。所以 complete 的 staged state 从未变成 Completed；即使删除 transaction rollback、让 staged state错误 commit，这个新断言仍会通过。

outbox 为 0 与事件未发布仍可证明 outbox failure 路径被触发，但不能证明核心 instance mutation 与 outbox 同事务回滚。第四轮 P2-4 未关闭。

**要求**：让 fake DB 按 production repository 的 UPDATE 参数真实更新 staged instance 的 status/version（或改用真实 SQLite transaction）；先用成功控制用例证明 complete 后为 Completed/version 推进，再在 outbox INSERT 失败用例中证明同一实例恢复为 Pending/原 version，并继续断言 outbox 0、事件不发布。

### W0--W3 回归判断

- **W0：durable receipt 主缺口已关闭。** Frozen ports、host 注入、Prisma/PowerSync transaction runner 与 fail-closed receipt 保持；真实文件关闭/重开及 receipt failure Goal CAS rollback 本轮通过。剩余两项是 W4 adapter/故障矩阵证据缺口，不构成新的 W0 结构回退。
- **W1--W2：未发现直接代码回退。** Reminder/Notification 核心文件不在第五轮增量范围；API 全量测试与 governance 通过。本轮不重新认证它们各自完整产品故障矩阵。
- **W3：未发现回退。** API 85/85 通过，Account bootstrap surface 保持通过；本轮没有修改 Account/Cloud Auth 核心实现。
- **W4：P0/P1 已清零，但 P2 仍为 2。** 当前实现不能按第四轮既定关闭条件提交放行。

### 第五轮验证记录

当前环境仍没有 `pnpm` 可执行文件，使用仓库 `./node_modules/.bin/nx` 与 Vitest，并对 Nx target 加 `--skip-nx-cache`：

- 第五轮新增 Goal 针对性测试：3 files，20/20 tests passed（PowerSync receipt/rollback、Electron register、DeleteGoal count）。
- `goal:test`：86 files，454/454 tests passed。
- `goal:test:integration`：2 files，7/7 tests passed，包含实际 API module register。
- `task:test`：68 files，792/792 tests passed。
- `task:test:integration`：6 files，29/29 tests passed，包含新增 Prisma binding adapter 测试。
- `api:test`：30 files，85/85 tests passed。
- `goal:lint`、`task:lint`：passed，均为 0 errors（Task 98 warnings）。
- `goal:typecheck`、`task:typecheck`：passed；依赖 build 随 target 成功执行。
- `memoflow:governance-check`：串行复跑 passed；inventory 975 files（unit 844、integration 18、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；package-internal boundary 648 files / 0 known violations。

首次并行运行 Goal/Task typecheck 与 governance 时，多个 Nx dependency build 同时清理/生成 Prisma client，governance 的 date-fns audit 一次因 `packages/database/src/generated/prisma/client.js` 瞬时不存在而 ENOENT。待 typecheck/build 完成后串行重跑 governance 全绿；与前轮已记录的生成产物并发竞争同类，不计为产品 finding。

### 第五轮放行前最低修复条件

1. 为 `PowerSyncTaskBindingReadPort` 补真实 adapter identity/schema/count/缺失/软删除测试，并给 Prisma lane 补软删除排除 fixture。
2. 修正 PowerSync outbox fake 的 instance UPDATE 语义或改用真实 SQLite；以成功控制组证明测试能观察 Completed，再以故障组证明 Pending/version、outbox 与事件完整回滚。
3. 修复后重跑 Task unit/integration、Goal/API 近邻回归、lint/typecheck/build、governance 与 inventory，再进行第六轮关闭审查。

最终判定：**P0 = 0，P1 = 0，P2 = 2；W4 不可放行，不得提交，不进入 W5。**

---

## 第六轮审查

本轮只对照第五轮遗留的 2 个 P2 及其关闭条件，复核 Task-owned PowerSync binding adapter 的真实数据库证据、Prisma 软删除 fixture，以及 PowerSync Task outbox fake DB 的实例 UPDATE 与事务回滚语义。审查只追加本文档；未修改业务代码，未 commit/push。

### 终审结论

**W4 第六轮可放行：可以提交 W4，并进入 W5。**

第五轮的两个 P2 均已关闭。PowerSync binding 测试使用真实 `better-sqlite3` 与实际 `task_templates` 表，同时锁定同 goal 下 A/B identity 隔离、软删除排除、缺失 goal 返回 0 及 frozen schema 负向输入；Prisma 集成 fixture 也加入同 identity/goal 的软删除模板并继续精确返回 2。PowerSync outbox fake DB 现在按生产 UPDATE 参数把 staged instance 的 status 实际更新为 `Completed`，outbox INSERT 随后抛错时事务不会提交 staged snapshot，失败后从 committed state 读回 `Pending`，因此断言已不是空证明。

本轮没有发现新的 P0/P1/P2。Task、Goal、API 回归、Task 集成、lint、typecheck、governance 与 inventory 均通过；未发现 W0--W3 回退。

### 第六轮分级

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现立即数据破坏或生产启动阻断。 |
| P1 | 0 | 第五轮前已清零，本轮未发现新问题。 |
| P2 | 0 | PowerSync binding/软删除证据与 outbox 实例回滚证据均已补齐。 |

### 已关闭：第五轮 P2-1 PowerSync binding adapter 与 Prisma 软删除覆盖

新增 PowerSync 测试以真实内存 SQLite 建立 `task_templates` 表并插入四行：identity A 对同一 goal 的两条活跃 binding、一条软删除 binding，以及 identity B 对同一 goal 的一条活跃 binding（[`powersync-task-binding-read-port.spec.ts:10`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.spec.ts:10)、[`powersync-task-binding-read-port.spec.ts:42`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.spec.ts:42)）。对 A/B 分别精确断言 2/1，证明两个 identity 互不可见且 `deleted_at IS NOT NULL` 不计入；缺失 goal 明确返回 `{ hasActiveBindings: false, activeCount: 0 }`（[`powersync-task-binding-read-port.spec.ts:52`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.spec.ts:52)、[`powersync-task-binding-read-port.spec.ts:59`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.spec.ts:59)）。空 identity 经生产 adapter 调用被拒绝，且测试显式引用 frozen `GoalTaskBindingQueryInputSchema`，schema 边界成立（[`powersync-task-binding-read-port.spec.ts:62`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.spec.ts:62)）。

生产 adapter 本身先执行 frozen schema parse，再用 `identity_id + goal_id + deleted_at IS NULL` 查询并将 SQLite count 归一为 number（[`powersync-task-binding-read-port.ts:15`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-binding-read-port.ts:15)）。测试穿过的正是该生产实现，不是复制 SQL 的 mock。

Prisma 集成 fixture 同时新增 identity A、goal A 的软删除模板，`deletedAt` 为非空；A/goal A 仍精确断言 `activeCount === 2`，因此 Prisma `deletedAt: null` 过滤也已被真实 PostgreSQL 数据锁定（[`prisma-task-binding-read-port.integration.test.ts:34`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.integration.test.ts:34)、[`prisma-task-binding-read-port.integration.test.ts:46`](../../packages/task/src/server/infrastructure/adapters/prisma/prisma-task-binding-read-port.integration.test.ts:46)）。第五轮 P2-1 关闭。

### 已关闭：第五轮 P2-2 PowerSync outbox 实例状态回滚非空证明

fake DB 的 `UPDATE task_instances` 分支现在从与生产 repository 一致的第四个 SET 参数读取 status，并写入 staged instance（[`powersync-task-write-transaction-runner.test.ts:161`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:161)）。生产 SQL 的列顺序确为 `template_id, identity_id, instance_date, status, ...`，第四个参数就是 `data.status`（[`task-instance-powersync.repository.ts:40`](../../packages/task/src/server/infrastructure/adapters/powersync/task-instance-powersync.repository.ts:40)、[`task-instance-powersync.repository.ts:56`](../../packages/task/src/server/infrastructure/adapters/powersync/task-instance-powersync.repository.ts:56)）。因此 complete 在 outbox append 前会把事务快照中的实例真实改为 `Completed`。

fake 的 `writeTransaction()` 只在 callback 成功返回后才以 staged snapshot 替换 committed state；outbox INSERT 在 `failOutbox` 开启时抛错，赋值不会执行（[`powersync-task-write-transaction-runner.test.ts:68`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:68)、[`powersync-task-write-transaction-runner.test.ts:105`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:105)）。故障用例随后从 committed DB 状态重新查询同一 instance 并断言 `Pending`，同时断言 outbox 为 0、事件未发布（[`powersync-task-write-transaction-runner.test.ts:372`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:372)、[`powersync-task-write-transaction-runner.test.ts:376`](../../packages/task/src/server/infrastructure/adapters/powersync/powersync-task-write-transaction-runner.test.ts:376)）。若失败路径错误提交 staged snapshot，该查询会读到 `Completed` 并失败；因此 Pending 已构成实例 mutation 回滚的非空证明。第五轮 P2-2 关闭。

### W0--W3 回归判断

- **W0：未发现回退。** Frozen reliable-messaging schema/ports、host 注入、Prisma/PowerSync transaction runner 与 durable receipt 闭环保持；本轮新增测试直接引用 frozen binding schema。
- **W1--W2：未发现直接代码回退。** 第六轮增量仅补 Task adapter/事务测试和 inventory，未修改 Reminder/Notification 核心实现；API 回归与 governance 通过。本轮不重新认证 W1--W2 各自完整产品故障矩阵。
- **W3：未发现回退。** API 85/85 通过，本轮没有修改 Account/Cloud Auth 核心实现。
- **W4：所有历轮 finding 已关闭。** P0/P1/P2 均为 0，满足提交 W4 并进入 W5 的条件。

### 第六轮验证记录

当前环境没有 `pnpm` 可执行文件，使用仓库 `./node_modules/.bin/nx`、Vitest，并对 Nx target 加 `--skip-nx-cache`：

- 本轮两个 PowerSync 针对性测试文件：4/4 tests passed（binding 真实 SQLite、Task write/outbox rollback）。
- `task:test`：69 files，793/793 tests passed。
- `task:test:integration`：6 files，29/29 tests passed，真实 PostgreSQL test database，包含 Prisma binding soft-delete fixture。
- `goal:test`：86 files，454/454 tests passed。
- `api:test`：30 files，85/85 tests passed。
- `task:lint`：passed（98 warnings，0 errors）。
- `goal:typecheck`、`task:typecheck`：passed；依赖 build 随 target 成功执行。
- `memoflow:governance-check`：passed；inventory 976 files（unit 845、integration 18、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；package-internal boundary 648 files / 0 known violations。

一次并行验证中，`goal:test` 的动态 import 用例在高负载下恰好超过 10 秒而超时，其余 453 项通过；资源空闲后单独以 `--skip-nx-cache` 完整复跑为 86 files、454/454 tests passed，判定为验证并发噪声，不记为产品 finding。

最终判定：**P0 = 0，P1 = 0，P2 = 0；W4 可放行，可以提交 W4 并进入 W5。**
