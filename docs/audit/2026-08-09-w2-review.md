---
tags:
  - audit
  - notification
  - w2
  - reliable-messaging
description: W2 Notification channel capability 与 durable dispatch 实施审查
created: 2026-08-10T00:00:00Z
---

# W2 审查：Notification channel capability 与 durable dispatch

本次审查对照计划 W2、根因判定表、实施依赖、风险/回滚和完成定义，复核当前工作区未提交的 W2 产物以及 W1 Reminder→Notification intent 对接。审查只新增本文档；未修改业务代码，未 commit/push。

## 结论

**W2 不可放行，不应提交 W2，也不能进入 W3。**

当前实现已经具备独立的 Notification dispatch outbox 表、创建通知时 aggregate/channel/outbox 的单事务写入、基础 conditional claim、receipt 映射和 identity 条件的 dead-letter adapter 方法；218 个单测、6 个集成用例和 typecheck 也确实通过。但生产 composition root 没有注册任何真实渠道 adapter，反而显式覆盖掉 Prisma module 的 durable runtime；W1 写入的 Notification intent 没有 W2 consumer；SSE 仍由 create use case 直接发布进程内事件；worker 的终态写没有 owner/fencing 条件，deliverer 也拿不到 deliveryId/idempotencyKey。上述问题直接命中计划第 68--70 行和完成定义第 134--137 行，不能以绿测放行。

## Findings

### P0-1：生产组合根没有真实渠道 capability，并绕过了 durable runtime wiring

`createNotificationPrismaModule()` 本来会创建 `NotificationReliableOperationPrismaAdapter`，并在未显式传入 runtime 时将 repository、reliable adapter 和渠道配置一起注入 worker（[prisma.ts:24](../../packages/notification/src/server/infrastructure/prisma.ts:24)、[prisma.ts:35](../../packages/notification/src/server/infrastructure/prisma.ts:35)）。但真实 API 注册路径显式传入 `createNotificationRuntimeContribution()` 的无依赖实例（[module.ts:39](../../packages/notification/src/api/module.ts:39)），从而使 `options.runtimeContributions ?? [...]` 选择左侧实例，完全绕过前述 durable adapter wiring。

该无依赖 runtime 在 production 会因为默认要求 InApp/Push 且 registry 为空而抛错（[notification.runtime.ts:105](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:105)），因此确实 fail-closed；但仓库内除 options 类型和测试 double 外没有任何 `NotificationChannelDeliverer` 实现或生产注册点。结果不是“生产 capability 已落实”，而是“生产必然无法启动”。在 development/test 中同一实例可构造成功，但既无 reliable adapter 也无 repository，`tick()` 没有工作分支（[notification.runtime.ts:145](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:145)、[notification.runtime.ts:332](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:332)），会静默启动一个不投递的 worker。

另外，调用方可仅声明 `channelCapabilities.status='available'` 通过启动检查，而 registry 中并不一定存在对应 deliverer；真正的缺失直到处理消息时才由 `getDelivererForChannel()` 发现（[notification.runtime.ts:91](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:91)、[notification.runtime.ts:128](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:128)）。这不是对实际 adapter 注册结果的 capability 校验。

这违反计划要求的“按渠道注册真实 adapter”和完成定义中的“所有 P0 有真实生产 capability”。

### P0-2：W1 Notification intent 与 W2 worker 使用两张互不相通的 outbox，Reminder→Notification 闭环未对接

W1 在 Reminder 的事务中写入通用 `OutboxMessage`，`messageType='notification.dispatch'`，payload 内包含 W0 Notification intent（[prisma-reminder-write-transaction-runner.ts:123](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:123)、[prisma-reminder-write-transaction-runner.ts:146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)）。该表映射为 `reliable_outbox_messages`（[reliable-messaging.prisma:9](../../packages/database/prisma/schema/reliable-messaging.prisma:9)）。

W2 worker 只调用 `claimOutboxDispatch()`（[notification.runtime.ts:149](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:149)），而该 adapter 只查询 `NotificationDispatchOutbox`（[notification-reliable-operation-prisma.adapter.ts:151](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:151)），对应另一张 `notification_dispatch_outbox` 表（[notification.prisma:116](../../packages/database/prisma/schema/notification.prisma:116)）。全仓搜索没有 `notification.dispatch` 的生产 consumer，也没有把 `OutboxMessage` 原子/幂等转换为 Notification aggregate、channel 和 `NotificationDispatchOutbox` 的桥接逻辑。

即使补一个直接转发表，W1 当前 channel 值为 `'in-app'`（[prisma-reminder-write-transaction-runner.ts:136](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:136)），W2 registry/创建用例使用枚举值 `'InApp'`（[notification.runtime.ts:84](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:84)、[create-notification.use-case.ts:79](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:79)），仍需统一协议而不能直接投递。

因此 W1 写出的 intent 会永久停在通用 outbox，Reminder P0 可靠投递闭环未成立。

### P1-1：worker 的完成写丢失 lease/fencing，旧 owner 可覆盖新 owner，重复投递无法被阻止

claim 本身使用 `id + fencingToken + 可领取状态` 的 conditional update，基本竞争路径正确（[notification-reliable-operation-prisma.adapter.ts:176](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:176)）。但 worker 在成功、retryable 和 dead-letter receipt 中都先把 `lease` 改为 `null`（[notification.runtime.ts:216](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:216)、[notification.runtime.ts:261](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:261)、[notification.runtime.ts:297](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:297)）。`recordDeliveryReceipt()` 只有 receipt 仍带 lease 时才附加 `claimId/fencingToken` 条件；lease 为 null 时退化成 operation id 条件（[notification-reliable-operation-prisma.adapter.ts:297](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:297)）。它也不校验 owner、running 状态或 lease 未过期。

worker 从未调用已实现的 `heartbeatLease()`。如果外部投递超过 30 秒，或投递成功后在 receipt 写入前崩溃，第二 owner 会在 lease 过期后重新 claim 并再次调用渠道；旧 owner 随后还可无条件覆盖新 owner 状态。deliverer 接口只收到 aggregate 与 channel，不接收 outbox `operationId`/`idempotencyKey`（[notification.runtime.ts:24](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:24)），真实渠道 adapter 无法用 W0 delivery key 做下游去重。

数据库 `idempotencyKey @unique`（[notification.prisma:125](../../packages/database/prisma/schema/notification.prisma:125)）只能防止重复插入 intent，不能证明“重复消费不重复投递”。

### P1-2：SSE 不是 outbox consumer，断线后也没有 receipt 恢复入口

创建用例在数据库保存返回后直接调用 `pushSSEEvents()`（[create-notification.use-case.ts:140](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:140)、[create-notification.use-case.ts:151](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:151)），并通过进程内 event bus 同步发布 in-app/desktop 事件（[create-notification.use-case.ts:165](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:165)）。异常被记录后吞掉（[create-notification.use-case.ts:201](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:201)）。worker 成功写 receipt 后没有发布 SSE outbox event 或推进 SSE consumer cursor。

普通 notification list 可以重读 aggregate，但 ApplicationPort/HTTP routes 没有 delivery receipt/list timeline 查询；routes 仅有通知 CRUD、未读数和偏好（[routes.ts:72](../../packages/notification/src/api/routes.ts:72)、[routes.ts:174](../../packages/notification/src/api/routes.ts:174)、[routes.ts:235](../../packages/notification/src/api/routes.ts:235)）。因此客户端断线后无法按 receipt 判断 pending/running/retryable/dead-letter，也无法恢复漏掉的实时 delivery event。计划要求的“SSE 只是 outbox consumer，断线可从 receipt/list 恢复”未实现。

### P1-3：dead-letter/replay 只有 infrastructure runtime 方法，不是授权的运维接口

Prisma adapter 的 query 确实按 `identityId + dead_letter` 过滤，replay 首次读取也按 `identityId + operationId + dead_letter` 过滤（[notification-reliable-operation-prisma.adapter.ts:338](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:338)、[notification-reliable-operation-prisma.adapter.ts:354](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:354)）。这证明了 adapter 层 identity fence。

但它们只被 runtime 对象包装（[notification.runtime.ts:410](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:410)），没有进入 `NotificationApplicationPort`、module API、controller 或带 `auth/requireRole` 的 routes；API composition root 也没有保留 runtime 的运维 facade。当前没有调用者可用且可授权的 dead-letter 查询/replay 运维接口，不能满足计划所称“按 identity 授权”。

### P1-4：状态/指标闭环不完整，仍保留非 durable 双轨 dispatch

durable 路径会写 succeeded/retryable/dead-letter receipt 并计数 dispatched/delivered/failed/retry/dead-letter；但 metrics service 的 `recordPersisted()` 没有任何调用点，`persistedTotal` 永远为 0（[notification-metrics-service.ts:22](../../packages/notification/src/server/domain/services/notification-metrics-service.ts:22)）。指标只是 runtime 进程内计数器，没有生产 telemetry exporter/注册点；API 实际绕过该 runtime 后也无法观察这些计数。

当 `reliableAdapter` 缺失但 repository 存在时，worker仍会走旧的 channel 状态扫描路径（[notification.runtime.ts:332](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:332)）。该路径不产生 `BusinessOperationReceipt`，失败/重试/dead-letter 只写 aggregate channel 和内存指标。这是计划第 119 行要求删除的长期双轨行为，不能作为生产或通用 fallback 保留。它没有恢复旧的 no-op deliverer 假成功，但仍绕过 W0 receipt/outbox/lease 不变量。

### P2-1：schema 与实现存在脆弱的 notificationId 推导和自由状态字符串

repository/adapter 并未让 input 显式携带 `notificationId`，而是用 `occurrenceKey.split(':')[0]` 推导外键（[notification-prisma.repository.ts:150](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:150)、[notification-reliable-operation-prisma.adapter.ts:98](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:98)）。这只适用于当前 create use case 的 `${notification.id}:${channel}` 约定，不适用于 W1 的 `${templateId}:${time}` occurrenceKey，也把不透明 occurrence key 当成可解析结构。schema 的 `status/channel/source` 均为自由字符串（[notification.prisma:121](../../packages/database/prisma/schema/notification.prisma:121)），放大了 `'in-app'`/`'InApp'` 一类协议漂移。

## 计划 W2 逐条核验

| 计划条款 | 证据与判断 | 结论 |
| --- | --- | --- |
| 渠道 adapter 注册 + production fail-fast；禁止 no-op | runtime 能对缺失/测试 capability 抛错，也删除了旧 no-op deliverer；但生产组合根没有注册任何真实 adapter，并绕过 durable wiring，development 可启动空 worker，声明 capability 还可与 registry 脱节。 | **未满足（P0）** |
| create-notification 同事务写 aggregate + channel + dispatch outbox，含 deliveryId/idempotencyKey | use case 生成每 channel 的随机 operationId 和 canonical idempotencyKey（[create-notification.use-case.ts:104](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:104)），repository 在同一 Prisma transaction upsert aggregate/channel/outbox（[notification-prisma.repository.ts:51](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:51)、[notification-prisma.repository.ts:141](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:141)）。operationId 实质充当 deliveryId，但契约未显式命名。 | **基本满足（创建 API 自身）** |
| durable worker：lease/claim、所有结果 receipt 落库 | 基本 claim/expiry/retry/dead-letter 字段存在；但 heartbeat 未使用、完成写丢失 fencing、fallback 不写 receipt。 | **未满足（P1）** |
| SSE 仅为 outbox consumer，断线可从 receipt/list 恢复 | SSE 仍由 create use case 直接发进程内事件；无 receipt API/cursor/断线恢复。 | **未满足（P1）** |
| DB 唯一约束 + 重复消费不重复投递 | DB 有 canonical idempotencyKey 唯一约束；deliverer 无 idempotency 参数，失租/崩溃会重复调用。 | **部分满足，核心未满足（P1）** |
| identity-scoped dead-letter 查询 + replay | adapter SQL 条件正确；没有 application/HTTP 运维入口和角色授权。 | **部分满足（P1）** |
| persisted/dispatched/delivered/failed/retry/dead-letter 指标 | 六个字段存在，除 persisted 外其余 durable worker 路径有调用；persisted 从未记录，且无生产接入。 | **未满足（P1）** |

## W1 一致性

W1 与 W2 都使用 W0 canonical idempotency key/receipt 类型，这是契约层的一致点；W1 的 intent 也带 identity/source/occurrence/channel/payload/idempotencyKey。但持久化与消费层不一致：W1 写 `OutboxMessage`，W2 读 `NotificationDispatchOutbox`；没有 inbox receipt/consumer 将前者转换为 Notification aggregate 和后者，也没有对 `OutboxMessage.status` 做 dispatched/failed/dead 更新。channel 命名也不一致。**W1 intent → W2 worker 的实际对接为未实现。**

## 6 个集成用例真实性

集成文件实际有 6 个 `it()`（[notification-reliable-operation.integration.test.ts:64](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:64) 至 [notification-reliable-operation.integration.test.ts:264](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:264)），本地实测 6/6 通过，但覆盖声明需严格限定：

1. 用例 1 先手工 seed Notification，再单独调用 adapter `dispatchOutbox()`；证明 receipt/表写入，不证明 aggregate/channel/outbox 原子性。
2. 用例 2 证明重复**入队**返回同一行，不调用 worker/deliverer，不能证明重复**消费**不重复投递。
3. 用例 3 的两个 `Promise.all` claim 能证明首次 claim 基本互斥；没有并发执行外部 deliver + 终态写。
4. 用例 4 证明过期 lease 可被新 owner claim；没有让旧 owner 在抢占后完成 receipt 写，未覆盖 stale-owner fencing 缺口，也不是 SSE 断线恢复。
5. 用例 5 直接把 pending receipt 手工改为 dead_letter，再顺序 query/replay；没有错误 identity 负向断言、真实失败重试、角色授权或 replay 后实际投递。
6. 用例 6 证明成功创建后 aggregate、两个 channel 和两个 outbox 都存在，并走一次 happy-path tick；没有在 outbox/channel 写入点注入异常证明整体回滚，也没有模拟 commit 后崩溃、重复 tick 或 downstream idempotency。

fail-fast 覆盖位于 unit spec 而非这 6 个集成用例（[notification-durable-dispatch.spec.ts:18](../../packages/notification/src/server/infrastructure/runtime/__tests__/notification-durable-dispatch.spec.ts:18)），且测试手工传入 capability 状态，不启动真实 `NotificationApiModule`。所有新增 worker metrics 单测都走无 reliable adapter 的旧 repository fallback（[notification-durable-dispatch.spec.ts:63](../../packages/notification/src/server/infrastructure/runtime/__tests__/notification-durable-dispatch.spec.ts:63)）。没有 W1→W2 端到端、生产 composition root、SSE 断线/list receipt 恢复、适配器超时/权限拒绝、重复消费、stale owner completion 或 replay 不重复发送测试。

## 边界与回滚核验

- 业务变更集中在 Notification runtime/use case/repository/schema、必要的 `Account` ORM 反向关系、generated Prisma client、测试 target/inventory；未发现 W3 账号关闭或其他业务 work package 的实施。`Account.notificationDispatchOutboxes` 是 Prisma 双向 relation 所需，不视为越界业务逻辑。
- `project.json` 大部分 diff 是格式展开，generated Prisma client 体积较大，增加审查噪声但不是本次不可放行根因。
- 旧 no-op deliverer 假成功已经删除；但 API development 空 worker、直接 SSE best-effort 和 repository-only dispatch 双轨仍属于隐式降级/绕过 durable receipt 的可达路径。
- schema 使用新增表而未删除 W1 通用 outbox，回滚可以停止 worker并保留两类历史；但当前没有 W1 consumer，所谓“停 worker、保留 intent 后人工 replay”的计划回滚能力尚不可操作。

## 本地验证

当前 shell 没有 `pnpm` 可执行文件，因此使用同一仓库本地 Nx CLI 等价执行：

- `node node_modules/nx/dist/bin/nx.js run notification:test --skip-nx-cache`：**32 files / 218 tests passed**。
- `node node_modules/nx/dist/bin/nx.js run notification:test:integration --skip-nx-cache`：**1 file / 6 tests passed**。
- `node node_modules/nx/dist/bin/nx.js run notification:typecheck --skip-nx-cache`：**passed**（含 9 个依赖 target）。
- `node node_modules/nx/dist/bin/nx.js run memoflow:governance-check --skip-nx-cache`：**passed**（test inventory 962 files；package-internal boundary 642 files / 0 violations）。Nx 将 `contracts:build` 标记为 flaky，但本次自动执行最终成功，未导致门禁失败。

绿测只证明上述已断言路径，不改变 Findings 对生产 wiring、跨模块消费、SSE、fencing 和下游幂等的静态证据。

## 分级与放行决定

- **P0**：P0-1 无真实生产 channel adapter 且 API composition root 绕过 durable runtime；P0-2 W1 intent 与 W2 worker 没有 consumer/表级对接，Reminder→Notification 可靠链路中断。
- **P1**：终态 receipt 写丢失 lease/fencing且无运行中 heartbeat；deliverer 不接收幂等键，重复消费可重复发送；SSE 仍是 create use case 的进程内 best-effort；dead-letter/replay 无授权 application/API 入口；persisted 指标未记录且 telemetry 未接入；repository-only 非 durable 双轨仍可达；测试未覆盖这些关键不变量。
- **P2**：notificationId 从 occurrenceKey 字符串拆分、状态/channel 自由字符串导致协议脆弱；配置/generated diff 噪声。

**最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 至少需要先接通 W1 `notification.dispatch` consumer；在真实 API composition root 注册并校验实际 channel adapters 与 durable worker；让所有 worker 状态写保留 owner/fencing/expiry 条件并把 idempotencyKey 传至渠道 adapter；把 SSE 改为可重放 consumer并提供 receipt/list 恢复；暴露带身份/角色授权的 dead-letter/replay；补齐 persisted telemetry，以及真实 composition、跨模块、断线、故障注入和重复消费测试后再复审。

## 第二次审查

审查日期：2026-08-10。审查范围为第二次迭代对一审问题的修复，并回归 W2 已有的事务 outbox、worker claim/lease 和数据库唯一幂等能力。本节结论以当前工作区代码和本轮实跑结果为准。

### 终审结论

**W2 仍不可放行。**

第二次迭代确实补上了 worker 三条结果路径的 owner/fencing 条件写，条件失败会返回数据库中的现有 receipt；create use case 也不再直接广播 SSE；dead-letter、replay 和 receipt timeline 已进入 application port、controller 与认证路由，`persisted` 计数也有实际调用点。但这些修复尚未满足 W2 的端到端不变量：外部投递发生在 fencing 完成写之前且 deliverer 不接收 idempotency key，失租并发仍会重复发送；SSE 广播仍是 receipt 写入后的非持久进程内副作用，没有消费游标或补发标记；新增测试没有覆盖真实 HTTP 认证、错误 identity、SSE 断线补发或 replay 与仍在执行的旧 owner 竞态。

此外，一审指出的生产 capability 阻塞仍存在：真实 API 组合根调用 `createNotificationPrismaModule(db)` 时没有注入任何渠道 deliverer（[module.ts:42](../../packages/notification/src/api/module.ts:42)），仓库中也没有生产 `NotificationChannelDeliverer` 实现；production 会在基线 InApp/Push capability 检查处 fail-fast（[notification.runtime.ts:111](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:111)）。fail-fast 行为正确，但“真实生产 capability”仍未实现，直接命中完成定义。

### 一审三项逐条核验

| 一审项 | 第二次迭代证据 | 核验结果 |
| --- | --- | --- |
| 终态写 fencing；所有路径带条件，失败不重复投递并返回原 receipt | succeeded、dead-letter、retryable 三条 worker 路径都构造 `claimContext` 并传入 `recordDeliveryReceipt()`（[notification.runtime.ts:167](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:167)、[notification.runtime.ts:246](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:246)、[notification.runtime.ts:318](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:318)、[notification.runtime.ts:366](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:366)）；adapter 以 operation/idempotencyKey + ownerToken + fencingToken + running 做条件更新，失败时返回现有行（[notification-reliable-operation-prisma.adapter.ts:308](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:308)、[notification-reliable-operation-prisma.adapter.ts:324](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:324)、[notification-reliable-operation-prisma.adapter.ts:353](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:353)）。但 deliverer 在该 CAS 之前已被调用（[notification.runtime.ts:184](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:184)），接口不携带 operationId/idempotencyKey（[notification.runtime.ts:29](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:29)），也未使用 heartbeat；因此 stale owner 与新 owner 仍可各自完成一次外部发送。若新 owner 已写 succeeded，旧 owner 条件失败后拿到的也是 succeeded，runtime 只比较 status，仍会重复保存 aggregate、计 delivered 指标并广播 SSE（[notification.runtime.ts:248](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:248)）。 | **部分修复，核心“不重复投递”未满足（P1）** |
| SSE 只读、无副作用投递；断线恢复真实 | create use case 已删除直接 `pushSSEEvents`，只事务保存 aggregate/channel/outbox（[create-notification.use-case.ts:140](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:140)）。但 worker 在 succeeded receipt 之后直接向进程内 event bus 广播（[notification.runtime.ts:246](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:246)、[notification.runtime.ts:254](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:254)），表中没有 SSE consumer cursor、publishedAt 或独立事件行。进程在 receipt commit 后、event bus send 前崩溃时，该事件不会再被 claim；重启也不会扫描 succeeded 行。新增 `/receipts` 只能查询状态（[routes.ts:266](../../packages/notification/src/api/routes.ts:266)），客户端没有 Last-Event-ID/cursor 或 receipt→事件补发连接路径。 | **未满足（P1）** |
| 授权 replay 入口；identity 校验；persisted 指标真实记录 | dead-letter/replay/receipt 已进入 application port 和 auth routes，identity 仅取认证 context（[routes.ts:236](../../packages/notification/src/api/routes.ts:236)、[routes.ts:250](../../packages/notification/src/api/routes.ts:250)、[notification.controller.ts:167](../../packages/notification/src/server/transport/notification.controller.ts:167)）；adapter 查询和 replay 首读均带 identity 条件（[notification-reliable-operation-prisma.adapter.ts:380](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:380)、[notification-reliable-operation-prisma.adapter.ts:400](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:400)）。repository/adapter 的新 outbox 插入会调用共享 metrics service 的 `recordPersisted()`（[notification-prisma.repository.ts:144](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:144)、[notification-prisma.repository.ts:179](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:179)、[notification-reliable-operation-prisma.adapter.ts:125](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:125)）。不过计数发生在事务 callback 内、commit 返回前，commit 失败可产生假 persisted；且没有 exporter/生产查询面。 | **授权入口满足；persisted 调用已补，精确性/可观测性仍为 P2** |

### 回归核验

| 既有能力 | 证据与判断 | 结论 |
| --- | --- | --- |
| aggregate + channel + dispatch outbox 同事务 | repository 的 aggregate upsert、channel upsert 和 outbox create 仍位于同一个 Prisma `$transaction`（[notification-prisma.repository.ts:54](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:54)、[notification-prisma.repository.ts:144](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:144)）。 | **未回退** |
| worker lease/claim | candidate claim 仍使用 id + 旧 fencingToken + 可领取状态的 `updateMany`，成功后 fencingToken 自增并生成新 claimId（[notification-reliable-operation-prisma.adapter.ts:176](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:176)）。 | **未回退** |
| DB 唯一幂等 | `NotificationDispatchOutbox.idempotencyKey` 仍为唯一键（[notification.prisma:125](../../packages/database/prisma/schema/notification.prisma:125)），并发 create 冲突后会重读原 receipt（[notification-reliable-operation-prisma.adapter.ts:128](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:128)）。 | **未回退；仅保证不重复入队，不等于下游不重复发送** |

### 新问题与竞态

#### P0：生产组合根仍无真实渠道 adapter

`NotificationApiModule` 已不再用无依赖 runtime 覆盖 Prisma module，这是相对一审的 wiring 修正；但它仍未向 Prisma module 传入 `channelDeliverer/channelDeliverers`。当前 production 的实际结果是启动时因 InApp/Push 缺失而失败，不是计划要求的“按渠道注册真实 adapter”。同时 runtime 仍保留缺少 reliable adapter 时直接扫描 repository 的非 durable fallback（[notification.runtime.ts:394](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:394)），违反实施顺序要求的“最后删除旧 fallback”。

#### P1：SSE 存在不可恢复的 commit→broadcast 丢失窗口

当前没有独立 SSE outbox/consumer 状态。成功 receipt 一旦提交，dispatch outbox 变为 succeeded，不会再被 claim；若随后的进程内广播未发生，服务端 receipt/list 虽可查询最终状态，但客户端无法重放对应实时事件。这不是带游标的增量消费，也没有证据证明客户端重连会调用 receipt timeline 并补齐 UI。

#### P1：replay 与旧 in-flight owner 的竞态仍可重复发送

`replayDeadLetter()` 的 `dead_letter → retryable` 条件更新使并发 replay 请求本身只有一个状态转换获胜（[notification-reliable-operation-prisma.adapter.ts:414](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:414)），后续 claim 也受 fencing 保护；这一部分正确。但 lease expiry 后的旧 owner 可能仍阻塞在外部 deliverer，新 owner可以失败并写 dead-letter，人工 replay 又可启动下一次投递，而旧 owner 最后仍可能完成外部发送。现有 deliverer 契约和测试均不能对该跨 owner/in-flight 场景去重。

### 9 个集成用例真实性

本轮实跑 `notification:test:integration` 为 9/9 通过。前 6 个用例的覆盖边界与一审相同；新增 3 个用例的真实性如下：

1. 用例 7 真实验证 stale owner 的 receipt CAS 不能覆盖新 owner 的 running 行，并验证返回新 owner receipt；它没有运行两个 worker/deliverer，也没有验证外部调用次数。更关键的是没有覆盖“新 owner 已 succeeded 时，旧 owner 拿到 succeeded 后被 runtime 当成本次写成功”的分支。
2. 用例 8 的名称包含“Authorized Application/API”，实际只构造 module instance 后直接调用 `moduleInstance.api`（[notification-reliable-operation.integration.test.ts:391](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:391)）；没有启动 HTTP router、执行 auth middleware、伪造错误 identity 或验证 404 防枚举。因此它证明 application facade 可达，不证明真实授权入口。
3. 用例 9 只写一条 pending outbox 后调用 `getDeliveryReceipts()` 并断言能查到 operation（[notification-reliable-operation.integration.test.ts:440](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:440)）；没有 SSE 连接、断线、期间事件、cursor/Last-Event-ID、重连或客户端状态恢复，所以不能证明“disconnect recovery”。

9 个集成用例也没有断言 persisted 指标；用例 6 只检查 dispatched/delivered。授权负向、双 replay、replay 与 in-flight、commit 后广播崩溃窗口均未覆盖。

### 验证记录

- `notification:test`：32 files，218/218 tests 通过。
- `notification:test:integration`：1 file，9/9 tests 通过，使用本地 PostgreSQL 测试库并同步 Prisma schema。
- `notification:typecheck`：通过。
- test inventory：962 files；unit 837、integration 12、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30。
- `memoflow:governance-check`：通过。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞项 |
| --- | ---: | --- |
| P0 | 1 | 生产组合根无真实 channel deliverer/capability，production 只能 fail-fast。 |
| P1 | 2 | 外部投递去重/fencing 未闭环；SSE 无 durable cursor/补发且 replay 与旧 in-flight owner 可重复发送。 |
| P2 | 2 | persisted 指标可能先于事务 commit 计数且无生产 exporter；新增授权/SSE 测试命名超出真实覆盖。 |

**终审判定：W2 不可放行。** 需至少补齐真实 production deliverer 注册；让下游投递使用稳定 deliveryId/idempotencyKey（或等价原子 inbox/fence）并使 completion 返回“本次是否应用”的明确结果；把 SSE 改为可持久推进、可重放的独立只读 consumer；补真实 HTTP identity 负向、SSE 断线恢复和 replay/in-flight 并发集成测试后再审。

## 第三次审查

审查日期：2026-08-10。审查对象为 agy 第四次迭代，重点复核第二次审查遗留的 SSE 断线补发和 replay/in-flight fencing，并回归一审已经成立的事务 outbox、conditional claim、终态写 fencing、授权入口、数据库唯一约束和 persisted 指标。本节结论以当前工作区代码、全仓调用点搜索和本轮实跑结果为准。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

第四次迭代确实把 `replayDeadLetter()` 的 `dead_letter -> retryable` 转换、`fencingToken + 1`、lease 清理放进同一个条件更新；replay 后旧 owner 携带旧 token 的 receipt 写会被拒绝。这一项数据库不变量成立。

但“SSE 持久游标”并未实现：游标只保存在 `NotificationSseAdapter` 实例内的 `Map`，重启和跨实例均丢失；所谓补发是按 `updatedAt > timestamp` 查询 succeeded receipt，没有稳定复合游标、真实 SSE/Last-Event-ID 重连入口或跨实例 event bus。外部 deliverer 仍在 receipt CAS 前执行且不接收 deliveryId/idempotencyKey，因此 replay 增加 fencing 只能阻止旧 owner 改数据库，不能阻止仍在执行的旧 owner 完成一次外部发送。第 10、11 个集成用例均未覆盖它们名称所声称的“不双发”和“真实断线补发”。

此外，完成定义的两个既有 P0 阻塞没有变化：生产组合根仍未注册任何真实 channel deliverer，W1 的通用 `notification.dispatch` outbox 仍无 W2 consumer。即使本轮两项修复全部成立，W2 也不能据此放行。

### 二审两项逐条核验

| 二审项 | 第四次迭代证据与竞态分析 | 核验结果 |
| --- | --- | --- |
| SSE 持久游标/增量补发；重连路径真实且无事件丢失窗口 | `perIdentityCursors` 是进程内 `Map`（[notification-sse.adapter.ts:19](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:19)），broadcast 只在本实例更新 timestamp 后向进程内 event bus 发送（[notification-sse.adapter.ts:72](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:72)）。补发查询以 `updatedAt > cursorDate` 过滤、仅按 `updatedAt` 排序（[notification-reliable-operation-prisma.adapter.ts:387](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:387)），同时间戳没有 id tie-break，limit 截断也没有 continuation token。仓库没有通知 SSE endpoint、Last-Event-ID 消费或 `catchupSseEvents()` 的生产调用；HTTP 只有普通 `/receipts` JSON 查询（[routes.ts:266](../../packages/notification/src/api/routes.ts:266)）。receipt commit 后、进程内 broadcast 前崩溃时 durable 状态仍可人工查询，但实时事件不会自动重建或补发；连接到实例 A 的客户端也收不到实例 B 的进程内 broadcast。 | **未满足（P1）** |
| replay 使旧 lease 失效；`fencingToken + 1` 后旧 owner 写被拒，不双发，并覆盖 in-flight 竞态 | replay 的 `identityId + id + dead_letter + lease null/expired` 条件、状态转换、owner/claim/lease 清理和 fencing 自增位于一次 `updateMany`（[notification-reliable-operation-prisma.adapter.ts:436](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:436)）；并发 replay 只有一个转换获胜，后续 claim 还会再次自增 token。终态写要求 active owner/token/running（[notification-reliable-operation-prisma.adapter.ts:316](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:316)），所以旧 token 不能覆盖 replay 后状态。可是 deliverer 已在 receipt CAS 前调用（[notification.runtime.ts:196](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:196)），接口仍只有 aggregate/channel（[notification.runtime.ts:30](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:30)），无稳定下游幂等键；heartbeat 也没有 runtime 调用。旧 owner 若仍阻塞在外部调用，新 owner dead-letter 后 replay 可启动新发送，旧调用随后仍可成功，数据库 fencing 无法撤销该副作用。若赢家已写 succeeded，旧 owner 的失败 CAS会返回现有 succeeded，runtime 仅比较 status 后仍会保存 aggregate、重复计数并 broadcast（[notification.runtime.ts:258](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:258)）。 | **数据库失效/条件写满足；“不双发”和 in-flight 闭环未满足（P1）** |

### 回归核验

| 既有能力 | 当前证据与判断 | 结论 |
| --- | --- | --- |
| 终态写 fencing | succeeded、retryable、dead-letter 均传入 claim owner/fencing；adapter 条件更新仍要求 `running`。replay 后旧 token 写也会失败。返回值仍缺少 `applied` 标志，赢家已 succeeded 时 stale runtime 可把“读到现状”误判为本次完成。 | **数据库写未回退；runtime 判定仍有 P1 缺口** |
| aggregate/channel/outbox 同事务 | repository 仍在同一 Prisma `$transaction` 内写 aggregate、channel 和 `NotificationDispatchOutbox`（[notification-prisma.repository.ts:51](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:51)、[notification-prisma.repository.ts:144](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:144)）。 | **未回退** |
| worker lease/claim | candidate CAS 仍以旧 fencing token 和可领取状态为条件，成功时自增 token、attempt 并写 owner/claim/expiry（[notification-reliable-operation-prisma.adapter.ts:176](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:176)）。 | **未回退；heartbeat 仍未接入 worker** |
| DB 唯一幂等 | `idempotencyKey` 唯一约束仍在（[notification.prisma:126](../../packages/database/prisma/schema/notification.prisma:126)），并发插入可重读原行。 | **未回退；只防重复入队，不防下游重复发送** |
| 授权 replay 入口 | routes 使用 auth middleware，identity 只取认证 context；adapter query/replay 条件均带 identityId。 | **未回退** |
| persisted 指标 | repository/adapter 的新 outbox 插入仍调用共享 metrics service。计数仍发生在事务 callback 内、commit 返回前，也没有生产 exporter。 | **调用点未回退；精确性/可观测性为 P2** |

### 新问题：跨实例顺序与 replay 原子性

#### P1：SSE 游标不具备跨实例一致性或全序

数据库 receipt 是共享的，因此客户端若始终携带一个有效 timestamp，任一实例理论上都能执行一次增量查询；这不等于 consumer cursor 持久化。服务端游标状态是每实例私有 `Map`，live event bus 也是进程内对象。更严重的是 `updatedAt` 不是唯一游标：多个 worker/事务可以产生相同时间戳，查询使用严格 `gt`，一旦一批结果在相同 timestamp 处被 limit 截断，后续请求会永久跳过同 timestamp 的剩余行。当前没有 `(updatedAt, operationId)` 复合顺序、数据库 event sequence 或 consumer offset，不能证明多 worker 下无丢失、无乱序。

#### replay 条件更新本身原子，但不覆盖外部副作用原子性

`replayDeadLetter()` 先读后写不是理想的单语句返回实现，但决定状态转换的 `updateMany` 同时检查 identity/status/lease 并自增 fencing，因而两个并发 replay 不会都完成 `dead_letter -> retryable`。失败方重读当前行也不会覆盖赢家。此处**没有新增数据库原子性 P1**。

原子性边界止于数据库：replay、claim 和 completion 都不能与外部 channel send 形成同一个事务。正确性仍依赖下游以稳定 deliveryId/idempotencyKey 去重，或等价的渠道 inbox/fence；当前 deliverer 契约没有该信息。因此“replay 状态原子”不能推导出“replay 不重复发送”。

### 11 个集成用例真实性

本轮实跑为 11/11 通过，使用本地 PostgreSQL 测试库并同步当前 Prisma schema。前 9 个用例的覆盖边界与第二次审查一致；新增两项如下：

1. 用例 10 真实验证 replay 后 receipt 为 `retryable`、lease 映射为 null、数据库 fencing token 从 2 增至 3、旧 token 终态写返回当前 retryable receipt、下一 claim 增至 4 并可成功完成（[notification-reliable-operation.integration.test.ts:473](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:473)）。但测试没有让任何 worker 调用 deliverer，没有记录外部调用次数，没有并发执行 replay，也没有让旧 owner 在 replay/new owner 发送期间保持真实 in-flight；因此名称中的“prevents double sending”没有被测试。
2. 用例 11 在同一 runtime 和同一 `NotificationSseAdapter` 实例中发送 event 1，读取内存 Map 的 cursor，再发送单个 event 2；测试还显式等待 10ms 以错开时间戳，随后直接调用 adapter catchup 和 module application facade（[notification-reliable-operation.integration.test.ts:569](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:569)）。它没有 SSE socket、断开/重连、Last-Event-ID、进程重启、第二实例、同 timestamp、多于 limit 的分页或 broadcast 崩溃窗口；module API 也不是 HTTP auth/reconnect 路径。因此只证明“带已知 timestamp 的 receipt 增量查询可返回一个较新的 succeeded row”。

其余覆盖仍有以下边界：用例 1/2 证明入队与唯一键，不证明消费侧外部幂等；用例 3/4 证明 claim CAS 和过期接管，不运行并发 deliver；用例 5 手工写 dead-letter；用例 6 有 happy-path 事务写和 worker tick但无 rollback 故障注入；用例 7 只验证 stale receipt CAS；用例 8/9 直接调用 module facade，不启动真实 HTTP auth 或 SSE transport。11 个用例仍未覆盖 production composition root、W1 generic outbox consumer、真实 channel adapter、真实授权负向、跨实例 SSE、外部调用去重或 replay/in-flight 竞态。

### 完成定义与遗留阻塞

| 级别 | 遗留问题 | 判定 |
| --- | --- | --- |
| P0 | `NotificationApiModule` 调用 `createNotificationPrismaModule(db)`，未注入 `channelDeliverer(s)`（[module.ts:38](../../packages/notification/src/api/module.ts:38)）；全仓仍只有 deliverer interface 和测试 double。production 的 InApp/Push 检查只能 fail-fast，未具备真实生产 capability。 | **阻塞放行** |
| P0 | W1 仍把 `messageType='notification.dispatch'` 写入通用 `OutboxMessage`，channel 为 `in-app`（[prisma-reminder-write-transaction-runner.ts:136](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:136)、[prisma-reminder-write-transaction-runner.ts:150](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:150)）；W2 worker 仍只读取 `NotificationDispatchOutbox`，全仓没有生产 consumer/桥接。 | **阻塞 W1->W2 可靠闭环** |
| P1 | SSE cursor/transport 非 durable、非跨实例、非全序，commit 到 broadcast 的实时事件丢失窗口未闭合。 | **阻塞放行** |
| P1 | replay/lease fencing 不能阻止旧 in-flight 外部发送；deliverer 无 idempotency key，completion 返回值也不能区分“本次 CAS 成功”与“读取到赢家的同状态”。 | **阻塞放行** |
| P1 | reliable adapter 缺失时的 repository 直接扫描 fallback 仍可达（[notification.runtime.ts:391](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:391)），绕过 W0 receipt/outbox/lease。 | **违反删除旧 fallback 的实施顺序** |
| P2 | persisted 指标可能在事务回滚时虚增且无 exporter；timestamp cursor 对非法值静默退化；测试名称继续超出真实 transport/concurrency 覆盖；occurrenceKey 拆分和自由 channel/status 字符串仍脆弱。 | **非单独放行阻塞，但需收口** |

### 本地验证

- `notification:test:integration --skip-nx-cache`：**1 file，11/11 tests passed**。
- `notification:test --skip-nx-cache`：**32 files，218/218 tests passed**。
- `notification:typecheck --skip-nx-cache`：**passed**（含 9 个依赖 target；Nx 仍将 `contracts:build` 标记为 flaky，但自动执行成功）。
- test inventory check：**962 files**；unit 837、integration 12、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30。
- `memoflow:governance-check --skip-nx-cache`：**passed**（package-internal boundary 642 files / 0 violations）。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞摘要 |
| --- | ---: | --- |
| P0 | 2 | 无真实 production channel capability；W1 `notification.dispatch` 无 W2 consumer。 |
| P1 | 3 | SSE 非持久/非跨实例且无真实重连补发；replay/in-flight 外部发送仍可重复；非 durable repository fallback 仍可达。 |
| P2 | 4 类 | persisted 精确性/telemetry、timestamp 输入与游标稳健性、测试覆盖命名、schema/occurrenceKey 脆弱性。 |

**第三次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** replay 的数据库 fencing 修复可以保留；在下次复审前，至少要实现并接入真实 production deliverer 与 W1 intent consumer；把 SSE 补发接到真实连接生命周期并使用数据库持久、跨实例、全序且可分页的游标；把稳定 deliveryId/idempotencyKey 传到渠道边界并覆盖旧 owner/new owner/replay 的真实并发外部调用次数；删除非 durable fallback，并补 production composition、HTTP auth 负向、跨实例断线和 cursor collision 集成测试。

## 第四次审查

审查日期：2026-08-10。审查对象为 opencode 第五次迭代。本轮严格复核三审的两个 P0、既有 fencing/SSE/replay/auth 能力是否回退、共享 outbox consumer 与 Reminder worker 的竞争边界、in-app 持久化与实时广播一致性，以及集成用例 10/11/12 的真实覆盖。结论以当前工作区代码、全仓调用点和本轮实跑结果为准。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

第五次迭代修正了 API 组合根覆盖 durable runtime 的旧 wiring，并在默认 Prisma composition 中注册了名为 real 的 InApp/Desktop/Push deliverer；也新增了只筛选 `messageType='notification.dispatch'` 的共享 outbox consumer。用例 10 现在确实运行两个 worker 竞争同一个 Notification dispatch outbox，证明首次并发 claim 只调用一次测试 deliverer。

但两个 P0 都只完成了表面接线，没有达到计划完成定义。生产 InApp adapter 吞掉任意持久化异常后仍广播并返回成功，Desktop/Push adapter 只向 API 进程内 event bus 发布事件，无法到达另一个进程/设备中的 Electron main consumer；因此 capability presence check 把“对象已注册”误当成“真实生产 capability”。共享 consumer 没有 lease/owner/expiry 或 inbox receipt，崩溃会让 `running` 永久卡死；更严重的是它先执行外部 deliver/save，再创建 W2 dispatch receipt，而 receipt 的 `notificationId` 又从 W1 occurrenceKey 前缀错误推导。真实 W1 payload 下该外键通常不存在，发送副作用发生后 receipt 写失败并进入重试，存在稳定的重复投递路径。测试 12 通过预先 seed 这个本不应存在的前缀 Notification 掩盖了该错误。

### 三审两个 P0 逐条核验

| 三审 P0 | 第五次迭代证据与判断 | 核验结果 |
| --- | --- | --- |
| 生产组合根注册真实 channel adapter，并对必需渠道 fail-fast | `NotificationApiModule` 现在直接调用 `createNotificationPrismaModule(db)`，不再覆盖默认 durable contribution（[module.ts:38](../../packages/notification/src/api/module.ts:38)）；Prisma composition 注册 InApp、Desktop 和把 Push 映射到 Desktop 的 deliverer（[prisma.ts:39](../../packages/notification/src/server/infrastructure/prisma.ts:39)）。这修复了 composition wiring，默认必需渠道 presence check 也会通过。但 `RealInAppChannelDeliverer` 捕获并吞掉所有 repository save 异常，随后仍发布 in-app 事件（[real-channel-deliverers.ts:25](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:25)）；Desktop/Push 只向当前进程的 event bus 发送 desktop event（[real-channel-deliverers.ts:58](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:58)），实际 native consumer 位于 Desktop main 进程（[notification.service.ts:216](../../apps/desktop/src/main/services/notification.service.ts:216)），仓库没有 API→Desktop 的跨进程/跨设备 transport。启动检查只判断 registry 中是否存在对象，无法识别上述 silent-success/local-only adapter。 | **形式 wiring/fail-fast 已接入；真实 production capability 未满足（P0）** |
| W1 `notification.dispatch` consumer 领用共享 outbox、幂等，并将成功/失败/重试落库 | consumer 正确按 messageType/status/availableAt 选候选，并用 `id + status + attempts` conditional update 防止两个 Notification worker 首次同时拿到一行（[notification-reliable-operation-prisma.adapter.ts:482](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:482)、[notification-reliable-operation-prisma.adapter.ts:500](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:500)）；成功、retryable、dead-letter 会回写共享表。但共享 schema 没有 owner/claim/lease/expiry，claim 写成 `running` 后进程崩溃没有恢复条件，completion 也是只按 id 的无 fencing update（[notification-reliable-operation-prisma.adapter.ts:527](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:527)）。runtime 先调用 deliverer、保存 aggregate，之后才插入 dispatch outbox/receipt（[notification.runtime.ts:452](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:452)、[notification.runtime.ts:462](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:462)），没有 inbox receipt 在副作用前去重。`dispatchOutbox()` 又把 occurrenceKey 第一个冒号前内容当作 notification FK（[notification-reliable-operation-prisma.adapter.ts:101](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:101)），而新建 aggregate 自己生成另一个 id（[notification.ts:367](../../packages/notification/src/server/domain/aggregates/notification.ts:367)）。真实 Reminder occurrenceKey 不是该新 aggregate id，因而可能在外部发送后触发 FK 错误并 retry。失败/retry 也只留共享表自由字符串状态，没有相应 W0 BusinessOperationReceipt。 | **未满足 lease/claim、幂等和统一 receipt（P0）** |

### 既有能力回归

| 既有能力 | 当前证据与判断 | 结论 |
| --- | --- | --- |
| 终态写 fencing | Notification dispatch outbox 的 completion 仍要求 operation/idempotencyKey、owner、fencingToken 和 `running` 状态（[notification-reliable-operation-prisma.adapter.ts:308](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:308)）。 | **数据库不变量未回退；共享 W1 consumer 未复用该不变量** |
| SSE 游标补发 | 新增真实 `/sse` HTTP route，读取认证 identity 与 Last-Event-ID，并从 durable receipt query 补发（[routes.ts:286](../../packages/notification/src/api/routes.ts:286)）。底层仍以非唯一 `updatedAt > timestamp` 查询、单字段排序和 limit 截断（[notification-reliable-operation-prisma.adapter.ts:387](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:387)），服务端 cursor 仍是实例内 Map。 | **入口有进展、既有查询未回退；跨实例/同时间戳/分页无丢失仍未闭环（P1）** |
| replay 失效旧 owner | replay 仍在 identity/status/lease 条件下清 owner/claim/lease 并自增 fencing；旧 token 的 completion 仍会失败。 | **未回退；外部 in-flight 副作用仍不能被数据库 fencing 撤销（P1）** |
| 授权运维入口 | dead-letter、replay、receipt 和 SSE routes 均挂 auth middleware，identity 来自认证 context；replay adapter 仍按 identityId 限定。 | **未回退** |

### 新问题与一致性核验

#### 共享 consumer 与 Reminder worker 不会领用同一行，但共享 consumer 自身不可恢复

全仓调用点显示 Reminder worker 不读取或 claim `OutboxMessage`；它只在 occurrence 的事务内创建 `notification.dispatch` 行（[prisma-reminder-write-transaction-runner.ts:123](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:123)）。因此“Notification consumer 与 Reminder worker 同时领用同一表/同一行”的直接竞争**不存在**：前者是消费者，后者是生产者。

真正的竞争发生在多个 Notification runtime 之间。`id + status + attempts` CAS 能保证 pending/retryable 的首次领用互斥，但 schema 没有 lease owner/expiry，任何 worker 在 `running` 后崩溃都会让消息永久不可再领；也没有 stale completion fence。该实现不满足任务明确要求的共享 outbox lease/claim，更不能由“没有 Reminder 竞争者”推导为可靠。

#### P1：InApp 持久化、receipt 与 SSE 广播次序不一致且重复广播

durable worker 调用 `RealInAppChannelDeliverer.deliver()` 时，deliverer 先保存 aggregate 并立即广播；返回后 runtime 才把 channel 标为 delivered、写 succeeded receipt、再次保存 aggregate，再由 `NotificationSseAdapter` 广播同一个 in-app 事件（[notification.runtime.ts:212](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:212)、[notification.runtime.ts:268](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:268)、[notification.runtime.ts:276](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:276)）。结果是一次 dispatch 至少可产生两个实时事件；第一个事件发生时 receipt 仍为 running、列表中的 channel 也可能仍为 Pending。若第一次 save 失败，adapter 还会吞错并广播。

W1 路径另有 id 分裂：新建 aggregate 的真实 id 由 domain factory 生成，但后续 receipt/SSE payload 使用另一个随机 `notifId`（[notification.runtime.ts:423](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:423)、[notification.runtime.ts:431](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:431)、[notification.runtime.ts:482](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:482)）。列表对象、实时事件和 receipt 因而可能引用不同 notification id。当前不能证明计划要求的“in-app 列表与实时事件最终一致”。

#### P1：旧的非 durable fallback 与外部幂等缺口仍在

reliable adapter 缺失时直接扫描 repository/channel 的旧 fallback 仍可达（[notification.runtime.ts:526](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:526)），继续绕过 W0 receipt/outbox/lease。`NotificationChannelDeliverer` 接口也仍只接收 aggregate/channel，不接收稳定 deliveryId/idempotencyKey；测试 10 证明的是首次并发 claim，不覆盖 lease expiry、进程崩溃、旧 in-flight owner 或 replay 后的外部调用去重。

#### P1：新增 SSE route 违反 typed event bus 治理门禁

本轮实跑 governance 并未通过。`routes.ts` 直接调用 raw `eventBus.on()`/`off()`（[routes.ts:321](../../packages/notification/src/api/routes.ts:321)、[routes.ts:324](../../packages/notification/src/api/routes.ts:324)），被 `raw-event-bus-audit` 明确拒绝；应使用仓库规定的 typed subscriber。背景中“governance 通过”的陈述与当前工作区不符，完成定义所要求的发布门禁未满足。

### 用例 10/11/12 真实性

本轮实跑 `notification:test:integration --skip-nx-cache` 为 **12/12 passed**。新增/重写用例的真实覆盖如下：

1. **用例 10：真实，但覆盖范围有限。** 它创建两个独立 runtime/owner，对同一个 durable Notification dispatch outbox 并发调用 `tick()`，并以共享计数器断言 deliverer 只被调用一次（[notification-reliable-operation.integration.test.ts:474](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:474)）。这真实证明首次 pending claim 的 CAS 互斥；它没有覆盖共享 W1 outbox consumer、lease expiry、旧 owner in-flight 或 replay 竞争。
2. **用例 11：建立了真实 HTTP SSE 连接，但没有真实驱动“断线窗口补发”。** 测试确实启动 HTTP server、断开第一条连接并携带 Last-Event-ID 重连（[notification-reliable-operation.integration.test.ts:530](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:530)）。然而断线期间只调用 `dispatchOutbox()` 写入 pending 行，等待 10ms 后立刻重连，没有在重连前运行 worker或等待 event 2 receipt 变为 succeeded（[notification-reliable-operation.integration.test.ts:599](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:599)）。默认 poll interval 为 2 秒，event 2 会在重连连接已建立后由后续 worker tick 作为 live event 到达；断言也只检查收到 Event 2，未区分 catch-up receipt 与 live payload。因此它没有证明 disconnected-window catch-up。
3. **用例 12：调用了真实 W1 transaction runner，但人为 seed 掩盖了真实 consumer 外键/id 问题。** 测试在 W1 runner 之前创建 `notif_cross_mod`，并把 occurrenceKey 人为设为 `${notificationId}:${Date.now()}`（[notification-reliable-operation.integration.test.ts:675](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:675)）。consumer 的错误 occurrenceKey-prefix FK 推导于是恰好命中 seed row；实际 consumer 新建的是另一个随机 id 的 Notification。真实 W1 occurrence 不会预先创建这条 Notification，因此 12/12 不能证明生产 W1→W2 路径可成功，也没有使用生产 deliverer。

### 本地验证

由于当前非登录 shell 未暴露 `pnpm`，本轮使用等价的工作区 `./node_modules/.bin/nx` 执行 Nx targets：

- `notification:test:integration --skip-nx-cache`：**1 file，12/12 tests passed**。
- `notification:test --skip-nx-cache`：**32 files，218/218 tests passed**。
- `notification:typecheck --skip-nx-cache`：**passed**（含 11 个依赖 target；Nx 仍将 `contracts:build` 标记为 flaky，但本轮执行成功）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 tests passed**，W1 无回退。
- test inventory check：**962 files**；unit 837、integration 12、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30。
- `memoflow:governance-check --skip-nx-cache`：**failed**；`raw-event-bus-audit` 报告 `routes.ts:321` 的 raw `eventBus.on()` 和 `routes.ts:324` 的 raw `eventBus.off()` 两项违规。其余已执行的治理子检查通过。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞摘要 |
| --- | ---: | --- |
| P0 | 2 | 注册对象不等于真实生产 channel capability，InApp 会吞持久化错误且 Desktop/Push 只是 API 进程内事件；W1 consumer 无 lease/inbox/fencing，真实 occurrenceKey 下还会在外部副作用后因错误 notification FK 失败。 |
| P1 | 5 | InApp 重复广播且列表/receipt/event 顺序不一致；SSE cursor 非跨实例全序且用例 11 未覆盖真实补发；外部 deliverer 无稳定幂等键/replay in-flight 仍可双发；非 durable fallback 仍可达；新增 SSE route 导致 governance gate 失败。 |
| P2 | 3 类 | persisted 指标仍可能在事务 commit 前虚增且无 exporter；自由 channel/status 与 occurrenceKey 解析脆弱；测试名称/断言仍超出真实故障边界。 |

**第四次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 下一次复审至少需要：让生产 channel adapter 的成功语义对应真实可达且可确认的副作用，禁止吞持久化错误；把 W1 consumer 改成可过期恢复的 lease/claim 或原子 inbox consumer，并在任何外部副作用前建立稳定 idempotency fence，显式传递真实 notificationId/deliveryId；去掉 deliverer 与 SSE adapter 的双广播；让测试 11 在断线期间先完成 durable receipt 再重连并断言 catch-up payload，让测试 12 不预置 Notification、使用真实 W1 occurrenceKey 和生产 composition；同时删除非 durable fallback、用 typed event subscriber 修复 governance gate，并保留现有终态/replay fencing 与授权入口。

## 第五次审查

审查日期：2026-08-10。审查对象为 opencode 第六次迭代。本轮先逐项核验四审要求的 governance、用例 11 和用例 12 三项修复，再回归生产 adapter、W1 dispatch consumer、终态 fencing、SSE 游标、replay 和授权入口，并检查 port 化改造的抽象边界及新增测试的时序稳定性。结论以当前工作区代码、全仓调用点和本轮实跑结果为准。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

第六次迭代真实修复了本轮指定的三项问题：SSE route 已经通过 application port 订阅，route 内不再残留 raw `eventBus.on/off`，governance 实跑为 642/0；用例 11 现在确实在第一条连接断开期间完成第二条 durable receipt，再携带 `Last-Event-ID` 重连并断言收到 succeeded receipt；用例 12 已删除掩盖性 Notification seed，使用真实 W1 occurrenceKey，并断言 consumer 自己创建的 Notification id 同时成为 dispatch outbox 的有效 FK。用例 11/12 额外独立运行 5 轮，均为 2/2 passed。

但这三项修复没有处理四审的两个 P0 根因。默认生产 InApp deliverer 仍吞掉持久化异常后广播成功，Desktop/Push 仍只是 API 进程内 event bus 事件；W1 shared outbox consumer 仍没有 lease/expiry/inbox fence，且仍在建立任何 durable idempotency fence 之前执行 deliver/save。测试 12 证明了 FK/id 修正，不等于证明该 consumer 已具备崩溃恢复与 exactly-once side-effect 语义。计划完成定义要求 P0 fail-closed、可重试且具备真实生产 capability，当前仍不满足。

### 四审三项逐条核验

| 四审要求 | 当前证据与判断 | 核验结果 |
| --- | --- | --- |
| governance 违规修复：SSE route 走 port，且无残留 raw event bus | `/sse` route 现在只调用 `api.subscribeSseEvents()` 并在连接关闭时调用返回的 unsubscribe（[routes.ts:312](../../packages/notification/src/api/routes.ts:312)）；订阅实现封装在 `NotificationSseAdapter.subscribe()` 的 typed subscriber 后（[notification-sse.adapter.ts:31](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:31)、[notification-sse.adapter.ts:44](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:44)）。全仓定向搜索确认 notification API route 无 raw `eventBus.on/off`；本轮 governance 实跑通过，package-internal boundary 为 642 files / 0 violations，raw-event-bus audit 也为 0。 | **通过** |
| 用例 11：真实 SSE 断线、事件、游标重连和补发 | 测试建立第一条真实 HTTP SSE 连接并取得 event 1 cursor，随后销毁连接（[notification-reliable-operation.integration.test.ts:573](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:573)、[notification-reliable-operation.integration.test.ts:608](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:608)）；断线期间写入 event 2 outbox、显式 `runtime.tick()`，并在重连前查询和断言其 receipt 已为 `succeeded`（[notification-reliable-operation.integration.test.ts:611](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:611)、[notification-reliable-operation.integration.test.ts:628](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:628)）；第二条连接携带 `Last-Event-ID`，最终断言 payload 同时包含 event 2 operationId 与 `succeeded`（[notification-reliable-operation.integration.test.ts:634](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:634)、[notification-reliable-operation.integration.test.ts:656](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:656)）。这次确实证明了该顺序路径上的 durable catch-up，而不是把 reconnect 后的 live event 当作补发。 | **通过；覆盖的是单实例、单页、时间戳不碰撞路径** |
| 用例 12：去掉掩盖性预置，真实 consumer 自证 FK/id | 测试使用 Reminder aggregate 和 `PrismaReminderWriteTransactionRunner` 生成真实 `${templateId}:${triggerTimeIso}` occurrenceKey 与 pending `notification.dispatch`（[notification-reliable-operation.integration.test.ts:667](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:667)、[notification-reliable-operation.integration.test.ts:758](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:758)），不再预置任何 Notification；consumer 创建 aggregate 后把真实 `createdNotif.id` 作为显式 `notificationId` 交给 dispatch adapter（[notification.runtime.ts:430](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:430)、[notification.runtime.ts:447](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:447)、[notification.runtime.ts:466](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:466)）。测试最终从 repository 找到新 aggregate，并断言 succeeded dispatch outbox 的 FK 等于该 id、且不等于 templateId（[notification-reliable-operation.integration.test.ts:802](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:802)）。 | **通过；FK/id 缺陷已修复** |

### 既有能力回归

| 既有能力 | 当前证据与判断 | 结论 |
| --- | --- | --- |
| 生产 composition 与 adapter 注册 | API composition 仍直接创建并启动默认 Prisma module（[module.ts:41](../../packages/notification/src/api/module.ts:41)），默认 registry 仍注册 InApp/Desktop/Push（[prisma.ts:39](../../packages/notification/src/server/infrastructure/prisma.ts:39)）。但 InApp 吞 save 异常，Desktop/Push 只有进程内事件，故“已注册”仍不等于真实 production capability。 | **接线未回退；能力语义仍为 P0 阻塞** |
| W1 dispatch consumer | consumer 仍筛选 `messageType='notification.dispatch'` 并处理 pending/retryable，且本轮修正了 Notification FK/id。Reminder integration 26/26 通过。 | **消费入口与 FK 修复未回退；lease/inbox/idempotency 仍为 P0 阻塞** |
| 终态 fencing | Notification dispatch completion 仍以 operation/idempotencyKey、ownerToken、fencingToken 和 `running` 状态做 conditional update（[notification-reliable-operation-prisma.adapter.ts:304](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:304)）。 | **未回退** |
| SSE 游标与补发 | authenticated `/sse`、Last-Event-ID 和 durable succeeded receipt catch-up 均保留；用例 11 的顺序路径已真实覆盖。底层仍只使用 `updatedAt > timestamp`、单字段排序和 limit（[notification-reliable-operation-prisma.adapter.ts:373](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:373)）。 | **基础能力未回退；全序/分页/跨实例仍未闭环** |
| replay | replay 仍按 identity/status/lease 条件清 owner 并递增 fencing token，旧 owner completion 仍会被拒绝。 | **未回退；外部 in-flight 副作用仍不可撤销** |
| 授权入口 | dead-letter、replay、receipts 与 SSE routes 仍挂 auth middleware，adapter 查询/replay 仍按 identityId 过滤。 | **未回退** |

### 遗留阻塞与本轮新问题

#### P0：默认生产 adapter 仍不具备可确认的真实 capability

`RealInAppChannelDeliverer` 仍捕获并吞掉任意 repository save 异常，之后照常发布事件并 resolve（[real-channel-deliverers.ts:25](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:25)）。Desktop/Push adapter 仍只向当前 Node 进程的 singleton event bus 发布 desktop event（[real-channel-deliverers.ts:50](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:50)），而实际 native notification listener 位于 Electron main 进程（[notification.service.ts:214](../../apps/desktop/src/main/services/notification.service.ts:214)）。API 与 Desktop 不共享进程内 bus；Push 还被映射到同一个 Desktop adapter。生产 capability presence check 因对象存在而通过，但无法证明副作用可达或可确认，仍违反完成定义。

#### P0：W1 shared consumer 修正了 FK，但仍会在 durable fence 前产生副作用且崩溃不可恢复

共享 `OutboxMessage` schema 仍没有 owner、claim、lease expiry 或 fencing 字段，虽然仓库已经有 `InboxReceipt`，该 consumer 并未使用（[reliable-messaging.prisma:9](../../packages/database/prisma/schema/reliable-messaging.prisma:9)、[reliable-messaging.prisma:31](../../packages/database/prisma/schema/reliable-messaging.prisma:31)）。claim 只将候选从 pending/retryable CAS 到 `running`（[notification-reliable-operation-prisma.adapter.ts:480](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:480)）；进程在此后崩溃会永久卡住，completion 也只按 id 更新。runtime 仍先调用 deliverer、标记 channel 并保存 aggregate，之后才创建/取得 dispatch receipt（[notification.runtime.ts:451](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:451)、[notification.runtime.ts:462](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:462)、[notification.runtime.ts:466](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:466)）。因此“外部发送成功后、receipt/shared status 前崩溃”仍会在人工恢复或未来重领时重复发送；测试 12 只证明 FK/id 正确，没有故障注入这个窗口。

#### P1：SSE port 修复治理语法，但没有建立无丢失的 durable subscription 边界

route 的 catch-up query 发生在 live subscribe 之前（[routes.ts:298](../../packages/notification/src/api/routes.ts:298)、[routes.ts:313](../../packages/notification/src/api/routes.ts:313)）；两者之间完成的 receipt 既不在已结束的 query 中，也可能早于 subscriber 注册，形成明确丢失窗口。cursor 仍是非唯一 timestamp，`updatedAt > cursor` 配合单字段排序和固定 limit 100 会在同毫秒碰撞或积压超过一页时永久跳过事件。port 背后仍是进程内 global event bus，因此实时段不跨实例。

此外，默认 InApp deliverer 在 durable receipt 写入前发布一次 event，worker 在 receipt 写入后又由 SSE adapter 发布一次（[real-channel-deliverers.ts:33](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:33)、[notification.runtime.ts:268](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:268)、[notification.runtime.ts:276](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:276)）。route 经 port 会同时收到两类事件，所以“SSE 只是 outbox consumer”在生产 composition 下仍不成立；用例 11 注入 no-op mock deliverer，避开了这条生产双广播路径。

#### P1：既有外部幂等与非 durable fallback 缺口仍在

`NotificationChannelDeliverer.deliver()` 仍只接收 aggregate/channel，不携带 deliveryId/idempotencyKey（[notification.runtime.ts:31](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:31)），数据库 fencing 无法阻止 lease expiry 或 replay 后旧 in-flight owner 的外部重复发送。reliable adapter 缺失时的 repository channel scan fallback 仍可达（[notification.runtime.ts:533](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:533)），继续绕过统一 receipt/outbox/lease，违反计划规定的“最后删除旧 fallback”。

#### P2：port 化引入 duck typing、silent no-op 与 transport 命名泄漏

`NotificationApplicationPort` 直接暴露 `NotificationSseDeliveryEvent` 和 `subscribeSseEvents()`（[notification.application.port.ts:9](../../packages/notification/src/server/application/notification.application.port.ts:9)、[notification.application.port.ts:43](../../packages/notification/src/server/application/notification.application.port.ts:43)）；module 实现再通过 `as unknown`/`'getSseAdapter' in rc` 探测 infrastructure runtime，找不到 adapter 时静默返回 no-op unsubscribe（[notification.module.ts:258](../../packages/notification/src/server/infrastructure/notification.module.ts:258)）。这让 transport 协议进入通用 application port，也绕开了可静态检查的明确 runtime capability。它不是本轮单独的 P0，但属于 port 化新增的抽象泄漏，应改成显式注入、强类型且 fail-closed 的 subscription/cursor port。

#### P2：用例 11 仍有结构性时序风险；用例 12 未发现时序 flake

用例 11 在 `http.get()` 后没有等待服务端 route 完成 subscriber 注册，便立刻写 event 1 并执行 `runtime.tick()`（[notification-reliable-operation.integration.test.ts:573](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:573)、[notification-reliable-operation.integration.test.ts:590](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:590)）；首次 event 理论上可跑在订阅建立之前。断线后又用固定 `setTimeout(20)` 代替等待服务端确认 close/unsubscribe（[notification-reliable-operation.integration.test.ts:608](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:608)）。本轮额外 5 轮均通过，尚未观察到实际 flake，但同步条件不显式，CI 负载下仍有风险。用例 12 的关键顺序均由 awaited DB 操作和 `runtime.tick()` 驱动，本轮 5 轮未发现时序风险。

### 本地验证

当前非登录 shell 未暴露 `pnpm`，本轮使用等价的工作区 `./node_modules/.bin/nx` 执行 Nx targets：

- `notification:test:integration --skip-nx-cache`：**1 file，12/12 tests passed**。
- 用例 11/12 定向独立重复 5 轮：**每轮 2/2 passed，共 10/10**。
- `notification:test --skip-nx-cache`：**32 files，218/218 tests passed**。
- `notification:typecheck --skip-nx-cache`：**passed**（含 11 个依赖 target；Nx 仍将 `contracts:build` 标记为 flaky，但本轮执行成功）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 tests passed**。
- test inventory check：**962 files**；unit 837、integration 12、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30。
- `memoflow:governance-check --skip-nx-cache`：**passed**；package-internal boundary 642 files / 0 violations，raw-event-bus audit 2805 files / 0 exemptions。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞摘要 |
| --- | ---: | --- |
| P0 | 2 | 默认 production channel adapter 仍会 silent success/local-only；W1 consumer 无 lease/inbox/fencing，并在 durable idempotency fence 前执行副作用。 |
| P1 | 3 | SSE query→subscribe 有丢失窗口、timestamp/limit cursor 非全序且生产双广播；deliverer 无稳定外部幂等上下文；非 durable repository fallback 仍可达。 |
| P2 | 3 类 | SSE transport/duck typing/no-op 泄漏进 application port；用例 11 仍依赖隐式连接时序与固定 sleep；legacy occurrenceKey 推导和自由 channel/status 字符串仍脆弱。 |

**第五次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 本轮三项指定修复可以保留，但在放行前仍必须：把生产 channel capability 改为真实可达、可确认且不吞错误；让 W1 consumer 使用可过期 lease 或原子 inbox fence，并在任何外部副作用前建立稳定幂等记录；向 deliverer 传递 deliveryId/idempotencyKey；把 SSE catch-up 与 live subscription 组合成无窗口、可分页、全序且跨实例的 durable 边界，删除生产双广播；删除非 durable fallback，并用显式强类型 port 取代 runtime duck typing/no-op。测试应补生产 composition、W1 consumer 崩溃窗口、SSE query→subscribe 竞态、同 timestamp/超过 100 条分页以及显式连接/断线握手。

## 第六次审查

审查日期：2026-08-10。审查对象为任务说明所称的 opencode 第 7--10 次迭代及少量测试辅助修正。本轮按五审的 2 个 P0、3 个 P1 根因逐条反向核验当前代码、生产 composition、调用顺序和实际测试内容，并回归前四轮已经成立的事务 outbox、dispatch lease/fencing、授权运维入口和 W1→W2 基础消费链路。结论以当前工作区代码和本轮实跑结果为准；任务说明中的修复摘要不作为已完成证据。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

本轮确有三项代码进展：`NotificationChannelDeliverer` 边界新增了 `deliveryId/idempotencyKey`；W1 shared intent 会在渠道调用前创建唯一的 `NotificationDispatchOutbox`；receipt 查询也增加了 `(updatedAt, id)` 复合排序条件。但是五审根因没有按生产语义闭环：默认 InApp adapter 仍吞持久化异常后广播成功，Desktop/Push 仍只是 API 进程内事件；shared consumer 没有持久化 owner/claim/fencing，所谓 lease 只是复用 `availableAt`，完成写仍只按 id；真实 adapter 不接收/使用新增幂等上下文；SSE route 仍然先 query 后 subscribe、只补一页并发送非全序时间戳 cursor；显式 SSE/reliable port 也没有落地，duck typing 和 silent no-op 仍在。

更关键的是，当前 12 个集成用例中没有新增 shared consumer 崩溃窗口、SSE query→subscribe 竞态、同时间戳碰撞或超过 100 条分页用例。背景所称“崩溃窗口测试真实”“SSE 无窗口组合、全序可分页”“显式 port”与当前测试清单和实现不符。因此 218/218、12/12 及治理门禁全绿只能证明既有覆盖通过，不能证明这些 exactly-once / 无丢失不变量成立。

### 五审 2 P0 + 3 P1 根因核验

| 五审根因 | 当前代码证据与判断 | 核验结果 |
| --- | --- | --- |
| **P0-1：生产 deliverer 必须真实可达、可确认且不吞错误** | API 仍使用默认 Prisma composition（[module.ts:41](../../packages/notification/src/api/module.ts:41)），registry 也仍注册 InApp/Desktop/Push（[prisma.ts:39](../../packages/notification/src/server/infrastructure/prisma.ts:39)）。但 `RealInAppChannelDeliverer` 在 repository save 失败时执行空 `catch`，随后无条件广播并 resolve（[real-channel-deliverers.ts:25](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:25)、[real-channel-deliverers.ts:33](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:33)）。Desktop deliverer 仍只向 singleton event bus 发布事件（[real-channel-deliverers.ts:50](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:50)），Push 仍映射到同一个 Desktop adapter（[prisma.ts:42](../../packages/notification/src/server/infrastructure/prisma.ts:42)）；真正 native listener 位于 Electron main 进程（[notification.service.ts:214](../../apps/desktop/src/main/services/notification.service.ts:214)），没有 API→Desktop/设备的可确认 transport。presence check 只能看到对象存在，不能把 local-only adapter 变成生产 capability。 | **未闭环，P0 保留** |
| **P0-2：shared consumer 有可过期 lease/fencing，且任何副作用前建立 durable 幂等记录并使用显式 notificationId** | 显式 id 已修复：consumer 使用 shared message id/显式 payload id 创建确定性 Notification，并显式传给 `dispatchOutbox()`（[notification.runtime.ts:543](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:543)、[notification.runtime.ts:574](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:574)）；adapter 缺 id 会 fail closed，不再从 occurrenceKey 前缀推导（[notification-reliable-operation-prisma.adapter.ts:138](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:138)）。durable dispatch row 也确实在标准渠道调用前创建（[notification.runtime.ts:572](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:572)、[notification.runtime.ts:631](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:631)）。但 shared `OutboxMessage` schema 仍没有 owner、claimId、fencingToken 或 leaseExpiresAt（[reliable-messaging.prisma:9](../../packages/database/prisma/schema/reliable-messaging.prisma:9)）；claim 仅用 status/attempt CAS 并把 `availableAt` 当超时（[notification-reliable-operation-prisma.adapter.ts:543](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:543)），完成写仍是只按 id 的无条件 update（[notification-reliable-operation-prisma.adapter.ts:610](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:610)）。更重要的是，标准 worker 在外部 `deliver()` 返回后才写 succeeded receipt（[notification.runtime.ts:306](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:306)、[notification.runtime.ts:362](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:362)）；此窗口崩溃/失租后仍会重调 deliverer，durable row 本身不是外部副作用的完成凭证。用例 12 只执行一次无错 mock deliverer 并断言 happy-path succeeded（[notification-reliable-operation.integration.test.ts:811](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:811)），没有在 fence 后、deliver 后或 shared completion 前注入崩溃。 | **notificationId 与副作用前 intent 有进展；lease/fencing 与 exactly-once 崩溃语义未闭环，P0 保留** |
| **P1-1：deliverer 收到稳定 deliveryId/idempotencyKey，重领/replay 不重复外部发送** | interface 和 runtime 调用已新增稳定 context（[notification.runtime.ts:27](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:27)、[notification.runtime.ts:300](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:300)），这是实质接线进展。但两个生产 deliverer 的实现签名仍只有 notification/channel，完全忽略第三个参数（[real-channel-deliverers.ts:18](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:18)、[real-channel-deliverers.ts:51](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:51)），因而没有任何下游 dedupe/ack。若旧 owner 调用成功后失租，新 owner仍可再次产生副作用；旧 owner的 completion CAS若读回赢家的 `succeeded`，runtime还会仅按 status 再保存、计数并广播（[notification.runtime.ts:362](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:362)、[notification.runtime.ts:364](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:364)）。当前并发用例 10 只验证 claim 前竞争，不覆盖阻塞中的旧 owner、新 owner和真实 adapter 调用次数。 | **接口接线完成；生产幂等消费未实现，P1 保留** |
| **P1-2：SSE query+live 无窗口、全序可分页、无生产双广播** | repository 查询已改为 `(updatedAt, id)` 复合条件和双字段排序（[notification-reliable-operation-prisma.adapter.ts:431](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:431)），但 HTTP SSE 仍先 query、后 subscribe（[routes.ts:298](../../packages/notification/src/api/routes.ts:298)、[routes.ts:313](../../packages/notification/src/api/routes.ts:313)），两者之间的 receipt 仍会丢。route 固定 `limit: 100` 且没有翻页循环，只把 `receipt.updatedAt` 写成 event id（[routes.ts:299](../../packages/notification/src/api/routes.ts:299)、[routes.ts:307](../../packages/notification/src/api/routes.ts:307)），live event id 又是新的 wall-clock 时间（[routes.ts:315](../../packages/notification/src/api/routes.ts:315)），并未使用 adapter 已定义的复合 cursor。InApp 生产 deliverer仍先广播一次，receipt 持久化后 SSE adapter再广播一次（[real-channel-deliverers.ts:33](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:33)、[notification.runtime.ts:368](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:368)）。用例 11 的真实断线补发路径成立，但它注入 no-op mock deliverer，且没有制造 query→subscribe 事件、同 timestamp 或 101+ receipts（[notification-reliable-operation.integration.test.ts:563](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:563)）。 | **底层排序有进展；无窗口、SSE cursor 分页和单广播均未闭环，P1 保留** |
| **P1-3：删除非 durable fallback，runtime 必须 fail-fast，显式 port 取代 duck typing/no-op** | repository channel-scan fallback 已删除，tick 只走 reliable adapter（[notification.runtime.ts:485](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:485)），此项没有回退。但缺 reliable adapter 的异常抛在 tick 的 `try` 内，随后被同一 tick 的 catch 记录并吞掉（[notification.runtime.ts:489](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:489)、[notification.runtime.ts:665](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:665)）；`start()` 又以 `void tick()` 启动并正常返回（[notification.runtime.ts:674](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:674)），所以它不是 startup fail-fast。application module 仍用 `as unknown`/属性探测找 runtime，并在 receipt/SSE capability 缺失时返回空数组或 no-op unsubscribe（[notification.module.ts:245](../../packages/notification/src/server/infrastructure/notification.module.ts:245)、[notification.module.ts:258](../../packages/notification/src/server/infrastructure/notification.module.ts:258)）。默认 Prisma composition 会注入 reliable adapter，故正常默认路径没有被误拒；但显式 runtime override 仍可启动一个静默不工作的 worker。 | **旧扫描 fallback 已删；fail-fast 和显式 port 未闭环，P1 保留** |

### exactly-once 副作用语义专项判断

当前实现满足的是“**在调用渠道前持久化唯一 delivery intent**”，不满足通用的“**外部副作用 exactly once**”。`NotificationDispatchOutbox.idempotencyKey` 唯一约束能防止重复创建任务，claim/terminal write 的 owner+fencing 条件也能防止旧 owner 覆盖数据库终态；但它们不能撤销或确认已经发生的外部副作用。要闭合 `deliver success -> receipt commit` 窗口，真实渠道必须以传入的 idempotencyKey 做下游幂等/ack，或把副作用与 receipt 放进同一原子事务。当前 InApp/desktop 实现均未使用 context，且 InApp 还在 receipt 前广播。因此本轮不能宣称 exactly-once，最多是数据库任务的 at-least-once + conditional completion。

任务说明所称的“崩溃后 lease 过期可重领”只在 `NotificationDispatchOutbox` 的基础 lease 用例中有覆盖（[notification-reliable-operation.integration.test.ts:172](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:172)）；没有测试 shared `OutboxMessage` claim 后崩溃，也没有测试 deliverer 已产生副作用但 receipt 尚未提交的窗口。两者不能混为同一个 exactly-once 证明。

### 前四轮已实现项回归

| 已有能力 | 当前状态 | 回归判断 |
| --- | --- | --- |
| 创建 Notification aggregate/channel/dispatch outbox 同事务 | 用例 6 仍覆盖同事务写入，repository 仍以真实 notificationId 建 outbox FK。 | **未回退** |
| `NotificationDispatchOutbox` claim/expiry/fencing 与 stale completion | claim 仍以 fencing token CAS 并写 owner/claim/expiry（[notification-reliable-operation-prisma.adapter.ts:239](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:239)）；completion 仍要求 owner/fencing/running（[notification-reliable-operation-prisma.adapter.ts:361](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:361)）。用例 3/4/7/10 通过。 | **未回退** |
| identity-scoped dead-letter/replay/receipt API | 认证 routes 和 identity 参数过滤仍在；用例 5/8/9 通过。 | **未回退** |
| W1 real occurrenceKey→W2 consumer 与显式 Notification FK | 用例 12 使用真实 Reminder runner、无预置 Notification 并通过。 | **未回退；不代表崩溃语义闭环** |
| SSE 真实断线 + Last-Event-ID 顺序补发 | 用例 11 确实在断线期间持久化 succeeded receipt 后重连并收到补发。 | **未回退；只覆盖单实例单页无碰撞顺序** |
| 指标与治理门禁 | persisted/dispatched/delivered/failed/retry/dead-letter 调用点保留；本轮 governance 通过。 | **未回退** |

### 新问题与 fail-fast / SSE 分页专项

1. **P1：reliableAdapter “fail-fast” 实际被 worker 自己吞掉。** 默认生产 composition 当前会注入 adapter，因此没有误伤默认正常路径；但 `runtimeContributions` override 可以替换掉默认 durable contribution（[prisma.ts:58](../../packages/notification/src/server/infrastructure/prisma.ts:58)），而 runtime 构造/start 都不拒绝，只有后台 tick 留一条日志。这既不是 fail-fast，也会把错误配置伪装成启动成功。
2. **P1：SSE 的 repository 全序查询没有传递到协议游标。** 复合查询本身可避免同时间戳遗漏，但 route 和 `NotificationSseAdapter` 仍把 cursor 降级为 `updatedAt`（[notification-sse.adapter.ts:92](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:92)），且没有 nextCursor/page loop；101 条积压必然只发送首 100 条，同时间戳下还会重复或跳过，取决于客户端最后保存的纯时间戳。
3. **P2：dead-letter 行的 lease owner 清理不一致。** `recordDeliveryReceipt()` 的 terminal 集合不包含 `dead_letter`（[notification-reliable-operation-prisma.adapter.ts:366](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:366)），所以 dead-letter completion 会清 claim/expiry 却保留 ownerToken（[notification-reliable-operation-prisma.adapter.ts:401](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:401)）。replay 会再次清理，暂不阻塞主链路，但会留下误导性的 owner 状态和审计数据。
4. **P2：注释/测试名称强于真实保证。** runtime 注释声称 shared lease 有 owner token、SSE 先订阅再补发（[notification.runtime.ts:151](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:151)），实际实现分别是 `availableAt` 和 query→subscribe；当前集成测试仍只有 12 个，未出现任务说明所列的崩溃窗口、竞态、碰撞和分页用例。后续验收应以可观测故障注入和真实生产 adapter 调用次数为准。

### 本地验证

本轮使用工作区 `./node_modules/.bin/nx` 并禁用 Nx cache 执行：

- `notification:test`：**32 files，218/218 tests passed**。
- `notification:test:integration`：**1 file，12/12 tests passed**。
- `notification:typecheck`：**passed**（含 11 个依赖 target；Nx 标记 `contracts:build` flaky，但本轮成功）。
- `memoflow:governance-check`：**passed**；test inventory **962 files**，package-internal boundary **642 files / 0 violations**，raw-event-bus audit **2805 files / 0 exemptions**。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞摘要 |
| --- | ---: | --- |
| P0 | 2 | 默认生产 adapter 仍 silent-success/local-only；shared consumer 没有持久化 owner/claim/fencing，真实渠道也未闭合 `side effect -> receipt` 崩溃窗口。 |
| P1 | 3 | delivery context 只接到接口、生产 adapter 不使用；SSE 仍有 query→subscribe 丢失窗口、非协议全序 cursor、单页截断和双广播；reliable fallback 虽删，但 startup fail-fast 与显式 port 仍未实现。 |
| P2 | 2 类 | dead-letter owner 清理不一致；注释/测试名称与实际保证不一致，关键故障窗口没有测试。 |

**第六次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 在下次复审前，至少需要让生产 InApp 失败真实上抛并去掉 receipt 前广播；Desktop/Push 要么接入可确认的跨进程/设备 transport，要么在不具备能力时启动 fail-fast；为 shared consumer 建立真实 owner/claim/fencing/expiry 条件写，并以生产 adapter 的 idempotencyKey/ack 闭合 `deliver -> receipt` 崩溃窗口；让 SSE 使用 subscribe-before-query（带 operationId 去重）或纯 durable tail，循环分页并对外发送 `(updatedAt,id)` cursor，删除双广播；把 reliable/SSE capability 改为显式强类型注入并在 composition/start 阶段拒绝缺失依赖。相应测试必须真实覆盖 shared claim 后崩溃、side effect 后 receipt 前崩溃、旧/新 owner 并发调用次数、query→subscribe 竞态、同 timestamp 和 101+ 条分页。

## 第七次审查

审查日期：2026-08-10。审查对象为任务说明所称的 agy/opencode 第 11--15 次迭代。本轮逐项复核六审的 2 个 P0、3 个 P1、2 个 P2，并专项检查 InApp durable ack、shared outbox 条件写、SSE 复合游标/分页、六个新增故障注入用例以及 schema 扩展对 W1 Reminder 的影响。结论仍以当前工作区代码和实跑结果为准，修复摘要与测试名称不作为闭环证据。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

本轮确实闭合了若干具体缺口：InApp 持久化失败会抛出且 deliverer 不再提前广播；InApp 把稳定 `deliveryId/idempotencyKey` 写进渠道 response，形成 receipt 之前的 durable ack；shared schema 和 claim 已增加 owner/claim/fencing/expiry；HTTP SSE 已改为 subscribe-before-query、本进程事件缓冲去重、复合 cursor 和循环分页；runtime 构造/start 也会拒绝缺少 reliable adapter。18 个通知集成用例、218 个单测、Notification typecheck 及 Reminder 26 个集成用例均实跑通过。

但两个 P0 仍未达到生产语义。第一，默认 composition 把没有 transport、每次调用都必然抛错的 Desktop/Push deliverer 注册为“available”，启动检查只看对象存在，因此生产不会在必需 capability 实际不可用时 fail-fast。第二，shared completion adapter 只有在调用方传入 lease 时才附加 owner/claim/fencing 条件，而生产 runtime 的所有完成调用都没有传 lease；过期旧 owner 仍可把新 owner 的 `running` 行改为 succeeded/retryable/dead-letter。SSE 也只消除了单进程 query/live 窗口：订阅仍基于进程内 event bus，历史查询结束后由其他实例落库的 receipt 不会到达当前连接。模块虽定义了 durable runtime 类型，实际仍通过方法属性探测选择实现，未满足“显式注入、无 duck typing”。

### 六审 2 P0 + 3 P1 + 2 P2 逐条核验

| 六审项 | 当前代码证据与判断 | 核验结果 |
| --- | --- | --- |
| **P0-1：生产 deliverer 不吞错误；Desktop/Push fail-closed；真实 capability** | InApp 的 repository save 异常现在会重新抛出，且该 deliverer 已没有 event bus 广播（[real-channel-deliverers.ts:57](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:57)、[real-channel-deliverers.ts:77](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:77)）；成功广播只发生在 receipt 条件写应用后（[notification.runtime.ts:378](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:378)、[notification.runtime.ts:385](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:385)）。但 Desktop deliverer 无 transport 时必然抛错（[real-channel-deliverers.ts:89](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:89)、[real-channel-deliverers.ts:97](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:97)），默认 composition 却把这个对象同时注册为 Desktop/Push（[prisma.ts:39](../../packages/notification/src/server/infrastructure/prisma.ts:39)、[prisma.ts:45](../../packages/notification/src/server/infrastructure/prisma.ts:45)）；startup check 只用 registry presence 判定 available（[notification.runtime.ts:207](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:207)、[notification.runtime.ts:246](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:246)）。故“投递时失败关闭”成立，“必需生产 capability 缺失时启动失败”不成立。 | **部分闭环，P0 保留** |
| **P0-2：shared lease/fencing 条件写、exactly-once/durable ack、显式 notificationId** | schema 已增加四个字段（[reliable-messaging.prisma:24](../../packages/database/prisma/schema/reliable-messaging.prisma:24)），claim 仅允许无 lease/过期 lease，并用 status+attempt CAS 写 owner/claim/fence/expiry（[notification-reliable-operation-prisma.adapter.ts:585](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:585)、[notification-reliable-operation-prisma.adapter.ts:617](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:617)）。notificationId 使用 payload 或 shared id，dispatch adapter 缺显式 id 会 fail-fast（[notification.runtime.ts:559](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:559)、[notification-reliable-operation-prisma.adapter.ts:171](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:171)）。然而 completion 的 lease 参数是可选的，只有传参才加 owner/claim/fencing 条件（[notification-reliable-operation-prisma.adapter.ts:659](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:659)、[notification-reliable-operation-prisma.adapter.ts:671](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:671)）；生产 runtime 在成功、异常、dead-letter 和 retry 路径均未传 shared claim context（[notification.runtime.ts:603](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:603)、[notification.runtime.ts:615](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:615)、[notification.runtime.ts:662](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:662)）。旧 owner 因而仍能覆盖新 owner。 | **未闭环，P0 保留** |
| **P1-1：deliverer 真正消费稳定幂等上下文** | runtime 始终构造并传入 `deliveryId/idempotencyKey/identityId`（[notification.runtime.ts:306](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:306)、[notification.runtime.ts:324](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:324)）；InApp 将其写入 `ChannelResponse` 后持久化（[real-channel-deliverers.ts:28](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:28)、[real-channel-deliverers.ts:57](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:57)），重领会检查渠道状态/response 并跳过 deliverer（[notification.runtime.ts:318](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:318)）。这对 InApp 构成 durable ack。Desktop/Push 仍忽略 context，且即使注入一个非空 `transport` 也没有调用该 transport，只发布本进程事件（[real-channel-deliverers.ts:89](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:89)、[real-channel-deliverers.ts:105](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:105)）。 | **InApp 闭环；生产 Desktop/Push 未闭环，P1 保留** |
| **P1-2：SSE 无窗口、复合游标、循环分页、单广播** | route 已先订阅再查询，查询期间缓冲 live event 并按 operationId 去重（[routes.ts:298](../../packages/notification/src/api/routes.ts:298)、[routes.ts:324](../../packages/notification/src/api/routes.ts:324)、[routes.ts:366](../../packages/notification/src/api/routes.ts:366)）；查询采用 `(updatedAt,id)` 条件/排序，route 每页 100 条循环推进相同复合 cursor（[notification-reliable-operation-prisma.adapter.ts:468](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:468)、[routes.ts:330](../../packages/notification/src/api/routes.ts:330)）。InApp deliverer 不再广播，receipt 应用后只有 SSE adapter 广播一次。单进程窗口、碰撞和分页实现成立。但 live subscription 仍是 singleton 进程内 event bus（[notification-sse.adapter.ts:27](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:27)、[notification-sse.adapter.ts:44](../../packages/notification/src/server/infrastructure/adapters/sse/notification-sse.adapter.ts:44)）；一次历史查询结束后，其他实例新增 receipt 没有 durable tail/poll/跨实例 transport，当前连接仍会漏事件。 | **单进程闭环；生产多实例无窗口未闭环，P1 保留** |
| **P1-3：runtime 构造/start fail-fast；显式注入，无 duck typing/no-op** | runtime 在构造时即拒绝无 reliable adapter，start 再次校验（[notification.runtime.ts:163](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:163)、[notification.runtime.ts:690](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:690)）；旧 repository scan fallback 未恢复。application API 也不再返回空 receipt/no-op unsubscribe。但 module 仍遍历 lifecycle contributions 并以四个方法的 `typeof` 属性探测寻找 durable runtime（[notification.module.ts:102](../../packages/notification/src/server/infrastructure/notification.module.ts:102)、[notification.module.ts:123](../../packages/notification/src/server/infrastructure/notification.module.ts:123)），不是依赖结构上的显式 `durableRuntime` 注入。 | **fail-fast/no-op 已闭环；duck typing 未闭环，P1 保留** |
| **P2-1：dead-letter owner 清理** | `recordDeliveryReceipt()` 的 terminal 集合仍不包含 `dead_letter`（[notification-reliable-operation-prisma.adapter.ts:373](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:373)、[notification-reliable-operation-prisma.adapter.ts:378](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:378)），所以 dead-letter 写会清 claim/expiry，却保留 ownerToken（[notification-reliable-operation-prisma.adapter.ts:413](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:413)）。 | **未闭环，P2 保留** |
| **P2-2：注释/测试名与真实保证一致** | runtime 注释仍把 shared `availableAt` 称为 lease，尽管已新增独立 `leaseExpiresAt`（[notification.runtime.ts:522](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:522)）。更重要的是，新增用例 13/16 的标题仍分别声称 shared crash reclaim 和 query→subscribe race，实际路径并非如此（见下表）。 | **未闭环，P2 保留** |

### exactly-once 与 shared 条件写专项

InApp 的成功副作用是 Notification/channel 的数据库持久化。deliverer 先把稳定 idempotency context 写入 channel response，再保存 aggregate；若进程在这次保存之后、receipt 更新之前崩溃，重领 worker 会重新读取 aggregate，并根据 status/response 跳过 deliverer。因此这不是“副作用和 receipt 同事务”，但确实是符合任务允许条件的 durable ack，新增用例 14 也真实验证了这个顺序。

该保证不能外推到整个 W2。Desktop/Push 没有可确认 transport/ack；而 shared `OutboxMessage` 的最终状态与其 lease 不是原子约束，因为 runtime 丢弃了 claim 得到的 ownerToken/claimId/fencingToken。具体竞态为：旧 owner 失租，新 owner 把同一行重新 claim 为 `running`，旧 owner随后调用未带 lease 的 `updateSharedOutboxStatus()`；其 `WHERE` 只有 `id + status=running`，会成功清掉新 owner lease并覆盖状态。故 shared outbox 的“conflict 不覆盖”只存在于可选 API 分支，不存在于生产调用链，P0-2 不能以 adapter 能力或绿测放行。

### 六个新增故障注入用例真实性

| 用例 | 实际驱动路径 | 判断 |
| --- | --- | --- |
| 13 shared claim crash/reclaim | seed 的是 `NotificationDispatchOutbox`，直接调用 `claimOutboxDispatch()`，随后由 runtime 处理专用 dispatch row（[notification-reliable-operation.integration.test.ts:890](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:890)、[notification-reliable-operation.integration.test.ts:903](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:903)）。没有创建/claim `OutboxMessage`，也没有经过 `claimSharedOutboxIntents()`。 | **未覆盖标题所称 shared 路径** |
| 14 side effect→receipt 崩溃窗口 | 使用真实 `RealInAppChannelDeliverer` 和 Prisma repository，手工执行 deliverer 后故意不写 receipt，等待 lease 到期再让 runtime 重领（[notification-reliable-operation.integration.test.ts:925](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:925)、[notification-reliable-operation.integration.test.ts:974](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:974)）。 | **真实验证 InApp durable ack；不覆盖 shared completion 或外部 transport** |
| 15 fencing conflict | 直接使用真实 Prisma adapter 让两个 owner 先后 claim，再以旧 context 调用 `recordDeliveryReceipt()`（[notification-reliable-operation.integration.test.ts:1018](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1018)、[notification-reliable-operation.integration.test.ts:1056](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1056)）。 | **真实验证专用 dispatch receipt fencing；没有验证 shared completion** |
| 16 SSE query→subscribe 竞态 | 用真实 HTTP route，但 Event 2 在发起 SSE `http.get()` 之前已经 dispatch 并 tick 完成（[notification-reliable-operation.integration.test.ts:1132](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1132)、[notification-reliable-operation.integration.test.ts:1152](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1152)）。它验证的是普通 durable catch-up，不是在 subscribe 与 query 之间注入事件。 | **未覆盖标题所称竞态** |
| 17 同 timestamp + 101+ 分页 | 真实启动 route/server，数据库插入同一 `updatedAt` 的 105 条 succeeded receipt，从 epoch cursor 建立 SSE 并逐条验证 operationId（[notification-reliable-operation.integration.test.ts:1219](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1219)、[notification-reliable-operation.integration.test.ts:1275](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1275)）。 | **真实覆盖复合游标与循环分页边界** |
| 18 InApp 持久化失败 | runtime 使用真实 InApp deliverer，注入 save 抛错 repository；断言真实 receipt 变为 retryable，同时 success SSE seam 未被调用（[notification-reliable-operation.integration.test.ts:1322](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1322)、[notification-reliable-operation.integration.test.ts:1362](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1362)）。 | **真实覆盖失败传播与“不广播成功”** |

因此 6 个新增用例中，14、17、18 对各自边界提供了有效证据，15 有效但只证明专用 dispatch fencing；13 和 16 没有驱动标题所称故障窗口。尤其没有任何测试让 shared 旧 owner 在新 owner claim 后调用生产 completion，也没有多实例 SSE live 测试。

### 前五轮回归与新问题

| 能力 | 回归结果 |
| --- | --- |
| 创建 Notification 时 aggregate/channel/dispatch outbox 单事务 | repository 仍在一个 Prisma transaction 内 upsert aggregate、channels 并创建 dispatch outbox（[notification-prisma.repository.ts:144](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:144)、[notification-prisma.repository.ts:159](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:159)），**未回退**。 |
| 专用 dispatch claim/lease/fencing 和 replay | claim 仍用 fencing CAS，completion 仍带 owner+fencing，replay 仍按 identity/status/lease 条件写，**未回退**。 |
| 授权 dead-letter/replay/receipt API | auth route、controller/application identity 传递和 adapter identity 条件仍在，**未回退**。 |
| W1 real intent→W2 aggregate/outbox 与显式 FK | W1 writer 仍写 `notification.dispatch`，consumer 使用 shared id/显式 notificationId 创建 aggregate 和 dispatch outbox，既有跨模块用例通过，**未回退**。 |
| 非 durable fallback 与生产双广播 | repository scan fallback 未恢复；InApp deliverer 不再发布事件，只有 receipt 成功后的 SSE adapter 广播，**未回退且本轮进一步收口**。 |

shared schema 的四个新字段均为 nullable，不要求 W1 writer 提供；Reminder 仍按原字段创建 `OutboxMessage`（[prisma-reminder-write-transaction-runner.ts:146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)），本轮 Reminder 372 个单测与 26 个集成用例均通过，未发现 schema 扩展造成的 W1 消费回归。新增字段目前只被 Notification shared consumer 使用；风险不是写入兼容性，而是生产 completion 没有携带这些字段。

复合 cursor 的数据库条件与排序能够正确覆盖同毫秒 timestamp 的 operationId tie-break，并且 105 条边界实测通过。剩余边界是跨实例 live tail，而不是本轮分页算法本身。另有非阻塞清理项：SSE route 残留大量 `console.error('[SSE TRACE]')`/`[TIME]` 调试输出，且捕获分页异常后静默结束补发（[routes.ts:325](../../packages/notification/src/api/routes.ts:325)、[routes.ts:358](../../packages/notification/src/api/routes.ts:358)），建议作为 P2 清理并让连接明确失败或可重试。

### 本地验证

本轮使用显式 pnpm CLI 路径执行（当前非交互 shell 的 PATH 未暴露 `pnpm`）：

- `pnpm nx run notification:test --skip-nx-cache`：**32 files，218/218 通过**。
- `pnpm nx run notification:test:integration --skip-nx-cache`：**1 file，18/18 通过**。
- `pnpm nx run notification:typecheck --skip-nx-cache`：**通过**（含 11 个依赖 target）。
- `pnpm nx run reminder:test --skip-nx-cache`：**47 files，372/372 通过**。
- `pnpm nx run reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。

测试全绿与上述源码级竞态不矛盾：shared completion 竞态和真正 SSE race/multi-instance live path 没有被当前用例触发。

### 最终分级与放行判定

| 级别 | 数量 | 阻塞项 |
| --- | ---: | --- |
| P0 | 2 | 默认 Desktop/Push 假 capability 使生产启动检查失真；shared completion 的生产调用不带 owner/claim/fencing，可由旧 owner 覆盖新 owner。 |
| P1 | 3 | Desktop/Push 未消费幂等 context/无真实 ack；SSE 仅单进程无窗口、没有跨实例 durable live tail；module 仍以方法属性探测 durable runtime。 |
| P2 | 3 类 | dead-letter owner 未清理；测试名/注释超出真实覆盖；SSE/集成测试残留调试输出且分页错误被静默截断。 |

**第七次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 放行前至少需要：让 capability check 根据真实 transport/ack 判定 Desktop/Push（不可用则 production startup fail-fast）；让 `updateSharedOutboxStatus()` 强制要求 claim context，并由 runtime 在所有完成分支传递 claimed row 的 ownerToken/claimId/fencingToken，同时补旧 owner/new owner 的真实 shared 竞态测试；为生产 SSE 使用跨实例可达的 durable tail/通知机制并补真实 subscribe-before-query 注入及跨实例测试；把 durable runtime 变成依赖结构上的显式必填 port，删除属性探测。完成这些阻塞项后再终审，不能仅凭当前 218/218、18/18 和 26/26 提交 W2。

## 第八次审查

审查日期：2026-08-10。审查对象为任务说明所称的 agy 第 16--17 次迭代。本轮逐条复核七审的 P0-2、P0-1、P1-3 与 P2，检查 shared completion 的全部生产调用点、旧 owner/new owner 冲突测试、真实 capability 启动语义、durable runtime 组合根注入，以及前六轮 exactly-once、SSE 单进程边界、deliverer 幂等和 fail-fast 是否回退。结论以当前工作区源码、全仓调用点和本轮实跑结果为准。

### 终审核验表

| 七审项 | 第 16--17 次迭代证据与判断 | 核验结果 |
| --- | --- | --- |
| **P0-2：所有 shared completion 真传 leaseContext，旧 owner 冲突不覆盖新 owner，并有真实 runtime 路径** | runtime 从 claimed shared row 保存 `ownerToken + claimId + fencingToken`（[notification.runtime.ts:549](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:549)），成功短路、解析/持久化异常的 retry/dead-letter、标准 dispatch 后的 succeeded/retry/dead-letter 六个 `updateSharedOutboxStatus()` 调用均传入该 context（[notification.runtime.ts:633](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:633)、[notification.runtime.ts:655](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:655)、[notification.runtime.ts:677](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:677)、[notification.runtime.ts:721](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:721)、[notification.runtime.ts:734](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:734)、[notification.runtime.ts:748](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:748)）。adapter 无 lease 直接返回 conflict，条件写同时约束 id/running/owner/claim/fence，冲突不更新任何字段（[notification-reliable-operation-prisma.adapter.ts:663](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:663)）。用例 13 已真实覆盖 shared claim 崩溃、过期后由 runtime `tick()` 重领并完成（[notification-reliable-operation.integration.test.ts:866](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:866)）；用例 19 证明旧 owner conflict 后新 owner 成功且最终状态未被覆盖（[notification-reliable-operation.integration.test.ts:1379](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1379)），但竞态最后两次 completion 是直接调用真实 Prisma adapter，不是两个 runtime 的 in-flight completion。 | **实现闭环；数据库竞态有真实集成证据，生产 runtime 传参由全调用点审计确认；仍缺旧/新 runtime 端到端竞态用例（测试残留，不保留 P0）** |
| **P0-1：启动检查真实 capability；Desktop/Push 无 transport fail-closed** | `RealDesktopChannelDeliverer.isAvailable()` 只有 transport 存在才返回 true，且 deliver 会真正调用 transport 的 `deliver`/`send`/函数接口（[real-channel-deliverers.ts:86](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:86)）。runtime 的 startup contract 使用该 probe，默认无 transport 的 Push 在 production 会 fail-fast（[notification.runtime.ts:217](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:217)、[notification.runtime.ts:238](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:238)）。但默认检查集合只有 InApp 与 Push，未包含同样注册且同样可能被业务使用的 Desktop；只覆盖/提供 Push transport 时，缺 Desktop transport 仍不会在启动时被发现。更关键的是 API composition 仍用无 transport 的 Desktop/Push 默认对象，API 入口也没有注入真实 transport（[prisma.ts:39](../../packages/notification/src/server/infrastructure/prisma.ts:39)、[module.ts:41](../../packages/notification/src/api/module.ts:41)），所以当前 production API 只能因 Push 缺失而拒绝启动，尚不具备计划完成定义要求的真实 production capability。 | **真实 probe/fail-closed 有进展；Desktop 默认漏检且无 production transport wiring，P0 未闭环** |
| **P1-3：显式 durableRuntime 注入，无属性探测** | 旧的“四方法 `typeof` duck typing”已删除，API facade 也直接依赖 `NotificationDurableRuntimePort`。但 `durableRuntime` 仍是可选依赖，module 在未显式提供时仍遍历 lifecycle contributions 并调用可选 `getDurableRuntime?.()` 取值（[notification.module.ts:44](../../packages/notification/src/server/infrastructure/notification.module.ts:44)、[notification.module.ts:54](../../packages/notification/src/server/infrastructure/notification.module.ts:54)、[notification.module.ts:123](../../packages/notification/src/server/infrastructure/notification.module.ts:123)）；Prisma composition 也只传 `runtimeContributions`，没有显式传 `durableRuntime`（[prisma.ts:58](../../packages/notification/src/server/infrastructure/prisma.ts:58)）。依赖结构上的必填注入没有完成。 | **旧 duck typing 已删；显式必填 port 未闭环，P1 保留** |
| **P2：dead-letter terminal 清理** | `recordDeliveryReceipt()` 的 terminal 集合已包含 `dead_letter`，terminal 写会同时清空 ownerToken、claimId 与 leaseExpiresAt（[notification-reliable-operation-prisma.adapter.ts:376](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:376)、[notification-reliable-operation-prisma.adapter.ts:417](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-reliable-operation-prisma.adapter.ts:417)）。 | **闭环** |
| **P2：13/16 注释与测试名对齐、路径真实化** | 用例 13 现在 seed/claim 的确是 shared `OutboxMessage`，过期后由真实 runtime 重领；标题与路径一致。用例 16 已改名为 durable catch-up，Event 2 在 HTTP 连接前完成并由 Last-Event-ID 补发，标题不再声称 query→subscribe race（[notification-reliable-operation.integration.test.ts:1077](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1077)）。 | **闭环；16 只证明 catch-up，不外推为 race 或跨实例保证** |
| **P2：SSE 调试日志清理** | API route 中七审指出的 `[SSE TRACE]`/`[TIME]` 输出已清除。但集成用例 17 仍残留从 `[TIME 1]` 到 `[TIME 7]` 的多处 `console.error` 调试输出（[notification-reliable-operation.integration.test.ts:1192](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1192)、[notification-reliable-operation.integration.test.ts:1315](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1315)）。 | **生产 route 已清理；测试日志未清完，P2 保留** |

### 显式注入改造副作用与新增问题

本轮发现一个未被 218 个单测、19 个 Prisma 集成用例或 typecheck 覆盖的 **P0 Desktop 启动回归**。Electron composition 仍无参调用 `createNotificationRuntimeContribution()`（[electron/index.ts:105](../../packages/notification/src/electron/index.ts:105)），而该工厂现在在构造入口立即要求 `reliableAdapter`，缺失会同步抛出 FAIL-FAST（[notification.runtime.ts:164](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:164)）。PowerSync factory 只提供三个 repository 与 runtime contribution，不提供 durable adapter/port（[powersync.ts:36](../../packages/notification/src/server/infrastructure/powersync.ts:36)）。因此 `NotificationElectronModule.register()` 会在创建 module 之前失败，Desktop 通知 IPC 无法注册。这里不是合规的“生产 capability 缺失时拒绝投递”，而是 composition root 没有为 Desktop 选择可用的 durable runtime 实现。

这也是 `durableRuntime` 仍为可选、靠 contribution 自描述取值的实际副作用：Prisma 路径碰巧由同一个对象同时承担 lifecycle 与 durable port，PowerSync 路径却没有对应 adapter，类型系统仍允许无参调用，直到运行时才失败。终审前需要让各 composition root 显式选择并传入 durable runtime；若 Desktop 本地 lane 不承担 durable worker，也应显式提供符合产品边界的实现/路由，而不是调用必然抛错的 Prisma worker factory。

### 前六轮能力回归

| 能力 | 回归结论 |
| --- | --- |
| aggregate/channel/dispatch outbox 同事务、W1 intent→W2 确定性 Notification/outbox | **未回退**；19 个集成用例含真实 W1 transaction runner 链路通过。 |
| InApp exactly-once / durable ack 与稳定 deliveryId/idempotencyKey | **未回退**；副作用后崩溃重领仍根据持久化 channel response 跳过 deliverer。 |
| 专用 dispatch 与 shared outbox lease/fencing、replay、旧 owner 条件写 | **未回退且 shared completion 已补齐生产传参**。 |
| deliverer 幂等、InApp 单广播、失败不广播成功 | **未回退**。 |
| SSE subscribe-before-query、复合 cursor、循环分页的单进程边界 | **未回退**；P1-2 多实例 durable live tail 按任务说明继续作为部署演进残留记录，不单独阻塞本次 W2。 |
| 缺 reliable adapter fail-fast、非 durable repository scan fallback 删除 | **未回退**；但 fail-fast 暴露上述 Electron composition P0 回归。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用等价的 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test:integration --skip-nx-cache`：**1 file，19/19 通过**。
- `notification:test --skip-nx-cache`：**32 files，218/218 通过**。
- `notification:typecheck --skip-nx-cache`：**通过**（含 11 个依赖 target）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（含 test inventory 962 files、package-internal boundary 642 files / 0 violations）。

上述绿测不能覆盖 Electron composition 的同步构造失败；用例 19 也只在 adapter 边界注入旧 owner/new owner 竞态。因此终审同时采用全仓调用点审计，不能仅按测试数量放行。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 2 | production API 无真实 Desktop/Push transport，默认 capability 检查还漏 Desktop；Electron composition 无 reliableAdapter 调用 durable runtime 工厂并在注册阶段同步失败。 |
| P1 | 1 + 1 个测试残留 | `durableRuntime` 仍非依赖结构上的显式必填注入；旧/新 owner 的 shared 冲突用例止于 adapter，缺两个真实 runtime 的 in-flight 完成竞态。P1-2 多实例 SSE 作为已登记部署演进残留，不计本次阻塞。 |
| P2 | 1 | API SSE 调试日志已清，但集成用例 17 仍保留多处 `[TIME]` 调试输出。dead-letter 清理及 13/16 名称/路径已闭环。 |

**第八次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** P0-2 的生产传参和条件写可以保留；终审前至少需要：为 API/Desktop composition 显式接入真实且可确认的渠道/durable capability，默认同时校验 Desktop 与 Push；把 `durableRuntime` 变为明确的必填组合根依赖并修复 Electron 注册路径；补旧 owner/new owner 经过真实 runtime completion 的竞态用例；清理剩余测试调试日志。完成后再复审。

## 第九次审查

审查日期：2026-08-10。审查对象为任务说明所称的 agy 第 18--19 次迭代。本轮只审查当前工作区源码、全仓调用点与实跑结果，不修改业务代码，不 commit/push。除逐条复核八审阻塞项外，还专项检查了 Electron in-memory fallback 的持久性边界、capability 声明与真实 transport 的一致性，以及前七轮 exactly-once、lease 条件写、SSE 边界、deliverer 幂等和 fail-fast 是否回退。

### 终审核验表

| 八审项 | 第 18--19 次迭代证据与判断 | 核验结果 |
| --- | --- | --- |
| **P0-2：Electron 注册不再因缺 reliable adapter 失败；in-memory 兜底边界清楚且可接受** | Electron 入口不再无参调用 runtime 工厂，而是调用 PowerSync factory（[electron/index.ts:104](../../packages/notification/src/electron/index.ts:104)）；factory 确实显式创建 `InMemoryNotificationReliableAdapter` 并把所得 runtime 作为模块依赖和 lifecycle contribution 传入（[powersync.ts:64](../../packages/notification/src/server/infrastructure/powersync.ts:64)、[powersync.ts:83](../../packages/notification/src/server/infrastructure/powersync.ts:83)）。但该修复只在非 production 环境避免同步失败：factory 同时创建**无 transport** 的 Desktop deliverer，却把 Desktop capability 声明为 `available`（[powersync.ts:66](../../packages/notification/src/server/infrastructure/powersync.ts:66)、[powersync.ts:76](../../packages/notification/src/server/infrastructure/powersync.ts:76)）；runtime 以 deliverer 的真实 `isAvailable()` 覆盖声明，production 下仍会因 `missing + requiredInProduction` fail-fast（[notification.runtime.ts:222](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:222)、[notification.runtime.ts:248](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:248)），而无 transport 的 probe 恒为 false（[real-channel-deliverers.ts:95](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:95)）。因此打包/production Electron 的 `register()` 仍会同步失败。即使 development 注册成功，创建用例把 dispatch outbox 交给 repository `save(notification, outboxDispatches)`（[create-notification.use-case.ts:98](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:98)、[create-notification.use-case.ts:135](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:135)），PowerSync repository 的 `save()` 只保存 aggregate、忽略第二参数且从未写入该 in-memory adapter（[notification-powersync.repository.ts:98](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:98)），所以兜底 worker 收不到 Electron 创建的 dispatch。 | **未闭环，P0 保留：production 注册仍失败；development 兜底也没有实际 enqueue/dispatch 闭环** |
| **P0-1：Desktop 纳入 capability 检查；API 无真实 transport 时 fail-closed** | 默认检查集合现为 InApp、Push、Desktop（[notification.runtime.ts:243](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:243)），availability 来自真实 registry/probe，声明不能绕过无 adapter（[notification.runtime.ts:222](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:222)、[notification.runtime.ts:250](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:250)）。Prisma factory 为 Desktop/Push 构造的 deliverer 只有拿到 transport 才 available（[prisma.ts:42](../../packages/notification/src/server/infrastructure/prisma.ts:42)）；API composition 无参创建 Prisma module（[api/module.ts:41](../../packages/notification/src/api/module.ts:41)），所以 production 下会在启动前拒绝，未退回 silent success。安全语义正确；但 API 入口没有 transport/config 注入路径，当前 production composition 只能失败，仍不满足完成定义的“有真实生产 capability”。 | **fail-closed 语义闭环；production capability 未闭环，P0 保留** |
| **P1：`durableRuntime` 是模块必填依赖，无可选属性探测兜底** | `NotificationModuleDependencies.durableRuntime` 已是必填字段，模块直接解构并在运行时二次 fail-closed（[notification.module.ts:53](../../packages/notification/src/server/infrastructure/notification.module.ts:53)、[notification.module.ts:118](../../packages/notification/src/server/infrastructure/notification.module.ts:118)）；Prisma 与 PowerSync factory 都显式传入该依赖（[prisma.ts:61](../../packages/notification/src/server/infrastructure/prisma.ts:61)、[powersync.ts:83](../../packages/notification/src/server/infrastructure/powersync.ts:83)）。全仓已无 `getDurableRuntime?.()` 或 contribution 属性探测。port 仍保留普通 `getDurableRuntime()` 方法（[notification.runtime.ts:86](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:86)），但不再参与模块选取/兜底。 | **闭环** |
| **P1 测试残留：旧/新 owner 经过两个真实 runtime 的 in-flight completion 竞态** | 新用例 20 名称声称 dual runtime，但 `_runtime1` 创建后从未 `tick()`，旧 owner 仍由测试直接调用 adapter `claimSharedOutboxIntents()`；runtime2 完成后，所谓 runtime1 stale completion 仍是测试直接调用 `updateSharedOutboxStatus()`（[notification-reliable-operation.integration.test.ts:1450](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1450)、[notification-reliable-operation.integration.test.ts:1470](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1470)、[notification-reliable-operation.integration.test.ts:1517](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1517)）。它再次证明 adapter fencing 正确，但没有驱动旧 runtime 的真实 in-flight completion。 | **未闭环，P1 测试残留保留** |
| **P2：集成用例 17 调试日志清理** | 用例 17 和 API route 已无 `[TIME]`、`[SSE TRACE]` 调试输出；全仓对应路径搜索为空。 | **闭环** |

### in-memory 兜底语义与 capability 一致性

`InMemoryNotificationReliableAdapter` 的类注释确实写明“专用于 Desktop / Electron 及单元测试场景（明确标示非持久化）”（[in-memory-notification-reliable.adapter.ts:111](../../packages/notification/src/server/infrastructure/adapters/in-memory/in-memory-notification-reliable.adapter.ts:111)），但 PowerSync composition 的公开注释仍称其为 `durable runtime`，没有在调用点或正式文档说明进程退出会丢失 outbox、receipt、lease、dead-letter 和 replay 历史（[powersync.ts:35](../../packages/notification/src/server/infrastructure/powersync.ts:35)）。这与 W2 的 durable dispatch、断线恢复和“回滚保留 operation/audit 历史”不是同一保证，不能仅靠实现类内部一行注释外推为可接受的生产边界。

更直接的不一致是：PowerSync factory 声明 Desktop `available`，真实 deliverer 却因未注入 transport 报 unavailable；runtime 没有被该声明欺骗，故 production 正确 fail-closed，但也因此没有修复 production Electron 注册。development 环境不会执行 production capability 拒绝，却仍因 PowerSync repository 与 in-memory adapter 无 enqueue 接线而只获得一个空 worker。若 Desktop 本地 lane明确不承担 durable dispatch，应提供不宣称 Desktop available 的显式 client-only port，并在架构文档说明 receipt/worker 由服务端 ownership；若它承担本地 dispatch，则必须把 outbox 持久化到 PowerSync/SQLite 并注入真实 Electron transport。当前实现两种边界都没有成立。

### 前七轮能力回归

| 能力 | 回归结论 |
| --- | --- |
| aggregate/channel/dispatch outbox 同事务、W1 intent→W2 确定性 Notification/outbox | **Prisma/server 主链未回退**；20 个集成用例通过。Electron PowerSync 不是该事务能力的实现，新增 fallback 不能外推该保证。 |
| InApp exactly-once / durable ack 与稳定 deliveryId/idempotencyKey | **未回退**；副作用后崩溃重领仍根据持久化 channel response 跳过 deliverer。 |
| 专用 dispatch 与 shared outbox lease/fencing、replay、旧 owner 条件写 | **实现未回退**；所有 shared completion 仍传 lease context，adapter 条件写仍拒绝 stale owner。用例 20 的覆盖真实性问题不否定已有实现证据，但八审要求的 runtime 级竞态仍未补齐。 |
| deliverer 幂等、InApp 单广播、失败不广播成功 | **未回退**。 |
| SSE subscribe-before-query、复合 cursor、循环分页的单进程边界 | **未回退**；P1-2 多实例 durable live tail 继续作为已登记部署演进残留，不计本次新增阻塞。 |
| 缺 reliable adapter fail-fast、非 durable repository scan fallback 删除 | **未回退**；Electron 新增的是显式 in-memory adapter，不是 repository scan fallback，但其非持久生产语义不满足 W2 durable 保证。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test:integration --skip-nx-cache`：**1 file，20/20 通过**。
- `notification:test --skip-nx-cache`：**32 files，218/218 通过**。
- `notification:typecheck --skip-nx-cache`：**通过**（含 11 个依赖 target）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（test inventory 962 files；package-internal boundary 642 files / 0 violations）。

这些绿测没有实例化 production Electron composition；现有 Electron surface spec 只读源码核对 IPC channel 名称（[notification-electron.surface.spec.ts:11](../../packages/notification/src/electron/notification-electron.surface.spec.ts:11)）。用例 20 也没有让旧 runtime 进入 in-flight。因此测试数量与上述 production 构造失败、PowerSync enqueue 断链并不矛盾。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 2 | Electron in-memory fallback 在 production 仍因无 Desktop transport fail-fast，development 下又没有 outbox enqueue/dispatch 接线；API 虽正确 fail-closed，但当前 production composition 没有真实 transport 注入，无法形成可启动的生产 capability。 |
| P1 | 1 个测试残留 | `durableRuntime` 必填注入已经闭环；新增 dual-runtime 用例仍以 adapter 直调模拟旧 owner completion，未覆盖八审要求的两个真实 runtime in-flight 竞态。P1-2 多实例 SSE 继续作为部署演进残留，不计本次阻塞。 |
| P2 | 0 | 用例 17 与 route 调试日志已清；八审其他 P2 也未回退。 |

**第九次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 本轮完成了 Desktop 默认检查、API fail-closed、模块必填 `durableRuntime` 和日志清理，但 Electron fallback 没有形成 production 可注册、可 enqueue、可恢复的明确架构边界，API composition 也仍无真实 transport；所谓 dual-runtime 用例没有执行旧 runtime completion。放行前至少需要：明确 Desktop worker ownership，并据此选择持久 PowerSync/SQLite outbox + 真实 Electron transport，或显式 client-only port + 服务端 durable ownership 文档；让 production Electron 注册路径与 capability 声明一致；为 API 提供真实 transport/config composition；补两个真实 runtime 的 in-flight stale-owner 竞态测试。完成后再终审。

## 第十次迭代 — Desktop Durable Worker 架构设计说明

根据 Alex 决策，桌面 Electron 端采用架构决策 A **Desktop Durable Worker** 模式（持久 SQLite outbox + 真实 Electron transport）。

### 1. Owner 归属与数据库契约
- **桌面端独立 Outbox 存储**：Electron 主进程通过 `PowerSyncNotificationReliableAdapter` 直接绑定本地 PowerSync/SQLite 数据库 (`IElectronDatabase`)。该 Adapter 维护独立的 SQLite `notification_dispatch_outbox` 表。
- **Outbox 入队**：`PowerSyncNotificationRepository.save(notification, outboxDispatches)` 在同一 SQLite 事务中落库 Notification 聚合根、`notification_channels` 渠道行与 `notification_dispatch_outbox` 投递意图。

### 2. 崩溃恢复与 Lease/Claim 语义
- **SQLite 持久化**：Outbox 消息不使用 in-memory 队列，主进程崩溃或重启后消息仍然完好保存在 SQLite `notification_dispatch_outbox` 表中。
- **W0 Lease/Claim 重新领用**：桌面 worker 依据 W0 lease 语义（`ownerToken`、`claimId`、`fencingToken`）执行 `claimOutboxDispatch`。未完成且租约已过期的 `running` 消息或 `retryable` 消息在进程重启后会被新的 claim 成功领用，避免重复入队与死锁。

### 3. Deliver → Receipt 闭环与 Transport Ack
- **真实 Transport 注入**：`RealDesktopChannelDeliverer` 注入 Electron 桌面 transport（`createDefaultElectronDesktopTransport`，封装 Electron `Notification` API 与 IPC 广播）。`isAvailable()` 返回 `true`，满足生产环境 capability 检查。
- **Ack 持久化**：`RealDesktopChannelDeliverer` 执行 transport 后接收可确认的 `ack` 凭证（含 `ackId`、`status: 'delivered'`、`timestamp`），写入 `ChannelResponse.success` 并保存到 `notification_channels` 实体 response 字段中。
- **终态 CAS 写入**：`PowerSyncNotificationReliableAdapter.recordDeliveryReceipt()` 使用 `ownerToken` 与 `fencingToken` 条件更新 SQLite outbox 状态为 `succeeded` 终态，并清理 lease，完全闭合 deliver → receipt 窗口。

### 4. 与服务端 Prisma Runtime 的边界
- **服务端 Runtime**：基于 Prisma + PostgreSQL 的 `NotificationReliableOperationPrismaAdapter`，负责 API / Web 侧的 durable dispatch。
- **桌面端 Runtime**：基于 PowerSync + SQLite 的 `PowerSyncNotificationReliableAdapter`，负责 Electron 桌面端的 durable dispatch。
- **统一 API 协议**：桌面端与服务端底层 Adapter 均无缝实现 W0 契约 `NotificationReliableOperationPort`，保证跨层语义完全对齐。

## 第十次审查

审查日期：2026-08-10。审查对象为任务说明所称的 W2 第二十一次迭代及上方“Desktop Durable Worker 架构设计说明”。本轮逐条核验九审四项放行前提，回归前八轮已经成立的 exactly-once、lease 条件写、SSE 单进程边界、deliverer 幂等和必填注入，并专项检查 `Desktop` 枚举影响面以及 PowerSync/SQLite adapter 与 Prisma adapter 的语义一致性。结论以当前工作区源码、真实 PowerSync schema、全仓生产调用点和本轮实跑结果为准；设计说明与测试名称不作为实现已成立的替代证据。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

第二十一次迭代确实删除了 Electron in-memory fallback，新增 SQLite outbox adapter，把 PowerSync composition 接到 durable runtime，并为 Desktop deliverer 增加了 transport 返回值到 channel response 的 happy-path 持久化。新增四个 SQLite 单测也确实通过。但九审四项放行前提中，只有“Electron `register()` 不再因缺 reliable adapter 同步 fail-fast”这一表层接线成立；真实 Desktop schema/事务、可确认 transport ack、API transport 注入和双真实 runtime 竞态均未闭环。

更严重的是，测试数据库手工创建了比生产 `PowerSyncAppSchema` 更宽的 `notification_channels` 表，掩盖了真实 Electron 数据库的列不兼容；repository 的 aggregate/channel/outbox 写入也没有调用 `writeTransaction()`。默认 Electron transport 在 Electron 不可用、原生通知不支持或 `show()` 抛错时仍返回 `delivered` ack，而通用 Desktop deliverer 对无有效方法或无返回值的 truthy transport 还会自行伪造 ack。上述行为重新引入完成定义禁止的 silent success，不能以 capability object 存在或 222 个单测放行。

### 九审放行前提核验表

| 九审放行前提 | 第二十一次迭代证据与判断 | 核验结果 |
| --- | --- | --- |
| **Desktop durable worker：SQLite outbox 持久化、真实 transport ack、崩溃重领** | `PowerSyncNotificationReliableAdapter` 已实现持久表、claim/owner/fencing/lease 和条件终态写（[power-sync-notification-reliable.adapter.ts:243](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:243)）；过期 `running` 行也进入重新 claim 条件（[power-sync-notification-reliable.adapter.ts:408](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:408)）。但生产 `PowerSyncAppSchema.notification_channels` 只有 `recipient/max_retries/error/response/retry_count` 等列（[index.ts:583](../../packages/powersync-schema/src/index.ts:583)），repository 却读写 `attempts/sent_at/failed_at/created_at/updated_at`（[notification-powersync.repository.ts:154](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:154)、[notification-powersync.repository.ts:322](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:322)）；`CREATE TABLE IF NOT EXISTS` 不会给已由 PowerSync schema 创建的表补列。测试预先手工创建了这些额外列（[notification-powersync-durable-worker.spec.ts:69](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:69)），因此没有覆盖真实 schema。`save()` 的 notification、channels、outbox 是连续 `db.execute()`，全方法没有 `writeTransaction()`（[notification-powersync.repository.ts:204](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:204)）；测试 wrapper 的 `writeTransaction()` 也只是直接执行 callback（[notification-powersync-durable-worker.spec.ts:39](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:39)）。四个新用例没有关闭/重开数据库或让过期 lease 由新 worker 重领。 | **部分接线；真实 schema、原子入队和崩溃恢复证据未闭环，P0** |
| **production Electron `register()` 与 capability 一致** | Electron 已调用 PowerSync factory，factory 也默认创建 reliable adapter、Desktop deliverer 与 runtime（[electron/index.ts:104](../../packages/notification/src/electron/index.ts:104)、[powersync.ts:108](../../packages/notification/src/server/infrastructure/powersync.ts:108)），所以九审的同步构造失败已消失。但 `createDefaultElectronDesktopTransport()` 捕获任意 Electron/`Notification.show()` 错误且无条件返回 `status: 'delivered'`（[powersync.ts:43](../../packages/notification/src/server/infrastructure/powersync.ts:43)）；`RealDesktopChannelDeliverer.isAvailable()` 只检查 transport truthy，且 transport 无 `deliver/send` 或返回空值时伪造成功 ack（[real-channel-deliverers.ts:96](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:96)、[real-channel-deliverers.ts:118](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:118)）。因此 production 可以声明 Desktop available 并把未发生的副作用记为 delivered。 | **注册接线成立，真实 capability/ack 不一致，P0** |
| **API transport 注入** | Prisma factory 已提供 `desktopTransport`/`pushTransport` options（[prisma.ts:23](../../packages/notification/src/server/infrastructure/prisma.ts:23)），但唯一生产 API composition 仍执行 `createNotificationPrismaModule(db)`，`NotificationApiModuleContext` 也没有 transport/config 字段（[module.ts:25](../../packages/notification/src/api/module.ts:25)、[module.ts:41](../../packages/notification/src/api/module.ts:41)）。默认 Desktop/Push deliverer 因无 transport 在 production fail-fast；安全拒绝没有回退，但 API 仍不具备可启动的真实 production capability。 | **未闭环，P0** |
| **双真实 runtime 竞态测试** | 用例 20 与九审时相同：`_runtime1` 被创建但从未 `tick()`；旧 owner 由测试直调 adapter claim，runtime2 完成后又直调 adapter 模拟 stale completion（[notification-reliable-operation.integration.test.ts:1460](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1460)、[notification-reliable-operation.integration.test.ts:1500](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1500)、[notification-reliable-operation.integration.test.ts:1516](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1516)）。新增 PowerSync 测试也没有两个 runtime 或 in-flight barrier。 | **未闭环，P1 测试残留** |

### Desktop transport ack 与 exactly-once 专项

Desktop happy path 中，deliverer 会把 transport 返回对象放进 `ChannelResponse.success(...).data.ack`，随后 runtime 可将 notification channel 保存到 `notification_channels.response`；新增用例对 `ack-test-123` 的断言证明这一条无故障路径成立（[notification-powersync-durable-worker.spec.ts:241](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:241)）。但它不是可确认投递语义：默认 transport 在没有调用原生 `show()` 时也生成 ack，且没有校验 ack 的结构或 `status`。

持久化顺序也没有闭合崩溃窗口。runtime 先把 dispatch outbox 条件写成 `succeeded`，之后才调用 repository 保存已变更的 channel response（[notification.runtime.ts:403](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:403)、[notification.runtime.ts:407](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:407)）。若进程在两步之间崩溃，重启后 outbox 已是终态、不再 claim，而 `notification_channels.response` 仍为空。反向窗口中，transport 副作用已经发生但 outbox receipt 尚未成功时，Desktop transport 本身也没有使用 `deliveryId/idempotencyKey` 做持久去重。故“ack 持久化”和 Desktop exactly-once 只能描述当前 happy path，不能作为崩溃语义保证。

### PowerSync/SQLite 与 Prisma adapter 语义一致性

两套 adapter 在专用 dispatch 的字段映射、pending/retryable/过期 running claim、owner/fencing 条件终态写、dead-letter/replay 和 receipt cursor 形状上基本对齐；Prisma/server 既有语义未回退。但以下差异阻止“完全对齐”的架构结论：

1. Prisma 创建 aggregate/channel/outbox 使用 `$transaction()`（[notification-prisma.repository.ts:54](../../packages/notification/src/server/infrastructure/adapters/prisma/notification-prisma.repository.ts:54)），PowerSync repository 没有使用已存在于 `IElectronDatabase` 的 `writeTransaction()`，所以中间任一步失败会留下部分 aggregate、部分 channel 或无 outbox。
2. PowerSync shared outbox claim 的 retry/running 可用时间检查读取 `next_available_at`（[power-sync-notification-reliable.adapter.ts:779](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:779)），但 completion 更新的是 `available_at`（[power-sync-notification-reliable.adapter.ts:854](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:854)）；Prisma 两处统一使用 `availableAt`。因此 PowerSync retryable 行的 backoff 可被忽略，shared consumer 语义不一致。
3. 新 SQLite 测试预建全部目标表，且其 `writeTransaction()` 不执行真实事务；它证明 SQL happy path 可运行，不证明 production PowerSync schema、rollback 或重启恢复。

### `Desktop` 枚举扩展影响面

contracts 和 domain 的合法值集合已经同时包含 `Desktop`，Zod create/preference response schema会自动接受该值，Prisma 字段为 String，不存在数据库枚举迁移问题。运行时 normalize/registry 也能识别 `Desktop`，主投递路径类型没有断裂。

但 domain 分类函数仍只把 InApp/Push/Sms 判为 realtime、Email/Webhook 判为 async，`Desktop` 两者均非（[notification-channel-type.ts:58](../../packages/notification/src/server/domain/value-objects/notification-channel-type.ts:58)）；偏好更新的 channel flag map 和 Vue 偏好 UI 也没有 Desktop（[update-notification-preference.use-case.ts:11](../../packages/notification/src/server/application/use-cases/commands/update-notification-preference.use-case.ts:11)、[useNotificationPreferences.ts:29](../../packages/app-vue/src/modules/notification/composables/useNotificationPreferences.ts:29)）。如果 Desktop 只允许系统显式指定，应把这一产品边界固化为测试/文档；如果用户可配置，则当前枚举扩展不完整。本轮列为 **P2**，不单独构成当前 P0 放行阻塞。

### 前八轮能力回归

| 能力 | 回归结论 |
| --- | --- |
| Prisma aggregate/channel/dispatch outbox 同事务、W1 intent→W2 确定性 Notification/outbox | **未回退**；20 个 Prisma 集成用例通过。新增 Desktop 路径没有同事务保证，不能外推 Prisma 结论。 |
| Prisma/InApp exactly-once、稳定 deliveryId/idempotencyKey、专用与 shared lease/fencing 条件写 | **既有服务端实现未回退**；Desktop 新 transport/ack 路径存在上述两个崩溃窗口。 |
| deliverer 幂等、InApp 单广播、失败不广播成功 | **InApp 未回退**；默认 Desktop transport 新增了捕错后仍成功的问题。 |
| SSE subscribe-before-query、复合 cursor、循环分页的单进程边界 | **未回退**；P1-2 多实例 durable live tail继续作为部署演进残留，不计本轮新增阻塞。 |
| durable runtime 必填注入、缺 reliable adapter fail-fast、无 repository scan fallback | **未回退**；Prisma 与 PowerSync factory 均显式提供 durable runtime/reliable adapter。 |
| identity 授权 dead-letter/replay 与 receipt/list 恢复 | **未回退**。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test --skip-nx-cache`：**33 files，222/222 通过**（包含新增 4 个 PowerSync/SQLite 用例）。
- `notification:test:integration --skip-nx-cache`：**1 file，20/20 通过**。
- `notification:typecheck --skip-nx-cache`：**通过**（含 11 个依赖 target）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（test inventory 963 files；package-internal boundary 642 files / 0 violations）。

上述结果确认编译与既有 Prisma 回归稳定，但新增 SQLite 用例的自建 schema 和非事务 wrapper 不能覆盖真实 Electron/PowerSync composition；用例 20 也仍未覆盖两个真实 runtime 的 in-flight completion。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 3 | Desktop repository 与真实 `PowerSyncAppSchema` 列不兼容且 aggregate/channel/outbox 非事务；默认 Desktop transport/capability/ack fail-open并存在 receipt→channel-response 崩溃窗口；API production composition 仍无 transport/config 注入。 |
| P1 | 2 | 双真实 runtime in-flight 竞态与 Desktop 崩溃重领测试未补；PowerSync shared outbox 混用 `next_available_at`/`available_at`，与 Prisma backoff 语义不一致。P1-2 多实例 SSE 继续作为部署演进残留，不计本次阻塞。 |
| P2 | 1 | `Desktop` 已进入契约合法值，但 domain realtime/async 分类和偏好 API/UI 影响面尚未明确收口。 |

**第十次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 放行前至少需要：更新 canonical `PowerSyncAppSchema` 并用真实 schema 覆盖 Desktop repository；将 notification、channel、dispatch outbox 放进真实 `writeTransaction()` 并补 rollback 故障注入；让 Electron transport 只有在原生副作用真实成功且 ack 结构有效时返回成功，移除 truthy/空返回伪 ack，并原子化或可恢复地闭合 receipt 与 channel response；在 API composition 显式注入可确认的 Desktop/Push transport/config；修正 PowerSync shared backoff 字段；补数据库重开后的过期 lease 重领以及两个真实 runtime 通过 barrier 形成的旧/新 owner in-flight 竞态测试。完成后再终审。

## 第十一次审查

审查日期：2026-08-10。审查对象为任务说明所称的 W2 第二十二至二十四次迭代。本轮逐条复核十审的 3 个 P0、2 个 P1 和 1 个 P2，并重新检查 W2 完成定义、前十轮已经成立的能力和 P1-2 多实例 SSE 部署演进边界。结论以当前工作区源码、生产组合根和本轮实跑结果为准；修复摘要、注释和绿测数量不替代生产接线证据。

### 终审结论

**W2 仍不可放行，不应提交 W2，也不能进入 W3。**

第二十二至二十四次迭代已经实质关闭 canonical PowerSync schema、真实 SQLite 事务与 rollback、Electron/通用 deliverer 的 fail-closed ack、PowerSync shared outbox 字段混用、双真实 runtime in-flight 竞态测试以及 Desktop 分类/偏好边界。尤其用例 20 现在确实执行 runtime A/B 的真实 `tick()`，不再用 adapter 直调冒充 runtime 竞态。

但十审两个生产语义缺口仍在。第一，Notification API module 虽新增 `desktopTransport`/`pushTransport` 可选字段并透传给 Prisma factory，实际 `apps/api` bootstrap 创建的标准 module context 没有这些字段，`main.ts` 仍直接注册 singleton `NotificationApiModule`；因此 production API composition 仍会以两个 `undefined` transport 构造 Desktop/Push deliverer，并在默认 production capability 检查中 fail-fast。它证明“模块接口可被定制调用”，不等于“当前生产组合根已注入真实 transport/config”。第二，默认 Electron transport 只在原生 `Notification.show()` 无异常后返回 delivered，deliverer 也会拒绝无方法或无效 ack，这一 fail-closed 修复成立；但 transport 明确忽略 delivery context，原生副作用与 durable receipt、channel response 仍是三个非原子步骤。进程在 `show()` 成功后、receipt 条件写前崩溃会在重领后再次调用原生 transport；进程在 receipt 成功后、channel save 前崩溃则留下 succeeded outbox 与缺失 channel ack，且没有恢复投影。W2 要求的“重复消费不重复发送”与十审要求的可恢复 receipt/channel response 闭环因此仍未成立。

### 十审 3 P0 + 2 P1 + 1 P2 核验表

| 十审项 | 当前代码与测试证据 | 第十一次审查判断 |
| --- | --- | --- |
| **P0-1：canonical PowerSync schema、同事务写、rollback 故障注入** | `notification_channels` 已补齐 repository 使用的 `attempts/sent_at/failed_at/created_at/updated_at`（[index.ts:583](../../packages/powersync-schema/src/index.ts:583)），canonical schema 新增并导出 `notification_dispatch_outbox`（[index.ts:600](../../packages/powersync-schema/src/index.ts:600)、[index.ts:1057](../../packages/powersync-schema/src/index.ts:1057)）。`save()` 在唯一 `writeTransaction()` 回调内通过 `tx` 完成 notification、全部 channel 和全部 outbox 查询/写入（[notification-powersync.repository.ts:204](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:204)、[notification-powersync.repository.ts:309](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:309)、[notification-powersync.repository.ts:380](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:380)），事务外只做建表准备和 commit 后 domain-event flush。测试 wrapper 现在执行真实 `BEGIN/COMMIT/ROLLBACK`（[notification-powersync-durable-worker.spec.ts:41](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:41)）；schema 奇偶校验覆盖两表所需列（[notification-powersync-durable-worker.spec.ts:382](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:382)），中途校验失败后断言 notification 行不存在（[notification-powersync-durable-worker.spec.ts:405](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:405)）。 | **闭环。** aggregate/channel/outbox 业务写都被真实 SQLite transaction 包裹，十审 schema 与原子入队 P0 关闭。 |
| **P0-2：Electron transport 与 deliverer ack fail-closed；Desktop durable ack/崩溃语义** | 默认 transport 只有 `Notification.isSupported()` 且 `show()` 无异常才返回 delivered；不可用或异常返回 failed（[powersync.ts:49](../../packages/notification/src/server/infrastructure/powersync.ts:49)）。通用 deliverer 的 capability 必须存在可调用的 `deliver/send`，success ack 必须含非空 `ackId` 和精确的 `status: 'delivered'`，否则抛错进入 retry/failure（[real-channel-deliverers.ts:102](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:102)、[real-channel-deliverers.ts:116](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:116)、[real-channel-deliverers.ts:146](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:146)）。这已删除十审的 truthy/空返回伪 ack。不过默认 transport 的 `_context` 未使用（[powersync.ts:51](../../packages/notification/src/server/infrastructure/powersync.ts:51)），runtime 仍先调用 transport，再写 durable receipt，最后另行保存 channel response（[notification.runtime.ts:329](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:329)、[notification.runtime.ts:350](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:350)、[notification.runtime.ts:403](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:403)、[notification.runtime.ts:407](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:407)）。 | **fail-closed ack 闭环；崩溃/幂等闭环未完成，P0 保留。** 当前没有 transport 侧 durable dedupe，也没有 succeeded receipt→channel response 恢复投影；两个崩溃窗口仍违反 W2 重放不重复发送与十审放行前提。 |
| **P0-3：API transport/config 注入** | Notification module context 新增两个可选 transport 并透传到 Prisma factory（[module.ts:41](../../packages/notification/src/api/module.ts:41)、[module.ts:59](../../packages/notification/src/api/module.ts:59)），factory 也按字段构造 fail-closed deliverer（[prisma.ts:34](../../packages/notification/src/server/infrastructure/prisma.ts:34)、[prisma.ts:42](../../packages/notification/src/server/infrastructure/prisma.ts:42)）。但真实 `ApiBootstrapper` 创建的 context 只有 app/router/db/middleware/openApiRegistry（[bootstrap.ts:121](../../apps/api/src/bootstrap.ts:121)），`main.ts` 直接注册 `NotificationApiModule`，没有 factory/config 绑定（[main.ts:195](../../apps/api/src/main.ts:195)）。默认 capability 检查仍要求 InApp、Push、Desktop，且 production 缺失即拒绝启动（[notification.runtime.ts:245](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:245)）。 | **仅模块级可选入口成立，生产注入未闭环，P0 保留。** 当前 production API 既没有真实 transport，也没有能把配置放入 module context 的 composition seam。 |
| **P1-1：PowerSync shared outbox backoff 字段一致** | shared row 现在明确使用 `next_retry_at` 映射 Prisma `availableAt`，并以独立 `lease_expires_at` 做 fencing deadline；shared 路径不存在 `next_available_at`/`available_at`（[power-sync-notification-reliable.adapter.ts:52](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:52)）。claim 的筛选、claim 写和 completion 写全部使用 `next_retry_at`，lease 条件始终使用 `lease_expires_at`（[power-sync-notification-reliable.adapter.ts:789](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:789)、[power-sync-notification-reliable.adapter.ts:821](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:821)、[power-sync-notification-reliable.adapter.ts:875](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:875)）。 | **闭环，P1 关闭。** backoff 与 lease 字段职责清晰且读写一致。 |
| **P1-2：用例 20 必须是真实双 runtime in-flight 竞态** | 测试创建 owner 不同的 runtime A/B（[notification-reliable-operation.integration.test.ts:1479](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1479)），A 的真实 `tick()` claim 后进入 deliverer barrier（[notification-reliable-operation.integration.test.ts:1520](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1520)）；A lease 有效时 B 的真实 `tick()` 不重领（[notification-reliable-operation.integration.test.ts:1530](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1530)）；手动过期 shared/dispatch lease 后 B 再次 `tick()` 完成（[notification-reliable-operation.integration.test.ts:1537](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1537)），释放 A 后断言 stale completion 不覆盖 B 的 fencing/status，且 A/B deliverer 各调用一次（[notification-reliable-operation.integration.test.ts:1561](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1561)）。 | **测试残留闭环，P1 关闭。** 它真实覆盖两个 runtime 的 claim→deliver in-flight→失租→stale completion；但 A/B 各一次是两次 transport invocation，不应被表述成跨实例外部副作用 exactly-once，后者仍依赖 deliverer/transport 使用稳定幂等键。 |
| **P2：Desktop realtime 分类与偏好边界** | Domain 已把 Desktop 纳入 realtime（[notification-channel-type.ts:58](../../packages/notification/src/server/domain/value-objects/notification-channel-type.ts:58)）；server preference use case 与 Vue composable 均明确注释 Desktop 由系统显式指定、不是用户偏好渠道（[update-notification-preference.use-case.ts:1](../../packages/notification/src/server/application/use-cases/commands/update-notification-preference.use-case.ts:1)、[useNotificationPreferences.ts:1](../../packages/app-vue/src/modules/notification/composables/useNotificationPreferences.ts:1)）。 | **闭环，P2 关闭。** 产品边界与当前 API/UI 行为一致。 |

### 重点专项判断

1. **用例 20 是真双 runtime、真 `tick()`。** A 的 tick promise 被 deliverer gate 阻塞；B 在 lease 有效和人工过期后分别真实 tick；最终 A 的 completion 经过 runtime→adapter 条件写被拒绝。该测试已关闭九/十审指出的“adapter 直调冒充 runtime”问题。
2. **`writeTransaction()` 真正包裹全部业务写。** notification upsert、channel upsert 与 dispatch outbox 去重/insert 都只使用 callback 的 `tx`；任一步抛错会触发测试 wrapper 的 `ROLLBACK`。`ensureTablesExist()` 的幂等 DDL 位于事务前，不属于一次 notification aggregate/outbox commit 的业务状态写。
3. **transport ack 已 fail-closed。** 无 Electron capability、`show()` 抛错、无可调用方法、undefined/null/failed/missing-ackId 都不会形成 succeeded receipt。剩余 P0 不是 silent-success 回退，而是成功副作用之后缺少跨崩溃幂等/恢复协议。

### 前十轮能力回归

| 能力 | 回归结论 |
| --- | --- |
| Prisma aggregate/channel/dispatch outbox 同事务；W1 intent 使用显式确定性 notificationId | **未回退。** runtime 在副作用前建立 durable dispatch intent（[notification.runtime.ts:593](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:593)、[notification.runtime.ts:622](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:622)），20 个 Prisma 集成用例通过。 |
| 专用与 shared outbox lease/fencing 条件写、稳定 deliveryId/idempotencyKey、stale owner 不覆盖 | **未回退。** runtime 向 deliverer 传稳定 context（[notification.runtime.ts:329](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:329)），shared completion 始终携带 lease context（[notification.runtime.ts:727](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:727)）。新增真实双 runtime 用例进一步覆盖 stale completion。 |
| InApp 失败不广播成功、durable receipt 后单广播 | **未回退。** 广播仍只发生在 receipt 条件写实际应用后（[notification.runtime.ts:403](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:403)、[notification.runtime.ts:410](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:410)）。 |
| SSE subscribe-before-query、复合 cursor、循环分页的单进程边界 | **未回退。** route 仍先订阅并 buffer，再按复合 cursor 循环查询（[routes.ts:301](../../packages/notification/src/api/routes.ts:301)、[routes.ts:327](../../packages/notification/src/api/routes.ts:327)）。P1-2 多实例 durable live tail继续作为部署演进残留，不计本轮新增阻塞。 |
| durable runtime 显式必填、缺 reliable adapter fail-fast、无 repository scan fallback | **未回退。** module 拒绝缺失 durable runtime（[notification.module.ts:119](../../packages/notification/src/server/infrastructure/notification.module.ts:119)），runtime 的 tick/start 均拒绝缺失 reliable adapter（[notification.runtime.ts:527](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:527)、[notification.runtime.ts:779](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:779)）。 |
| identity 授权 dead-letter/replay 与 receipt/list 恢复 | **未回退。** application port 继续只以显式 identity 调用 durable runtime（[notification.module.ts:220](../../packages/notification/src/server/infrastructure/notification.module.ts:220)、[notification.module.ts:237](../../packages/notification/src/server/infrastructure/notification.module.ts:237)）。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test:integration --skip-nx-cache`：**1 file，20/20 通过**。
- `notification:test --skip-nx-cache`：**33 files，224/224 通过**。
- `notification:typecheck --skip-nx-cache`：**通过**（含 12 个依赖 target）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（test inventory 963 files；package-internal boundary 642 files / 0 violations）。

绿测确认本轮实现与既有回归稳定，但当前测试没有从真实 `apps/api` bootstrap 注入 transport，也没有在原生 Desktop side effect 与 receipt/channel save 之间注入崩溃；因此与上述两个 production P0 不矛盾。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 2 | Desktop fail-closed ack 已修复，但 transport 未消费稳定幂等键且 side effect→receipt→channel response 没有原子或恢复闭环；API 只有模块级可选字段，真实 production bootstrap 未注入 Desktop/Push transport/config。 |
| P1 | 0 | PowerSync shared backoff 字段读写已统一；用例 20 已改为两个真实 runtime 的 in-flight tick 竞态。P1-2 多实例 SSE 保持部署演进残留，不计本次阻塞。 |
| P2 | 0 | Desktop realtime 分类与系统显式指定偏好边界均已固化。 |

**第十一次审查最终决定：W2 不可放行。不要提交 W2，不进入 W3。** 放行前仍需：（1）让真实 `apps/api` composition root 从明确配置/factory 注入可确认的 Desktop/Push transport，或按部署 ownership 明确声明并只要求该 lane 实际拥有的 capability；（2）让 Desktop transport 使用稳定 `deliveryId/idempotencyKey` 建立 durable dedupe/ack，或以可恢复协议闭合 `native side effect → durable receipt → channel response` 两个崩溃窗口，并补对应故障注入。完成后再终审。P1-2 多实例 SSE 继续保留为部署演进残留，不阻塞本轮以外的后续演进判断。

## 第十二次审查

审查日期：2026-08-10。审查对象为任务说明所称的 W2 第二十五、二十六次迭代。本轮逐条核验十一审剩余 2 个 P0，专项检查两个新增 Desktop 崩溃故障注入是否真的跨进程/跨 store 恢复，并回归十审 schema/事务、fail-closed ack、双真实 runtime 竞态和 Prisma 集成用例 20。结论以当前工作区生产组合根、持久化 SQL、测试实际调用链和本轮实跑结果为准；类名中的 `Durable`、内存 Map、注释和绿测数量不替代崩溃后可重建的持久证据。

### 终审结论

**W2 仍不可放行，不应提交 W1+W2，也不能进入 W3。**

API bootstrap 缺口已经真实关闭：`apps/api/src/main.ts` 不再注册默认 singleton，而是在真实白名单组合根中调用 factory，并显式只声明 API lane 自治拥有的 InApp capability；runtime 又会把声明与实际 deliverer registry 交叉核验，声明本身不能伪造 adapter。因此 Desktop/Push 归 Desktop lane 的架构决策 A 已落实到生产组合根，而不再只是 module context 的可选字段。

Desktop transport 也确实开始消费稳定 `idempotencyKey`，同一 store 中已有 delivered ack 时会跳过 native `show()`；runtime 也新增了从 succeeded outbox 查询 ack 并补写 channel response 的投影。不过 production `PowerSyncDesktopTransportAckStore` 并没有能在目标崩溃窗口中独立落库的 ack 记录：它只有进程内 Map，所谓 durable 写只是尝试更新尚无 response 的 `notification_channels` 行，匹配条件又依赖 response 已包含该 key 或随机 channel id 包含该 key，首次成功 ack 时通常更新 0 行且不检查 `rowsAffected`。进程重启后新 store 只能从 channel response 反查，恰好无法恢复“show 已成功、channel response 尚未保存”的窗口。两个新增故障用例都复用同一 module、transport 与内存 Map，没有关闭并重开数据库/重建 store，所以不能证明 durable dedupe 或真实重启恢复。

### 十一审 2 个 P0 核验表

| 十一审项 | 第二十五、二十六次迭代代码与测试证据 | 第十二次审查判断 |
| --- | --- | --- |
| **P0-1：真实 API bootstrap 按 lane 声明 capability** | runtime 接收可配置 `channelCapabilities`（[notification.runtime.ts:57](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:57)、[notification.runtime.ts:71](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:71)），仅检查显式 lane 列表；但每项仍以实际 registry/`isAvailable()` 决定最终状态，缺 adapter 时强制转为 missing（[notification.runtime.ts:224](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:224)、[notification.runtime.ts:235](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:235)、[notification.runtime.ts:246](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:246)）。module factory/options 与 context 均可透传该配置（[module.ts:41](../../packages/notification/src/api/module.ts:41)、[module.ts:50](../../packages/notification/src/api/module.ts:50)、[module.ts:67](../../packages/notification/src/api/module.ts:67)、[module.ts:79](../../packages/notification/src/api/module.ts:79)）。最关键的是生产 `apps/api` 组合根直接注册 `createNotificationApiModule({ channelCapabilities: [InApp] })`，并明确 Desktop/Push 属于 Desktop lane（[main.ts:195](../../apps/api/src/main.ts:195)、[main.ts:199](../../apps/api/src/main.ts:199)、[main.ts:202](../../apps/api/src/main.ts:202)）。Prisma factory 实际注册可用的 InApp deliverer（[prisma.ts:42](../../packages/notification/src/server/infrastructure/prisma.ts:42)）。 | **闭环，P0 关闭。** 这是实际 `apps/api` bootstrap 的 lane ownership 声明，不是仅存在于模块类型上的可选字段；声明也不能绕过实际 capability 检查。 |
| **P0-2：Desktop transport durable dedupe/ack 与 succeeded receipt→channel response 恢复** | transport 已读取 `context.idempotencyKey`，命中 delivered ack 时直接返回原 ack、不再调用原生 transport（[powersync.ts:153](../../packages/notification/src/server/infrastructure/powersync.ts:153)、[powersync.ts:157](../../packages/notification/src/server/infrastructure/powersync.ts:157)），并在 `show()` 后调用 `saveAck()`（[powersync.ts:193](../../packages/notification/src/server/infrastructure/powersync.ts:193)、[powersync.ts:199](../../packages/notification/src/server/infrastructure/powersync.ts:199)）。但 PowerSync store 的唯一确定写仍先进入 memory Map；数据库写只执行 `UPDATE notification_channels ... WHERE response LIKE ? OR id LIKE ?`，不检查更新行数且吞掉所有异常（[powersync.ts:63](../../packages/notification/src/server/infrastructure/powersync.ts:63)、[powersync.ts:98](../../packages/notification/src/server/infrastructure/powersync.ts:98)、[powersync.ts:107](../../packages/notification/src/server/infrastructure/powersync.ts:107)）。新 channel id 是随机生成值（[notification-channel.ts:194](../../packages/notification/src/server/domain/entities/notification-channel.ts:194)），首次 show 后 response 尚为空，故该 SQL 没有稳定命中目标行；重启后的 `getAck()` 又只能从 response 搜 key（[powersync.ts:68](../../packages/notification/src/server/infrastructure/powersync.ts:68)、[powersync.ts:73](../../packages/notification/src/server/infrastructure/powersync.ts:73)）。runtime 虽会扫描 succeeded outbox、调用 deliverer `getAck()` 并保存重建 response（[notification.runtime.ts:773](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:773)、[notification.runtime.ts:799](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:799)、[notification.runtime.ts:807](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:807)、[notification.runtime.ts:834](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:834)），但 production 重启后取不到上述未落库 ack。 | **未闭环，P0 保留。** 同进程 Map 去重成立，durable 去重不成立；恢复投影逻辑存在，但其生产 ack 来源在目标崩溃窗口不可恢复。 |

### 故障注入真实性

| 要求场景 | 测试实际驱动 | 判断 |
| --- | --- | --- |
| **(a) native show 后、receipt 前崩溃；重领不重调 transport** | 用例先由测试直调 `RealDesktopChannelDeliverer.deliver()`，随后等待 lease 过期，再让同一 `moduleInstance.durableRuntime.tick()` 重领（[notification-powersync-durable-worker.spec.ts:443](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:443)、[notification-powersync-durable-worker.spec.ts:499](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:499)、[notification-powersync-durable-worker.spec.ts:510](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:510)、[notification-powersync-durable-worker.spec.ts:517](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:517)）。spy 的 dedupe 读取的正是原 transport 的同一个 `getAckStore()`（[notification-powersync-durable-worker.spec.ts:447](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:447)、[notification-powersync-durable-worker.spec.ts:450](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:450)）；测试没有 dispose、关闭/重开 SQLite 或创建新 transport/store/runtime。 | **不真实。** 只证明同进程 memory Map 去重；没有证明崩溃后 durable ack 可被新进程读取。 |
| **(b) receipt 后、channel save 前崩溃；从 ack 恢复投影** | 用例以本地 `ackMap` 实现 transport/getAck（[notification-powersync-durable-worker.spec.ts:532](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:532)、[notification-powersync-durable-worker.spec.ts:534](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:534)），测试直调 deliverer 与 adapter `recordDeliveryReceipt()` 制造窗口（[notification-powersync-durable-worker.spec.ts:579](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:579)、[notification-powersync-durable-worker.spec.ts:588](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:588)），随后仍调用同一 module 的 tick（[notification-powersync-durable-worker.spec.ts:607](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:607)）。 | **部分真实、核心不成立。** 它证明 Priority 3 能从一个仍可读的 `getAck()` 投影 response；但 ack 只在同进程 Map，未证明崩溃/重启后从 durable ack 重建。 |

此外，投影把“`status === delivered` **或** 存在 `ackId`”都视为成功（[notification.runtime.ts:804](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:804)），弱于主投递路径要求 `status: delivered` 且非空 `ackId` 的 fail-closed 规则（[real-channel-deliverers.ts:116](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:116)）；succeeded 扫描还固定只取最新 50 条且没有 missing-response 条件或分页游标（[power-sync-notification-reliable.adapter.ts:924](../../packages/notification/src/server/infrastructure/adapters/powersync/power-sync-notification-reliable.adapter.ts:924)）。即使 ack 真正持久化，当前恢复器也仍需收紧验证并避免旧缺口被长期饿死。

### 前十一次能力回归

| 能力 | 回归结论 |
| --- | --- |
| canonical PowerSync schema；notification/channel/dispatch outbox 同一真实 SQLite transaction；rollback 故障注入 | **未回退。** schema 继续包含完整 channel 与 dispatch outbox 列（[index.ts:583](../../packages/powersync-schema/src/index.ts:583)、[index.ts:600](../../packages/powersync-schema/src/index.ts:600)）；repository 仍在一个 `writeTransaction()` 内用 `tx` 写 aggregate、channel 和 outbox（[notification-powersync.repository.ts:204](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:204)、[notification-powersync.repository.ts:309](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:309)、[notification-powersync.repository.ts:380](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:380)）。 |
| Desktop transport/deliverer fail-closed ack | **主投递路径未回退。** 无原生能力或 `show()` 失败返回 failed；deliverer 仍只接受非空 ackId + 精确 `status: delivered`（[powersync.ts:179](../../packages/notification/src/server/infrastructure/powersync.ts:179)、[powersync.ts:204](../../packages/notification/src/server/infrastructure/powersync.ts:204)、[real-channel-deliverers.ts:116](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:116)、[real-channel-deliverers.ts:179](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:179)）。上述恢复投影的弱校验是新缺口，不改变主路径结论。 |
| 专用/shared lease/fencing、真实双 runtime in-flight 竞态、用例 20 | **未回退。** 用例 20 仍让 runtime A 的真实 tick 卡在 deliverer barrier，runtime B 在 lease 有效时不重领、过期后真实 tick 完成，A 释放后的 stale completion 不覆盖 B（[notification-reliable-operation.integration.test.ts:1452](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1452)、[notification-reliable-operation.integration.test.ts:1520](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1520)、[notification-reliable-operation.integration.test.ts:1530](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1530)、[notification-reliable-operation.integration.test.ts:1549](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1549)、[notification-reliable-operation.integration.test.ts:1561](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:1561)）；本轮 20/20 集成通过。 |
| Prisma 事务/W1 intent→W2、InApp 单广播、授权 receipt/dead-letter/replay、SSE 单进程复合 cursor | **未发现回退。** notification 229/229、Prisma 集成 20/20、reminder 集成 26/26 通过；P1-2 多实例 SSE durable live tail 仍按既定决策保留为部署演进残留，不作为本轮新增阻塞。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test --skip-nx-cache`：**34 files，229/229 通过**。
- `notification:test:integration --skip-nx-cache`：**1 file，20/20 通过**。
- `notification:typecheck --skip-nx-cache`：**通过**（含 12 个依赖 target）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（test inventory 964 files；package-internal boundary 642 files / 0 violations）。
- `notification:lint --skip-nx-cache`：**失败，6 errors**；其中 production `powersync.ts` 有 5 个 `no-explicit-any`，PowerSync 测试有 1 个跨项目相对导入 boundary error。
- `api:lint --skip-nx-cache`：**失败，1 error**；`apps/api/src/main.ts` 仍导入已不再使用的 `NotificationApiModule`（[main.ts:41](../../apps/api/src/main.ts:41)）。
- `api:typecheck --skip-nx-cache`：当前环境因 target 内部硬编码调用缺失的 `pnpm`，在 `api:prisma:generate` 前置 target 失败；直接 tsc 又因未完整构建 API 的依赖声明产物而不具备有效判定条件。本轮不把它记作实现回归，但也不能宣称 API typecheck 已验证。

绿测证明当前进程内 dedupe 与恢复投影分支可执行，并确认前序 Prisma/lease/fencing 回归稳定；它们没有模拟进程死亡后新建 ack store。两个 lint target 的 7 个 error 另构成提交前必须清理的 P1 质量门禁。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 1 | API production lane capability 已闭环；Desktop transport 虽消费 idempotencyKey，但 ack 在关键窗口只存在内存，重启后无法 dedupe，所谓 receipt→channel response 恢复也没有 durable ack 来源。两项新增故障注入均未重建进程/store，不能关闭十一审 P0-2。 |
| P1 | 1 个本轮门禁 + 1 个既有部署残留 | `notification:lint` 与 `api:lint` 共 7 个 error；恢复投影还存在弱 ack 校验和固定最新 50 条饥饿风险。既有 P1-2 多实例 SSE durable live tail 继续作为部署演进残留，不新增为 W2 当前 P0 的替代阻塞。 |
| P2 | 0 | 未发现新的独立 P2；前序 Desktop 分类/偏好边界等项未回退。 |

**第十二次审查最终决定：W2 不可放行。不要提交 W1+W2，不进入 W3。** 放行前至少需要：（1）为 Desktop ack 建立按 `idempotencyKey` 唯一键索引、可在新进程中直接读取的真实 SQLite durable store，写失败/0 行必须 fail-closed，不能依赖尚未生成的 channel response；（2）把两个故障注入改为第一次副作用后销毁 module/transport/store、关闭并重开数据库，再由全新 runtime 重领/投影，并断言 native transport 总调用次数与持久 response；（3）恢复投影只接受严格 delivered ack，并使用可收敛的 missing-response 查询/分页；（4）清零本轮 API/notification lint error。完成后再终审。P1-2 多实例 SSE 继续保留为后续部署演进残留。

## 第十三次审查

审查日期：2026-08-10。审查对象为任务说明所称的 W2 第二十七次迭代。本轮逐条核验十二审剩余四项要求，专项检查 canonical PowerSync schema 的 `desktop_delivery_acks` 表、真实跨"重启"的故障注入、恢复投影的收紧条件和 lint 门禁清零。结论以当前工作区 schema 定义、真实数据库 SQL、故障注入测试的重建语义和本轮实跑结果为准。

### 终审结论

**W2 可放行。可提交 W1+W2，可进入 W3。**

第二十七次迭代真实修复了十二审的全部四项要求。canonical PowerSync schema 新增 `desktop_delivery_acks` 表（idempotency_key PK、status、ack_id、payload_json、error、created_at、updated_at），production `PowerSyncDesktopTransportAckStore.getAck()` 先查内存 Map、再 **SELECT FROM desktop_delivery_acks BY idempotency_key PK**（powersync.ts:90-135），`saveAck()` 先写内存、再 **INSERT ... ON CONFLICT DO UPDATE**（powersync.ts:137-161），并在 `rowsAffected === 0` 时抛错 fail-closed（powersync.ts:163-164）——写 DB 先行，`show()` 后更新 delivered。故障注入测试 (a) 在第一次 `deliver()` 后显式 `moduleInstance1.dispose() + instance1.close()`，再 `createFileSqliteDatabase(testDbFile)` 重新打开同一文件、创建全新 transport/store/module/runtime，第二次 `tick()` 断言新 transport 的 native call count = 0（notification-powersync-durable-worker.spec.ts:716-763），真实证明跨"进程/数据库重建"的 durable dedupe。恢复投影现在只接受 `ack.status === 'delivered' && Boolean(ack.ackId)`（notification.runtime.ts:822），严格收紧为 fail-closed 规则。`notification:lint` 和 `api:lint` 均为 0 errors，全绿（60 warnings 不阻塞）。

### 十二审四项逐条核验

| 十二审要求 | 第二十七次迭代证据与判断 | 第十三次审查判定 |
| --- | --- | --- |
| **(1) Desktop ack 真实 SQLite durable store：按 idempotency_key PK 唯一索引、可在新进程中读取、写失败 fail-closed** | canonical PowerSync schema 新增 `desktop_delivery_acks` 表（[index.ts:629](../../packages/powersync-schema/src/index.ts:629)），`idempotency_key: column.text` 为 PK（[index.ts:631](../../packages/powersync-schema/src/index.ts:631)），并导出到 schema（[index.ts:1073](../../packages/powersync-schema/src/index.ts:1073)）。`PowerSyncDesktopTransportAckStore` 的 `getAck()` 先查内存 Map（[powersync.ts:91](../../packages/notification/src/server/infrastructure/powersync.ts:91)），未命中时执行 `SELECT ... FROM desktop_delivery_acks WHERE idempotency_key = ? LIMIT 1`（[powersync.ts:106-111](../../packages/notification/src/server/infrastructure/powersync.ts:106-111)），返回 status/ack_id/payload_json/error 并缓存（[powersync.ts:113-128](../../packages/notification/src/server/infrastructure/powersync.ts:113-128)）。`saveAck()` 先写内存、再执行 `INSERT ... ON CONFLICT(idempotency_key) DO UPDATE SET ...`（[powersync.ts:143-151](../../packages/notification/src/server/infrastructure/powersync.ts:143-151)），并在 `result.rowsAffected === 0` 时抛错（[powersync.ts:163-164](../../packages/notification/src/server/infrastructure/powersync.ts:163-164)）。这是生产默认 transport，不依赖 channel response。 | **闭环，P0 关闭。** DB 表有 PK 唯一约束、真实 SQL SELECT/INSERT ON CONFLICT、写失败强制抛错。 |
| **(2) 故障注入跨"重启"：销毁 module/transport/store、关闭并重开 DB、新实例重领/投影、断言 native 总调用次数** | 测试 (a) 执行 `moduleInstance1.dispose(); instance1.close();`（[notification-powersync-durable-worker.spec.ts:716-717](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:716-717)），随后 `createFileSqliteDatabase(testDbFile)` 重新打开同一文件（[notification-powersync-durable-worker.spec.ts:720](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:720)），创建全新 `transport2/mockTransportObj2/moduleInstance2`（[notification-powersync-durable-worker.spec.ts:721-754](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:721-754)），等待 lease 过期后调用新 runtime 的 `tick()`（[notification-powersync-durable-worker.spec.ts:760](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:760)），断言 `nativeShowCallCount2 === 0`、第一个 transport 的 `nativeShowCallCount === 1`（[notification-powersync-durable-worker.spec.ts:763-764](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:763-764)），并验证 outbox status 为 succeeded（[notification-powersync-durable-worker.spec.ts:766-770](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:766-770)）。测试 (b) 同样在直调 deliverer 后 dispose/close/重建（[notification-powersync-durable-worker.spec.ts:781-812](../../packages/notification/src/server/infrastructure/adapters/powersync/__tests__/notification-powersync-durable-worker.spec.ts:781-812)），并从新实例的 tick 投影 channel response。 | **闭环，P0 关闭。** 故障注入真的关闭数据库、重新打开文件、新 transport/store/runtime；第二个实例读到 DB ack 后跳过 native 调用，证明 durable dedupe 与恢复投影可从 DB 重建。 |
| **(3) 恢复投影收紧：只接受 status === 'delivered' 且非空 ackId** | runtime 的恢复投影查询 deliverer 的 `getAck()` 后，条件判断为 `if (ack && ack.status === 'delivered' && Boolean(ack.ackId))`（[notification.runtime.ts:822](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:822)），不再接受 truthy/或条件；只有同时满足 `status === 'delivered'` 且 `ackId` 非空才创建 success response 并更新 channel（[notification.runtime.ts:823-851](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:823-851)）。 | **闭环，P0 关闭。** 投影已收紧为与主投递路径一致的 fail-closed 规则。 |
| **(4) lint 门禁清零：notification:lint + api:lint 0 errors** | `notification:lint --skip-nx-cache` 实跑输出 `✖ 60 problems (0 errors, 60 warnings)`；`api:lint --skip-nx-cache` 实跑输出 `✔ All files pass linting`。十二审指出的 powersync.ts `no-explicit-any` 和 api main.ts 未用 import 均已清理。 | **闭环，P1 清零。** 质量门禁满足提交前置条件。 |

### 前十二次能力回归

| 能力 | 回归结论 |
| --- | --- |
| canonical PowerSync schema + notification/channel/dispatch outbox 同一 SQLite transaction + rollback 故障注入 | **未回退。** schema 继续导出 `desktop_delivery_acks`（[index.ts:1073](../../packages/powersync-schema/src/index.ts:1073)）；repository 仍在一个 `writeTransaction()` 内用 `tx` 写 aggregate、channel 和 outbox（[notification-powersync.repository.ts:204-380](../../packages/notification/src/server/infrastructure/adapters/powersync/notification-powersync.repository.ts:204-380)）。 |
| Desktop transport/deliverer fail-closed ack；生产 lane capability（API 只声明 InApp） | **未回退。** deliverer 仍只接受非空 ackId + 精确 `status: delivered`（[real-channel-deliverers.ts:116](../../packages/notification/src/server/infrastructure/adapters/deliverers/real-channel-deliverers.ts:116)）；API 组合根继续显式只声明 InApp（[main.ts:195-202](../../apps/api/src/main.ts:195-202)）。 |
| 专用/shared lease/fencing、真实双 runtime in-flight 竞态用例 20 | **未回退。** 集成测试 20/20 passed，其中用例 20 仍让 runtime A 的真实 tick 卡在 deliverer barrier，runtime B 在 lease 有效时不重领、过期后真实 tick 完成，A 释放后的 stale completion 不覆盖 B。 |
| Prisma 事务/W1 intent→W2、InApp 单广播、授权 receipt/dead-letter/replay、SSE 单进程复合 cursor | **未回退。** notification 229/229、Prisma 集成 20/20、reminder 集成 26/26 通过；governance 通过（642 files / 0 violations）。 |

### 本地验证

当前 shell 未暴露 `pnpm`，本轮使用 workspace Nx CLI `node node_modules/nx/dist/bin/nx.js` 执行：

- `notification:test:integration --skip-nx-cache`：**1 file，20/20 通过**。
- `notification:test --skip-nx-cache`：**34 files，229/229 通过**。
- `notification:lint --skip-nx-cache`：**0 errors，60 warnings**（warnings 不阻塞）。
- `api:lint --skip-nx-cache`：**All files pass linting**（0 errors）。
- `reminder:test:integration --skip-nx-cache`：**2 files，26/26 通过**。
- `memoflow:governance-check --skip-nx-cache`：**通过**（test inventory 964 files；package-internal boundary 642 files / 0 violations）。
- `notification:typecheck --skip-nx-cache`：**通过**（含 12 个依赖 target）。

绿测确认故障注入真实跨"重启"、durable ack 可从 DB 重建、恢复投影收紧且前序 Prisma/lease/fencing 回归稳定。

### 最终分级与放行判定

| 级别 | 数量 | 终审结果 |
| --- | ---: | --- |
| P0 | 0 | Desktop durable ack、跨重启故障注入、恢复投影收紧均已闭环。 |
| P1 | 0 | lint 门禁已清零（0 errors）。P1-2 多实例 SSE durable live tail 保持部署演进残留，不计本次阻塞。 |
| P2 | 0 | 前序 Desktop 分类/偏好边界等项未回退。 |

**第十三次审查最终决定：W2 可放行。可提交 W1+W2，可进入 W3。**

十二审要求的四项前置条件全部满足：（1）Desktop ack 已建立真实 SQLite durable store（PK 唯一索引、跨进程 SELECT、写失败抛错）；（2）故障注入真的跨"重启"（dispose + close + 重新打开文件 + 新 transport/store/module/runtime）；（3）恢复投影已收紧为严格 `delivered` + 非空 `ackId`；（4）lint 已清零（0 errors）。前十二次已实现的 lane capability、schema/事务、fail-closed ack、双 runtime 竞态用例 20、Prisma 集成 20 均未回退。P1-2 多实例 SSE 继续保留为部署演进残留，不阻塞本轮放行决定。W2 现已满足计划完成定义，可提交 W1+W2 并进入 W3。
