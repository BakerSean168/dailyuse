# W6（Knowledge projection、Settings、认证体验收口）第一轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=fcdd25a23185ba60adf1c213c06a89c12403b5a5`，W6 为未提交改动  
审查方式：只审查并新增本文档；未修改业务代码，未 commit/push。

## 终审结论

**W6 第一轮不可放行：不要提交 W6，不进入 W7。**

Knowledge 的服务端 ledger 已把 Git commit 与 projection operation 分开；Prisma 条件更新阻止 `Succeeded` 被 `Pending/Failed` 覆盖，手工 replay 能在真实 PostgreSQL 中写入 projection 并推进到 `Succeeded`。Settings 的无记录 reset 也会创建并持久化完整默认 aggregate。认证失败 receipt 能写入 store/localStorage，测试中的 reload 与成功清除成立。

但申报的是“体验收口”，当前三条用户链路均未闭环：app-vue 没有 Knowledge ledger/replay UI；Settings defaults 方法没有生产调用点，分类 reset 参数还被 composable 丢弃；认证 receipt 没有页面消费者，刷新后用户仍看不到可操作错误。认证持久化还会原样保存可能来自服务端的错误 message，“不泄露凭据”测试并未注入包含凭据的错误消息，不能证明安全不变量。与此同时 `repository:lint` 实测有 3 个 W6 错误，与“lint 0 errors”的验证声明冲突。

最终分级：**P0 = 0，P1 = 5，P2 = 2。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 5 | Knowledge 没有用户可见/replay 闭环；Settings defaults 未初始化且分类 reset 丢参；认证错误没有恢复 UI；持久化 message 仍可能携带凭据；repository lint 门禁失败。 |
| P2 | 2 | Knowledge 集成矩阵没有真正驱动 webhook/reconcile/重复投递；全局 localStorage 补丁在当前 Node 下未生效且不隔离测试。 |

## P1-1：Knowledge ledger/replay 只到 HTTP client，没有形成 UI 与 desktop 可用闭环

服务端 list/replay 实现本身存在（[`knowledge-repository-projection.service.ts:544`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:544)、[`knowledge-repository-projection.service.ts:598`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:598)），HTTP adapter 也暴露了对应方法。但全仓搜索表明 app-vue 没有任何 `listKnowledgeWriteRequests`、`replayKnowledgeWriteRequestProjection` 或 `projectionStatus` 的消费点；因此用户无法看到 `Committed + Pending/Failed`，也无法触发 replay 或刷新状态。

Desktop transport 更不是“forward”：IPC adapter 的两个新增方法都直接返回 `serverProjectionUnavailable()`（[`repository-ipc.adapter.ts:170`](../../packages/repository/src/infrastructure-client/adapters/ipc/repository-ipc.adapter.ts:170)）。所谓双 transport surface test 只断言源码包含方法名（[`knowledge-write-request-ledger-contract.surface.spec.ts:53`](../../packages/repository/src/application-client/knowledge-write-request-ledger-contract.surface.spec.ts:53)），没有断言 IPC 行为，更无法证明 UI/服务端一致。

关闭条件：在实际 Knowledge 页面接入 list、状态刷新和 Pending/Failed replay，显示 commit 与 projection 两条状态及可操作错误；为 web 与 desktop 明确可用 transport（或明确 desktop 统一走 HTTP），补组件/页面行为测试，证明失败、刷新、重放成功后的 UI 与服务端 ledger 一致。

## P1-2：Settings defaults 没有生产初始化，分类 reset 参数被静默丢弃

`loadDefaults()` 现在能调用 service 并写 store（[`useUserSetting.ts:69`](../../packages/app-vue/src/modules/setting/composables/useUserSetting.ts:69)），但它只在新增单测里被调用；生产代码没有任何调用点。`UserSettingsView` 只解构并调用 `loadSettings`，presentation bootstrap 也只请求 `getUserSettings()`（[`UserSettingsView.vue:58`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:58)、[`usePresentationBootstrap.ts:60`](../../packages/app-vue/src/modules/setting/composables/usePresentationBootstrap.ts:60)）。所以新 getter 的 defaults fallback 在真实启动中仍通常是 `null`。

同时 `resetToDefaults(_category)` 明确忽略参数，并始终调用无参 `service.resetUserSettings()`（[`useUserSetting.ts:99`](../../packages/app-vue/src/modules/setting/composables/useUserSetting.ts:99)）。分类 reset 会意外重置全部分类。新增 app-vue 测试只覆盖手工 `loadDefaults()` 和 store fallback，没有覆盖 reset 后 UI，也没有捕获丢参。

真实 DB 的无记录 reset 两项测试通过，证明 server use case 能 materialize 默认 aggregate；这个局部通过不能替代客户端初始化与分类命令链路。

关闭条件：在唯一启动/设置 bootstrap 中初始化 defaults，并定义与 `loadSettings` 的失败及并发顺序；传递 category 到 client；补页面级测试证明新用户启动可见默认值、分类 reset 不改其他分类、全量 reset 后 UI 与返回 aggregate 一致。

## P1-3：认证 receipt 能恢复到 store，但没有恢复到用户界面

store 初始化会从 localStorage 读取 receipt（[`authentication-store.ts:38`](../../packages/app-vue/src/modules/authentication/stores/authentication-store.ts:38)），成功 mutation 会清除（[`usePassword.ts:52`](../../packages/app-vue/src/modules/authentication/composables/usePassword.ts:52)）。然而 app-vue 中 `passwordMutationError` 除 store 实现和测试外没有任何消费点，`usePassword()` 本身也没有生产调用点。刷新后 receipt 虽在内存中，用户页面仍不会显示 message、request id、retryability 或 retry action，因而不满足“页面卸载/刷新恢复可操作错误”的体验目标。

关闭条件：在真实密码修改/忘记密码/重置密码页面使用 `usePassword`，渲染恢复后的结构化错误与 request id，并按 `retryable` 提供动作；补真实组件卸载、重新挂载/应用 bootstrap 测试，而不只是新建 Pinia 后直接读取 store。

## P1-4：“错误不泄露凭据”没有由实现或测试保证

receipt builder 保存传入的 `message`。该 message 来自 `translateResultError`；当没有翻译 key 时，公共 translator 会回退为原始 `ResultError.message`（[`result-error.ts:175`](../../packages/http-client/src/result-error.ts:175)）。因此如果上游错误消息包含 reset token、当前/新密码、邮箱参数或其他敏感输入，它会被原样写进持久 localStorage（[`authentication-store.ts:57`](../../packages/app-vue/src/modules/authentication/stores/authentication-store.ts:57)）。

现有“不泄露”测试给出的错误消息只是 `Current password is incorrect`，再断言 receipt 不含请求中的两个 secret（[`usePassword.spec.ts:187`](../../packages/app-vue/src/modules/authentication/composables/usePassword.spec.ts:187)）；实现从未接触该 request 对象，这个断言天然会通过。测试没有把 secret/token 放进 `error.message` 或 context，也没有检查持久化字符串。

关闭条件：持久 receipt 只保存 code、经过 allowlist 的本地化安全文案、request id、operation、retryability、timestamp；不得保存任意 server message/context。测试必须注入包含 password/token/email 的恶意/回显错误，断言 store 与 localStorage 均不含敏感值。

## P1-5：W6 自身 lint 门禁失败

实测 `repository:lint` 有 3 个错误：未使用的 `ListKnowledgeWriteRequestsSchema`（route 第 22 行）、可改为 `const` 的 `candidates`（projection service 第 641 行）、未使用的 `now` 参数（Prisma repository 第 130 行）。因此相关项目 lint 并非申报的“0 errors”，`run-many` 以失败退出。

关闭条件：修复 W6 引入的 3 个 lint error，并重新执行最近项目 lint；若声明“全量 lint”，必须提供实际全 workspace 命令与退出码，不能把 warnings 或失败 target 记为通过。

## P2-1：四项 Knowledge 集成测试不是所描述的 webhook 故障矩阵

“webhook delay”测试直接依次调用 repository 的 `markCommitted`、`bindProjectionSource`；“webhook failure”直接调用 `markProjectionFailed`，没有发送 webhook，也没有让 `applyChanges` 抛错（[`knowledge-write-request-projection-ledger.integration.test.ts:76`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:76)、[`knowledge-write-request-projection-ledger.integration.test.ts:101`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:101)）。“external Git refresh”也直接调用 repository 的 `markProjectionSucceededByCommit`，没有经过 reconcile/webhook service（[`knowledge-write-request-projection-ledger.integration.test.ts:214`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:214)）。

重复 replay 测试先把记录设为 `Succeeded`，随后两次调用只走 service 的早返回；它没有把同一 Pending/Failed operation 重投，也没有证明 projection `applyChanges` 的重复执行幂等。状态 `Succeeded` 不倒退的 Prisma guard 是有效局部证据，但测试名称与验证声明扩大了覆盖范围。

关闭条件：用真实 route/service 驱动 commit 后延迟 webhook、真实 projection 异常、重复 delivery、Pending/Failed replay、reconcile refresh；断言 ledger、projection row、attempts 与 HTTP DTO，并覆盖乱序/并发 `Failed` 与 `Succeeded` 状态竞争。

## P2-2：预存 `setup.ts` localStorage 补丁在当前 Node 下没有真正安装，并缺少测试隔离

补丁只在 `typeof globalThis.localStorage === 'undefined'` 时创建 happy-dom Storage（[`setup.ts:8`](../../packages/app-vue/src/test/setup.ts:8)）。当前 Node 暴露了 `globalThis.localStorage`，但未配置 `--localstorage-file`；全量 app-vue 测试的每个 worker 都报告 `ExperimentalWarning: localStorage is not available because --localstorage-file was not provided`。因此 guard 判断为“已存在”，实际却不可用。认证新测试之所以通过，是该 spec 自己无条件覆盖了 global localStorage，而不是公共 setup 生效。

此外公共 hooks 不会在每项测试前清空 storage；一旦公共 setup 真正安装共享 Storage，同一 worker 内测试可能互相污染。当前 874/874 绿不能证明该补丁正确，只证明现有顺序没有触发失败。

关闭条件：公共 setup 应安装经功能探测可读写的 Storage，并在测试间清理或明确按 suite 隔离；删除 spec 内的重复全局覆盖后，认证 reload 测试与全量 app-vue 仍应无 localStorage warning 通过。

## 已确认成立的局部实现

- Knowledge commit 与 projection 状态字段已分离；`markProjectionSucceeded/Failed/bindProjectionSource` 均以 `projectionStatus != Succeeded` 为条件，已成功状态不会被这些路径倒退（[`knowledge-write-request-prisma.repository.ts:84`](../../packages/repository/src/server/infrastructure/adapters/prisma/knowledge-write-request-prisma.repository.ts:84)）。
- 手工 replay 在 connection lease 下执行，并在真实 PostgreSQL 中写入 projection 后推进 ledger；4 项 integration test 实测通过。
- 无记录 reset 会 `UserSetting.create({ identityId })` 并保存，返回完整默认 preference tree；2 项真实 PostgreSQL integration test 实测通过。
- 密码 Result failure 能生成 code/requestId/retryable/operation/failedAt receipt，测试中的 store rebuild 恢复与成功清除成立。
- `useCalendarView.spec.ts` 改为本地日期构造，避免固定 `+08:00` 与运行 TZ 的耦合，未发现语义回退。

## W0--W5 回归与验证记录

本次环境没有 `pnpm` 可执行文件，以下均直接调用同一 workspace 的 Nx binary；未改代码以迁就验证。

| 验证 | 结果 | 判断 |
| --- | --- | --- |
| `nx run repository:test --skip-nx-cache` | 29 files，188/188 | 通过 |
| `nx run repository:test:integration --skip-nx-cache` | 1 file，4/4，PostgreSQL `127.0.0.1:5433` | 通过，但覆盖范围见 P2-1 |
| `nx run setting:test --skip-nx-cache` | 14 files，81/81 | 通过 |
| `nx run setting:test:integration --skip-nx-cache` | 1 file，2/2，真实 PostgreSQL | 通过 |
| `nx run app-vue:test --skip-nx-cache` | 166 files，874/874 | 通过；持续 localStorage warning，见 P2-2 |
| `nx run-many -t typecheck -p repository,setting,app-vue,contracts,database --skip-nx-cache` | 5 项及 25 个依赖任务通过 | 通过；依赖 build 覆盖 schedule/reminder/task/notification/goal/account 等 W0--W5 相关包 |
| `nx run-many -t lint -p repository,setting,app-vue,contracts,database --skip-nx-cache` | 失败 | repository 3 errors；其余已运行项目仅既有 warnings |
| `nx run memoflow:governance-check --skip-nx-cache` | 通过 | governance tools 35/35、test-system checks、inventory 986 与全部审计通过 |
| `git diff --check` | 无输出 | 通过 |

本轮未重复执行 local Docker prod-like 与全 workspace 所有包单测；在上述 P1 未关闭前，这些额外通过也不能改变放行结论。

## 放行门槛

1. 关闭 P1-1 至 P1-5，并为三条真实页面链路补行为测试。
2. 把 Knowledge 故障矩阵改为真实 webhook/reconcile/replay 路径，不用 repository 状态直写替代业务证据。
3. 修正公共 localStorage setup，移除 spec 私有兜底后复跑 874 项。
4. 重新执行 repository/setting/app-vue 的 test、integration、lint、typecheck，以及 `memoflow:governance-check`；全部门禁通过后再做第二轮审查。

在这些条件满足前，**W6 不可提交，不得进入 W7。**


---

# 第二轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=fcdd25a23185ba60adf1c213c06a89c12403b5a5`，W6 r2-r4 仍为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W6 第二轮仍不可放行：不要提交 W6，不进入 W7。**

第一轮 7 项中，P1-1、P1-4、P1-5、P2-2 已关闭；P1-2、P1-3、P2-1 只有部分关闭。Knowledge ledger 已接入真实设置页组件，Web 与 Desktop transport 都能到达 HTTP 服务端；认证持久 receipt 已改为安全本地化文案并有恶意 message 注入测试；3 个 lint error 已消失；公共 localStorage setup 也已具备读写探测和逐测试清理。

剩余问题不是门禁红灯，而是申报的真实体验/故障矩阵仍与代码不符：Settings 的“新用户启动可见默认值”仍只有 composable/store 断言，不是页面可见性测试；真实 Web 忘记/重置密码页仍走 `useWebAuth`，尤其 reset-password 没有 receipt 恢复 UI；Knowledge integration 没有 Pending replay、没有 `reconcileNow` refresh，且所谓并发 Failed/Succeeded 竞争是在记录已经 `Succeeded` 后才开始，实际只验证两个早拒绝/no-op。

最终分级：**P0 = 0，P1 = 2，P2 = 1。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全事故。 |
| P1 | 2 | Settings 缺页面级新用户启动证据；真实 Web reset-password（以及 Web forgot-password）未接入可恢复 receipt UI。 |
| P2 | 1 | Knowledge 真实集成矩阵仍缺 Pending replay、reconcile refresh 和有效的 Failed/Succeeded 竞争。 |

## P1-1：已关闭——Knowledge UI 与 Desktop transport 已形成真实闭环

实际设置页的 Knowledge 区域已挂载 `KnowledgeWriteRequestLedger`（[`KnowledgeRepositorySettings.vue:334`](../../packages/app-vue/src/modules/setting/components/KnowledgeRepositorySettings.vue:334)）。组件同时显示 commit、commit SHA、projection、attempts 与 projection error，并只为 `Committed + Pending/Failed` 提供 replay（[`KnowledgeWriteRequestLedger.vue:142`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.vue:142)）；刷新和 replay 成功后都会重新 list 服务端 ledger（[`KnowledgeWriteRequestLedger.vue:62`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.vue:62)、[`KnowledgeWriteRequestLedger.vue:67`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.vue:67)）。

新增测试是真实 Vue 组件 mount，而非源码 surface：覆盖双状态/失败信息、手工刷新、Pending replay 后重新读取为 Succeeded、失败可操作信息和 connection scope（[`KnowledgeWriteRequestLedger.spec.ts:133`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.spec.ts:133)、[`KnowledgeWriteRequestLedger.spec.ts:177`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.spec.ts:177)、[`KnowledgeWriteRequestLedger.spec.ts:192`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.spec.ts:192)、[`KnowledgeWriteRequestLedger.spec.ts:232`](../../packages/app-vue/src/modules/repository/components/KnowledgeWriteRequestLedger.spec.ts:232)）。测试服务是 mock，所以它证明组件行为与“重新读取所得 server DTO”一致，不单独证明端到端网络；网络路径由下述 transport 与 integration 证据补足。

Desktop 不再返回 unavailable stub：renderer adapter 调用两个正式 IPC channel（[`repository-ipc.adapter.ts:170`](../../packages/repository/src/infrastructure-client/adapters/ipc/repository-ipc.adapter.ts:170)），preload allowlist 包含这两个 channel（[`allowed-channels.ts:23`](../../apps/desktop/src/preload/allowed-channels.ts:23)），main handler 经认证转发到 connection port（[`electron/index.ts:217`](../../packages/repository/src/electron/index.ts:217)），生产 main 将 `KnowledgeRepositoryRemoteGateway` 注入该 port（[`main.ts:145`](../../apps/desktop/src/main/main.ts:145)、[`main.ts:187`](../../apps/desktop/src/main/main.ts:187)），gateway 最终请求真实 list/replay HTTP route（[`knowledge-repository-remote.gateway.ts:137`](../../apps/desktop/src/main/modules/repository/knowledge-repository-remote.gateway.ts:137)）。

## P1-2：未关闭——生产 defaults 初始化存在，但申报的页面级新用户证据仍缺失

功能实现已有实质修复：Web/Desktop 根组件都调用 `usePresentationBootstrap`（[`apps/web/src/App.vue:24`](../../apps/web/src/App.vue:24)、[`apps/desktop/src/renderer/App.vue:28`](../../apps/desktop/src/renderer/App.vue:28)）；bootstrap 并发启动 defaults 与 user settings，请求 defaults 失败不阻塞 settings，请求 settings 失败仍按原错误路径处理（[`usePresentationBootstrap.ts:60`](../../packages/app-vue/src/modules/setting/composables/usePresentationBootstrap.ts:60)、[`usePresentationBootstrap.ts:81`](../../packages/app-vue/src/modules/setting/composables/usePresentationBootstrap.ts:81)）。分类参数也已从 composable 传到 client（[`useUserSetting.ts:99`](../../packages/app-vue/src/modules/setting/composables/useUserSetting.ts:99)），HTTP/IPC adapter 和服务端 use case 均保留 category；真实 reset 组件会把服务端返回 aggregate 写回 store/UI（[`SettingsResetSection.vue:32`](../../packages/app-vue/src/modules/setting/components/SettingsResetSection.vue:32)）。

分类 reset 与全量 reset 的新增测试都是真实组件 mount，分别断言 category 透传、其他分类保持、以及 UI 与返回 aggregate 一致（[`SettingsResetSection.spec.ts:124`](../../packages/app-vue/src/modules/setting/components/SettingsResetSection.spec.ts:124)、[`SettingsResetSection.spec.ts:169`](../../packages/app-vue/src/modules/setting/components/SettingsResetSection.spec.ts:169)）。这部分关闭。

但“新用户启动可见默认值”的测试只 mount 一个承载 composable 的空 `div`，随后直接读取 Pinia store getter（[`usePresentationBootstrap.spec.ts:54`](../../packages/app-vue/src/modules/setting/composables/usePresentationBootstrap.spec.ts:54)、[`usePresentationBootstrap.spec.ts:117`](../../packages/app-vue/src/modules/setting/composables/usePresentationBootstrap.spec.ts:117)）；它没有 mount `UserSettingsView`/`AppearanceSettings`，没有断言任何可见 DOM。与此同时设置页自身仍会独立调用 `loadSettings()` 并只在 `userSetting` 变化时 hydrate（[`UserSettingsView.vue:315`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:315)、[`UserSettingsView.vue:356`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:356)），新增测试没有覆盖根 bootstrap 与页面 load 并发时的最终可见值。因此不满足第一轮明确的“页面级测试证明新用户启动可见默认值”关闭条件。

关闭条件：mount 真实 `UserSettingsView`（或等价真实设置页面入口），驱动根/设置 bootstrap 的实际并发顺序，断言无已有记录的新用户最终在 DOM 中看到服务端 defaults；同时覆盖 defaults 先/后完成及 defaults/settings 各自失败的最终 UI。

## P1-3：未关闭——change/forgot 组件已恢复 receipt，但真实 Web reset-password 仍是另一条链路

`CloudPasswordSection` 是真实设置页组件，使用 `usePassword` 执行 change/forgot，渲染安全 message、request id、retryable action 和 dismiss（[`CloudPasswordSection.vue:24`](../../packages/app-vue/src/modules/account/components/CloudPasswordSection.vue:24)、[`CloudPasswordSection.vue:100`](../../packages/app-vue/src/modules/account/components/CloudPasswordSection.vue:100)），并已挂载到设置账户页（[`UserSettingsView.vue:446`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:446)）。组件测试真实 unmount、创建新 Pinia、remount 并从 localStorage 恢复 banner，再点击 retry（[`CloudPasswordSection.spec.ts:213`](../../packages/app-vue/src/modules/account/components/CloudPasswordSection.spec.ts:213)）；这不是只建 Pinia，change/forgot 的该消费点成立。

但用户真正从邮件进入的 Web reset-password 页面仍调用 `useWebAuth().resetPassword`（[`WebAuthView.vue:197`](../../apps/web/src/auth/WebAuthView.vue:197)），而 `useWebAuth` 只维护组件内 `error` ref，不写 `passwordMutationError` receipt（[`useWebAuth.ts:10`](../../apps/web/src/auth/useWebAuth.ts:10)、[`useWebAuth.ts:71`](../../apps/web/src/auth/useWebAuth.ts:71)）。Web forgot-password 页面也走同一临时错误链路（[`WebAuthView.vue:187`](../../apps/web/src/auth/WebAuthView.vue:187)、[`useWebAuth.ts:64`](../../apps/web/src/auth/useWebAuth.ts:64)）。所以刷新/卸载重挂后，这两个真实认证页面不会恢复结构化错误、request id 或 retry action；`CloudPasswordSection` 对 `reset-password` receipt 的 retry 只是改为发送忘记密码邮件（[`CloudPasswordSection.vue:71`](../../packages/app-vue/src/modules/account/components/CloudPasswordSection.vue:71)），并不使真实 reset form 成为 receipt 消费者。

关闭条件：让真实 Web forgot/reset 页面进入统一的安全 receipt 路径并渲染恢复后的结构化错误/request id/retryability；至少为 reset-password 补真实页面 unmount/remount 测试，证明 token 不持久化且用户仍有安全、可执行的恢复动作。

## P1-4：已关闭——持久 receipt 不再保存任意 server message/context

receipt message 现在只从明确错误码 allowlist 映射到本地化 key，未知码回退 `auth.errors.UNKNOWN`，不再使用服务端原文（[`reportAuthOperationFailure.ts:61`](../../packages/app-vue/src/modules/authentication/composables/reportAuthOperationFailure.ts:61)、[`reportAuthOperationFailure.ts:88`](../../packages/app-vue/src/modules/authentication/composables/reportAuthOperationFailure.ts:88)）；builder 只落 code、安全 message、requestId、retryable、operation、failedAt（[`reportAuthOperationFailure.ts:105`](../../packages/app-vue/src/modules/authentication/composables/reportAuthOperationFailure.ts:105)）。

恶意注入测试把 password、reset token、email 同时放入 server message/context，并断言 store、localStorage 原串和 reload 后 receipt 都不含这些值（[`usePassword.spec.ts:225`](../../packages/app-vue/src/modules/authentication/composables/usePassword.spec.ts:225)）；已知错误码也验证持久化的是 allowlist 本地化文案，而不是含邮箱/密码的 server message（[`usePassword.spec.ts:270`](../../packages/app-vue/src/modules/authentication/composables/usePassword.spec.ts:270)）。第一轮指出的泄露路径已关闭。

## P1-5：已关闭——repository lint 的 3 个 error 已消失

独立执行 `repository:lint` 退出码 0、0 errors。第一轮的 unused schema、`prefer-const`、unused `now` 已修复。当前尚有 integration 文件未使用 `ok` 的 1 个 warning（[`knowledge-write-request-projection-ledger.integration.test.ts:8`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:8)），不属于原 3 个 error，也不使当前 lint target 失败，但建议提交前清理以保持新增代码无噪声。

## P2-1：未关闭——已是真 route/service/webhook，但矩阵仍不完整且竞争测试无有效竞争

本轮集成测试确实启动 Express、注册生产 route/controller/application port，并使用真实 Prisma repositories 与 PostgreSQL（[`knowledge-write-request-projection-ledger.integration.test.ts:162`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:162)、[`knowledge-write-request-projection-ledger.integration.test.ts:211`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:211)）。commit/replay 经过 HTTP route，projection wrapper 让真实 `applyChanges` 抛错后再委托真实 Prisma 实现（[`knowledge-write-request-projection-ledger.integration.test.ts:85`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:85)、[`knowledge-write-request-projection-ledger.integration.test.ts:282`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:282)）；webhook 使用真实 raw body HMAC 和 delivery headers，并等待真实 delivery processing（[`knowledge-write-request-projection-ledger.integration.test.ts:358`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:358)）；重复 delivery 也走真实 webhook route（[`knowledge-write-request-projection-ledger.integration.test.ts:429`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:429)）。这些修复有效，不能再评价为“直调 repository 冒充 webhook”。

但第一轮关闭矩阵仍有三处缺口：

1. replay 只覆盖 Failed 与“已经 Succeeded 的早返回”，没有构造并通过真实 route replay `Pending`（[`knowledge-write-request-projection-ledger.integration.test.ts:282`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:282)、[`knowledge-write-request-projection-ledger.integration.test.ts:476`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:476)）。
2. 全文件没有调用 `reconcileNow()`；所谓 refresh 是 webhook delivery 按 commit SHA 更新，不是 reconciliation refresh。`TestRuntime` 暴露了 `projectionService`，但测试未使用它驱动 reconcile（[`knowledge-write-request-projection-ledger.integration.test.ts:153`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:153)）。
3. “concurrent Failed vs Succeeded”先通过正常 commit 把 row 投影为 `Succeeded`，随后 replay 在 service 第一个读取点就早返回，直接 `markProjectionFailed` 又被 Prisma `projectionStatus != Succeeded` guard 拒绝（[`knowledge-write-request-projection-ledger.integration.test.ts:530`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:530)、[`knowledge-repository-projection.service.ts:554`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:554)、[`knowledge-write-request-prisma.repository.ts:125`](../../packages/repository/src/server/infrastructure/adapters/prisma/knowledge-write-request-prisma.repository.ts:125)）。两项操作没有竞争写入同一 Pending/Failed 状态；最终 Succeeded 是测试开始竞争前就已成立的前置状态。

关闭条件：新增真实 Pending replay；显式驱动 `reconcileNow` 并断言 projection row、ledger、attempts、HTTP DTO；用可控 barrier/延迟在同一非终态记录上真正并发或乱序落下 service 驱动的 Succeeded 与 Failed，再证明终态不倒退。允许用 repository 读取/断言 DB，但不应再用 repository 直写代替被申报的业务路径。

## P2-2：已关闭——localStorage 功能探测、公共安装与隔离成立

公共 setup 会真实调用 `setItem/getItem/removeItem` 探测可用性，失败时安装 happy-dom Storage（[`setup.ts:16`](../../packages/app-vue/src/test/setup.ts:16)），并在每项测试前 clear（[`setup.ts:39`](../../packages/app-vue/src/test/setup.ts:39)）。`usePassword.spec.ts` 已删除私有 localStorage 覆盖，只依赖公共 setup（[`usePassword.spec.ts:11`](../../packages/app-vue/src/modules/authentication/composables/usePassword.spec.ts:11)）。

独立全量 app-vue 运行 169 files / 890 tests 通过，输出没有 `localStorage is not available` warning。配置额外用 `--disable-warning=ExperimentalWarning` 抑制 Node ExperimentalWarning（[`vitest.config.ts:30`](../../packages/app-vue/vitest.config.ts:30)）；虽然这意味着“无 warning”不能单独证明安装成功，但真实持久化/reload 测试、公共功能探测和逐测试清理共同提供了足够证据，P2-2 关闭。

## W0--W5 回归与独立验证

本环境仍没有 `pnpm` 可执行文件，以下直接调用 workspace 的 Nx binary；所有命令均加 `--skip-nx-cache`。未执行 local Docker prod-like，也未运行全 workspace 所有项目单测。

| 验证 | 结果 | 判断 |
| --- | --- | --- |
| `repository:test` | 29 files，191/191 | 通过 |
| `repository:test:integration` | 1 file，5/5，PostgreSQL `127.0.0.1:5433` | 通过；覆盖缺口见 P2-1 |
| `setting:test` | 14 files，81/81 | 通过 |
| `setting:test:integration` | 1 file，2/2，真实 PostgreSQL | 通过 |
| `app-vue:test` | 169 files，890/890 | 通过；无 localStorage warning |
| `repository/app-vue/setting:lint` | 三项目均退出码 0、0 errors | 通过；repository 1 个新增 warning，app-vue/setting 有既有及少量新增 warnings |
| `repository/app-vue/setting:typecheck` | 3 项及 25 个依赖任务通过 | 通过；依赖 build 覆盖 W0--W5 的 schedule/reminder/task/notification/goal/account 等包 |
| `app-vue:build` | 本项目及 25 个依赖任务通过 | 通过 |
| `memoflow:governance-check` | governance tools 35/35、governance tests、全部 audit、inventory 989 | 通过 |

上述单测、类型检查、依赖构建与治理结果未显示 W0--W5 回退。由于本轮未执行 W0--W5 全量集成/E2E 与 local Docker，这一结论限于已复验范围。

## 第二轮放行门槛

1. 为 Settings 补真实页面级新用户 defaults 可见性测试，并覆盖根 bootstrap/设置页并发及失败顺序。
2. 把真实 Web forgot/reset-password 页面接入安全、可恢复的 receipt UI；重点补 reset 页面卸载/重挂与 retry 行为测试。
3. 补齐 Knowledge Pending replay、`reconcileNow` refresh 和真正发生的 Failed/Succeeded 非终态竞争测试。
4. 复跑本轮相同 test/integration/lint/typecheck/build/governance 门禁；全部满足后再做第三轮审查。

在这些条件满足前，**W6 不可提交，不得进入 W7。**

---

# 第三轮审查

审查日期：2026-08-12  
审查基线：同前两轮，`HEAD=fcdd25a23185ba60adf1c213c06a89c12403b5a5`，W6 r5 为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W6 第三轮仍不可放行：不要提交 W6，不进入 W7。**

P1-2 与 P1-3 已关闭。Settings 新测试挂载真实 `UserSettingsView`，同时驱动根 presentation bootstrap 与页面 `onMounted` load，并在四种完成/失败顺序下对真实页面 DOM 作最终值断言。Web 真实 forgot/reset form 已统一使用 `usePassword`；reset 页面测试通过真实组件提交、卸载、新 Pinia 重挂，证明安全 receipt、request id 和恢复动作可见，且持久化内容不含 token/密码。

P2-1 的 Pending replay 与 `reconcileNow` 两个缺口已关闭，但申报的 `Succeeded`/`Failed` 非终态竞争仍没有被测试真正驱动。新增并发用例的第二个 replay 在 connection lease 处返回 `CONFLICT`，从未写 `Failed`；新增“out-of-order”用例只是先完整落下 `Failed`、再顺序落下 `Succeeded`，成功之后没有迟到的失败写入，因而没有触发或验证 `projectionStatus != Succeeded` 的防倒退 guard。实现中的 guard 仍是有效局部证据，但不满足第二轮明确要求的可控竞争/乱序集成证据。

最终分级：**P0 = 0，P1 = 0，P2 = 1。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全事故。 |
| P1 | 0 | 第二轮的 Settings 页面级默认值与 Web reset receipt 缺口均已关闭。 |
| P2 | 1 | Knowledge 的 Pending replay/reconcile 已补齐，但 `Succeeded` 与迟到 `Failed` 的真实竞争/乱序仍无有效集成证据。 |

## P1-2：已关闭——Settings 是真实页面 DOM 证据且覆盖四种时序

新增测试挂载真实 `UserSettingsView`，外层 host 调用生产 `usePresentationBootstrap()`，从而同时启动根 defaults/settings bootstrap 与页面自身的 `loadSettings()`（[`UserSettingsView.defaults.spec.ts:149`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:149)、[`UserSettingsView.defaults.spec.ts:219`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:219)）。Select 仅作为可观察 model value 的渲染 stub；页面、store getter、bootstrap composable 与 hydration watch 均为真实实现。

四个用例分别覆盖 defaults 先完成、defaults 后完成、defaults 失败、settings 失败，并最终读取 appearance card 内的 DOM `data-value`，而非只读取 store getter（[`UserSettingsView.defaults.spec.ts:193`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:193)、[`UserSettingsView.defaults.spec.ts:245`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:245)、[`UserSettingsView.defaults.spec.ts:272`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:272)、[`UserSettingsView.defaults.spec.ts:294`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.defaults.spec.ts:294)）。生产页面也同时 watch `userSetting` 与 `defaults`，defaults 后到时会重新 hydrate 本地表单（[`UserSettingsView.vue:317`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:317)、[`UserSettingsView.vue:343`](../../packages/app-vue/src/modules/setting/views/UserSettingsView.vue:343)）。聚焦实跑 4/4、app-vue 全量 894/894 均通过，P1-2 关闭。

## P1-3：已关闭——真实 Web reset 页面消费安全 receipt 并可重挂恢复

`WebAuthView` 的 forgot/reset 提交已改走生产 `usePassword()`，失败写共享 authentication store receipt，成功由该 composable 清除 receipt（[`WebAuthView.vue:87`](../../apps/web/src/auth/WebAuthView.vue:87)、[`WebAuthView.vue:204`](../../apps/web/src/auth/WebAuthView.vue:204)、[`WebAuthView.vue:218`](../../apps/web/src/auth/WebAuthView.vue:218)、[`usePassword.ts:68`](../../packages/app-vue/src/modules/authentication/composables/usePassword.ts:68)）。真实页面渲染安全 allowlist message、request id，并仅在 retryable 时提供动作；reset receipt 的恢复动作切到 forgot form 申请新链接，不复用或持久化旧 token（[`WebAuthView.vue:244`](../../apps/web/src/auth/WebAuthView.vue:244)、[`WebAuthView.vue:350`](../../apps/web/src/auth/WebAuthView.vue:350)）。Web auth bootstrap 安装 Pinia 并把同一 cloud auth client 同时提供给 Web auth 与 `usePassword`（[`auth.ts:20`](../../apps/web/src/bootstrap/auth.ts:20)、[`di-auth.ts:6`](../../apps/web/src/platform/di-auth.ts:6)）。

新增测试挂载真实 `WebAuthView`，从 URL 进入 reset scene，提交包含 token/password 回显的恶意失败；随后断言页面 receipt、request id、retry control 和 localStorage 均不泄露输入，再 unmount，以新 Pinia remount 并执行恢复动作（[`WebAuthView.spec.ts:206`](../../apps/web/src/auth/WebAuthView.spec.ts:206)）。它只 mock 与本项无关的 `useWebAuth` login/register 路径，reset 实际调用注入的 `AUTH_SERVICE_KEY` service。聚焦实跑 4/4、web 全量 66/66 均通过；app-vue 的 `usePassword` 测试另行覆盖成功清除持久 receipt。P1-3 关闭。

## P2-1：部分关闭——Pending replay 与 reconcile 成立，Succeeded/Failed 竞争仍未发生

以下两项已形成足够的真实集成证据：

1. Pending fixture 从 `Committed + Pending + attempts=0` 开始，经真实 HTTP replay route 推进；测试断言 projection row、repository ledger、attempts/projectedAt 和再次读取的 HTTP DTO 均为 `Succeeded`（[`knowledge-write-request-projection-ledger.integration.test.ts:666`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:666)）。
2. reconcile 用例先经真实 commit service 制造 projection exception 和 `Failed + attempts=1`，再显式调用 `projectionService.reconcileNow()`；随后断言 snapshot projection row、ledger `Succeeded + attempts=2` 以及 HTTP DTO（[`knowledge-write-request-projection-ledger.integration.test.ts:713`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:713)）。

竞争部分仍不满足关闭条件：

- “concurrent service replays”确实从同一个 Pending row 启动，并用 `applyChanges` gate 形成时间重叠；但 replay B 被 connection lease 拒绝为 `CONFLICT`，注释也明确它“never reaches the ledger”。因此实际只有 replay A 写入一次 `Succeeded`，没有 `Failed` 状态参与竞争（[`knowledge-write-request-projection-ledger.integration.test.ts:770`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:770)）。这证明 lease 互斥，不证明 ledger 的 Succeeded/Failed 竞争收敛。
- “out-of-order service-driven Failed then Succeeded”没有 barrier 或并发：第一次 service replay 已完成并写入 `Failed` 后，才启动第二次 replay 写入 `Succeeded`；测试到此结束，没有安排在 `Succeeded` 之后到达的旧失败，也没有断言该迟到失败被拒绝（[`knowledge-write-request-projection-ledger.integration.test.ts:821`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:821)）。这是正常恢复顺序，不是会挑战终态防倒退不变量的乱序。

Prisma `markProjectionFailed` 以 `projectionStatus != Succeeded` 为条件，代码层面确有防倒退 guard（[`knowledge-write-request-prisma.repository.ts:125`](../../packages/repository/src/server/infrastructure/adapters/prisma/knowledge-write-request-prisma.repository.ts:125)）；但第三轮新增测试从未在已由竞争推进为 Succeeded 的同一记录上尝试落下迟到 Failed，因而仍是未被申报业务路径覆盖的局部证据。

关闭条件：从同一非终态记录启动两个 service 驱动的 operation，用可控 barrier 让一条形成 `Succeeded`、另一条形成迟到 `Failed`（或用等价真实业务路径制造相反完成顺序），并断言失败写实际到达 ledger transition、被终态 guard 拒绝，最终 projection row、ledger、attempts 与 HTTP DTO 均不倒退。仅有 lease loser `CONFLICT` 或顺序 `Failed → Succeeded` 不足以关闭。

## W0--W5 回归与独立验证

本环境没有 `pnpm` 可执行文件，以下直接调用 workspace Nx binary，均使用 `--skip-nx-cache`；未执行 local Docker prod-like、W0--W5 全量集成或 E2E。

| 验证 | 结果 | 判断 |
| --- | --- | --- |
| `repository:test` | 29 files，191/191 | 通过 |
| `repository:test:integration` | 1 file，8/8，真实 PostgreSQL `127.0.0.1:5433` | 通过；竞争覆盖缺口见 P2-1 |
| `app-vue:test` | 170 files，894/894 | 通过；新增 Settings 聚焦用例 4/4 |
| `web:test` | 16 files，66/66 | 通过；新增真实 Web auth 聚焦用例 4/4 |
| `repository/app-vue/web:lint` | 三项目退出码 0、0 errors | 通过；web 有 3 warnings，其中新增 `AuthValidationKey` unused warning 1 项；app-vue 有 43 个既有/新增 warnings |
| `repository/app-vue/web:typecheck` | 三项及 26 个依赖任务通过 | 通过；依赖 build 覆盖 W0--W5 主要业务包 |
| `app-vue:build` | 随 typecheck 依赖执行通过 | 通过 |
| `memoflow:governance-check` | governance tools 35/35、governance tests、全部 audit、inventory 990 | 通过 |

上述测试、类型检查、依赖构建与治理结果未显示 W0--W5 回退；结论限于已复验范围。lint warning 不使当前 target 失败，但建议提交前清理 Web 新增的未使用 `AuthValidationKey` import。

## 第三轮放行门槛

仅剩 P2-1 的竞争证据：补一个真正让 service 驱动的 `Succeeded` 与迟到 `Failed` 都到达同一非终态记录 ledger transition 的可控测试，并证明终态 guard 拒绝倒退；复跑 repository integration、lint/typecheck 与治理门禁。完成前，**W6 不可提交，不得进入 W7。**


---

# 第四轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=fcdd25a23185ba60adf1c213c06a89c12403b5a5`，W6 r6 仍为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W6 第四轮可放行：可以提交 W6，并进入 W7。**

第三轮仅存的 P2-1 已关闭。新增集成测试从同一 `Committed + Pending` 记录启动两个 service replay：B 先读取非终态快照并暂停在 connection lookup，A 经真实 service、connection lease、projection repository 和 ledger transition 完成 `Succeeded`；A 释放 lease 后，B 才继续并成功取得 lease，其受控 `applyChanges` 异常使 service 进入 `markProjectionFailed`。因此 B 不是 lease loser，也不返回 `CONFLICT`，而是走完失败业务路径后返回 `SERVICE_UNAVAILABLE`。

B 的迟到 Failed 写入到达真实 Prisma ledger transition；`markProjectionFailed` 的 `updateMany` 以 `projectionStatus != Succeeded` 为条件，面对 A 已提交的终态更新 0 行。service 虽返回 projection replay failure，数据库终态不倒退。测试随后同时验证 projection row、ledger record、attempt counter 与 HTTP DTO，证据覆盖了第三轮要求的全部观察面。

最终分级：**P0 = 0，P1 = 0，P2 = 0。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全问题。 |
| P1 | 0 | 前三轮 P1 均已关闭，本轮未发现新的阻断问题。 |
| P2 | 0 | P2-1 的真实迟到 Failed / Succeeded 状态竞争证据已补齐。 |

## P2-1：已关闭——迟到 Failed 真正到达 ledger guard，非 lease `CONFLICT`

可控屏障位于 B 的 connection lookup：B 已在 `replayWriteRequestProjection` 中读取到同一记录的 `Pending` 快照并越过 `Succeeded` early return，但尚未获取 lease（[`knowledge-write-request-projection-ledger.integration.test.ts:977`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:977)）。此时 A 从同一非终态记录启动，经真实 service replay 完成 projection 写入和 `markProjectionSucceeded`，返回 `Succeeded`（[`knowledge-write-request-projection-ledger.integration.test.ts:987`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:987)）。

A 完成并释放 lease 后，测试才为 B 设置一次真实 `applyChanges` 异常并释放 connection barrier。B 随后取得空闲 lease、使用先前读取的 Pending 快照进入 `replayWriteRequestCore`；`applyChanges` 抛错后，service 的 catch 在有效 lease 下调用真实 `markProjectionFailed`（[`knowledge-repository-projection.service.ts:715`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:715)）。B 的断言结果是 `SERVICE_UNAVAILABLE`，而不是未取得 lease 时的 `CONFLICT`（[`knowledge-write-request-projection-ledger.integration.test.ts:996`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:996)）。结合该 catch 路径无其他返回分支，这证明迟到失败写实际到达 ledger transition。

真实 Prisma `markProjectionFailed` 使用 `where: { id, identityId, projectionStatus: { not: 'Succeeded' } }`；A 已落下 `Succeeded` 后，B 的 updateMany 更新 0 行，终态 guard 拒绝倒退（[`knowledge-write-request-prisma.repository.ts:125`](../../packages/repository/src/server/infrastructure/adapters/prisma/knowledge-write-request-prisma.repository.ts:125)）。该 repository 返回值虽未由 service 向外透传，但最终数据库状态和 attempts 断言能够区分“guard 拒绝”与“Failed 曾落库”。

## 终态不倒退证据

- ledger record 保持 `projectionStatus=Succeeded`、error 为 null、`projectionAttempts=1` 且 `projectedAt` 非空；若迟到 Failed 落库，状态、error、attempts 和 projectedAt 四项都会与断言冲突（[`knowledge-write-request-projection-ledger.integration.test.ts:1008`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1008)）。
- A 写入的真实 `knowledgeNoteProjection` row 仍存在，commit SHA 保持预期值（[`knowledge-write-request-projection-ledger.integration.test.ts:1015`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1015)）。
- 真实 list HTTP route 返回的 DTO 同样保持 `Succeeded`、error 为 null、attempts 为 1、projectedAt 非空（[`knowledge-write-request-projection-ledger.integration.test.ts:1022`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1022)）。
- B 返回 `SERVICE_UNAVAILABLE`，同时最终 attempts 没有增加，证明失败业务路径已执行但失败 transition 被终态 guard 拒绝，而不是第二次状态转换成功。

## W0--W5 回归与独立验证

本环境没有 `pnpm` 可执行文件，以下直接调用 workspace Nx binary，均使用 `--skip-nx-cache`；未执行 local Docker prod-like、W0--W5 全量集成或 E2E。

| 验证 | 结果 | 判断 |
| --- | --- | --- |
| `repository:test:integration` | 1 file，9/9，真实 PostgreSQL `127.0.0.1:5433` | 通过；包含新增迟到 Failed 竞争测试。 |
| `repository:test` | 29 files，191/191 | 通过。 |
| `repository:lint` | 退出码 0，0 errors | 通过。 |
| `repository:typecheck` | repository 及 9 个依赖任务通过 | 通过；依赖 build 包含 contracts、database 等共享边界。 |
| `memoflow:governance-check` | governance tools 35/35、governance tests、全部 audit、inventory 990 | 通过。 |

上述 repository 单测、真实 PostgreSQL 集成、类型检查及其依赖构建、lint 与治理结果未显示 W0--W5 回退；结论限于本轮复验范围。前三轮已关闭项未发现因 r6 测试辅助设施而重新打开。

## 放行判定

第四轮未发现新增 P0/P1/P2，第三轮唯一放行门槛已满足。**W6 可提交，随后可进入 W7。**本次审查自身未执行 commit 或 push。
