# W7（发布门禁：统一状态投影 + 运维入口 + 回归门禁）第一轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=53899b47b`，W7 为未提交改动  
审查方式：只审查并新增本文档；未修改业务代码，未 commit/push。

## 终审结论

**W7 第一轮不可放行：不要提交 W7，W0--W7 尚未闭环。**

五个服务端模块都已经存在真实 ledger → `OperationTimelineEntry` 的查询映射，Prisma 路径也都使用认证上下文中的 identity 做数据层过滤；各模块的 replay 基本能推进真实状态，相关单包和 PostgreSQL integration test 全绿。这些是有效进展，不是 stub。

但 W7 是最终发布门禁，当前仍有发布阻断项：`@memoflow/contracts/operations` 的声明 export 没有对应 JavaScript 构建产物，真实 ESM import 失败；governance-check 也因两个新增 export 未登记而失败。审计契约虽然定义了 `timeline_query`，五个 timeline 查询却没有一处写查询审计；所有 replay 都先改变 operation 状态再写审计，审计失败会留下“已重放但无审计”的事实，Knowledge 甚至在缺 audit repository 时仍可成功 replay。统一指标只接到 Reminder/Notification 的进程内计数器，另外三个模块、worker 指标和 dashboard/export 都没有接入。所谓跨模块故障矩阵只覆盖四模块的一组 dead-letter/failed seed + replay，没有 Knowledge，也不是 W0--W6 关键故障路径横切。

最终分级：**P0 = 0，P1 = 6，P2 = 2。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 6 | contracts 子路径发布产物缺失；governance 红灯；查询审计完全缺失；replay 与审计不原子且 Knowledge 可无审计成功；统一指标未覆盖五模块/worker/dashboard；跨模块矩阵及 W3--W6 文档证据不完整。 |
| P2 | 2 | Desktop/PowerSync 审计表与组合根未闭环；生成 Prisma 声明导致 `git diff --check` 失败。 |

## P1-1：`@memoflow/contracts/operations` 是无法运行时导入的声明 export

`packages/contracts/package.json` 将 `./operations` 指向 `dist/modules/operations/index.js`（[`package.json:32`](../../packages/contracts/package.json:32)），但 contracts 的 tsup entry 没有 `src/modules/operations/index.ts`（[`tsup.config.ts:18`](../../packages/contracts/tsup.config.ts:18)）。本轮实际执行 `contracts:build` 后，目录中只有 `.d.ts/.map`，没有 `index.js`；随后执行：

```text
node --input-type=module -e "import('@memoflow/contracts/operations')..."
ERR_MODULE_NOT_FOUND .../packages/contracts/dist/modules/operations/index.js
```

源码 path alias 使 typecheck 与 Vitest 都能通过，不能证明发布子路径可用。W7 新增的五模块代码大量运行时 import 该子路径中的 Zod schema，因此这是发布阻断，不是单纯打包整洁度问题。

关闭条件：把 operations 加入 contracts 的真实 JS/DTS 构建入口；清空/重建产物后用 package export 做 ESM smoke import，并为每个新 export 增加产物存在性门禁。

## P1-2：治理门禁实测失败，与“governance 通过”申报冲突

`nx run memoflow:governance-check` 本轮退出码为 1。失败来自 Package Export Audit：

```text
packages/contracts/package.json exports "./operations" which is not in the allowed whitelist
packages/patterns/package.json exports "./operations" which is not in the allowed whitelist
```

其余已运行治理子检查通过，但总门禁失败就不能记为“governance 通过”。这也说明新增公共 surface 没有完成治理同步。

关闭条件：按 canonical 公共 surface 规则登记两个 export，并复跑完整 `memoflow:governance-check` 到退出码 0；不得只引用其中先打印的局部 `[governance-check] passed`。

## P1-3：五个 timeline 查询均未写 `timeline_query` 审计

审计 contract 明确定义 `replay | timeline_query`（[`audit-record.ts:4`](../../packages/contracts/src/modules/operations/audit-record.ts:4)），schema 注释和 ADR 也宣称 query/replay 都必须审计。但全仓 `timeline_query` 只出现在 contract 与 ADR，没有任何生产记录调用。

例如 Reminder 查询只调用可靠端口后直接返回（[`reminder.module.ts:346`](../../packages/reminder/src/server/infrastructure/reminder.module.ts:346)），Notification 查询同样直接返回（[`notification.module.ts:274`](../../packages/notification/src/server/infrastructure/notification.module.ts:274)）；Knowledge、Schedule、Account 也是相同结构。现有集成测试只断言 replay audit，不断言 timeline query audit。

identity 数据隔离本身大体成立：查询 identity 来自认证 context，Prisma/ledger 查询也按 identity 过滤。但“最小权限 + 审计”只完成了前半段。

关闭条件：明确 query 审计粒度（每次请求记录 source、过滤条件、结果计数，避免把任意 operationId 伪造成被访问事实），五模块实现并测试 `timeline_query`；审计失败时采用一致、文档化的 fail-closed 策略。

## P1-4：Replay 状态变更与审计不原子，且 Knowledge 存在无审计成功路径

Reminder 先执行 `replayDeadLetter` 改状态，再 `auditRepository.record`（[`reminder.module.ts:356`](../../packages/reminder/src/server/infrastructure/reminder.module.ts:356)）；Notification（[`notification.module.ts:244`](../../packages/notification/src/server/infrastructure/notification.module.ts:244)）、Account（[`account.module.ts:167`](../../packages/account/src/server/infrastructure/account.module.ts:167)）、Schedule（[`schedule.module.ts:388`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:388)）顺序相同。若第二步失败，API 虽返回错误，operation 已经进入 retryable/pending/running，留下 ADR 明令禁止的“发生 replay 但没有审计记录”。catch 不能回滚前一步。

Knowledge 更直接：它先执行真实 projection replay，只在 `result.ok && auditRepository` 时记录；没有 audit repository 时照样返回成功（[`repository.module.ts:156`](../../packages/repository/src/server/infrastructure/repository.module.ts:156)）。这与同文件 audit 查询的 `FAIL_CLOSED` 形成不一致，也违反“禁止 silent success/no-op fallback”。现有测试只覆盖数据库和审计都成功的 happy path，没有注入 audit write failure。

关闭条件：把 operation 状态推进与 audit 事实放进同一事务/同一原子应用端口；对无法共享事务的外部 projection 定义可靠 audit intent/outbox，再由可证明的状态机完成，不得靠“先执行、后写日志、失败返回错误”。五模块补 audit repository 故障注入测试，并让 Knowledge 缺依赖时 replay fail-closed。

## P1-5：统一指标只完成命名函数和两个进程内 collector，未形成五模块 dashboard 证据

共享 recorder 是进程内 `Map` + snapshot（[`unified-operation-metrics.ts:24`](../../packages/patterns/src/operations/unified-operation-metrics.ts:24)）。全仓真实 `recordOutbox/recordWorker` 调用只存在于 Reminder 与 Notification metrics service：

- Reminder 只记录 `claimed/failed/retried/dead_letter`，没有 `persisted/succeeded`。
- Notification 记录六个 outbox state。
- Knowledge、Schedule rebuild、Account closure 没有任何 recorder 调用。
- 五模块都没有真实 `recordWorker` 调用；`workerMetricKey` 只在 surface test 中出现。
- 没有 dashboard、Prometheus/OTel exporter 或其他生产消费点读取 unified snapshot。

新增 metrics spec 是直接手工调用 collector 后断言 key 名称；它证明命名函数正确，不证明 persisted/claimed/retried/failed 在真实工作流都发射，更不证明“五模块接入同一 dashboard”。

关闭条件：五个模块在真实 persistence/claim/retry/failure/dead-letter/worker outcome 路径接统一 recorder；接入现有 telemetry exporter/dashboard；用 worker/adapter 行为测试断言事件触发，而不是直接调用 metrics 方法。

## P1-6：跨模块门禁不是申报的五模块、W0--W6 故障注入矩阵，ADR/runbook 也未覆盖 W3--W6 关键决策

新增唯一跨模块 test 自己就说明只覆盖 Reminder、Notification、Schedule rebuild、Account closure 四模块（[`w7-cross-module-operation-gate.integration.test.ts:18`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/w7-cross-module-operation-gate.integration.test.ts:18)）。实际聚合长度固定为 4，source 断言也只有四项（[`w7-cross-module-operation-gate.integration.test.ts:214`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/w7-cross-module-operation-gate.integration.test.ts:214)）；Knowledge 没有进入同一门禁。

该测试只 seed 四条最终失败记录，验证 identity 拒绝、状态 reset 和共享 replay audit。它没有横切 W0--W6 的关键故障：事务提交/发布边界、lease fencing/丢 lease、渠道 capability/silent success、account closure 分阶段恢复、schedule expectedVersion/冲突与 rebuild 幂等、knowledge webhook/reconcile/乱序 replay 等。因此它是有效的 W7 四模块 happy recovery test，但不是申报的跨版本故障矩阵。

ADR-043 也把回归门禁写成“四模块”，并只描述 W7 overlay；runbook 只列端点和通用 replay。两者没有回收 W3--W6 的关键决策、判障信号、恢复步骤与禁止回退项，且文档中的“query/replay 都审计”“审计先行”“五模块指标”与代码不符。

关闭条件：建立可枚举的 W0--W6 故障矩阵（故障、注入层、预期 durable fact、恢复动作、禁止状态、测试文件），把 Knowledge 纳入同一跨模块 gate；ADR/runbook 引用并覆盖 W3--W6 的关键状态机、告警和恢复流程，内容必须与最终代码一致。

## P2-1：Desktop/PowerSync 宣称接入审计，但本地表与组合根未闭环

Electron audit adapter 直接读写 `reliable_operation_audit_logs`（[`electron-operation-audit-repository.ts:19`](../../packages/patterns/src/operations/electron-operation-audit-repository.ts:19)），但全仓没有对应 SQLite/PowerSync `CREATE TABLE` 或 schema 定义；只有 Prisma 模型。Notification 和 Schedule PowerSync 组合根却已经实例化该 adapter（[`notification/powersync.ts:367`](../../packages/notification/src/server/infrastructure/powersync.ts:367)、[`schedule/powersync.ts:56`](../../packages/schedule/src/server/infrastructure/powersync.ts:56)），实际调用 audit 会命中不存在的本地表。

同时 Reminder PowerSync 组合根没有注入 `reliablePort/auditRepository`（[`reminder/powersync.ts:63`](../../packages/reminder/src/server/infrastructure/powersync.ts:63)），Account PowerSync 也没有 closure operation repository/audit repository（[`account/powersync.ts:15`](../../packages/account/src/server/infrastructure/powersync.ts:15)），其 W7 API 会 fail-closed/抛错。当前 W7 integration 全是 Prisma lane，未覆盖这些组合根。

关闭条件：要么明确 W7 运维入口只属于 server lane并删除 Electron adapter/错误文档，要么补齐本地 schema、四/五模块组合根与真实 PowerSync 测试；不得维持“类型上可调用、运行时缺表/缺依赖”的半接入状态。

## P2-2：Prisma 生成声明使 diff hygiene 门禁失败

`git diff --check` 报告 `packages/database/src/generated/prisma/index.d.ts` 多处 trailing whitespace。文件虽为生成物，但当前在 W7 变更集内，提交前仍会造成常见 diff/patch 门禁失败和无效噪音。

关闭条件：使用仓库 canonical Prisma generate/format 流程产生稳定、无 trailing whitespace 的受管产物，并让 `git diff --check` 无输出。

## 已确认成立的局部实现

- `OperationTimelineEntry` 的八个要求字段和五种 source 已由 Zod contract 统一，patterns 映射输出会过 schema parse。
- 五个 Prisma 服务端模块都有真实 ledger 查询，不是 stub；identity 由 auth context 进入应用端口，数据层以 identity 过滤。
- Reminder、Notification、Schedule rebuild、Account closure、Knowledge projection 都有真实 replay 入口；成功路径测试能观察到状态推进。
- Prisma 共享 `OperationAuditLog` repository 与 actor-scoped audit 查询存在；成功 replay 的审计 happy path 测试成立。
- patterns `src/operations` 已加入 patterns tsup entry，`patterns:build` 本轮确实生成 `dist/operations/index.js/.d.ts`。
- test inventory 当前为 994：unit 858、integration 23、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30。

## 独立验证记录

本次环境没有 `pnpm` 可执行文件，以下直接调用 workspace 的 `node_modules/.bin/nx`；未修改代码以迁就验证。Nx 部分依赖任务命中本地 cache，单元/集成测试输出为本轮实际 Vitest 运行结果。

| 验证 | 结果 | 判断 |
| --- | --- | --- |
| `nx run-many --target=typecheck --projects=contracts,patterns,database,reminder,notification,account,schedule,repository,setting` | 9 项及 15 个依赖任务通过 | 通过；但源码 alias 没捕获 P1-1 |
| `nx run-many --target=lint --projects=reminder,notification,account,schedule,repository` | 5 项退出码 0，0 errors；存在既有/测试 warnings | 通过 |
| `nx run-many --target=test --projects=patterns,reminder,notification,account,schedule,repository` | patterns 31、reminder 382、notification 239、account 171、schedule 344、repository 191，全部通过 | 通过；申报的 account 344 与实际 target 不一致 |
| `nx run-many --target=test:integration --projects=reminder,notification,account,schedule,repository,setting --parallel=1` | 27、22、16、22、10、2，合计 99/99 | 通过；包含新增四模块 W7 gate |
| `nx run memoflow:governance-check` | 失败，Package Export Audit 两项 | **不通过，见 P1-2** |
| `contracts:build` 后 ESM import `@memoflow/contracts/operations` | `ERR_MODULE_NOT_FOUND` | **不通过，见 P1-1** |
| `git diff --check` | Prisma generated declaration 多处 trailing whitespace | 不通过，见 P2-2 |

未执行 local Docker prod-like 验证。当前已有 packaging/governance/功能证据 P1，启动完整本地发布车道不能改变本轮不放行结论；关闭 P1 后仍必须按 W7 计划完成该验证再进入 PR。

## 放行门槛

1. 关闭 P1-1/P1-2：新 package exports 的构建产物、真实 package import 与 governance 全绿。
2. 关闭 P1-3/P1-4：五模块 query/replay 审计语义完整，replay 与审计不会产生部分成功；补 audit failure 注入。
3. 关闭 P1-5：五模块真实 outbox/worker 路径统一发射并进入可消费 telemetry/dashboard。
4. 关闭 P1-6：Knowledge 纳入跨模块 gate，补 W0--W6 可枚举故障矩阵，并修正文档与代码偏差。
5. 明确并验证 Desktop/PowerSync lane，清理 diff hygiene；复跑 typecheck、lint、六包 test/integration、governance、package import smoke。
6. 最后执行 `pnpm docker:local:up` 对应的 local prod-like 验证，确认新审计表、API routes、worker 与 telemetry 在生产式装配中可用。

在上述条件满足前，**W7 不可提交，不能宣称 W0--W7 全部闭环。**

---

# 第二轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=53899b47b`，W7 仍为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W7 第二轮仍不可放行：不要提交 W7，W0--W7 尚未闭环。**

第一轮的 packaging、governance、Desktop/PowerSync 和 Prisma diff hygiene 问题已经关闭；五模块 timeline 查询也都接入了共享 `timeline_query` 记录器，四个纯 Prisma replay 已改为状态与审计同事务并通过审计失败回滚测试，Knowledge 已进入五模块真实数据库 gate。上述进展均经本轮独立构建、ESM import、governance 和五包 PostgreSQL integration test 复验。

但三项核心申报与代码不符：Knowledge 仍然是“先执行投影，后写审计”，不是 ADR 宣称的审计先行；统一指标只是在五模块放入了若干调用点，并未覆盖申报的 persistence/claim/retry/failure/dead-letter/worker outcome 全路径，也没有对应五模块行为测试；故障矩阵把未存在的 Knowledge 审计故障测试、五模块 query/replay 审计故障测试写成了既有证据。因此 P1-4、P1-5、P1-6 未关闭。P1-3 的生产实现成立，但第五模块与 fail-closed 的测试证据仍未达到第一轮关闭条件。

最终分级：**P0 = 0，P1 = 4，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全事故。 |
| P1 | 4 | P1-3 测试闭环不足；Knowledge replay 仍先执行后审计；统一指标未覆盖五模块全状态且缺行为测试；故障矩阵与真实测试不一致。 |
| P2 | 1 | dist surface test 依赖预先存在的构建产物，但两个 package 的 `test` target 均不依赖自身 `build`，clean checkout 下不是自洽门禁。 |

## 8 项关闭核验

| 第一轮项 | 第二轮判定 | 代码/测试证据 |
| --- | --- | --- |
| P1-1 operations 构建产物 | **核心实现关闭；遗留 P2 测试编排** | contracts 已把 operations 加入 tsup entry（[`packages/contracts/tsup.config.ts:31`](../../packages/contracts/tsup.config.ts:31)），patterns 同样登记入口（[`packages/patterns/tsup.config.ts:11`](../../packages/patterns/tsup.config.ts:11)）；本轮 clean build 后两包 JS/DTS 均存在，两个真实 package ESM import 均成功。surface spec 确实检查 JS/DTS（[`operations-dist.surface.spec.ts:25`](../../packages/contracts/src/modules/operations/operations-dist.surface.spec.ts:25)、[`patterns operations-dist.surface.spec.ts:25`](../../packages/patterns/src/operations/operations-dist.surface.spec.ts:25)），但 `contracts:test`/`patterns:test` target 不依赖自身 build（[`contracts/project.json:37`](../../packages/contracts/project.json:37)、[`patterns/project.json:32`](../../packages/patterns/project.json:32)），故该测试只在 dist 已由其他步骤生成时成立。 |
| P1-2 治理门禁 | **关闭** | canonical whitelist 已登记 contracts/patterns 的 `./operations`（[`package-export-audit.mjs:41`](../../tools/governance/package-export-audit.mjs:41)、[`package-export-audit.mjs:45`](../../tools/governance/package-export-audit.mjs:45)）；本轮完整 `memoflow:governance-check --skip-nx-cache` 退出码 0。 |
| P1-3 timeline query 审计 | **部分关闭，P1 保留** | 五模块生产入口都通过 `runTimelineQueryWithAudit` 写 source、filters、resultCount，审计写失败会抛出而不返回 timeline（[`operation-audit-repository.ts:93`](../../packages/patterns/src/operations/operation-audit-repository.ts:93)；Reminder [`reminder.module.ts:351`](../../packages/reminder/src/server/infrastructure/reminder.module.ts:351)、Notification [`notification.module.ts:271`](../../packages/notification/src/server/infrastructure/notification.module.ts:271)、Schedule [`schedule.module.ts:388`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:388)、Account [`account.module.ts:157`](../../packages/account/src/server/infrastructure/account.module.ts:157)、Knowledge [`repository.module.ts:181`](../../packages/repository/src/server/infrastructure/repository.module.ts:181)）。Reminder/Notification/Schedule/Account 测试断言了 marker/resultCount；Knowledge 测试只查询 timeline 后寻找 replay audit（[`knowledge-write-request-projection-ledger.integration.test.ts:1049`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1049)），没有断言其 query audit 的 filters/resultCount。全仓也没有一个 `timeline_query` 审计写失败的调用方测试，未达到第一轮要求的五模块实现并测试 fail-closed。 |
| P1-4 replay 原子性 / Knowledge fail-closed | **部分关闭，P1 未关闭** | Reminder/Notification/Schedule/Account 均提供同事务 replay 端口并传 Prisma tx 给 audit repository，例如 Reminder（[`reminder-reliable-operation-prisma.adapter.ts:480`](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:480)）和 Account（[`account-closure-operation-prisma.repository.ts:158`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:158)）；四模块均有审计写失败后状态不前进的 DB 断言。Knowledge 缺 `auditRepository` 时现在确实返回 `FAIL_CLOSED`（[`repository.module.ts:159`](../../packages/repository/src/server/infrastructure/repository.module.ts:159)），但随即仍先调用真实 projection（[`repository.module.ts:166`](../../packages/repository/src/server/infrastructure/repository.module.ts:166)），完成外部/ledger 状态推进后才写审计（[`repository.module.ts:170`](../../packages/repository/src/server/infrastructure/repository.module.ts:170)）；审计写失败会留下“投影已成功但 replay audit 缺失”。这正是 ADR 禁止的先执行后写日志模式（[`ADR-043:68`](../architecture/adr/ADR-043-unified-operation-timeline-replay-audit.md:68)），且 Knowledge 测试没有缺依赖或 failing-audit 注入。 |
| P1-5 五模块统一指标 | **部分接线，P1 未关闭** | recorder 已由 metrics controller 暴露（[`metrics.controller.ts:108`](../../apps/api/src/shared/infrastructure/http/controllers/metrics.controller.ts:108)），五模块也都能找到真实调用点。但覆盖不符合申报：Account 只发 persisted/succeeded/failed 与 completed/failed（[`account-closure-coordinator.ts:173`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:173)、[`account-closure-coordinator.ts:443`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:443)），没有 claimed/retried/dead_letter；Schedule 只发 claimed/succeeded/failed 和部分 worker outcome（[`schedule-rebuild-worker-service.ts:51`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:51)），失败后究竟 retry 还是 dead-letter 没有对应指标；Knowledge 只发 succeeded/failed 与 worker.failed（[`knowledge-repository-projection.service.ts:711`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:711)），没有 persistence/claim/retry/dead-letter/worker.completed。测试仍只有 Reminder/Notification 直接调用 metrics service 的 surface spec（[`reminder-metrics-unified.surface.spec.ts:10`](../../packages/reminder/src/server/domain/services/reminder-metrics-unified.surface.spec.ts:10)、[`notification-metrics-unified.surface.spec.ts:10`](../../packages/notification/src/server/domain/services/notification-metrics-unified.surface.spec.ts:10)）；Account/Schedule/Knowledge 没有 worker/adapter 行为断言。 |
| P1-6 W0--W6 故障矩阵 | **部分关闭，P1 未关闭** | ADR-044 已建立所需列，五模块 gate 也确实包含 Knowledge，并验证五条 timeline/replay/shared-audit（[`w7-cross-module-operation-gate.integration.test.ts:19`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/w7-cross-module-operation-gate.integration.test.ts:19)）。但矩阵 W6-3 指向的 Knowledge 文件没有缺 audit 测试；W7-1 宣称“五模块 audit 故障注入测试”，实际只有四个事务型模块；W7-2 宣称“五模块 timeline_query 测试”，实际缺 Knowledge 细节断言且五模块都没有 audit-write-failure 测试（[`ADR-044:42`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:42)）。W5-2 还使用 `.../schedule-rebuild-worker*` 而非可定位的真实测试文件（[`ADR-044:39`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:39)）。矩阵目前不能作为可追溯发布 gate。 |
| P2-1 Desktop/PowerSync | **关闭** | 全仓生产代码已无 `ElectronOperationAuditRepository`；ADR/runbook 明确 W7 运维入口仅属 server lane，Desktop/PowerSync 缺依赖时 fail-closed（[`ADR-043:85`](../architecture/adr/ADR-043-unified-operation-timeline-replay-audit.md:85)、[`operation-timeline-ops-runbook.md:20`](../guides/operation-timeline-ops-runbook.md:20)）。 |
| P2-2 Prisma 产物 | **关闭** | canonical database build/prisma-generate/postinstall 均串联 normalize 脚本（[`packages/database/project.json:14`](../../packages/database/project.json:14)、[`packages/database/package.json:21`](../../packages/database/package.json:21)）；本轮实际 database build 后 `git diff --check` 无输出。 |

## 回归与独立验证

本轮环境仍没有 `pnpm` 可执行文件，使用 workspace 的 `node_modules/nx/dist/bin/nx.js` 调用相同 Nx target；所有命令均加 `--skip-nx-cache`（integration target 串行执行）。

| 验证 | 第二轮实测结果 |
| --- | --- |
| `contracts,patterns:build` | 2 项及依赖构建通过；contracts/patterns operations JS/DTS 均存在 |
| 两个 package ESM smoke import | `@memoflow/contracts/operations`、`@memoflow/patterns/operations` 均成功 |
| `contracts,patterns:test` | contracts 60 files / 522 tests；patterns 6 files / 35 tests，全部通过 |
| 五包 `test:integration` | Reminder 28、Notification 24、Account 17、Schedule 23、Repository 10，合计 102/102 通过 |
| `memoflow:governance-check` | 退出码 0；inventory 996（unit 860、integration 23、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30） |
| `git diff --check` | 无输出 |

上述 integration 覆盖 W0--W6 既有关键状态机与 W7 五模块 gate，本轮未观察到既有测试回退；但“测试全绿”不能证明未被测试覆盖的 Knowledge 审计部分成功与指标缺口不存在。

未执行 local Docker prod-like 验证。当前仍有发布阻断 P1，启动完整本地发布车道不能改变本轮不放行结论。

## 第二轮关闭条件

1. Knowledge replay 改为 ADR 所述的可靠审计先行/intent 状态机，或其他能证明外部投影与审计不会部分成功的原子端口；补缺依赖与审计写失败注入，断言外部调用和 durable state 均符合所选语义。
2. 为五模块逐个列出实际存在的 lifecycle，并在真实 persistence/claim/retry/failure/dead-letter/worker outcome 分支发射准确指标；不得把 retry/dead-letter 都笼统计为 failed。Account/Schedule/Knowledge 以及 Reminder/Notification worker/adapter 均补行为测试，不再只直接调用 collector。
3. 为 Knowledge 补 `timeline_query` 的 marker/filters/resultCount 断言；为五模块补 timeline query 审计写失败的 fail-closed 断言。
4. 修正 ADR-044 的每一条测试引用为真实、精确文件，并删除没有断言支撑的“五模块”声明；矩阵逐行与测试内容核对。
5. 让 operations dist surface test 在 clean checkout 自洽：package test target 显式依赖 build，或把 package export smoke 放入明确先 build 的独立门禁。
6. 上述关闭后重跑本轮验证矩阵，再按仓库发布主线执行 local Docker prod-like 验证。

在这些条件满足前，**P0 = 0、P1 = 4、P2 = 1；W7 不可放行，不得提交 W7，也不能宣称 W0--W7 闭环。**

---

# 第三轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=53899b47b`，W7 r6 为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W7 第三轮仍不可放行：不要提交 W7，W0--W7 尚未闭环。**

P1-3 与 P1-4 已关闭：五模块均有 query marker/filters/resultCount 证据和审计失败 fail-closed 测试；Knowledge replay 也已真正改成 audit intent 先落库，缺依赖或首次审计写失败时不会调用 projection，durable ledger 与 projection 均不变化。四个 Prisma replay 继续保持状态与审计同事务。

但 P1-5、P1-6 与 P2-1 未关闭。Reminder/Notification 的 retry 与 dead-letter 分支仍同时增加 `outbox.failed`，直接违反第二轮“retry/dead-letter 不笼统计为 failed”的关闭条件；Schedule 也仍没有 persistence 指标发射点。ADR-044 的 W7-2 行仍用 `packages/<module>/.../<file>` 形式的省略路径，并非真实精确文件引用。contracts/patterns 的 `test` target 仍不依赖自身 `build`；本轮移走两包 operations dist 后直接执行 test，两包 surface test 各有 2 项失败，clean checkout 门禁不自洽。

local Docker prod-like 未能完成。当前环境没有 `pnpm`，且缺 `.env.production.local`；额外执行等价的 `web:build` target 后复现了 `@memoflow/app-vue` 根 alias 把认证子路径解析成 `src/index.ts/modules/authentication` 的 production build 错误。该 import 与 alias 配置在 `HEAD` 中已原样存在，W7 没有修改对应文件，因此它是 pre-existing 阻断，不计为 W7 新增 finding；但按仓库发布规范，完整 prod-like 不能据此记为通过。

最终分级：**P0 = 0，P1 = 2，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全事故。 |
| P1 | 2 | 五模块指标语义仍不准确/不完整；ADR-044 仍含非真实精确测试路径。 |
| P2 | 1 | operations dist surface test 在 clean checkout 下仍不自洽。 |

## 六条关闭条件核验

| 第二轮关闭条件 | 第三轮判定 | 证据 |
| --- | --- | --- |
| 1. Knowledge replay 审计先行/原子 | **关闭** | Knowledge 在调用 projection 前先写 durable replay intent（[`repository.module.ts:166`](../../packages/repository/src/server/infrastructure/repository.module.ts:166)、[`repository.module.ts:171`](../../packages/repository/src/server/infrastructure/repository.module.ts:171)），只有 intent 成功后才进入真实 projection（[`repository.module.ts:184`](../../packages/repository/src/server/infrastructure/repository.module.ts:184)）。缺 audit dependency 测试断言 `FAIL_CLOSED`、ledger 仍为 `Pending/attempts=0` 且无 projection row（[`knowledge-write-request-projection-ledger.integration.test.ts:1109`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1109)）；审计写失败注入也断言相同外部调用与 durable state 语义（[`knowledge-write-request-projection-ledger.integration.test.ts:1142`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1142)）。本轮 repository integration 15/15 通过。 |
| 2. 五模块真实指标 | **未关闭，P1 保留** | Account 已在 create/claim/retry/success/failure 的 coordinator 分支发射指标（[`account-closure-coordinator.ts:115`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:115)、[`account-closure-coordinator.ts:140`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:140)、[`account-closure-coordinator.ts:177`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:177)、[`account-closure-coordinator.ts:446`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:446)）；Knowledge 覆盖 persisted/claimed/retried/succeeded/failed 和 worker outcome（[`knowledge-note-commit.service.ts:257`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:257)、[`knowledge-repository-projection.service.ts:583`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:583)、[`knowledge-repository-projection.service.ts:600`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:600)、[`knowledge-repository-projection.service.ts:717`](../../packages/repository/src/server/application/services/knowledge-repository-projection.service.ts:717)）；Schedule worker 能区分 retried/dead_letter（[`schedule-rebuild-worker-service.ts:93`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:93)）。但 Schedule 全仓没有 `schedule-rebuild.outbox.persisted` 发射点。更关键的是 Reminder 先无条件 `recordFailed`，再记录 retry/dead-letter（[`reminder-scheduler-service.ts:339`](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:339)）；其真实 DB 行为测试明确断言两次 retry/dead-letter 同时累计 `failed=2`（[`reminder-reliable-operation.integration.test.ts:431`](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:431)、[`reminder-reliable-operation.integration.test.ts:468`](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:468)）。Notification 也在 dead-letter 分支同时调用 failed/dead-letter，在 retry 分支同时调用 failed/retry（[`notification.runtime.ts:470`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:470)、[`notification.runtime.ts:514`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:514)）。这不是“准确区分”，而是仍把 retry/dead-letter 笼统计入 failed。 |
| 3. timeline_query 测试闭环 | **关闭** | Knowledge 已断言 source、固定 marker、filters 与 resultCount（[`knowledge-write-request-projection-ledger.integration.test.ts:1067`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1067)），并有 audit write failure fail-closed（[`knowledge-write-request-projection-ledger.integration.test.ts:1180`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1180)）。其余四模块分别有同类失败注入：Reminder（[`reminder-reliable-operation.integration.test.ts:1750`](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1750)）、Notification（[`notification-reliable-operation.integration.test.ts:777`](../../packages/notification/src/server/infrastructure/adapters/prisma/__tests__/notification-reliable-operation.integration.test.ts:777)）、Schedule（[`schedule-w5-real-concurrency.integration.test.ts:971`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:971)）、Account（[`account-closure-coordinator-db.integration.test.ts:777`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:777)）。共享 helper 只有审计写成功才返回 entries（[`operation-audit-repository.ts:96`](../../packages/patterns/src/operations/operation-audit-repository.ts:96)）。 |
| 4. ADR-044 真实引用 | **未关闭，P1 保留** | W6-3 与 W7-1 已改成真实文件并标出准确测试名称（[`ADR-044:42`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:42)、[`ADR-044:43`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:43)），W5-2 也不再使用通配符（[`ADR-044:39`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:39)）。但 W7-2 五条引用仍分别写成 `packages/reminder/...`、`packages/notification/...`、`packages/schedule/...`、`packages/account/...`、`packages/repository/...`（[`ADR-044:44`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:44)）；这些不是仓库中的真实路径，不满足“每条测试引用为真实、精确文件”。 |
| 5. dist surface clean-checkout 自洽 | **未关闭，P2 保留** | contracts/patterns 的 `test` target 都仍只有 command，没有 `dependsOn: ["build"]`（[`contracts/project.json:37`](../../packages/contracts/project.json:37)、[`patterns/project.json:32`](../../packages/patterns/project.json:32)）；根 `test` default 同样没有 build 依赖（[`nx.json:32`](../../nx.json:32)）。本轮将两个 operations dist 暂存到临时目录后直接运行两包 test：contracts 为 59 files passed、1 failed，520 passed/2 failed；patterns 为 5 files passed、1 failed，33 passed/2 failed，失败均为 operations `index.js`/`index.d.ts` 不存在。测试后已恢复原 dist；未改业务文件。 |
| 6. local Docker prod-like | **未完成；pre-existing 阻断已核实** | 环境有可用 Docker daemon，但没有 `pnpm` 且缺 `.env.production.local`，按 `validate-local-deploy` 规则只能判 inconclusive。直接用 workspace Nx 运行 `web:build --skip-nx-cache`，依赖构建成功后 Web production build 因 `@memoflow/app-vue/modules/authentication` 被根 alias 解析为 `packages/app-vue/src/index.ts/modules/authentication` 而失败。`apps/web/vite.config.ts` 与 `apps/web/src/auth/WebAuthView.vue` 相对 `HEAD` 均无 diff，且 `git show HEAD` 已包含该 alias/import（[`vite.config.ts:13`](../../apps/web/vite.config.ts:13)、[`WebAuthView.vue:12`](../../apps/web/src/auth/WebAuthView.vue:12)），故确认是 HEAD 前问题，不归因于 W7 r6；但完整 local Docker 仍不是 green。 |

## 指标生命周期专项结论

第三轮新增的行为测试不是全部停留在直接调用 collector：Schedule 使用真实 `ScheduleRebuildWorkerService` + Prisma repository（[`schedule-w5-real-concurrency.integration.test.ts:772`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:772)），Knowledge 驱动真实 projection service 与 Prisma ledger（[`knowledge-write-request-projection-ledger.integration.test.ts:1207`](../../packages/repository/src/server/infrastructure/adapters/prisma/__tests__/knowledge-write-request-projection-ledger.integration.test.ts:1207)），Account 驱动真实 coordinator（[`account-closure-coordinator.spec.ts:128`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.spec.ts:128)），Reminder/Notification 也各有真实 scheduler/runtime 路径断言。这一方向有效。

问题在于行为测试把错误语义锁成了 green：Reminder 测试把 retry 与 dead-letter 都计入 failed；Notification 的真实 runtime 测试只断言 legacy `failedTotal/deadLetterTotal`，没有断言统一 retry/dead-letter key 与 failed key 互斥（[`notification.runtime.spec.ts:184`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.spec.ts:184)）。因此“有行为测试”不能关闭“指标准确性”条件。

按当前代码实际存在的 lifecycle：

- Reminder：persisted → claimed → succeeded，失败分叉为 retryable 或 dead_letter，外加 worker completed/failed/skipped；retryable/dead_letter 不应再增加终态 failed。
- Notification：persisted → claimed → succeeded，失败分叉为 retryable 或 dead_letter，外加 worker completed/failed；同样不应双计 failed。
- Schedule rebuild：当前持久化 outbox → claimed → succeeded 或 retried/dead_letter，外加 worker completed/failed/skipped；缺 persisted 发射点。
- Account closure：persisted → running claim/reclaim → succeeded 或 failed；failed 再认领为 retried，worker completed/failed。当前没有实现独立 dead-letter 转移，不能为追求六个统一 key 而伪造不存在的 lifecycle。
- Knowledge：commit 持久化 → replay claim → succeeded/failed，Failed 再 replay 为 retried，worker completed/failed；当前没有独立 dead-letter 状态。

## 回归与独立验证

本轮环境没有 `pnpm`，使用 workspace `node_modules/nx/dist/bin/nx.js` 调用相同 Nx target；均禁用 Nx cache。

| 验证 | 第三轮实测结果 |
| --- | --- |
| 五包 `test:integration` | Reminder 29、Notification 25、Account 18、Schedule 26、Repository 15，合计 **113/113** 通过 |
| `memoflow:governance-check` | 退出码 0；inventory 996（unit 860、integration 23、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30） |
| clean-dist `contracts,patterns:test` | **按预期失败**：两包各 2 项 dist surface 失败，证明 P2-1 未关闭；dist 随后恢复 |
| `web:build` | **失败**：pre-existing `@memoflow/app-vue` alias 子路径解析问题；W7 未修改失败文件 |
| `git diff --check` | 无输出 |

任务给定的独立复验结果为 repository 192、reminder 382、notification 239、account 173、schedule 344，五包 lint 0 errors，inventory 已重生成；本轮五包 PostgreSQL integration 与治理门禁没有观察到 W0--W6 回退。ADR-044 所指的事务/outbox、lease fencing、capability、Account 分阶段恢复、Schedule CAS/rebuild 和 Knowledge webhook/replay 测试文件仍存在且本轮相关 integration 全绿。当前阻断来自 W7 指标语义、W7 文档追溯与 dist 门禁编排，不是已观察到的 W0--W6 状态机回退。

## 第三轮关闭要求

1. Reminder/Notification 的 retryable 与 dead-letter 分支不得再同时累计 `outbox.failed`；用真实 scheduler/runtime 行为测试断言 `retried`、`dead_letter` 与 `failed` 的互斥语义。为 Schedule rebuild 在真实 outbox 持久化成功点补 `persisted` 指标与行为断言；仅覆盖各模块实际存在的 lifecycle，不为 Account/Knowledge 伪造 dead-letter。
2. 把 ADR-044 W7-2 的五个省略路径改成仓库中真实、完整、精确的测试文件路径，并逐项核对测试名称/断言。
3. 为 contracts/patterns 的 `test` target 增加自身 `build` 依赖，或建立明确 build-first 的独立 package-export 门禁；重新从无 operations dist 的状态证明两包 test 自洽。
4. 在具备 `pnpm` 与 `.env.production.local` 的真实本地环境重跑 `pnpm docker:local:up`。pre-existing Web alias 应单独修复或在明确的基线修复后重跑；在所有必需服务 healthy 前不得把 prod-like 记为通过。

在这些条件满足前，**P0 = 0、P1 = 2、P2 = 1；W7 不可放行，不得提交 W7，也不能宣称 W0--W7 闭环。**

---

# 第四轮审查

审查日期：2026-08-12  
审查基线：`feat/business-p0-w2-notification`，`HEAD=53899b47b0e4211c3de4895375639ab825ed2872`，W7 r7 为未提交改动  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

## 终审结论

**W7 第四轮可放行：可以提交 W7，W0--W7 审查闭环。**

第三轮遗留的 P1-5、P1-6 和 P2-1 均已关闭。本轮未发现 W7 新的 P0、P1 或 P2：Reminder 与 Notification 的 retryable/dead-letter 分支不再累计 `outbox.failed`，且真实 scheduler/runtime 行为测试锁住互斥关系；Schedule rebuild 在真实 Prisma outbox 创建成功点发射 `persisted`；ADR-044 五条 W7-2 测试引用均为真实完整路径，并逐项核对到相应测试名称；从无 contracts/patterns dist 的状态执行两个 package test target 会先自行 build 并通过。

最终分级：**P0 = 0，P1 = 0，P2 = 0。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现不可逆数据破坏或生产安全事故。 |
| P1 | 0 | 指标生命周期/互斥语义与 ADR-044 可追溯性均已形成真实行为证据。 |
| P2 | 0 | dist surface 门禁已在 clean-dist 状态下自洽。 |

## 第三轮关闭项核验

| 关闭项 | 第四轮判定 | 独立证据 |
| --- | --- | --- |
| 1. 指标互斥与 Schedule persisted | **关闭** | Reminder 的失败分支只在 `retryable` 时记录 `retried`、在终止尝试时记录 `dead_letter`，不调用 `recordFailed`（[`reminder-scheduler-service.ts:339`](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:339)）；真实 Prisma scheduler 测试同时断言 durable 状态和三个统一 key，`failed` 不存在（[`reminder-reliable-operation.integration.test.ts:431`](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:431)）。Notification runtime 的两个真实失败分支同样分别只记录 `retried`/`dead_letter`（[`notification.runtime.ts:470`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:470)、[`notification.runtime.ts:515`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.ts:515)）；runtime/durable-dispatch tests 对两个状态分别断言 `failedTotal=0` 及统一 `failed` key 不存在（[`notification.runtime.spec.ts:183`](../../packages/notification/src/server/infrastructure/runtime/notification.runtime.spec.ts:183)、[`notification-durable-dispatch.spec.ts:130`](../../packages/notification/src/server/infrastructure/runtime/__tests__/notification-durable-dispatch.spec.ts:130)）。Schedule 在 `scheduleRebuildOutbox.create` 成功返回后发射 `persisted`（[`schedule-prisma.repository.ts:251`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:251)）；真实 Prisma worker test 先观察该 key，再验证 claim/success/worker completed（[`schedule-w5-real-concurrency.integration.test.ts:772`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:772)）。Account/Knowledge 仅覆盖其实际存在的 failed/retry lifecycle，未伪造 dead-letter。 |
| 2. ADR-044 精确路径 | **关闭** | W7-2 的五项均为仓库中的完整精确文件路径，并列出真实 `it(...)` 名称（[`ADR-044-w0-w6-fault-matrix.md:44`](../architecture/adr/ADR-044-w0-w6-fault-matrix.md:44)）。本轮逐项以精确字符串检索：Reminder `P1-3: reminder ...`、Notification `9f. P1-3 ...`、Schedule、Account、Knowledge 的四条同类名称均存在；ADR 中也不再含 `packages/<module>/...` 形式的省略路径。 |
| 3. clean-dist package surface | **关闭** | contracts/patterns 的 `test` target 均声明 `dependsOn: ["build"]`（[`contracts project.json:40`](../../packages/contracts/project.json:40)、[`patterns project.json:35`](../../packages/patterns/project.json:35)）。本轮将两个 `dist` 暂时移出后，以 `--skip-nx-cache` 分别执行 target：`contracts:test` 先运行 `contracts:build`，60 files / 522 tests 通过；`patterns:test` 先完成其 build dependency，6 files / 35 tests 通过。原 dist 已恢复，证明 package-export surface test 不依赖预先遗留产物。 |

## 回归与独立验证

本环境没有 `pnpm` 可执行文件，以下 Nx 命令使用 workspace 的 `node_modules/nx/dist/bin/nx.js`，均带 `--skip-nx-cache`。这不改变 target 内容；所有实际 Vitest、Prisma schema sync 和治理脚本均由仓库本地依赖执行。

| 验证 | 第四轮实测结果 |
| --- | --- |
| 五包 unit test | Repository 192、Reminder 382、Notification 239、Account 173、Schedule 344，全部通过。 |
| 五包 PostgreSQL integration | Reminder 29、Notification 25、Account 18、Schedule 26、Repository 15，合计 **113/113** 通过。 |
| 五包 lint | `reminder,notification,account,schedule,repository` 退出码 0、0 errors；仅有既有及测试文件 warnings。 |
| clean-dist `contracts:test` / `patterns:test` | 522/522 与 35/35 通过；两个 target 均实际执行了自身/依赖 build。 |
| `memoflow:governance-check` | 退出码 0；inventory 为 996（unit 860、integration 23、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。Nx 将 `test-system-v2:test:governance` 标为 flaky，但该 task 和总门禁均成功，按仓库规则仅记 warning。 |
| `git diff --check` | 无输出。 |

上述五包 integration 覆盖了 W0--W6 的 durable outbox、lease/fencing、capability、Account closure、Schedule CAS/rebuild、Knowledge replay 等相关状态机；本轮未观察到回退。

## local Docker prod-like 状态

**不是本 revision 的通过证据，且不得记为 green。** 依 `validate-local-deploy` 执行仓库标准脚本时，主机缺少 `pnpm`，也没有必需的 `.env.production.local`，所以 `pnpm docker:local:up` 被跳过。脚本报告显示的 lint/typecheck/test failure 原始原因均为 `spawnSync pnpm ENOENT`，不是已执行 target 的代码失败；本轮已用 workspace Nx binary 重跑本审查所需的定向 target，结果如上。

Docker daemon 可用，端口上的 api/web/ai-service/powersync 等容器虽为 healthy，但它们的 compose working directory 是 `/home/ubuntu/backups/memoflow-legacy-feature-repository`，应用镜像 revision 为 `fdefb466a7bf80087e29ae7e2be8035a954c28a5`（2026-07-31），不是当前 `53899b47b`/W7 工作树，故不能作为本次状态或服务健康的证据。

在当前工作树无缓存运行 `web:build`，最终 Vite bundle 仍失败：`@memoflow/app-vue/modules/authentication` 被根 alias 解析为 `packages/app-vue/src/index.ts/modules/authentication`。失败文件 [`vite.config.ts:13`](../../apps/web/vite.config.ts:13) 与 [`WebAuthView.vue:12`](../../apps/web/src/auth/WebAuthView.vue:12) 相对 `HEAD` 均无 W7 diff，且 `HEAD` 已包含相同 alias/import；这是第三轮已核实的 pre-existing Web build 阻断，不重新计入 W7 P0/P1/P2。

因此本轮的范围结论是 **W7 可提交、W0--W7 审查闭环**；但仓库级 PR/prod-like readiness 仍为否。须由该 Web alias 的所有者单独修复基线问题，并在具备 `pnpm` 与 `.env.production.local` 的环境重跑 `pnpm docker:local:up`，确认当前 revision 的 api、web、ai-service、powersync 均 healthy 后，才能把部署验证记为通过。
