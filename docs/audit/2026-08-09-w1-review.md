---
tags:
  - audit
  - reminder
  - w1
  - reliability
description: W1 Reminder 调度与投递意图实施审查
created: 2026-08-09T00:00:00Z
---

# W1 审查：Reminder 调度与投递意图重构

审查范围为 `docs/plan/active/2026-08-09-business-p0-elegant-fix-plan.md` 的 W1 小节、W0 reliable-messaging 契约，以及工作区中未提交的 W1 产物。未修改业务代码、未提交或推送。

## 结论

**W1 不可放行，不能进入 W2/W3。** 当前实现对“真实 cron + durable occurrence + 单事务写入”有部分实现，但没有满足 W0 的 fail-closed 和完整 receipt/lease 状态机不变量。

## Findings

### P0-1：运行时仍存在可达的空 runtime/no-op

`createReminderRuntimeContribution()` 在没有 `cronContribution` 时返回空的 `start/stop`（[reminder.runtime.ts:49-57](../../packages/reminder/src/server/infrastructure/runtime/reminder.runtime.ts:49)）。同时默认 `isEnabled` 为 true（[reminder.runtime.ts:32-35](../../packages/reminder/src/server/infrastructure/runtime/reminder.runtime.ts:32)），因此生产代码直接调用该工厂不会 fail-fast，而会宣称 capability available 后静默不调度。W0/计划明确禁止 implicit fallback/no-op success；这使 Reminder P0 仍可在错误组合下完全不执行。

### P0-2：调度器保留旧的进程内触发 fallback

`ReminderSchedulerService` 的可靠依赖是可选参数（[reminder-scheduler-service.ts:72-81](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:72)）；任一依赖缺失就走 `triggerService.triggerRemindersBatch()`（[reminder-scheduler-service.ts:220-229](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:220)）。该路径分步保存聚合/历史，绕过 occurrence、lease 和事务 outbox，正是 W1 要删除的根因路径。组合根当前传入依赖不能抵消这个可达 API/构造路径。

### P1-1：lease 只有过期抢占，没有 heartbeat/续租操作

数据库保存 `lastHeartbeatAt` 与 `heartbeatIntervalMs`（[reminder.prisma:191-196](../../packages/database/prisma/schema/reminder.prisma:191)），但 `ReminderReliableOperationPort` 只定义 `claimOccurrence` 和 `recordDeliveryIntent`（[ports.ts:85-94](../../packages/contracts/src/modules/reliable-messaging/ports.ts:85)），适配器也没有 heartbeat 方法。长事务超过 30 秒会被第二实例抢占；旧 owner 只在最终事务开始时检查 fencing（[prisma-reminder-write-transaction-runner.ts:49-83](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:49)），没有运行中续租闭环，不能满足 owner/expiry/heartbeat 的计划语义。

### P1-2：失败、重试和死信没有状态机落库

事务 runner 只产生 `succeeded` 或 `skipped`（[prisma-reminder-write-transaction-runner.ts:85-93](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:85)），异常在调度器中仅转换成返回值并调用 `recordFailed()`（[reminder-scheduler-service.ts:205-213](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:205)）。没有写入 `retryable`、`nextRetryAt`、`dead_letter` 或 `deadLetterAt`；metrics 的 `recordRetry/recordDeadLetter` 仅有未被调用的计数器（[reminder-metrics-service.ts:38-44](../../packages/reminder/src/server/domain/services/reminder-metrics-service.ts:38)）。因此 W0 要求的可重试、死信和可观测指标不成立。

### P1-3：Notification intent 没有数据库级幂等约束

runner 每次创建 outbox 时生成随机消息 ID（[prisma-reminder-write-transaction-runner.ts:140-158](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:140)），幂等键只嵌在 `payloadJson`；`OutboxMessage` 没有 `idempotencyKey` 列或唯一索引（[reliable-messaging.prisma:9-27](../../packages/database/prisma/schema/reliable-messaging.prisma:9)）。W0 要求的是 durable、可查询且唯一的 delivery intent。当前无法由数据库阻止相同 notification intent 的重复插入，也无法按键进行条件 upsert。

### P1-4：暂停/分组禁用不会生成 durable skipped occurrence

`getPendingReminders()` 在 claim 前过滤掉未启用模板（[reminder-trigger-service.ts:183-209](../../packages/reminder/src/server/domain/services/reminder-trigger-service.ts:183)）。因此调度器不会为暂停、全局禁用或分组禁用写 occurrence/history；runner 的 `isEnabled=false` 分支（[prisma-reminder-write-transaction-runner.ts:89-93](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:89)）只有被直接调用才可达。测试声称的 disabled skip 没有覆盖真实 cron 调度路径，且过期/时区边界同样没有调度级证据。

## W0/schema 对照

`ReminderOccurrence` 已包含 `templateId + occurrenceKey` 唯一约束、canonical `idempotencyKey`、owner/expiry/fencing/attempt/重试字段（[reminder.prisma:182-214](../../packages/database/prisma/schema/reminder.prisma:182)），其 receipt 映射也调用 schema 校验（[reminder-reliable-operation-prisma.adapter.ts:20-66](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:20)）。但 Prisma 状态字段为自由字符串，且上述 heartbeat、retry/dead-letter 字段没有实际状态转换。新增 `Account.reminderOccurrences` 关系与 occurrence 的 identity 外键在数据库层合理，但它不能代替业务 identity-scoped 查询；当前关系还未证明是 W1 必需而非仅为 ORM 导航。

## 测试质量

文件名与实施说明声称 7 个用例，但实际只有 5 个（[reminder-reliable-operation.integration.test.ts:45-314](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:45)）。双实例测试使用 `Promise.all` 发起两个 claim，能覆盖基本竞争；崩溃测试通过 100ms lease + 150ms sleep 模拟过期（[reminder-reliable-operation.integration.test.ts:125-155](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:125)），但没有在写历史、推进 next trigger、写 outbox 各个点注入崩溃，也没有断言回滚后不重复。没有 heartbeat、retry/dead-letter、暂停/分组、过期、时区、真实 cron 生命周期或 outbox 幂等断言。

本地验证受环境阻断：`pnpm` 不在当前 shell 中，无法运行 Nx integration/typecheck/lint targets。

## 分级与放行决定

- **P0**：P0-1 空 runtime/no-op；P0-2 旧 fallback 仍可达。
- **P1**：P1-1 heartbeat/续租缺失；P1-2 retry/dead-letter 未落库；P1-3 outbox 缺少唯一幂等约束；P1-4 disabled/paused 调度路径未形成 durable receipt；测试覆盖不足。
- **P2**：生成 Prisma 文件产生大量与业务无关的 diff/格式噪声（`git diff --check` 报告 generated `index.d.ts` 多处 trailing whitespace），增加审查和回滚风险，但不是放行阻断的根因。

在 P0/P1 问题修复并补齐真实并发、故障注入、状态机和边界测试前，W1 不应宣称完成，也不应进入后续工作包。

## 第二次审查（W1 修复后）

本次只复核 W1 修复产物与测试真实性，未修改业务代码、未提交或推送。

### 第一轮问题逐条核验

| 项目 | 证据与判断 | 结论 |
| --- | --- | --- |
| P0-1 runtime 空 start/stop | `createReminderRuntimeContribution()` 在 production/development 且缺少 cron capability 时由 `assertProductionCapabilityOrFailFast()` 抛错（[reminder.runtime.ts:37-47](../../packages/reminder/src/server/infrastructure/runtime/reminder.runtime.ts:37)）；返回的缺失 capability 即使在 test 中调用 `start()` 也抛错（[reminder.runtime.ts:53-58](../../packages/reminder/src/server/infrastructure/runtime/reminder.runtime.ts:53)）。`stop()` 仍为空，但该对象只在 capability 缺失的 test-double 场景返回；真实 cron 的 stop 会调用 `cronTask.stop()`。 | **P0 已关闭**。空 stop 不是生产成功路径，但完整 graceful shutdown 仍见下方新问题。 |
| P0-2 调度器 fallback | 构造函数现在把 `reliablePort`、`transactionRunner`、`controlService` 设为必需，并缺失即抛错；原 `triggerService.triggerRemindersBatch()` fallback 已从 `schedule()` 删除（[reminder-scheduler-service.ts:77-95](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:77)）。 | **P0 已关闭**。 |
| P1 heartbeat/续租 | port 声明、Prisma adapter 的 owner/fencing/expiry 校验和 `lastHeartbeatAt`/`leaseExpiresAt` 更新均真实存在（[ports.ts:97-112](../../packages/contracts/src/modules/reliable-messaging/ports.ts:97)、[reminder-reliable-operation-prisma.adapter.ts:218-280](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:218)），scheduler 也暴露转发入口。集成用例 6 证明过期前续租及过期后拒绝；但执行事务期间没有自动 heartbeat loop，30 秒以上事务仍可能失租。 | **P1 部分修复，未闭环**。 |
| P1 failed/retryable/dead-letter 落库与指标 | `recordOccurrenceFailure()` 按 attempt 写入 `retryable` 或 `dead_letter`、错误、退避时间、死信时间和 attempts history，并调用 retry/dead-letter/failed metrics（[reminder-scheduler-service.ts:267-322](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:267)）；集成用例 7 检查了 DB 与指标。没有任何路径把 receipt 状态写成契约中的终态 `failed`；并且下方的重试/死信领取语义会回归。 | **P1 部分修复，未闭环**。 |
| P1 Notification outbox 幂等 | `OutboxMessage.idempotencyKey` 为数据库 `@unique`，runner 在同一事务写入 notification intent（[reliable-messaging.prisma:9-29](../../packages/database/prisma/schema/reliable-messaging.prisma:9)、[prisma-reminder-write-transaction-runner.ts:132-169](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:132)）；用例 8 直接验证重复 insert 被 DB 拒绝。 | **P1 已关闭**。 |
| P1 paused/group-disabled durable skipped | `getPendingReminders()` 不再在扫描前过滤有效性（[reminder-trigger-service.ts:183-189](../../packages/reminder/src/server/domain/services/reminder-trigger-service.ts:183)）；scheduler claim 后调用 control service，再由同一事务写 history、推进 next trigger 和 `skipped` occurrence。用例 9 通过 scheduler scan 验证 paused group 的 durable receipt；用例 5 还验证 disabled transaction 分支。 | **P1 已关闭**。 |

### 新问题与回归

**P1-5：retryable/dead_letter 没有遵守重试与死信语义。** `recordOccurrenceFailure()` 写入 `retryable` 的 `nextRetryAt`，但 `claimOccurrence()` 的 existing 分支只判断终态和 lease 是否过期；对无 lease 的 `retryable` 或 `dead_letter`，`isLeaseExpired` 为真，会立即重新 claim（[reminder-reliable-operation-prisma.adapter.ts:144-186](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:144)）。scheduler/trigger 查询也没有按 `nextRetryAt` 或 dead-letter 状态拦截。由于失败发生在事务 runner 外，模板的 `nextTriggerAt` 未推进，真实 cron 下一分钟可重复领取死信，且不会等待退避或人工 replay。W0 将 `dead_letter` 定义为等待运维 replay 的非终态；当前行为违反该契约。**此为放行阻断的 P1。**

**P1-6：cron stop 不是 graceful drain。** `stop()` 只停止 node-cron task 并清空句柄，不等待已进入 `execute()` 的 Promise；`ReminderModule.dispose()` 随后即可返回，而正在运行的 scheduler 仍可能写入 occurrence/history/outbox（[reminder-trigger-cron-job.ts:87-123,140-146](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:87)）。W1 完成定义要求 start/stop/dispose 生命周期和 graceful shutdown 可观察，当前没有 in-flight drain 或停止标志。**P1。**

**P2-1：组合根 API 留有绕过强制依赖的注入口。** `createReminderTriggerCronJob()` 允许传入可选 `schedulerService`，此时跳过 `reliablePort`/`transactionRunner` 检查（[reminder-trigger-cron-job.ts:37-82](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:37)）。当前生产组合根没有使用该口，且 scheduler 构造本身已强制依赖，但该公开工厂仍可被错误 adapter/test double 接线，形成契约边界不一致。**P2（若该入口进入生产 wiring 则升级 P1）。**

### 测试真实性与边界

- 所谓“12 个集成测试”当前文件实际只有 10 个 `it()`（编号 1--10）。用例 1 的 `Promise.all` 确实并发发起两个 adapter claim，但没有并发执行 scheduler 的完整事务写入；用例 2 只是等待 100ms lease 过期后重新 claim，并未在 history、next trigger 或 outbox 各写入点做故障注入和回滚不重复断言。
- 用例 6 直接调用 adapter heartbeat，证明续租 API 的 DB 行为，不证明长事务期间存在自动续租闭环。用例 7 通过复制 receipt 手工把 `attempt` 改为 3，验证的是写入映射，不是 cron 重试调度、退避或死信 replay；也没有暴露上述 P1-5。
- 用例 9 已经走 `ReminderSchedulerService.schedule()`，因此 paused/group-disabled 的 skipped 声明有真实扫描路径证据。用例 10 的断言（Tokyo 09:00 对应 UTC 00:00）在该固定日期/非 DST 时区下合理，但只覆盖一个时区点，未覆盖 DST 或日期边界；没有 cron start/stop/dispose 生命周期测试。
- outbox 唯一约束用例直接操作 Prisma `create`，能证明数据库约束，但没有证明 runner 在并发重复事务下以幂等方式处理唯一冲突并返回原 receipt。

### 分级与放行结论

- **P0：无新增。** 第一轮 P0-1、P0-2 已按生产/开发 fail-fast 与强制构造路径修复。
- **P1：P1-5 retry/dead-letter 领取语义；P1-6 cron graceful drain；heartbeat 自动续租仍未闭环；failed 终态缺少真实落库路径；测试声明数量与覆盖范围不符。**
- **P2：P2-1 可选 schedulerService 绕过 capability 检查；P2-2 测试与文档将 10 个用例称为 12 个、时区/故障注入覆盖偏窄。**

**结论：W1 仍不可放行，不能进入 W2/W3。** 只有修复 retry/dead-letter 的 claim/replay/退避不变量、补齐运行中 heartbeat 与 graceful drain，并用真实 cron/并发/故障注入测试证明后，才可重新评估放行。

## 第三次审查（W1 终审）

本次按第二轮 4 个阻断项复核当前工作区，并回归第一、二轮已关闭项。只更新本审查文档，未修改业务代码，未提交或推送。

### 第二轮阻断项逐条核验

| 阻断项 | 代码与测试证据 | 结论 |
| --- | --- | --- |
| retryable 遵守 `nextRetryAt` | `claimOccurrence()` 对 `retryable` 且 `nextRetryAt > now` 返回 `claimed=false`，到期后才进入重新 claim（[reminder-reliable-operation-prisma.adapter.ts:155](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:155)）。所有 cron 扫描最终都调用该 adapter claim，因此基本领取路径已修复。用例 11 只直接调用 adapter，并未像标题所称驱动 cron；而重新 claim 的 `updateMany` 仅以 `id + fencingToken` 为条件，没有把 `status=retryable`、`nextRetryAt <= now` 纳入原子条件（[reminder-reliable-operation-prisma.adapter.ts:173](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:173)），读后写窗口内仍可覆盖并发状态变化。 | **部分关闭，仍有 P1 原子性缺口。** |
| dead_letter 从 cron 排除 | existing occurrence 为 `dead_letter` 时在统一 claim 入口直接返回 `claimed=false`（[reminder-reliable-operation-prisma.adapter.ts:144](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:144)）；scheduler 没有其他领取路径，用例 12 也通过真实 `scheduler.schedule()` 保持死信状态。因此 cron 无法领取已落死信的 occurrence。可是 Reminder 没有 identity-scoped dead-letter 查询或 replay port/实现；仓库中的 `replayDeadLetter` 只属于 Notification 契约（[ports.ts:128](../../packages/contracts/src/modules/reliable-messaging/ports.ts:128)）。当前是“任何入口均不可 replay”，并非“仅 replay 入口可恢复”。 | **cron 排除已关闭；replay 闭环未完成（P1）。** |
| heartbeat 自动续租 | scheduler 在 claim 后创建 interval 并真实调用 `heartbeatLease()`，事务结束后清理 timer（[reminder-scheduler-service.ts:189](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:189)）；adapter 校验 status、owner、fencing、expiry 并更新心跳（[reminder-reliable-operation-prisma.adapter.ts:230](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:230)）。但 timer 在 `isTemplateEffectivelyEnabled()` 完成后才启动，claim 到该查询结束之间没有续租；更重要的是 heartbeat 采用先读后按 `id` 更新，更新条件没有 owner/fencing/status/未过期约束（[reminder-reliable-operation-prisma.adapter.ts:277](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:277)），可与过期抢占交错。被抢占后 timer 只设置内存 flag，已开始的事务不会被取消；其 fencing 异常随后进入通用 failure 分支，`recordDeliveryIntent()` 又按 idempotency key 无条件更新，可能把新 owner 的 `running` 状态和 lease 覆盖掉（[reminder-scheduler-service.ts:266](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:266)、[reminder-reliable-operation-prisma.adapter.ts:294](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:294)）。 | **未关闭，P1。** |
| `stop()` graceful drain | cron contribution 的 `stop()` 会先置 stopping、停止 cron，再对 `currentExecutionPromise` 与 10 秒 timeout 做 `Promise.race`（[reminder-trigger-cron-job.ts:145](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:145)）。正常完成时确实等待；超时后明确返回且不取消 in-flight 工作，这是 bounded drain 语义。但 API composition root 的 `destroy()` 没有返回/await `activeReminderModule.dispose()`，随后立刻清空引用（[module.ts:75](../../packages/reminder/src/api/module.ts:75)），所以真实模块销毁调用者仍无法等待 drain。用例 14 启动后立即 stop，没有触发 cron tick、没有 active execution，也没有断言等待或超时。 | **cron 层实现部分关闭；生产 dispose 链路与证据未关闭（P1）。** |

### 回归与边界

- 第一轮两个 P0 无回退：production/development 缺失 cron capability 继续 fail-fast；scheduler 的可靠依赖仍为强制依赖，旧 `triggerRemindersBatch()` fallback 不在 `schedule()` 路径中。
- 失败会持久化 `retryable/dead_letter` 并记录 failed/retry/dead-letter metrics；但所有执行异常都只进入 retryable，达到次数后进入 dead-letter，仍没有契约中不可重试终态 `failed` 的分类和落库路径。加上上述 stale-owner 无条件 failure write，状态机不能视为完整关闭。
- outbox 的 `idempotencyKey @unique` 和同事务写入仍在；durable `skipped` 仍由真实 scheduler scan、control check 和 transaction runner 形成，未见回退。
- 业务改动集中在 Reminder、reliable-messaging contract 与所需 Prisma schema/generated client，未发现越界接入 W2/W3 业务；生产组合根也没有新增 implicit fallback/no-op。第二轮的可选 `schedulerService` 绕过强制依赖入口仍存在（[reminder-trigger-cron-job.ts:37](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:37)），维持 P2。

### 测试真实性与 lint

- 实际运行 integration target 等价命令，结果为 **2 files / 16 tests 全部通过**。其中本轮 reliable-operation 文件有 14 个用例；另 2 个是既有 `ReminderTemplatePrismaRepository` 持久化/identity 隔离测试，不能计作 cron、Retry、DeadLetter、Heartbeat 或 Drain 覆盖。
- 用例 1 的 `Promise.all` 是两个 adapter claim 的真实并发提交，能证明基本唯一约束/conditional update 竞争；没有并发执行完整 scheduler transaction，也没有覆盖 claim 与 heartbeat/failure write 的交错。
- 用例 11 直接调用 adapter，不是真实 cron。用例 12 确实经过 scheduler scan。用例 13 把返回对象中的 lease 改为 200ms，却没有修改数据库内实际 30 秒 lease，且没有第二 worker reclaim；350ms 延迟远小于真实 DB lease，因此只证明 timer 被调用，不能证明自动续租阻止过期抢占。用例 14 没有 fake timer 或分钟等待，start 后立即 stop，既未执行 cron scan，也无 in-flight Promise 可 drain。
- Reminder lint 实测为 **0 errors / 65 warnings**；连同 contracts 范围为 67 warnings。W1 业务实现文件没有新增 warning，但改动后的 scheduler 单测新增 8 个 `no-explicit-any` warning，其余为存量。它们不是本次 P1 放行阻断，但“65 全为存量”并不成立。
- `git diff --check` 仍报告 Prisma generated `index.d.ts` 多处 trailing whitespace，以及两个源码/schema EOF 空行；维持 P2 生成物噪声问题。

本地验证：integration **2 files / 16 tests passed**；Reminder typecheck **passed**；Reminder lint **0 errors / 65 warnings**。当前 shell 无 `pnpm`/Nx CLI，以上通过仓库内 Vitest、TypeScript、ESLint 入口执行等价检查。

### 终审分级与放行判定

- **P0：无新增，第一轮 P0 均保持关闭。**
- **P1：** retry claim 的状态/退避条件未原子化；Reminder dead-letter 无查询/replay 入口；heartbeat 与抢占的条件更新及 stale-owner failure write 不安全；API `destroy()` 不传播异步 drain；终态 `failed` 仍无分类路径；关键 cron/heartbeat/drain 测试名义覆盖但未真实驱动目标行为。
- **P2：** cron 工厂可选 scheduler 注入口仍可绕过工厂依赖检查；8 个新增测试 `any` warning；generated diff whitespace/格式噪声。

**终审结论：W1 不可放行，不能进入 W2/W3。** 当前 4 个第二轮阻断项只有 retry/dead-letter 的 cron 基本过滤已落实；heartbeat 的抢占安全、生产 dispose drain、dead-letter replay 和对应真实性测试仍未闭环。

## 第四次审查（agy 第 5 次迭代终审）

本次逐条复核前三轮遗留项、回归项与新增 20 个 integration 用例。只追加本审查文档，未修改业务代码，未提交或推送。

### 遗留项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| re-claim 原子性 | `updateMany` 已把 `fencingToken` 与 `status=retryable + nextRetryAt<=now` 放入同一条件，竞争失败会重新读取并返回 `claimed=false`（[reminder-reliable-operation-prisma.adapter.ts:176](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:176)）。用例 15 在退避前并发拒绝、到期后并发只允许一个 owner，断言有效。条件同时允许 `nextRetryAt=null`（同文件 182--184 行），比“到期”不变量更宽，但当前 failure/replay 写路径都会填充该字段。 | **第三轮原子性阻断关闭**；`retryable + nextRetryAt=null` 的 fail-open 行为记 P2。 |
| Reminder replay 闭环 | port 已增加 identity-scoped `queryDeadLetters`/`replayDeadLetter`，adapter 查询带 `identityId + dead_letter`，replay 也先以 identity/status 定位并转为立即到期的 `retryable`（[ports.ts:124](../../packages/contracts/src/modules/reliable-messaging/ports.ts:124)、[reminder-reliable-operation-prisma.adapter.ts:360](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:360)）。统一 claim 仍先排除 `dead_letter`，顺序 replay 后可以重新领取。可是 replay 是 `findFirst` 后按 `id` 无条件 `update`（同文件 391--412 行）：若另一个 replay 已转为 retryable 且 cron 已 claim 为 running，迟到的 replay 仍会清空新 owner/claim/lease。按 `occurrenceKey` replay 也未带 `templateId`，同 identity 下多个模板同一触发时刻会选择不确定记录。 | **顺序闭环已实现，但并发 replay 不安全，P1。** |
| heartbeat 原子性与启动时机 | heartbeat timer 在 claim 成功后、control 查询前创建；production 30 秒 lease 的 interval 为 lease/3。adapter 的更新条件已同时包含 owner、fencing、running 和未过期（[reminder-scheduler-service.ts:189](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:189)、[reminder-reliable-operation-prisma.adapter.ts:257](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:257)）。`recordDeliveryIntent` 也会按 claimId/fencing 条件更新，stale failure 不再覆盖新 owner（同文件 315--357 行）。但是重新领取会生成新的随机 `claimId`，heartbeat 却把 `claimId` 当 occurrence 主键 `id` 查询（同文件 173、259 行）；首次 claim 恰好 `id=claimId`，任何 retry/reclaim/preemption 后的 owner 都无法续租。并且 transaction runner 只在开头读后校验 fencing，最后按 `id` 无条件写终态（[prisma-reminder-write-transaction-runner.ts:48](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:48)、同文件 173--184 行）；timer 在事务执行中失租只改内存 flag，不会取消或回滚已开始的事务。旧事务仍可能在新 owner claim 后写 history/template/outbox 并覆盖 occurrence。 | **条件 heartbeat 与 stale failure write 已修复；reclaim heartbeat 和事务中止仍为 P1，未闭环。** |
| dispose/stop drain | cron `stop()` 会先阻止新执行、停止 task，再等待 `currentExecutionPromise`；API `destroy()` 现已 `await activeReminderModule.dispose()`，API bootstrap 也 await module destroy，生产调用链真实可达（[reminder-trigger-cron-job.ts:146](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:146)、[module.ts:75](../../packages/reminder/src/api/module.ts:75)）。但默认 10 秒 timeout 到期后以成功返回，既不取消 in-flight scan，也不向 dispose/bootstrap 报告超时；调用方随后可关闭数据库，而最长 30 秒 lease 下的批处理仍在写。 | **生产 await 链路已关闭第三轮缺口；超时后的成功返回/继续写为 P1。** |

### 回归、边界与新问题

- 两个第一轮 P0 保持关闭：production/development 缺 cron capability 仍 fail-fast；`schedule()` 的可靠依赖仍强制注入，未恢复旧的 `triggerRemindersBatch()` fallback。cron 工厂的可选 `schedulerService` 绕过依赖检查仍是测试注入口，维持 P2。
- failure 仍会持久化 `retryable/dead_letter` 及 attempt history，并记录 failed/retry/dead-letter metrics；retryable 退避过滤与 dead-letter cron 排除未回退。不可重试终态 `failed` 仍没有分类/落库路径，维持 P1。
- outbox 仍在同一事务创建且 `idempotencyKey @unique`；paused/group-disabled 的 durable `skipped` 仍经 scheduler scan 和 transaction runner 落库，未见回退。
- **新增 P1：同 identity 的两个模板若 `nextTriggerAt` 相同，会生成相同 Reminder idempotency key。** scheduler 的 key 只含 `identityId + reminder + ISO triggerTime`，不含 template（[reminder-scheduler-service.ts:145](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:145)），而数据库同时声明全局唯一 `idempotencyKey` 与 `templateId + occurrenceKey` 唯一。第二个模板会命中第一个模板的 receipt 并被当作重复/活跃 lease 跳过；Notification outbox key也只含同一时间，无法表达两个独立 intent。这会在常见的整点多提醒场景漏投。
- 业务改动仍集中在 Reminder、reliable-messaging contract 与必要 Prisma schema/generated client，未发现 W2/W3 业务越界或新增隐式 no-op；但包内分层已经越界：domain service 直接 import infrastructure 的具体 `PrismaReminderWriteTransactionRunner`（[reminder-scheduler-service.ts:29](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:29)），`memoflow:governance-check` 的 package-internal boundary audit 因此失败。domain 应依赖 application/domain port，而不是 Prisma 实现。此为 P1 门禁阻断。generated client 大量格式 diff、schema/source EOF 空行及 `index.d.ts` trailing whitespace 维持 P2 审查噪声。

### 20 个测试的真实性

- 实测 `reminder:test:integration` 为 **2 files / 20 tests 全部通过**，Reminder typecheck 通过。20 个中，本文件有 18 个，另 2 个是既有 repository 持久化/identity 隔离用例，不是本轮新增的可靠调度路径。
- 用例 15 真实并发调用两个 adapter，能够证明 retryable 到期条件更新的互斥。它没有在 adapter 读与写之间主动改变状态，但 DB 条件和“恰好一个 claimed”断言足以覆盖本轮 re-claim 修复。
- 用例 16 依次完成过期、抢占、stale heartbeat 与 stale `recordDeliveryIntent`，能证明 adapter 的两个条件写；它对 heartbeat 传入首次 claim 的 operation id，未尝试让新 owner heartbeat，也没有启动旧 worker 的 transaction runner，因而没有暴露 `claimId != id` 或“旧事务抢占后仍提交”的问题。
- 用例 17 只直接调用 adapter 的 query/replay/claim；未经过 scheduler 或 cron，未做 cross-identity 否定断言，也没有 replay/claim 并发交错。它只能证明顺序状态转换。
- 用例 18 在 `cronJob.start()` 后仅等待 20ms，而表达式每分钟触发；随后直接 stop，`scanExecuted` 从未断言（[reminder-reliable-operation.integration.test.ts:1094](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1094)）。因此没有 active execution，不能证明 stop 或 API dispose 等待 in-flight。用例 14 存在同样问题。用例 13 仍只改返回 receipt 的 expiry，没有缩短数据库内 30 秒 lease，也没有第二 worker 抢占，不能证明 heartbeat 防抢占。
- 未覆盖同一 identity 多模板同一触发时刻、replay 与 claim 交错、reclaim 后新 owner heartbeat、旧事务与新 owner 抢占交错、drain timeout 结果或 API `destroy()` 到 active scan 的端到端等待。

本地验证：`reminder:test:integration` **2 files / 20 tests passed**；Reminder typecheck **passed**；Reminder lint **0 errors / 66 warnings**（新增的第 18 用例包含未使用的 `scanExecuted` warning）；`memoflow:governance-check` **failed**，唯一新增违规为 domain→infrastructure 的 Prisma runner import。`git diff --check` 仍报告 generated `index.d.ts` trailing whitespace 与两个 EOF blank-line 问题。

### 第四次终审分级与放行判定

- **P0：无新增；第一轮 P0 均保持关闭。**
- **P1：** replay 与 claim 非原子交错可清空新 owner；reclaim 后 heartbeat 用 claimId 错查 occurrence 主键而无法续租；旧事务在抢占后没有条件终态写/中止保证；drain timeout 静默成功且 in-flight 继续；同一 identity 多模板同一时刻发生 idempotency collision 并漏投；不可重试 `failed` 终态仍无分类路径；domain 直接依赖 Prisma infrastructure，governance gate 失败。关键事务、replay、heartbeat、drain 测试没有真实覆盖上述路径。
- **P2：** retryable 的原子条件额外允许 `nextRetryAt=null`；cron 工厂可选 scheduler 注入口；generated diff/whitespace 噪声及部分测试标题夸大实际覆盖。

**第四次终审结论：W1 不可放行，不应提交 W1 或进入 W2/W3。** 本轮确实关闭了 re-claim 条件更新、identity-scoped replay 基本闭环、heartbeat 条件更新、stale failure write 和 API await drain 链路，但抢占事务安全、reclaim heartbeat、并发 replay、实际 drain、多模板幂等及分层治理仍存在 P1 阻断。

## 第五次审查（W1 第六/七次迭代终审）

本次按前四轮遗留、回归项、新增竞态与 22 个 integration 用例逐项终审。只追加本审查文档，未修改业务代码，未提交或推送。

### 前四轮遗留逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| re-claim 原子性 | retryable 到期后的 `updateMany` 同时约束原 occurrence `id`、旧 `fencingToken`、`status=retryable` 与 `nextRetryAt<=now`，竞争失败会重读 receipt（[reminder-reliable-operation-prisma.adapter.ts:182](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:182)）。用例 15 在退避前后分别并发 claim，并断言到期后恰好一个 owner。 | **已关闭。** 原子 claim 条件真实存在；`nextRetryAt=null` 仍被放行，但正常 failure/replay 写路径均会填值，降为 P2 fail-open 防御缺口。 |
| dead-letter replay 闭环 | `queryDeadLetters()` 按 identity/status 查询；顺序 replay 会把死信转成已到期 `retryable`，随后真实 `scheduler.schedule()` 可领取并完成（[reminder-reliable-operation-prisma.adapter.ts:366](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:366)、[reminder-reliable-operation.integration.test.ts:1047](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1047)）。但 replay 仍是 `findFirst(dead_letter)` 后按 `id` 无条件 `update`（[reminder-reliable-operation-prisma.adapter.ts:397](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:397)）：两个 replay 同时读到 dead-letter 后，A replay、cron claim 为 running，迟到的 B 仍可清空新 owner/claim/lease。 | **顺序闭环关闭；replay/claim 竞态未关闭，P1。** |
| heartbeat 原子性与抢占安全 | heartbeat 条件写已约束 owner/fencing/running/未过期，30 秒 lease 配 10 秒（lease/3）间隔合理；用例 13 也真实让 scheduler 的首次 claim 跨越短 lease，并以第二 worker claim 验证续租。但 re-claim 会生成新的随机 `claimId`，heartbeat 却用该 `claimId` 查询 occurrence 主键 `id`（[reminder-reliable-operation-prisma.adapter.ts:179](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:179)、同文件 [265](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:265)）；仅首次 claim 因 `id=claimId` 偶然可用。更关键的是 transaction runner 只在事务开头读后校验 fencing，终态仍按 `id` 无条件更新（[prisma-reminder-write-transaction-runner.ts:42](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:42)、同文件 [165](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:165)），失租旧事务仍可能在新 owner claim 后提交 history/template/outbox/终态。 | **部分关闭，仍有 P1。** heartbeat adapter 的条件更新安全，但 reclaim owner 无法 heartbeat，事务提交也没有最终 fencing 条件。 |
| dispose drain（超时显式失败） | cron `stop()` 先停止新 tick，再等待 active promise；超时会 `logger.error` 并 reject，API `destroy()` 与 module `dispose()` 均向上 await（[reminder-trigger-cron-job.ts:146](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:146)、[module.ts:75](../../packages/reminder/src/api/module.ts:75)）。用例 18/19 主动启动同一 `execute()` 路径，分别证明等待与超时 reject。可是每次 cron callback 都会用新的 wrapper 覆盖 `currentExecutionPromise`；当已有扫描超过一分钟时，下一 tick 的 `execute()` 因 `isRunning` 立即返回，其 `finally` 随即把指针清空（[reminder-trigger-cron-job.ts:137](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:137)），此后 `stop()` 无法等待仍在运行的首个扫描。 | **超时静默成功已关闭；重叠 tick 会丢失 in-flight 引用，graceful drain 仍有 P1。** |
| 幂等键模板命名空间 | scheduler 使用 `${template.id}:${rawTimeIso}` 作为 occurrenceKey，Reminder receipt 与 Notification outbox 均以该值构造 canonical key（[reminder-scheduler-service.ts:144](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:144)、[prisma-reminder-write-transaction-runner.ts:125](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:125)）。用例 20 经真实 scheduler scan 让同 identity、同刻两个模板都成功并产生两个 outbox。canonical key 使用长度前缀编码，occurrenceKey 被当作不透明字符串，因此 `templateId` 含 `:` 不会造成解析歧义（[operation-receipt.ts:83](../../packages/contracts/src/modules/reliable-messaging/operation-receipt.ts:83)）。 | **已关闭，未发现新碰撞。** |
| 分层（端口注入） | domain scheduler 只依赖 `ReminderTransactionRunner` 接口，不再 import Prisma runner（[reminder-scheduler-service.ts:33](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:33)、[reminder-transaction-runner.port.ts:20](../../packages/reminder/src/server/domain/ports/reminder-transaction-runner.port.ts:20)）；governance 的 package-internal boundary audit 实测 640 文件、0 违规。cron infrastructure 仍把参数声明为 Prisma 具体类，但没有形成 domain→infrastructure 反向依赖。 | **治理阻断已关闭。** 具体类类型限制测试/替换能力，记 P2。 |

### 回归、边界与新问题

- fail-fast 未回退：production/development 缺 scheduler capability 会抛错；scheduler 构造依赖仍为强制项，旧 `triggerRemindersBatch()` fallback 未回归。测试环境的缺 capability 对象调用 `start()` 也会抛错，没有生产可达的 silent no-op success。
- failure 会持久化 `retryable/dead_letter`、退避时间、错误和 attempts history，并记录 failed/retry/dead-letter metrics；claim 会过滤未到期 retryable，统一入口排除 dead-letter。不可重试终态 `failed` 仍没有错误分类与落库路径，维持 P2 契约完整性问题，但不是本轮新增回归。
- outbox 仍在 history、next trigger 与 occurrence 终态的同一 Prisma transaction 中创建，`OutboxMessage.idempotencyKey @unique`；paused/group-disabled 仍由 scheduler scan 形成 durable `skipped` receipt。未发现相关回退。
- 变更范围集中于 Reminder、W0 reliable-messaging contract、所需 Prisma schema/generated client 与 Account 关系回链，未接入 W2/W3 业务；未发现越界业务改动或新增隐式 fallback/no-op。
- 新增的命名空间没有字符串解析碰撞；真正的新增风险仍是上表中的 replay/claim 非原子交错。heartbeat 的 10 秒间隔对 30 秒 lease 合理，但无法弥补 reclaim 查询键错误和事务最终 fencing 缺失。
- drain timeout 现在语义明确为“停止接收新 tick、等待至上限，超时 reject；不承诺取消 in-flight”。该语义本身可接受且可观察；阻断点是重叠 tick 会让实现失去实际 in-flight 引用，而不是 timeout 仍静默成功。

### 22 个 integration 用例真实性

- 实测 `reminder:test:integration` 为 **2 files / 22 tests passed**。可靠调度文件实际有 20 个用例；另 2 个是 `ReminderTemplatePrismaRepository` 持久化/identity 隔离用例，不能计作 cron、抢占、drain 或 replay 覆盖。
- 用例 1、15 真实并发 adapter claim，足以证明首次 claim 与 retryable re-claim 的条件写互斥；没有并发执行完整 scheduler transaction。
- 用例 13 真实驱动 scheduler heartbeat 和第二 worker claim，证明的是**首次 claim**的自动续租。它没有先 retry/replay/reclaim，未暴露 `new claimId != occurrence.id`；用例 16 只验证 stale owner 的 heartbeat/failure write 被拒绝，也没有让新 owner heartbeat 或让旧事务跨越抢占后提交。
- 用例 17 的 replay 后确实经过 `scheduler.schedule()` 并完成，但 replay、claim 严格串行，且事务在首次 heartbeat 前已完成；没有覆盖两个 replay 与 cron claim 的交错，也没有覆盖 reclaim owner 的 heartbeat。
- 用例 18/19 通过显式 `execute()` 真实进入 cron contribution 的同一执行与 drain 路径，等待和 timeout reject 断言有效；它们没有由 node-cron tick 触发，也没有制造“长扫描跨过下一分钟 tick”，因而未覆盖 `currentExecutionPromise` 被覆盖的缺口。
- 用例 20 真实经 scheduler 同批处理两个模板，并断言两个 occurrence 和两个 outbox，足以关闭原多模板漏投问题。没有 templateId 含冒号的专门用例，但 canonical 长度前缀实现不依赖冒号分割。

本地验证：Reminder integration **2 files / 22 tests passed**；Reminder unit **46 files / 362 tests passed**；Reminder typecheck **passed**；`memoflow:governance-check` **passed**（package-internal boundary 640 文件、0 违规）。`git diff --check` 仍因 generated Prisma `index.d.ts` trailing whitespace 及三个 EOF blank line 返回失败，维持 P2 审查噪声。

### 第五次终审分级与放行判定

- **P0：无新增。** 第一轮 fail-fast 与旧 fallback 问题保持关闭。
- **P1：** replay 仍以读后无条件更新与 cron claim 竞态，可清空新 owner；reclaim 后新 claimId 被误当 occurrence 主键，自动 heartbeat 失效；transaction runner 缺少提交时的 owner/fencing/未过期条件，旧事务失租后仍可能提交并覆盖新 owner；长扫描跨下一 cron tick 时会丢失 in-flight promise，`stop()/dispose()` 不能保证 drain。现有 22 个绿测均未覆盖这些交错。
- **P2：** retryable 原子条件允许 `nextRetryAt=null`；不可重试 `failed` 终态无分类路径；cron infrastructure 参数绑定 Prisma 具体 runner；generated diff/EOF whitespace 噪声。

**第五次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第六/七次迭代已真实关闭多模板幂等碰撞、domain 分层治理和 drain timeout 静默成功，并补强了顺序 replay 与测试；但 lease/replay 的核心并发不变量和长任务 drain 仍有 P1 阻断，不能仅凭 22/22 与治理全绿宣称 W1 完成。

## 第六次审查（W1 第八次迭代终审）

本次逐条复核第五轮剩余的提交时 fencing 与跨 tick 防重叠，并回归前五轮全部已关闭项、25 个 integration 用例、变更边界及完整治理门禁。只追加本审查文档，未修改业务代码，未提交或推送。

### 第五轮 2 项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| 提交时 fencing：所有 occurrence 写与被抢占回滚 | claimed occurrence 的业务写仍全部位于同一个 Prisma transaction：history、template `nextTriggerAt`、Notification outbox 后，终态写改为 `updateMany`，同时约束 `id + ownerToken + fencingToken + status=running`（并叠加 lease 条件）；条件不命中即抛 `LeaseFencingException`，因此前述业务写随事务整体回滚（[prisma-reminder-write-transaction-runner.ts:40](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:40)、同文件 [94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、[115](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:115)、[146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、[165](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:165)）。统一 adapter 的其余 occurrence 更新也保持条件写：re-claim 带原 fencing/status/due 条件，heartbeat 带 claim/owner/fencing/running/未过期，failure receipt 带 claim/fencing，replay 带 `dead_letter` 状态（[reminder-reliable-operation-prisma.adapter.ts:182](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:182)、同文件 [263](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:263)、[334](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:334)、[420](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:420)）。迟到的 stale failure 因 claim/fencing 不匹配只能重读当前 receipt，不会覆盖新 owner。 | **代码缺口已关闭。** 提交前后的 owner/fencing/status 条件能阻止旧 owner 覆盖，条件终态写失败会回滚同事务的 history/template/outbox。可是新增用例 21 并未证明其标题所称的“事务执行中抢占”，见测试真实性；核心交错仍缺真实测试证据。 |
| 跨 tick 防重叠与真实 drain Promise | `runScan()` 在已有扫描或 stopping 时直接返回同一个 `currentExecutionPromise`，不再创建 wrapper 覆盖指针；唯一 promise 仅由自身 `finally` 做 identity-checked 清理。node-cron callback 与公开 `execute()` 都进入 `runScan()`，`stop()` 停止新 tick 后对该真实 promise 与显式 reject 的 timeout 做 race（[reminder-trigger-cron-job.ts:129](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:129)、同文件 [135](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:135)、[153](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:153)、[160](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:160)）。用例 23 启动一次 250ms 扫描，再次调用同一入口并断言两个 Promise 引用相同，随后 `stop()` 等到 `scanCompleted=true`（[reminder-reliable-operation.integration.test.ts:1436](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1436)）。 | **已关闭。** 同进程 tick 真正串行，重复 tick 不启动第二次扫描，drain 始终持有首个活动扫描的真实 Promise，未发现新的指针清理竞态。 |

### 前五轮关闭项回归

| 回归项 | 核验结果 |
| --- | --- |
| fail-fast / 无旧调度 fallback | production/development 缺 cron capability 仍拒绝启动；scheduler 的 reliable port、transaction runner、control service 仍为强制依赖，`schedule()` 未恢复 `triggerRemindersBatch()` 旧路径。**保持关闭。** |
| 状态落库与指标 | failure 仍写 `retryable/dead_letter`、错误、退避/死信时间及 attempts history，并记录 failed/retry/dead-letter；claimed 与 due latency 指标仍在真实 scheduler 路径。**保持关闭。** 不可重试 `failed` 分类仍缺，维持 P2。 |
| outbox 幂等 | Notification intent 与 history/template/occurrence 同 transaction，`OutboxMessage.idempotencyKey` 仍为数据库唯一；key 含模板命名空间后的 occurrenceKey。**保持关闭。** |
| durable skipped | paused/group-disabled 不在 scan 前过滤，claim 后由 transaction runner 写 history、推进模板并落 `skipped` receipt。**保持关闭。** |
| retryable 过滤 / dead-letter cron 排除 | 未到期 retryable 在统一 claim 入口拒绝；dead-letter 不能被普通 cron claim。**保持关闭。** `retryable + nextRetryAt=null` 仍可 claim，维持 P2 fail-open 防御缺口。 |
| re-claim 原子性 | `updateMany` 继续约束旧 id/fencing/status 与 retry due/lease expiry，竞争失败返回当前 receipt。**保持关闭。** |
| replay 闭环 | identity-scoped query 保持；replay 的 `updateMany` 已增加 `status=dead_letter` 条件，并发迟到 replay 不再覆盖已被 cron claim 的 running owner。**保持关闭。** 按 occurrenceKey 未带 templateId 的不确定选择被模板命名空间后的 occurrenceKey 消除。 |
| heartbeat 原子性 | heartbeat 按当前 `claimId`（兼容首次 `id=claimId`）查询，并约束 owner/fencing/running/未过期；reclaim 新 owner 可续租。**保持关闭。** |
| 幂等键命名空间 | Reminder 与 Notification key 均使用包含 template id 的 occurrenceKey；同 identity、同刻双模板仍生成两条 occurrence/outbox。**保持关闭。** |
| 分层 | domain scheduler 只依赖 `ReminderTransactionRunner` port；package-internal boundary audit 实测 **640 files / 0 violations**。**保持关闭。** cron infrastructure 仍把 runner 参数绑定 Prisma 具体类，维持 P2。 |
| drain 超时拒绝 | timeout 仍记录 error 并 reject，API destroy/module dispose 继续 await。**保持关闭。** |

### 新问题、边界与防重叠影响

- **P1：新增时区路径存在隐式 fallback，会把无效 IANA timezone 静默改成宿主机本地时间。** `FixedTimeTriggerSchema` 只校验任意字符串；`Intl.DateTimeFormat` 抛错后，`getZonedEpochMs()` 返回 `new Date(...)` 的宿主机本地 epoch，一次性与循环计算的外层 catch 还会先静默改用 UTC 日期（[trigger-config.ts:11](../../packages/contracts/src/modules/reminder/value-objects/trigger-config.ts:11)、[upcoming-reminder-calculation-service.ts:227](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:227)、同文件 [253](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:253)、[286](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:286)、[379](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:379)）。同一持久化数据会随服务器时区产生不同触发时刻，且没有 failed receipt；这违反本轮“无隐式 fallback”和时区正确性边界。现有测试只有一个 `Asia/Tokyo` 正向点，没有无效 timezone 或 DST gap/fold 证据。
- 防重叠采用 skip/coalesce 语义而不是排队：若扫描跨过一个 tick，中间 tick 复用原 Promise，扫描完成后要等下一分钟才重新读取新到期项，额外延迟上界接近一个 cron interval。该取舍能保证串行且不会丢失已被首次 scan 读到的项，但目前只有 debug log，没有 overlap-skipped 计数或延迟指标，记 **P2 可观测性缺口**。
- final occurrence 条件额外允许 `leaseExpiresAt=null`，而正常 running claim 必然有 lease；这对损坏数据是 fail-open，连同 retryable 的 `nextRetryAt=null` 记 **P2 防御缺口**。条件叠加本身未与现有 owner/fencing/status 更新冲突。
- 业务范围仍集中在 Reminder、W0 reliable-messaging contract、必要 Prisma schema/generated client 与 Account 关系回链；时区计算属于 W1 的时区边界修复。未发现 W2/W3 业务实施或其他越界改动。除上述时区回退外，未发现新增生产可达的 no-op/silent-success 路径。
- `git diff --check` 当前只报告 generated Prisma `index.d.ts` 的 trailing whitespace；没有手写源文件 whitespace 错误，维持 P2 generated 审查噪声。

### 25 个 integration 用例真实性

- 实测 `reminder:test:integration` 为 **2 files / 25 tests passed**。可靠调度文件有 23 个用例，另 2 个仍是既有 `ReminderTemplatePrismaRepository` 持久化/identity 隔离用例；因此“25/25”是 target 总数，不能表述为 25 个可靠调度/cron 用例。
- 用例 22 真实执行首次 claim→DB 过期→第二 owner re-claim，并明确断言新 `claimId != occurrence.id` 后调用 heartbeat，足以关闭第五轮 reclaim heartbeat 查询键缺口。
- 用例 23 直接两次调用 cron contribution 的 `execute()`（node-cron callback 使用同一 `runScan()`），能够证明同入口防重叠、Promise identity 与 stop drain；它没有等待真实分钟 tick，但不以另一套 test-only implementation 代替生产路径。
- **用例 21 的标题和注释夸大覆盖。** 它在调用 transaction runner **之前**直接把 occurrence 改成 worker-2/fencing=2（[reminder-reliable-operation.integration.test.ts:1320](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1320)），runner 随后在事务第一个 fencing read 就抛错，history/template/outbox 从未执行；所以零 history/outbox 不是“提交条件失败导致整事务回滚”的证据。它没有在 history 后、template 后或 outbox 后暂停并抢占，也没有并发运行旧/new worker。实现的单事务与最终条件写从静态代码上能建立回滚保证，但 W1 计划要求的“各写入点崩溃/抢占可恢复且不重复”仍没有真实故障注入证据。此为 **P1 测试门禁缺口**。
- 其余 1--20 用例的真实性结论沿用第五轮：claim/retry/skipped/replay/outbox/heartbeat/drain/multi-template 的已声明顺序路径保持覆盖；仍没有 invalid timezone、DST gap/fold，以及上述事务中途抢占覆盖。

### 本地验证

- Reminder integration：**2 files / 25 tests passed**。
- Reminder unit：**46 files / 362 tests passed**。
- Reminder typecheck：**passed**。
- Package-internal boundary audit：**640 files / 0 violations**。
- 完整 `memoflow:governance-check`：**failed**。串行重跑时 `test-system-v2:test:governance` 报 test inventory stale；当前 inventory 计算为 957 files，而已提交清单为 955，缺少 `packages/contracts/src/modules/reliable-messaging/reliable-messaging-contracts.spec.ts` 与本轮新增的 `reminder-reliable-operation.integration.test.ts`。因此“640/0”只证明分层子审计，不能等同完整 governance 全绿。此前并行启动多个 Nx target 还触发过一次共享 `contracts/dist` 清理竞态；上述 inventory 失败来自随后串行独立重跑，与该工具竞态无关。

### 第六次终审分级与放行判定

- **P0：无新增。** 第一轮 production capability fail-fast 与旧调度 fallback 保持关闭。
- **P1：** invalid timezone 被静默回退为宿主机/UTC 时间，可能无失败 receipt 地错时投递；事务中途抢占及各写入点回滚/不重复没有真实故障注入测试；完整 governance 因 stale test inventory 实测失败。
- **P2：** overlapping tick 被 coalesce 后可让下一批新增 due item 延迟近一分钟且无专用指标；running lease/null 与 retryable nextRetryAt/null 的 fail-open 防御缺口；不可重试 `failed` 无分类路径；cron infrastructure 绑定 Prisma 具体 runner；generated Prisma whitespace 噪声。

**第六次终审结论：W1 仍不可放行，不应提交 W1 或进入 W2/W3。** 第八次迭代已经关闭第五轮的提交时 fencing 代码缺口、reclaim heartbeat、replay 条件更新及跨 tick Promise 跟踪；但在移除时区隐式 fallback、补上真实事务中途抢占/各写入点故障注入，并更新 test inventory 使完整 governance 通过前，W1 尚未满足完成定义。

## 第七次审查（W1 第九次迭代终审）

本次按第六轮 3 项阻断逐条复核，并回归前六轮已关闭项、检查新误伤与测试真实性。只追加本审查文档，未修改业务代码，未提交或推送。

### 第六轮 3 项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| timezone fail-fast：所有 FixedTime/时区计算路径 | `calculateNextTriggerTime()` 的一次性与循环 FixedTime 分支均调用 `validateTimezone()`，底层 `Intl.DateTimeFormat` 拒绝时抛出明确错误，外层 catch 对该错误重新抛出；原来的宿主机/UTC catch fallback 已从这两条 next-trigger 路径删除（[upcoming-reminder-calculation-service.ts:205](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:205)、同文件 [218](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:218)、[283](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:283)、[358](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:358)）。但是 `calculateTodaySchedule()` 的 FixedTime 路径仍进入 `generateFixedTimeTriggersForToday()`，该方法完全不读取 `fixedTime.timezone`，而是无条件硬编码 UTC+8（[upcoming-reminder-calculation-service.ts:624](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:624)、同文件 [651](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:651)）。因此 `Asia/Tokyo`、`UTC` 与无效 timezone 都会被静默按北京时间返回；创建/更新契约仍仅为 `z.string().nullable()`，不能在持久化边界阻止无效值。另有 `afterTime < activatedAt` 时在读取 trigger 前直接返回，以及已有 `nextTriggerAt` 命中时不重算的旁路。 | **部分关闭，仍有 P1。** cron/next-trigger 主路径已 fail-fast，但用户可达的 Today Schedule 固定时间路径仍有确定的静默错时；不能声称“所有 FixedTime 路径均校验、无 fallback”。 |
| fencing：事务执行中抢占与各业务写回滚 | runner 先在同一 Prisma transaction 内写 history、template `nextTriggerAt` 和 Notification outbox，再执行 `beforeCommitHook`，最后以 owner/fencing/status/lease 条件更新 occurrence（[prisma-reminder-write-transaction-runner.ts:40](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:40)、同文件 [94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、[115](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:115)、[146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、[164](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:164)）。用例 21 通过事务外 Prisma 连接在 hook 内把 DB owner/fencing 改成 worker-2，旧事务的最终条件写真实失败并抛 `LeaseFencingException`；随后分别断言 history=0、outbox=0、template 未推进，并确认 worker-2 状态保留（[reminder-reliable-operation.integration.test.ts:1289](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1289)）。实测该用例随 integration target 通过。 | **第六轮测试缺口已关闭。** 这是真实事务中段故障注入，且证明已执行的三个业务写随旧事务整体回滚；它不是在每条语句后分别注入，也不单独证明合法 re-claim 流程，但后者已有 claim/re-claim 用例覆盖。新的 lease-at-commit 缺口见下文。 |
| inventory / 完整 governance | inventory 已登记 reliable-messaging contract unit 与 Reminder integration 文件（[test-inventory.json:4310](../../tools/test-system-v2/test-inventory.json:4310)、[test-inventory.json:6749](../../tools/test-system-v2/test-inventory.json:6749)），清单为 957 files：unit 833、integration 11，其余分类无 missing/duplicate/unexpected（[test-inventory.json:9776](../../tools/test-system-v2/test-inventory.json:9776)）。串行实测 `pnpm nx run memoflow:governance-check --skip-nx-cache` 全部通过，inventory 输出 957，package-internal boundary 为 640 files / 0 violations。 | **已关闭。** |

### 回归、新问题与边界

- 前六轮已关闭项未见回退：production/development capability 仍 fail-fast，`schedule()` 的 reliable port、transaction runner、control service 仍为强制依赖；cron 主路径未恢复旧 `triggerRemindersBatch()` fallback。heartbeat/re-claim、retry due、dead-letter 排除与 replay 条件写、outbox 唯一幂等、durable skipped、多模板 occurrence namespace、跨 tick 单一 in-flight Promise、drain timeout reject、domain→transaction runner port 分层均保持现状。
- **P1：Today Schedule 仍是隐式 timezone fallback。** `getTodaySchedule()` 是真实 application query，FixedTime 设置不论合法、无效或所属地区都被忽略并按 UTC+8 计算。新增 invalid-timezone 单测只调用 `calculateNextTriggerTime()`，没有覆盖该路径；因此第六轮 timezone 阻断没有完整关闭。
- **P1：提交时 lease expiry 使用的是事务开始时间，不是提交时刻。** runner 在进入 transaction 前只取一次 `const now = new Date()`，初始校验与最终 `updateMany` 都复用它（[prisma-reminder-write-transaction-runner.ts:37](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:37)、同文件 [169](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:169)）。若事务在 hook/数据库写入期间跨过 `leaseExpiresAt`，但尚无新 owner 改写行，条件 `leaseExpiresAt >= now` 仍成立，旧 owner 可在实际租约已过期后提交。用例 21 通过直接改 owner/fencing 能验证抢占 fencing，却不会暴露“仅过期、尚未抢占”的 fail-open。lease 的提交时不变量仍未闭环。
- 合法但非常见 timezone 暂未发现误伤：当前 Node 26 / ICU 78 环境下，`Asia/Kathmandu`、`Australia/Eucla`、`Pacific/Chatham`、`America/Nuuk`、`Antarctica/Troll`、`Etc/GMT+12` 及常见历史 alias 均被 `Intl.DateTimeFormat` 接受；实现没有自建 whitelist。该结论只证明当前运行时代表样本，不替代生产 Node/ICU 一致性和 DST gap/fold 自动化测试。
- 用例 21 使用生产 Prisma runner 与真实数据库事务，抢占写不是 mock；但 `beforeCommitHook` 被加入 domain port 的生产参数（[reminder-transaction-runner.port.ts:18](../../packages/reminder/src/server/domain/ports/reminder-transaction-runner.port.ts:18)），属于测试 seam 泄漏到正式接口，记 **P2**。它不会自行形成 fallback/no-op，但扩大了业务调用方暂停事务的表面。
- 改动范围仍集中在 Reminder、W0 reliable-messaging contract、必要 Prisma schema/generated client、Account 关系回链和 test inventory；未发现 W2/W3 业务实施或其他越界改动。除 Today Schedule 的 timezone 忽略和上述过期提交外，未发现新增生产可达的 silent-success/no-op。既有 P2 仍包括 overlapping tick coalesce 可观测性、`leaseExpiresAt=null`/`nextRetryAt=null` fail-open、防御性 `failed` 分类缺失、cron infrastructure 绑定 Prisma 具体 runner及 generated Prisma whitespace 噪声；`git diff --check` 仍只因 generated `index.d.ts` trailing whitespace 失败。

### 本地验证

- Reminder integration：**2 files / 25 tests passed**。
- Reminder unit：**46 files / 363 tests passed**。
- Reminder typecheck（含依赖构建）：**passed**。
- 完整 `memoflow:governance-check`：**passed**；test inventory **957 files**，package-internal boundary **640 files / 0 violations**。
- `git diff --check`：**failed**，仅报告 generated Prisma `index.d.ts` trailing whitespace，维持 P2 审查噪声。

### 第七次终审分级与放行判定

- **P0：无新增。** production capability fail-fast 与 cron 主调度强制可靠依赖保持关闭。
- **P1：** Today Schedule 的 FixedTime 路径忽略 timezone 并硬编码 UTC+8，invalid timezone 在该用户可达路径不会 fail-fast；transaction runner 以事务开始时间判断最终 lease 有效性，允许未被抢占但实际已过期的旧 owner 提交。
- **P2：** `beforeCommitHook` 测试 seam 进入正式 domain port；并保留上轮列出的 coalesce 指标、防御性 null 条件、`failed` 分类、具体 runner 类型绑定和 generated whitespace 噪声。

**第七次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第九次迭代已真实关闭事务中段抢占/三类业务写回滚的测试门禁和 stale inventory，并让 next-trigger 的 invalid timezone 改为 fail-fast；但 timezone 修复没有覆盖 Today Schedule，且提交时 lease expiry 仍使用陈旧时间。两项 P1 修复并补相应负向测试前，W1 不满足“时区边界正确、lease conditional-write fail-closed”的完成定义。

## 第八次审查（W1 第十次迭代终审）

本次逐条复核第七轮剩余的 lease 提交时校验与 Today Schedule 时区问题，并回归前七轮已关闭项、检查本轮性能/一致性代价与时区误伤。只追加本审查文档，未修改业务代码，未提交或推送。

### 第七轮 2 项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| lease TOCTOU：覆盖提交路径与校验/写入窗口 | `succeeded` 与 `skipped` 在 history、template `nextTriggerAt`、可选 Notification outbox 写入后汇合到唯一的 occurrence 终态条件写；没有提前返回或替代 production runner。最终 `updateMany` 同时约束 `id + ownerToken + fencingToken + status=running + leaseExpiresAt >= commitNow`，不命中即抛 `LeaseFencingException`，使此前业务写整体回滚（[prisma-reminder-write-transaction-runner.ts:76](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:76)、同文件 [94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、[115](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:115)、[146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、[169](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:169)、[192](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:192)）。用例 24 在业务写后把 DB lease 改为过去并证明 history/outbox/template 全回滚（[reminder-reliable-operation.integration.test.ts:1495](../../packages/reminder/src/server/infrastructure/adapters/prisma/__tests__/reminder-reliable-operation.integration.test.ts:1495)）。但实施说明所称“69 行提交前重读”不准确：69 行仍校验事务开头读取的对象；真正修复是 169 行重新取应用进程 `commitNow` 并用于同一条件写。`commitNow` 生成到 SQL 实际执行之间仍有网络/锁等待窗口，测试也只覆盖显式改 DB expiry，未覆盖自然跨时或锁等待；终态 UPDATE 后到 COMMIT 的跨时因 occurrence 已被锁定并终态化，不会与 reclaim 双成功。 | **第七轮长事务复用入口时间的 P1 已关闭；保留 P2。** 所有成功/跳过业务提交路径已覆盖，原子条件写是正确线性化点；但应用时钟 capture→SQL 的窄窗口应改用数据库时钟谓词并补锁等待测试。 |
| Today Schedule 时区：计算传播、硬编码与生产可达性 | Today helper 已移除数值 UTC+8 硬编码；`todayStart/todayEnd`、FixedTime epoch 和过滤在显式传入 `options.timezone` 时会使用该时区，模板 FixedTime timezone 也会被校验并用于 epoch（[upcoming-reminder-calculation-service.ts:529](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:529)、同文件 [545](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:545)、[667](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:667)、[776](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:776)）。然而真实请求契约只有 `limit/includeExpired`，application query 也只转发 `includeExpired`，生产路径根本没有传入 viewer timezone，日期窗口仍取宿主机本地时区（[reminder-template.dto.ts:141](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:141)、[reminder-schedule-query-application-service.ts:53](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:53)）。此外 `nextTriggerDisplay` 仍由宿主机 `getFullYear/getHours` 格式化，没有使用 options 或模板时区（[upcoming-reminder-calculation-service.ts:480](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:480)、同文件 [511](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:511)），React 客户端直接展示该字段（[RemindersScreen.tsx:74](../../packages/app-react/src/screens/RemindersScreen.tsx:74)）。Tokyo 新用例只直接调用纯函数，同时把模板与 options 都设为 Tokyo，所选 `now` 又没有跨 UTC/Tokyo 日期边界；它能证明 `12:00 Tokyo = 03:00Z` 的单点 epoch，却不能证明生产参数传播、日期边界或显示正确（[upcoming-reminder-calculation.service.spec.ts:215](../../packages/reminder/src/server/domain/services/__tests__/upcoming-reminder-calculation.service.spec.ts:215)）。 | **仍为 P1，未闭环。** UTC+8 硬编码本身已删除，但真实 Today API 仍由宿主时区定义“今天”，同一账号在不同部署环境可得到不同结果，用户可见显示也会错时；属于 W1“时区边界正确”的完成定义。 |

### 前七轮关闭项回归

| 回归项 | 核验结果 |
| --- | --- |
| production capability / 旧 fallback | production/development capability 缺失仍 fail-fast；scheduler 的 reliable port、transaction runner、control service 仍为强制依赖，未恢复 `triggerRemindersBatch()` 旧路径。**保持关闭。** |
| occurrence 状态机、指标与幂等 | retry/dead-letter cron 过滤、attempt/error/backoff/dead-letter 落库、claimed/due/failed/retry/dead-letter 指标、Notification outbox 唯一幂等和多模板 occurrence namespace 均保持。**保持关闭。** |
| skipped、re-claim、replay 与 heartbeat | paused/group-disabled durable skipped、re-claim 条件竞争、identity-scoped replay 条件写、reclaimed owner heartbeat 和 stale owner/failure fencing 未见回退。**保持关闭。** |
| 单事务 fencing 与故障回滚 | history、template、outbox、occurrence 继续位于同一 Prisma transaction；中途抢占和本轮显式 lease expiry 用例均验证条件失败时整体回滚。**保持关闭。** |
| cron 生命周期与分层 | 跨 tick 仍复用唯一 in-flight Promise，stop/dispose await drain 且 timeout reject；domain scheduler 仍只依赖 transaction runner port，boundary audit 为 640 files / 0 violations。**保持关闭。** |
| inventory / governance | inventory 仍为 957 files（unit 833、integration 11，其余分类无异常）；完整 governance 实测通过。**保持关闭。** |

未发现 W2/W3 实施或新的生产 no-op/silent-success。变更仍限于 Reminder、W0 reliable-messaging contract、必要 Prisma schema/generated client、Account 关系回链和 test inventory。

### 新问题与代价

- **P1 测试门禁回归：Reminder unit 当前不是 46 files / 367 passed。** 本地 Nx target 实测为 **46 files：45 passed / 1 failed，363 passed / 2 failed（共 365 tests）**。两处失败都在 Today Schedule spec：本轮给原有 interval 断言错误增加了 `20:20Z`，但仓库测试环境 `TZ=Asia/Shanghai` 的今日结束是 `15:59:59.999Z`，实现不会返回该点（[upcoming-reminder-calculation.service.spec.ts:96](../../packages/reminder/src/server/domain/services/__tests__/upcoming-reminder-calculation.service.spec.ts:96)、同文件 [146](../../packages/reminder/src/server/domain/services/__tests__/upcoming-reminder-calculation.service.spec.ts:146)、[.env.test:7](../../.env.test:7)）。这既否定本轮“单测全绿”的验收事实，也暴露默认宿主时区与新增期望不一致。
- lease 修复没有增加成功路径数据库往返，只新增一次 `new Date()`；条件失败后的诊断重读才有额外查询，性能代价可忽略。新增一致性瑕疵是 DB occurrence 的 `finishedAt` 使用 `commitNow`，返回 receipt 却仍使用事务入口 `now`，长事务下二者不同（[prisma-reminder-write-transaction-runner.ts:187](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:187)、同文件 [262](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:262)），记 **P2**。
- failure receipt 是另一条 occurrence 状态写：它只按 idempotency/claim/fencing 条件更新，没有约束 owner/status/lease 未过期；已被新 owner reclaim 时 fencing 会阻止覆盖，且它不提交 history/template/outbox，但旧 worker 可在仅过期、尚未 reclaim 时把 running 写为 retryable/dead-letter，记 **P2 fail-closed 加固**（[reminder-scheduler-service.ts:267](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:267)、[reminder-reliable-operation-prisma.adapter.ts:334](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:334)）。
- 时区误伤方面，`nextTriggerDisplay` 的宿主格式化是新增确认的用户可见 P1；DST gap/fold、跨午夜、负偏移和混合模板时区仍无测试，`getZonedEpochMs()` 的三轮猜测没有明确 gap/fold 策略，记 **P2**。Reminder schedule projection 对缺失或枚举外 timezone 仍静默回退 `Asia/Shanghai`，也是残留的 UTC+8 默认与 **P2**（[schedule-projection-source.ts:86](../../packages/reminder/src/server/infrastructure/schedule-projection-source.ts:86)）。
- 既有 P2 保持：`leaseExpiresAt=null`/`nextRetryAt=null` fail-open、overlap coalesce 缺专用指标、不可重试 `failed` 分类缺失、cron infrastructure 绑定 Prisma runner、`beforeCommitHook` 测试 seam 进入正式 port，以及 generated Prisma whitespace 噪声。`git diff --check` 仍只因 generated `index.d.ts` trailing whitespace 失败。

### 本地验证

- Reminder integration：**2 files / 26 tests passed**。
- Reminder unit：**46 files，363 passed / 2 failed（共 365 tests）**；失败均为本轮 Today Schedule interval 期望回归。
- Reminder typecheck（含依赖构建）：**passed**。
- 完整 `memoflow:governance-check`：**passed**；test inventory **957 files**，package-internal boundary **640 files / 0 violations**。
- `git diff --check`：**failed**，仅报告 generated Prisma `index.d.ts` trailing whitespace。
- 当前 shell 无 `pnpm` 命令，以上 target 使用仓库本地 `node node_modules/nx/dist/bin/nx.js run ...` 等价入口执行；integration 与 unit 均禁用结果臆测，以本轮实际输出为准。

### 第八次终审分级与放行判定

- **P0：无新增。** production capability fail-fast、旧调度 fallback 删除与可靠写入主线保持关闭。
- **P1：** Today Schedule 的真实 API/application 链路没有 viewer timezone，日期窗口继续依赖宿主机；`nextTriggerDisplay` 同样忽略时区并在部分客户端直接展示；Tokyo 测试没有覆盖这两条生产问题。Reminder unit 还有 2 个本轮引入的失败，测试门禁不成立。以上均属于 W1“时区边界正确”和 work package 验证边界。
- **P2：** lease 的入口时间长窗口已关闭，但保留应用 `commitNow` capture→SQL 窄窗口、自然跨时测试缺口、null lease fail-open、failure receipt 未校验 lease、返回/DB `finishedAt` 不一致；另保留 DST/跨日覆盖、projection Shanghai fallback、coalesce 指标、`failed` 分类、具体 runner 类型绑定、测试 seam 与 generated whitespace 等既有缺口。

**第八次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第十次迭代已把成功/跳过事务的 lease 校验移动到最终条件写线性化点，并移除 Today helper 的固定 UTC+8 算法；但生产 Today query 没有传递账号/查看者时区、返回显示仍按宿主机格式化，且 Reminder unit 实测红灯。上述 P1 均在 W1 完成定义边界内；修复真实契约与调用链、补跨日期边界/显示测试并恢复完整单测全绿后，方可重新评估提交 W1 与进入 W2/W3。

## 第九次审查（W1 第十一次迭代终审）

本次逐条复核第八轮剩余的 Today Schedule 时区生产链路与 `nextTriggerDisplay`，并回归前八轮已关闭项、审查缺省时区策略。只追加本审查文档，未修改业务代码，未提交或推送。

### 第八轮 2 项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| Today Schedule API/use-case→domain 时区穿透 | HTTP route 已读取 `timezone`，controller 也从原始 query 手工提取并传给 application service；application service 最终把它传入 `calculateTodaySchedule()`（[reminder-template.routes.ts:133](../../packages/reminder/src/api/routes/reminder-template.routes.ts:133)、[reminder.controller.ts:66](../../packages/reminder/src/server/transport/reminder.controller.ts:66)、[reminder-schedule-query-application-service.ts:54](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:54)）。HTTP/IPC client port 也新增了可选字段，因此“调用者显式传入字符串”时，运行时内部链路确实可达 domain。可是 canonical `GetReminderTodayScheduleSchema` 仍只有 `limit/includeExpired`，`GetUpcomingRemindersSchema` 也没有 `timezone`，OpenAPI/query contract 与其 `z.infer` 请求类型均未包含该字段（[reminder-template.dto.ts:103](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:103)、同文件 [141](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:141)）；`ReminderApplicationPort` 继续使用这两个原始 request type（[reminder.application.port.ts:30](../../packages/reminder/src/server/application/reminder.application.port.ts:30)）。本地 typecheck 因 controller 向 port 传入未知 `timezone` 在 63、79 行报两处 TS2353。实际 React Today 请求未传时区，Vue composable 甚至尚未暴露该参数（[useReminders.ts:28](../../packages/app-react/src/hooks/useReminders.ts:28)、[useReminderTemplates.ts:34](../../packages/app-vue/src/modules/reminder/composables/useReminderTemplates.ts:34)），server 也没有按 `ctx.identityId` 读取账号时区。 | **部分修复，仍为 P1。** 代码存在一条绕过 canonical schema/type 的运行时透传 seam，但“用户时区从真实客户端或账号设置进入、经正式 API contract 到 domain”的生产闭环没有建立，且 typecheck 已直接否定验收。 |
| `nextTriggerDisplay` 按模板/用户时区且不依赖服务器 TZ | DTO 转换对 FixedTime 优先选模板 timezone，否则选显式/effective timezone，最后才使用 UTC；`formatDateTime()` 对 UTC 使用 UTC getter，对 IANA timezone 使用带 `timeZone` 的 `Intl.DateTimeFormat`，不再调用宿主机 local getter（[upcoming-reminder-calculation-service.ts:456](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:456)、同文件 [493](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:493)）。Today FixedTime 生成把所选模板时区传给 DTO，Interval 则传入 viewer/effective timezone（同文件 [676](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:676)、[731](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:731)）。Reminder unit 在 `TZ=UTC` 与 `TZ=Asia/Tokyo` 下均实测 46 files / 365 tests 全绿。现有 spec 只断言 timestamp，没有直接断言 `nextTriggerDisplay` 的 Tokyo/UTC 文本（[upcoming-reminder-calculation.service.spec.ts:213](../../packages/reminder/src/server/domain/services/__tests__/upcoming-reminder-calculation.service.spec.ts:213)）。 | **实现缺口已关闭；保留 P2 测试证据缺口。** 显示算法已有充分静态证据证明不取服务器 TZ，但应增加直接文本断言，防止以后只保持 epoch 正确而显示回退。 |

### 前八轮关闭项回归

| 回归项 | 核验结果 |
| --- | --- |
| production capability / 旧 fallback | production/development 仍经 capability contract fail-fast；缺少 cron contribution 时 `start()` 明确抛错，scheduler reliable port/transaction runner/control service 仍为强制依赖，未恢复旧 `triggerRemindersBatch()` 路径（[reminder.runtime.ts:26](../../packages/reminder/src/server/infrastructure/runtime/reminder.runtime.ts:26)、[reminder-trigger-cron-job.ts:73](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:73)）。**保持关闭。** |
| lease TOCTOU 与单事务 fencing | history、template、outbox 后仍以 `ownerToken + fencingToken + running + leaseExpiresAt >= commitNow` 的唯一终态条件写作为线性化点，不命中抛 `LeaseFencingException` 并回滚整个事务（[prisma-reminder-write-transaction-runner.ts:94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、同文件 [115](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:115)、[146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、[169](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:169)）。用例 21/24 随 26 个 integration 用例通过。**保持关闭。** |
| occurrence / intent 幂等命名空间 | occurrence 继续以 `templateId + occurrenceKey` 唯一，occurrence idempotency key 也唯一；scheduler 的 occurrence key 包含模板 ID，Notification intent 使用 `identityId + source=notification + occurrenceKey`，outbox `idempotencyKey` 仍有数据库唯一约束（[reminder.prisma:185](../../packages/database/prisma/schema/reminder.prisma:185)、[reminder-scheduler-service.ts:146](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:146)、[prisma-reminder-write-transaction-runner.ts:123](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:123)、[reliable-messaging.prisma:17](../../packages/database/prisma/schema/reliable-messaging.prisma:17)）。**保持关闭。** |
| retry/re-claim/replay/heartbeat/skipped | retryable due 的原子 claim 条件、dead-letter 排除、identity-scoped replay 条件写、heartbeat 的 owner/fencing/status/expiry 条件，以及 paused/group-disabled durable skipped 主线均未见回退（[reminder-reliable-operation-prisma.adapter.ts:163](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:163)、同文件 [253](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:253)、[395](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:395)）。**保持关闭。** |
| cron overlap 与 graceful drain | 跨 tick 仍返回同一个 `currentExecutionPromise`；`stop()` 先阻止新 scan，再 await in-flight，timeout 会记录 error 并 reject，module dispose 继续 await stop（[reminder-trigger-cron-job.ts:129](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:129)、同文件 [160](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:160)、[module.ts:69](../../packages/reminder/src/api/module.ts:69)）。**保持关闭。** |

未发现 W2/W3 业务实施，也未发现上述历史 P0/P1 主线回退。第八轮已有的 application-clock→SQL 窄窗口、null lease/failure receipt 加固等 P2 仍在。

### 缺省时区策略与新静默路径

- **P1：缺失 viewer timezone 不会 fail-fast，也没有读取账号 timezone。** `calculateTodaySchedule()` 在请求不带 timezone 时选择“仓储返回列表中第一个带 timezone 的 FixedTime 模板”，再退回 UTC（[upcoming-reminder-calculation-service.ts:548](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:548)）。这虽然消除了服务器 TZ，却使 Today 的日期窗口依赖模板查询顺序；混合 Tokyo/New York 模板时，整张列表的“今天”由碰巧排在前面的模板定义。没有 FixedTime timezone 时又静默使用 UTC，而账号默认设置实际是 `Asia/Shanghai`（[account-settings.ts:19](../../packages/account/src/server/domain/value-objects/account-settings.ts:19)）。这是新的确定性 fallback，但不是用户语义，也不是 fail-fast。
- **P1：null 模板 timezone 在读模型与实际调度之间语义不一致。** Today FixedTime 使用 `fixedTime.timezone ?? effectiveTimezone ?? 'UTC'`，因此显式 viewer timezone 会解释无模板时区的墙上时间（[upcoming-reminder-calculation-service.ts:685](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:685)）；实际 one-time/recurring next-trigger 计算却对同一 null 模板直接默认 UTC（同文件 [295](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:295)、[362](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:362)）。例如 null timezone 的 `09:00` 可在 Today 中显示为用户本地 09:00，但 scheduler occurrence 仍按 09:00Z 生成，属于用户可见静默错配。
- **P2：空字符串绕过统一校验。** route 的共享 `parseString()` 保留空串，controller 也把空串视为 string；`validateTimezone()` 对 falsy 值直接返回，而后续代码有的通过 `timezone || 'UTC'` 静默取 UTC，有的把空串交给 `Intl` 抛原生 `RangeError`（[parse-query-value.ts:12](../../packages/utils/src/shared/parse-query-value.ts:12)、[reminder.controller.ts:78](../../packages/reminder/src/server/transport/reminder.controller.ts:78)、[upcoming-reminder-calculation-service.ts:226](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:226)）。补齐 canonical schema 后应统一 trim/非空/IANA 校验及错误 envelope，而不是保留分支相关行为。

有效但不存在的 IANA timezone 会在 domain 的 `Intl.DateTimeFormat` 校验处抛错，因此不再静默落到服务器本地时间；问题集中在“未提供/空字符串”的策略与正式 API contract 缺失。

### 本地验证

- `TZ=UTC` Reminder unit：**46 files / 365 tests passed**。
- `TZ=Asia/Tokyo` Reminder unit：**46 files / 365 tests passed**。
- Reminder integration：**2 files / 26 tests passed**。
- Reminder typecheck（含依赖构建）：**failed**；`reminder.controller.ts:63` 与 `:79` 各有一处 TS2353，均为 `timezone` 不存在于 canonical request/port type。
- 完整 `memoflow:governance-check`：**passed**；test inventory **957 files**，package-internal boundary **640 files / 0 violations**。
- `git diff --check`：**failed**，仍只报告 generated Prisma `index.d.ts` 的既有 trailing whitespace。

### 第九次终审分级与放行判定

- **P0：无新增。** production capability fail-fast、可靠调度与事务 intent 主线保持关闭。
- **P1：** 正式 query schema/application port 未接纳 timezone，导致 typecheck 红灯；真实 React/Vue 请求及 server account lookup 均未把用户 timezone 送入链路；缺省时区按首个模板/UTC 静默决定 Today 窗口；null 模板 timezone 的 Today 解释与 scheduler UTC 解释不一致。这些都直接落在 W1“时区边界正确”和最小验证门禁内。
- **P2：** 缺少 `nextTriggerDisplay` 文本断言；空 timezone 行为不统一；并保留第八轮记录的 DB clock 窄窗口、null lease/failure receipt、DST gap/fold/跨日、projection Shanghai fallback、coalesce 指标、`failed` 分类、具体 runner 类型绑定、测试 seam 与 generated whitespace 等加固项。

**第九次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第十一次迭代已经关闭 `nextTriggerDisplay` 对服务器 TZ 的实现依赖，并恢复双 TZ 365/365 与 integration 26/26；但用户时区只形成了绕过正式契约的内部参数 seam，typecheck 实际失败，真实客户端/账号设置没有供给该参数，缺省策略还会产生跨模板和调度/读模型错配。应先把 timezone 纳入 canonical request schema 与 application port，明确由账号设置还是客户端提供 viewer timezone，统一 null/空值/default 语义并补 controller→application→domain、显示文本及混合时区测试，再重新终审。

## 第十次审查（W1 第十二次迭代终审）

本次逐条复核第九轮的 TS2353 与 null 模板时区语义两项修复，回归前九轮已关闭主线，并专门检查新增 optional query 字段的缺省行为。只追加本审查文档，未修改业务代码，未提交或推送。

### 第九轮 2 项逐条核验

| 项目 | 当前实现与测试证据 | 结论 |
| --- | --- | --- |
| TS2353：canonical schema 与 controller/application port 类型穿透 | `GetUpcomingRemindersSchema` 与 `GetReminderTodayScheduleSchema` 均已声明 `timezone: z.string().optional()`，相应 `z.infer` request type 因此正式包含该字段（[reminder-template.dto.ts:105](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:105)、同文件 [142](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:142)）。controller 不再从原始 query 手工拼入超出 request type 的属性，而是将各自 schema 的 `parsed.data` 直接传给 `ReminderApplicationPort`；application service 再把 `params.timezone` 传到 domain calculation（[reminder.controller.ts:50](../../packages/reminder/src/server/transport/reminder.controller.ts:50)、同文件 [65](../../packages/reminder/src/server/transport/reminder.controller.ts:65)、[reminder.application.port.ts:30](../../packages/reminder/src/server/application/reminder.application.port.ts:30)、[reminder-schedule-query-application-service.ts:38](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:38)、同文件 [61](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:61)）。本轮实跑 `reminder:typecheck`（含 10 个依赖 target）通过，两处 TS2353 消失。 | **已关闭。** canonical contract、controller 与 application port 的静态类型链已对齐。 |
| null 模板 timezone：Today Schedule 与 scheduler 一致 | scheduler 使用的 one-time/recurring FixedTime 计算均以 `fixedTime.timezone ?? 'UTC'` 解释 null（[upcoming-reminder-calculation-service.ts:285](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:285)、同文件 [354](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:354)）；Today FixedTime 生成也改为相同的 `fixedTime.timezone ?? 'UTC'`，不再用 viewer/effective timezone 改写 null 模板的墙上时间（同文件 [676](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:676)）。新增 dual-path 用例在混合 UTC-null/Tokyo 模板并显式传 Tokyo viewer timezone 时，直接断言两条路径都得到 `2026-08-10T09:00:00.000Z`（[upcoming-reminder-calculation.service.spec.ts:273](../../packages/reminder/src/server/domain/services/__tests__/upcoming-reminder-calculation.service.spec.ts:273)）。canonical `FixedTimeTriggerSchema` 注释也明确 null 表示 UTC 且不得依赖宿主/隐式 fallback（[trigger-config.ts:11](../../packages/contracts/src/modules/reminder/value-objects/trigger-config.ts:11)）。 | **已关闭。** 实现、测试与模板契约注释一致；但 request timezone 的缺省语义是另一项未关闭问题，见下文。 |

### 前九轮关闭项回归

| 回归项 | 核验结果 |
| --- | --- |
| production capability / 旧 fallback | production/development capability 缺失仍 fail-fast；scheduler 的 reliable port、transaction runner 与 control service 仍为强制依赖，未恢复进程内 `triggerRemindersBatch()` 旧主线。**保持关闭。** |
| lease TOCTOU 与 fencing 提交校验 | history、template、Notification outbox 写入后仍汇合到唯一 occurrence 终态 `updateMany`，条件继续约束 owner、fencing、`running` 和 `leaseExpiresAt >= commitNow`；不命中抛 fencing 异常并回滚整个事务（[prisma-reminder-write-transaction-runner.ts:94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、同文件 [115](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:115)、[146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、[169](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:169)）。integration 26/26 含既有 lease/fencing/回滚用例通过。**保持关闭。** |
| occurrence / intent 幂等命名空间 | occurrence key 继续包含模板 ID，数据库仍有 `templateId + occurrenceKey` 与 canonical occurrence idempotency 唯一约束；Notification intent 继续使用 `identityId + source=notification + occurrenceKey`，outbox `idempotencyKey` 保持唯一（[reminder-scheduler-service.ts:144](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:144)、[reminder.prisma:182](../../packages/database/prisma/schema/reminder.prisma:182)、[prisma-reminder-write-transaction-runner.ts:123](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:123)、[reliable-messaging.prisma:9](../../packages/database/prisma/schema/reliable-messaging.prisma:9)）。**保持关闭。** |
| retry/re-claim/replay/heartbeat/skipped | retryable due 原子条件、dead-letter 排除、identity-scoped replay、heartbeat 的 owner/fencing/status/expiry 条件及 paused/group-disabled durable skipped 主线未见回退（[reminder-reliable-operation-prisma.adapter.ts:163](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:163)、同文件 [253](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:253)、[395](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:395)）。**保持关闭。** |
| cron overlap / drain / dispose | 跨 tick 仍复用唯一 `currentExecutionPromise`；`stop()` 先阻止新 scan，再等待 in-flight，timeout 记录错误并 reject，module dispose 仍 await stop（[reminder-trigger-cron-job.ts:129](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:129)、同文件 [160](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:160)、[module.ts:69](../../packages/reminder/src/api/module.ts:69)）。**保持关闭。** |
| 双 TZ 独立性与显示 | 本轮在 `TZ=UTC`、`TZ=Asia/Tokyo` 下分别实跑 46 files / 366 tests，结果完全一致；FixedTime display 继续采用模板时区/null→UTC，Interval display 使用显式 effective timezone，不读取宿主 local getter（[upcoming-reminder-calculation-service.ts:456](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:456)、同文件 [493](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:493)）。**保持关闭。** |

未发现 W2/W3 业务实施，也未发现历史 P0/P1 可靠调度、事务 intent 或生命周期主线回退。

### timezone optional 缺省处理与新静默路径

**P1：正式生产调用仍不给 viewer timezone，optional 字段使第九轮的缺省静默路径保持可达。** React Today 调用只传 `limit/includeExpired`，Vue composable 的公开参数甚至仍不包含 `timezone`；两个 Vue widget 调用同样不传该字段（[useReminders.ts:28](../../packages/app-react/src/hooks/useReminders.ts:28)、[useReminderTemplates.ts:34](../../packages/app-vue/src/modules/reminder/composables/useReminderTemplates.ts:34)、[UpcomingRemindersWidget.vue:147](../../packages/app-vue/src/modules/reminder/components/widgets/UpcomingRemindersWidget.vue:147)、[ReminderCapsulePreview.vue:47](../../packages/app-vue/src/layouts/shell/previews/ReminderCapsulePreview.vue:47)）。服务端 application service 只转发 request timezone，没有按 `ctx.identityId` 读取账号 timezone（[reminder-schedule-query-application-service.ts:54](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:54)）。当字段缺失时，`calculateTodaySchedule()` 仍从仓储结果中选择第一个带 timezone 的 FixedTime 模板，再退回 UTC，因此同一组混合时区模板的 Today 日期窗口取决于查询顺序（[upcoming-reminder-calculation-service.ts:539](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:539)）。这不是本轮新增代码造成的新分支，但新增 optional contract 没有关闭或显式定义该既有静默行为；真实客户端默认进入此路径，仍违反 W1“时区边界正确”。

query schema 对 `timezone` 仅使用 `z.string().optional()`，没有注释缺省来源/语义，也没有 trim、非空或 IANA schema 约束（[reminder-template.dto.ts:105](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:105)、同文件 [142](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:142)）。空字符串仍会通过 schema，并因 domain 的 falsy 判断走相同 fallback；非空非法值则在 domain `Intl` 校验处 fail-fast。空串及错误 envelope 不统一维持 **P2**，但“字段缺失时由首模板决定用户今日窗口”是确定的生产 P1。

既有 P2 保持：DB application-clock→SQL 的窄窗口、`leaseExpiresAt=null`/`nextRetryAt=null` fail-open、failure receipt lease 加固、DST gap/fold/负偏移与跨日覆盖、projection Shanghai fallback、overlap coalesce 指标、不可重试 `failed` 分类、cron infrastructure 绑定具体 Prisma runner、正式 port 中 `beforeCommitHook` 测试 seam，以及 generated Prisma whitespace。`git diff --check` 仍只因 generated `index.d.ts` trailing whitespace 失败。

### 本地验证

- Reminder typecheck（含 10 个依赖 target）：**passed**。
- `TZ=UTC` Reminder unit：**46 files / 366 tests passed**。
- `TZ=Asia/Tokyo` Reminder unit：**46 files / 366 tests passed**。
- Reminder integration：**2 files / 26 tests passed**。
- 完整 `memoflow:governance-check`：**passed**；test inventory **957 files**，package-internal boundary **640 files / 0 violations**。Nx 另报告 `test-system-v2:test:governance` 的 flaky warning，但整个 target 成功。
- `git diff --check`：**failed**，仅报告 generated Prisma `index.d.ts` 的既有 trailing whitespace。

### 第十次终审分级与放行判定

- **P0：无新增。** production capability fail-fast、可靠调度、单事务 intent 与 fencing 主线保持关闭。
- **P1：** canonical schema 的 timezone 仍是没有缺省语义的 optional 字段；真实 React/Vue 不提供它，server 也不读取账号时区，导致 Today 日期窗口继续按仓储顺序中的首个带时区模板/UTC 静默选择。TS2353 与 null 模板双路径错配虽已关闭，但用户时区的生产来源与缺省行为仍未闭环。
- **P2：** query timezone 缺少非空/IANA schema 约束及统一错误 envelope；并保留 DB clock 窄窗口、null lease/retry、failure receipt、DST/跨日、projection fallback、coalesce 指标、`failed` 分类、具体 runner 绑定、测试 seam 与 generated whitespace 等既有加固项。

**第十次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第十二次迭代真实关闭了 TS2353 和 null 模板 timezone 的 scheduler/Today 语义错配，并恢复 typecheck、双 TZ 366/366 与 integration 26/26；但新增 optional contract 只让调用者“可以”传 timezone，没有为真实调用提供用户时区，也没有定义缺省策略。应先选定并实现唯一生产语义（例如由认证账号设置提供 authoritative timezone，或要求客户端必传并 fail-fast），移除首模板/查询顺序 fallback，并补 controller→application→domain 的缺省/空值、混合时区跨日期边界测试，之后再重新评估提交 W1 与进入 W2/W3。

## 第十一次审查（W1 第十三次迭代终审）

本次逐条复核第十轮唯一阻断项——端到端用户时区来源与缺省链，并回归前十轮已关闭主线；另专项审查新增 `AccountTimezonePort` 的依赖方向、生产组合根接线及读取失败语义。只追加本审查文档，未修改业务代码，未提交或推送。

### 第十轮 1 项逐条核验

| 核验项 | 当前实现与证据 | 结论 |
| --- | --- | --- |
| 客户端请求携带用户时区 | app-vue 的 `getTodaySchedule()` 在调用 service 前按“显式参数 → account store → setting store → 浏览器 `Intl` 时区”补齐 query timezone，两个实际 widget 均经该 composable 调用；`TemplateDialog` 创建 FixedTime/activeHours 时也不再硬编码 `timezone: null`（[useReminderTemplates.ts:35](../../packages/app-vue/src/modules/reminder/composables/useReminderTemplates.ts:35)、[user-timezone.ts:8](../../packages/app-vue/src/modules/reminder/utils/user-timezone.ts:8)、[TemplateDialog.vue:507](../../packages/app-vue/src/modules/reminder/components/TemplateDialog.vue:507)）。新增 app-vue 定向用例 3/3 通过。 | **app-vue 客户端部分已关闭。** Today 请求会显式携带客户端解析出的时区，模板创建也保存显式时区；这不是服务器宿主时区。测试只断言 `expect.any(String)`，没有用 Pinia fixture 证明 account/setting 优先级或 HTTP query 序列化，保留 P2 证据缺口。React 生产调用仍不传 timezone（[useReminders.ts:28](../../packages/app-react/src/hooks/useReminders.ts:28)），因此仍依赖服务端账号兜底。 |
| 服务端读取账号时区 | application service 已定义并调用 reminder-owned `AccountTimezonePort`，单测覆盖 request 优先、account 次之、UTC 最后（[reminder-schedule-query-application-service.ts:14](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:14)、同文件 [31](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:31)）。但生产 `ReminderApiModule` 只把 runtime contribution 传给 `createReminderPrismaModule()`；该 factory 的 options 也只有 runtime contribution，最终没有构造或注入 `AccountApplicationTimezoneAdapter`（[module.ts:44](../../packages/reminder/src/api/module.ts:44)、[prisma.ts:28](../../packages/reminder/src/server/infrastructure/prisma.ts:28)）。全仓对 adapter 类的引用仅有其自身与单测。 | **P1 未关闭。** “服务端读取账号时区”只在手工注入 fake port 的 application unit 中成立，生产 API 组合根不可达。请求缺失时 `accountTimezonePort` 为 `undefined`，直接返回 UTC；React 等不传时区的真实调用因此仍静默错用 UTC。 |
| 兜底链请求 → 账号 → 显式默认及文档化 | 两个 canonical query schema 的注释和 application service 注释均明确“请求 → 账号 → UTC、无服务器时区”；domain 的 Today window、display 与 null FixedTime 也均显式使用参数或 UTC，不读取 Node 进程本地时区（[reminder-template.dto.ts:105](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:105)、同文件 [145](../../packages/contracts/src/modules/reminder/api/reminder-template.dto.ts:145)、[upcoming-reminder-calculation-service.ts:491](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:491)、同文件 [548](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:548)、同文件 [681](../../packages/reminder/src/server/domain/services/upcoming-reminder-calculation-service.ts:681)）。 | **文档与纯实现分支已具备，但生产链未实现。** 当前生产事实是“请求 → UTC”，不是所文档化的三段链。未发现静默读取服务器 `TZ` 的回退；阻断点是账号段不可达及失败被静默等同于缺省。 |

### 前十轮关闭项回归

| 回归项 | 核验结果 |
| --- | --- |
| production capability / 旧 fallback | API 仍注入真实 cron、reliable port 与 transaction runner；scheduler 强制可靠依赖且未恢复 `triggerRemindersBatch()` 旧路径。**保持关闭。** |
| lease TOCTOU / fencing 提交校验 | history、template、Notification outbox 后仍以 owner、fencing、`running`、lease 未过期的终态条件写作为线性化点，不命中抛 fencing 异常并回滚事务；integration 26/26 覆盖既有抢占/回滚用例（[prisma-reminder-write-transaction-runner.ts:94](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:94)、同文件 [146](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:146)、同文件 [168](../../packages/reminder/src/server/infrastructure/adapters/prisma/prisma-reminder-write-transaction-runner.ts:168)）。**保持关闭。** |
| occurrence / intent 幂等命名空间 | scheduler occurrence key 继续包含 template ID，DB 保持 `templateId + occurrenceKey` 和 canonical occurrence key 唯一；Notification intent 继续使用 `identityId + source=notification + occurrenceKey` 且 outbox key 唯一（[reminder-scheduler-service.ts:145](../../packages/reminder/src/server/domain/services/reminder-scheduler-service.ts:145)、[reminder.prisma:181](../../packages/database/prisma/schema/reminder.prisma:181)、[reliable-messaging.prisma:9](../../packages/database/prisma/schema/reliable-messaging.prisma:9)）。**保持关闭。** |
| retry / re-claim / replay / heartbeat / skipped | retryable due 原子条件、dead-letter 排除、identity-scoped replay 条件写、heartbeat owner/fencing/status/expiry 条件和 paused/group-disabled durable skipped 主线未见回退（[reminder-reliable-operation-prisma.adapter.ts:163](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:163)、同文件 [253](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:253)、同文件 [395](../../packages/reminder/src/server/infrastructure/adapters/prisma/reminder-reliable-operation-prisma.adapter.ts:395)）。**保持关闭。** |
| cron overlap / drain / dispose | 跨 tick 继续复用唯一 in-flight Promise；`stop()` 先禁止新 scan，再 await in-flight，timeout 记录并 reject；module dispose 仍 await stop（[reminder-trigger-cron-job.ts:129](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:129)、同文件 [160](../../packages/reminder/src/server/infrastructure/cron/reminder-trigger-cron-job.ts:160)、[module.ts:75](../../packages/reminder/src/api/module.ts:75)）。**保持关闭。** |
| 双 TZ 独立性 / null 时区语义 | 本轮在 `TZ=UTC` 与 `TZ=Asia/Tokyo` 下分别实跑 47 files / 372 tests，结果完全一致；null FixedTime 在 scheduler、Today 与契约中继续显式表示 UTC。**保持关闭。** |

### Account timezone port 依赖方向与失败行为

- **依赖方向本身合理，无循环依赖。** `AccountTimezonePort` 由 Reminder domain 拥有，application 依赖抽象，adapter 位于 Reminder infrastructure；adapter 只消费一个结构化 account application surface，没有从 Reminder domain/application 反向 import Account infrastructure/ORM。当前 governance 的 package-internal boundary 为 641 files / 0 violations，未形成 `reminder ↔ account` 项目循环。
- **P1：账号读取失败被两层静默吞掉并伪装成“账号未配置”。** adapter 捕获 `getProfile()` 异常后返回 `null`，application 又捕获 port 异常后返回 UTC，均无日志、metric、结构化错误或 retryability；adapter 注释甚至写“异常时不吞吐错误”，行为与注释相反（[account-timezone-adapter.ts:17](../../packages/reminder/src/server/infrastructure/adapters/account-timezone-adapter.ts:17)、[reminder-schedule-query-application-service.ts:38](../../packages/reminder/src/server/application/services/reminder-schedule-query-application-service.ts:38)）。账号不存在/未配置可以按契约进入显式 UTC，但依赖超时、数据库错误或 account API 失败不应被无观测地视为同一状态，否则 Today window 可成功返回错误日期。至少应在 port result 中区分 absent 与 unavailable，并选定 fail-fast 或有日志/指标的明确降级策略。
- **P2：当前 port 仍是可选 capability，且出现未使用的 domain scheduler 参数。** query service、module dependencies 都把账号 port 声明为 optional；`ReminderSchedulerService` 也新增同名可选构造参数却从未读取。真正接线前应收敛到唯一 composition seam，避免“可选即默认 UTC”的第二条生产路径与无效依赖。

### 本地验证

- Reminder unit：`TZ=UTC` 与 `TZ=Asia/Tokyo` 各 **47 files / 372 tests passed**。
- Reminder integration：**2 files / 26 tests passed**。
- Reminder typecheck：**passed**；app-vue typecheck：**passed**。
- app-vue 新增 timezone 定向用例：**2 files / 3 tests passed**。
- test inventory：**960 files**（unit 836、integration 11，其余分类无异常）；完整 `memoflow:governance-check`：**passed**，package-internal boundary **641 files / 0 violations**。
- 当前 shell 无全局 `pnpm`，以上 Nx target 使用仓库本地 `node node_modules/nx/dist/bin/nx.js` 等价入口。一次并行运行两个 typecheck 时，共享 `contracts:build` 清理 `dist` 发生 ENOENT 竞态；改为顺序重跑后两个 target 均通过，不判为业务代码失败。
- `git diff --check` 仍只报告 generated Prisma `index.d.ts` 的既有 trailing whitespace，保留既有 P2 generated noise。

### 第十一次终审分级与放行判定

- **P0：无新增。** 可靠调度、production capability、事务 intent 与 fencing 主线保持关闭。
- **P1：** 生产组合根没有注入 `AccountTimezonePort`，所以文档化的“请求 → 账号 → UTC”在真实服务端退化为“请求 → UTC”；账号读取异常又被 adapter/application 两层静默吞掉，无法区分未配置与依赖故障，可能以成功响应返回错误的 Today 日期窗口。
- **P2：** app-vue 测试未证明 account/setting store 优先级和实际 HTTP 序列化；account port optional、scheduler 未使用参数；query timezone 仍缺少 trim/非空/IANA schema 与统一错误 envelope。并保留既有 DB clock/null lease/failure receipt、DST/跨日、projection fallback、coalesce 指标、`failed` 分类、具体 runner 绑定、测试 seam 与 generated whitespace 等加固项。

**第十一次终审结论：W1 仍不可放行，不应提交 W1，也不能据此进入 W2/W3。** 第十三次迭代已让 app-vue Today 请求携带显式时区、模板创建不再写 null，并在 application 层实现且文档化三段 fallback；但新增 account port 尚未进入任何生产组合根，账号读取失败语义也仍是无观测的 UTC 静默降级。应先在外层 composition root 以无循环的 application port/adapter 接好真实 Account capability，区分账号未配置与读取失败并补 production wiring/失败路径测试；完成后再重新评估 W1 提交与 W2/W3 准入。
