# W5（Schedule 并发与冲突投影）第一轮审查

审查日期：2026-08-11  
审查范围：`docs/plan/active/2026-08-09-business-p0-elegant-fix-plan.md` W5 第一轮实现  
审查方式：只审查并新增本文档；未修改业务代码，未 commit/push。

## 终审结论

**W5 第一轮不可放行：不要提交 W5，不进入 W6。**

Prisma repository 的单条 CAS 基础方向成立：更新使用 `identityId + id + version` 的 `updateMany`，删除使用相同条件的 `deleteMany`，失败后重新读取当前记录并返回带 `currentVersion` 的 `CONFLICT`（[`schedule-prisma.repository.ts:63`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:63)、[`schedule-prisma.repository.ts:156`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:156)）。应用服务更新窗口也使用旧窗口与新窗口的包络 union，删除使用被删事件的旧窗口（[`schedule-event-application-service.ts:120`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:120)、[`schedule-event-application-service.ts:167`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:167)）。application 目录未反向导入 infrastructure，`IScheduleLeaseRepository` 已位于 application port（[`schedule-lease.port.ts:1`](../../packages/schedule/src/server/application/ports/schedule-lease.port.ts:1)）。

但这只是局部骨架。真实 HTTP/IPC/client 删除链路没有传递版本，写契约仍允许无版本；PowerSync 写路径没有 CAS、没有持久化 version、没有真实事务和 outbox；rebuild worker 未接入任何生产 runtime，失败任务不可重试，且完全不使用 source revision/lease guard；冲突投影仍以无条件整聚合 save 写回并改变业务 version，可能覆盖并发业务更新，也不满足重复消费幂等。当前新增测试是顺序内存测试，并未覆盖 Prisma 双连接竞争、PowerSync、worker 崩溃重启或真实事务。另有 W5 直接引出的 app-vue 类型回归。

最终分级：**P0 = 0，P1 = 5，P2 = 2。**

## 分级汇总

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 5 | OCC 未贯通；PowerSync/事务能力退化；worker 未装配且不可重试；投影写不并发安全/不幂等；删除事件可先于事务提交外泄。 |
| P2 | 2 | W5 验证矩阵是内存顺序测试而非真实证明；Schedule DTO 变更导致 app-vue fixture 类型回归。 |

## P1-1：`expectedVersion` 没有形成强制的端到端命令契约

更新和删除 schema 都把 `expectedVersion` 声明为 optional（[`schedule-requests.ts:39`](../../packages/contracts/src/modules/schedule/api/requests/schedule-requests.ts:39)、[`schedule-requests.ts:127`](../../packages/contracts/src/modules/schedule/api/requests/schedule-requests.ts:127)），application port 和 service 同样接受 optional 版本（[`schedule.application.port.ts:38`](../../packages/schedule/src/server/application/schedule.application.port.ts:38)、[`schedule-event-application-service.ts:73`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:73)、[`schedule-event-application-service.ts:145`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:145)）。这使调用者仍可省略版本，service 会读取当前版本后自行作为 CAS 条件，语义等价于“以服务端刚读到的版本覆盖”，不能表达客户端所编辑的 revision。

删除链路甚至不可传递新增字段：HTTP DELETE 路由既未声明 body schema，也只调用 `controller.delete(id, ctx)`（[`schedule-event.routes.ts:153`](../../packages/schedule/src/api/schedule-event.routes.ts:153)）；Electron handler 只接收 id（[`electron/index.ts:131`](../../packages/schedule/src/electron/index.ts:131)）；HTTP/IPC adapters 和两层 client port 的 `deleteSchedule` 都只有 id（[`schedule-event-http.adapter.ts:58`](../../packages/schedule/src/infrastructure-client/adapters/http/schedule-event-http.adapter.ts:58)、[`schedule-event-ipc.adapter.ts:50`](../../packages/schedule/src/infrastructure-client/adapters/ipc/schedule-event-ipc.adapter.ts:50)、[`schedule-event-api-client.port.ts:33`](../../packages/schedule/src/application-client/ports/schedule-event-api-client.port.ts:33)、[`schedule-client.port.ts:22`](../../packages/schedule/src/application-client/schedule-client.port.ts:22)），UI 也仅发送 id（[`useScheduleCalendar.ts:61`](../../packages/app-vue/src/modules/schedule/composables/useScheduleCalendar.ts:61)）。因此真实客户端删除永远不会收到 stale-version conflict。

关闭条件：update/delete 的公共命令 schema、application/client ports、HTTP OpenAPI、IPC handler/adapter 和 UI 全链路要求并传递 `expectedVersion`；缺失版本必须 validation fail，不能在 service 内补成当前版本；冲突 Result 必须稳定携带结构化 `currentVersion`，并有 HTTP 与 IPC contract test。

## P1-2：PowerSync 组合根绕过 CAS、version、事务和 durable outbox

`PowerSyncScheduleRepository.save()` 不接收/检查 `expectedVersion`；UPDATE 只按 id，既不含 identity，也不含 version，且 SET 列表没有 version（[`schedule-powersync.repository.ts:21`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:21)）。INSERT 同样未写 version（[`schedule-powersync.repository.ts:59`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:59)），mapper 的 persistence shape 也没有 version（[`powersync-schedule.mapper.ts:48`](../../packages/schedule/src/server/infrastructure/adapters/powersync/mappers/powersync-schedule.mapper.ts:48)）。delete 只按 `id + identity_id`，忽略传入的 expectedVersion（[`schedule-powersync.repository.ts:100`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:100)）。

更严重的是，PowerSync 的 `withTransaction()` 只是直接执行 callback，并未调用数据库事务（[`schedule-powersync.repository.ts:144`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:144)）；它也没有 rebuild outbox 方法。application service 把 `withTransaction` 和全部 outbox 方法设计成 optional，并在缺失时继续执行（[`i-schedule-repository.ts:69`](../../packages/schedule/src/server/domain/repositories/i-schedule-repository.ts:69)、[`i-schedule-repository.ts:92`](../../packages/schedule/src/server/domain/repositories/i-schedule-repository.ts:92)、[`schedule-event-application-service.ts:200`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:200)）。PowerSync 又是显式可用的 production composition root（[`powersync.ts:41`](../../packages/schedule/src/server/infrastructure/powersync.ts:41)），所以不是测试-only adapter。

关闭条件：所有可启动组合根必须提供强制 conditional-write、真实 transaction runner 和 durable rebuild outbox 能力；无能力时组合根 fail-fast。PowerSync 必须用真实数据库 affected-row 证明 `identity_id + id + version` CAS，并用故障注入证明 aggregate/projection/outbox 同 commit 或按明确的 offline durable protocol 收敛，不能保留 inline transaction fallback。

## P1-3：rebuild worker 没有生产生命周期，失败后也不可重试/不可重建

`ScheduleRebuildWorkerService` 只出现在自身文件和 W5 单测中，没有在 Prisma module、PowerSync module、API/desktop bootstrap 或 runtime contribution 中实例化/启动；`createSchedulePrismaModule()` 只是原样透传外部 runtime contributions（[`prisma.ts:29`](../../packages/schedule/src/server/infrastructure/prisma.ts:29)）。因此进程重启后没有 worker 自动扫描持久化 outbox，计划要求的“删除后 worker 重启重建”在生产中不存在。

状态机也不是 retryable。worker 只 fetch `status = pending`（[`schedule-prisma.repository.ts:225`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:225)），任何异常都把记录标成 `failed`（[`schedule-rebuild-worker-service.ts:62`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:62)、[`schedule-prisma.repository.ts:240`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:240)），后续扫描永远不再读取。记录没有 next-attempt/claim/lease owner；worker 收到的 `guard` 从未调用 `ensureHeld()`，处理中即使 lease 丢失也会继续写（[`schedule-rebuild-worker-service.ts:37`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:37)）。

关闭条件：把 worker 作为 module-owned runtime 接入 API/desktop 需要的组合根，启动扫描并可安全停止；实现 pending/processing/retry/failed/completed 的可恢复状态机、claim token/超时回收/退避与最大尝试策略；每批及关键写前检查 lease guard；用真实持久化测试证明进程在 dequeue 前、投影后、ack 前分别崩溃均可重启收敛。

## P1-4：投影写会改业务 revision、无条件覆盖并发更新，source revision 也未生效

`ScheduleConflictCacheService.refreshSchedule()` 无论投影是否变化都会调用 repository `save(schedule)`，且在有冲突时每次都调用 `markAsConflicting`（[`schedule-conflict-cache-service.ts:24`](../../packages/schedule/src/server/application/services/schedule-conflict-cache-service.ts:24)）。`markAsConflicting`/`clearConflicts` 会增加 aggregate version（[`calendar-entry.ts:268`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:268)），随后 save 未带 expectedVersion；Prisma 分支因此走整行 upsert（[`schedule-prisma.repository.ts:101`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:101)）。worker 若读到旧快照并与客户端更新竞争，可能把 title/time 等业务字段回写为旧值；重复 rebuild 即使投影相同也会继续增加 version，并非幂等。

command revision 自身也不一致。每个字段 mutation 都单独增加 domain version（例如 title 与 reschedule 分别递增，[`calendar-entry.ts:292`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:292)、[`calendar-entry.ts:318`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:318)），但 Prisma CAS 永远落为 `expectedVersion + 1`（[`schedule-prisma.repository.ts:66`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:66)）；outbox 却记录内存 aggregate 的 `schedule.version`（[`schedule-event-application-service.ts:129`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:129)）。一次多字段更新即可令 outbox `sourceRevision` 与数据库 revision 不同。worker 从未读取或比较 `item.sourceRevision`，所以 stale item 仍会重建并覆盖新状态（[`schedule-rebuild-worker-service.ts:50`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:50)）。

关闭条件：将 conflict projection 与业务 aggregate revision/字段隔离，或提供只更新投影列且带 projection/source revision CAS 的专用 port；相同投影重复应用不得改变任何持久状态；command 一次只推进一个明确 revision；worker 必须拒绝/合并 stale source revision，并以真实 DB 测试覆盖 ack 前重复、乱序、新旧 revision 交错以及 worker/client 并发。

## P1-5：删除的外部事件不与数据库事务原子，回滚仍可留下已发布副作用

Prisma `deleteAggregate()` 在删除行后立即调用进程 event bus（[`schedule-prisma.repository.ts:187`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:187)），application service 随后才刷新冲突并写 rebuild outbox（[`schedule-event-application-service.ts:164`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:164)）。若投影或 outbox 写失败，Prisma transaction 会回滚删除，但已经发布的 delete domain event 无法回滚；消费者可在数据库仍保留事件时执行删除相关副作用。新增 fault test 只 mock outbox 并检查内存 aggregate，没有经过真实 event bus，也只覆盖 update，不覆盖 delete（[`schedule-w5-concurrency-projection.spec.ts:222`](../../packages/schedule/src/server/application/services/__tests__/schedule-w5-concurrency-projection.spec.ts:222)）。

关闭条件：delete event 必须作为同事务 durable outbox 写入，commit 后再发布；故障注入应证明 projection/outbox 任一写失败时 aggregate、conflict projection、rebuild outbox、domain-event outbox 全部回滚且没有外部事件可见。

## P2-1：新增测试没有证明申报的 W5 并发/重启/幂等矩阵

所谓“双客户端并发”实际先 await A 成功，再调用 B，不存在并发竞争（[`schedule-w5-concurrency-projection.spec.ts:171`](../../packages/schedule/src/server/application/services/__tests__/schedule-w5-concurrency-projection.spec.ts:171)）。worker “重启”只是复用同一个 in-memory repo 新建/调用同一 worker；“重复消费”只是第二次扫描已 completed 队列得到 0，并未把同一 message 再投递（[`schedule-w5-concurrency-projection.spec.ts:251`](../../packages/schedule/src/server/application/services/__tests__/schedule-w5-concurrency-projection.spec.ts:251)）。内存 transaction snapshot 和 outbox 状态机复制了期望语义，没有穿过 Prisma/PowerSync production adapter。

`schedule:test:integration` 当前唯一文件是 ScheduleTask repository 的两项持久化/identity 测试，与 CalendarEntry OCC、outbox、lease 和 conflict projection 无关（[`schedule-task-prisma.repository.integration.test.ts:54`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-task-prisma.repository.integration.test.ts:54)）。因此 329+2 全绿不能作为 W5 验证结论。

关闭条件：至少补真实 PostgreSQL 两连接/两事务 CAS 竞争、真实 PowerSync CAS/rollback、Prisma delete/update/outbox 回滚、worker crash/restart、同 message 重投、乱序 source revision、cache/base query 对比；并断言一个成功、一个结构化 conflict、最终 revision 与投影精确一致。

## P2-2：Schedule version contract 引入 app-vue 类型回归

`CalendarEntryResponseSchema` 现在要求输出 version，但 server DTO 又把 version 标成 optional（[`response-schemas.ts:92`](../../packages/contracts/src/modules/schedule/api/response-schemas.ts:92)、[`calendar-entry-server.ts:12`](../../packages/contracts/src/modules/schedule/aggregates/calendar-entry-server.ts:12)）。`createScheduleStoryEvent()` 的 overrides 因而保留 `version?: number`，且默认 fixture 没有 version，不能赋给必填 `CalendarEntryClientDTO`（[`story-fixtures.ts:35`](../../packages/app-vue/src/modules/schedule/components/story-fixtures.ts:35)）。本轮 `app-vue` declaration/typecheck 路径稳定报告 TS2322。

关闭条件：统一 response/server/client DTO 的 required version 语义，不用 `.default(1)` 掩盖服务端漏字段；修复所有 fixture，并让 `app-vue:typecheck`、desktop 依赖 build/typecheck 全绿。

## union 窗口与架构边界判断

- **union 窗口：局部通过。** Update 用 `min(oldStart,newStart)` 与 `max(oldEnd,newEnd)` 得到覆盖旧、新窗口的包络范围，delete 使用旧窗口；这能覆盖全部受影响事件，虽对完全分离的窗口会多扫中间区间。当前放行阻断来自投影写与 worker 语义，不是范围漏算。
- **application 边界：通过。** application 没有导入 infrastructure；lease repository port 已迁入 application。worker 内部另行声明了 coordinator structural port，但仍保持依赖方向正确。
- **Prisma 单条 CAS：局部通过。** 条件列与冲突 current version 正确，但缺少真实并发集成证明，且不能抵消端到端契约、PowerSync 和投影写问题。

## W0--W4 回归判断

- **W0--W3：未发现本轮直接修改其核心业务实现。** `api:test` 85/85 通过；本轮不重新认证 W0--W3 的完整故障矩阵。
- **W4：未发现 Goal/Task 核心文件被本轮直接修改。** Schedule unit 329/329、API 85/85 通过；但 W5 新增的 shared Schedule version contract 已造成 app-vue fixture 类型回归，因此不能宣称整个工作树“全绿”。
- **基础设施回归风险：存在。** PowerSync Schedule production adapter 被新 interface 以 optional 参数/能力绕过，编译通过但语义退化，说明当前 typecheck 不能充当 capability 完整性门禁。

## 验证记录

当前环境没有 `pnpm` 可执行文件，使用仓库 `./node_modules/.bin/nx`、Vitest/TypeScript 直接入口，并对 Nx target 加 `--skip-nx-cache`：

- `schedule:test`：34 files，329/329 tests passed。
- `schedule:test:integration`：1 file，2/2 tests passed；两项均为 ScheduleTask repository 测试，不覆盖 W5。
- `api:test`：30 files，85/85 tests passed。
- `schedule:lint`：passed，42 warnings / 0 errors；W5 新测试新增 5 个 explicit-any warnings。
- `schedule:typecheck`：passed（在组合 run 中执行成功）。
- API 直接 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`：passed。
- desktop `typecheck`：passed（组合 run 中执行成功），但其依赖的 `app-vue` build/declaration 阶段报告 Schedule fixture TS2322。
- `app-vue` 直接 typecheck：failed；除当前环境生成声明中已有的 utils 子路径问题外，包含 W5 直接引入的 `story-fixtures.ts:41` TS2322。
- 三项目组合 Nx typecheck 未完成：`api:prisma:generate` target 内部调用 `pnpm`，当前环境不可用；因此改用已生成 Prisma client 后的 API 直接 tsc。
- `memoflow:governance-check`：passed；package-internal boundary 651 files / 0 known violations。
- inventory `--check`：passed；977 files（unit 846、integration 18、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。

## 下一轮放行条件

1. 关闭上述 5 个 P1：强制并贯通 expectedVersion；PowerSync/所有组合根具备真实 CAS、事务、outbox；worker 接入生命周期并可重试/重启；投影写并发安全、revision-aware 且重复消费幂等；delete domain event 改为同事务 durable publication。
2. 关闭 2 个 P2：用真实 adapter/数据库补齐 W5 验证矩阵；修复并统一 version DTO/fixture 类型。
3. 重跑 Schedule unit + W5 Prisma/PowerSync integration、API、app-vue、desktop 的 typecheck/build，以及 governance/inventory；所有门禁有效全绿后再复审。

在这些条件全部满足前，最终判定保持：**W5 不可放行，不得提交，不进入 W6。**

---

## 第二轮审查

审查日期：2026-08-11  
审查范围：W5 r2 对第一轮 `5 P1 + 2 P2` 的关闭实现与验证证据  
审查方式：只审查并追加本文档；未修改业务代码，未 commit/push。

### 第二轮终审结论

**W5 第二轮仍不可放行：不要提交 W5，不进入 W6。**

本轮确实完成了两项重要主体修复：`expectedVersion` 已进入 update/delete 的公共 schema、HTTP/OpenAPI、IPC、client port 与 repository CAS，冲突错误也稳定携带 `context.currentVersion`；PowerSync repository 已有 affected-row CAS、`writeTransaction` fail-fast 和持久化 rebuild outbox 的实现骨架。app-vue fixture 的 required `version` 类型回归也已关闭。

但原 P1-3、P1-4、P1-5 仍未关闭：生产组合根没有给新 worker 注入持久化 lease coordinator，outbox claim/ack 不校验 claim token；一次多字段命令仍会让内存 aggregate revision 与数据库 revision/outbox source revision 分叉，worker 也完全不消费 outbox 的 `sourceRevision`；delete domain event 没有写入同事务 durable event outbox，而是在 commit 后直接向进程 event bus 发布。新增测试又没有覆盖这些失败窗口，且治理门禁实际失败。因此不能以“7 个 integration tests 全绿”替代语义关闭。

第二轮最终分级：**P0 = 0，P1 = 3，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已发生的不可逆数据破坏或生产安全事故。 |
| P1 | 3 | worker claim/lease 不安全；command/source revision 仍不一致；delete event 仍非 durable transaction outbox。 |
| P2 | 2 | 所谓真实矩阵仍混入 singleton Prisma client 与内存 PowerSync，且缺 crash/replay/乱序场景；UI 仍可伪造 version=1 且缺 HTTP/IPC contract test。 |

### 已关闭：原 P1-1 的服务端与传输主体链路

`UpdateScheduleRequestSchema` 与 `DeleteScheduleRequestSchema` 都把 `expectedVersion` 声明为 required positive integer（[`schedule-requests.ts:39`](../../packages/contracts/src/modules/schedule/api/requests/schedule-requests.ts:39)、[`schedule-requests.ts:127`](../../packages/contracts/src/modules/schedule/api/requests/schedule-requests.ts:127)）。DELETE OpenAPI 路由声明 JSON body，并把 `req.body` 传入 controller（[`schedule-event.routes.ts:154`](../../packages/schedule/src/api/schedule-event.routes.ts:154)）；HTTP adapter 使用 delete config 的 `data`，IPC adapter 也发送 `{ expectedVersion }`（[`schedule-event-http.adapter.ts:58`](../../packages/schedule/src/infrastructure-client/adapters/http/schedule-event-http.adapter.ts:58)、[`schedule-event-ipc.adapter.ts:50`](../../packages/schedule/src/infrastructure-client/adapters/ipc/schedule-event-ipc.adapter.ts:50)）。application service 不再补读版本作为命令版本，Prisma 与 PowerSync CAS 冲突均携带结构化 `currentVersion/expectedVersion`（[`schedule-event-application-service.ts:109`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:109)、[`schedule-prisma.repository.ts:72`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:72)、[`schedule-powersync.repository.ts:99`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:99)）。

因此原 P1-1 的服务端安全边界与主要传输实现可关闭；缺失版本会在 controller schema 校验失败，而 stale version 会进入 CAS/CONFLICT，不再退化成无条件覆盖。UI 和 transport contract test 的残余问题降列在 P2，不再构成服务端 P1。

### 已关闭实现主体、但证据并未完全闭环：原 P1-2

PowerSync update 使用 `id + identity_id + version` 条件并检查 `rowsAffected`，delete 同样要求三列 CAS（[`schedule-powersync.repository.ts:56`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:56)、[`schedule-powersync.repository.ts:197`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:197)）；`withTransaction()` 缺少 `writeTransaction` 时会 fail-fast，并在 transaction handle 上创建 scoped repository（[`schedule-powersync.repository.ts:462`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:462)）。version 与 rebuild outbox 字段也已经进入 PowerSync/Prisma schema。

就 production adapter 的代码主体而言，原 P1-2 可关闭；但“真实 PowerSync affected-row/rollback 证明”仍未提供。新增 integration test 调用的是文件内手写 `Map` 数据库 `createInMemoryPowerSyncDb()`，不是真实 PowerSync/SQLite adapter（[`schedule-w5-real-concurrency.integration.test.ts:16`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:16)、[`schedule-w5-real-concurrency.integration.test.ts:280`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:280)）。该证据缺口并入 P2-1。

### P1-1（原 P1-3）：worker 已接生命周期，但生产 lease、claim 与 ack 仍不安全

`ScheduleRebuildWorkerRuntime` 已作为 module-owned contribution 启停，这是有效进展（[`schedule.module.ts:276`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:276)、[`schedule-rebuild-worker-service.ts:97`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:97)）。但 `createScheduleModule()` 在未注入时回退到 `new ScheduleLeaseCoordinator(null)`，Prisma 与 PowerSync 两个 production composition root 都没有传 `leaseCoordinator`（[`schedule.module.ts:280`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:280)、[`prisma.ts:29`](../../packages/schedule/src/server/infrastructure/prisma.ts:29)、[`powersync.ts:41`](../../packages/schedule/src/server/infrastructure/powersync.ts:41)）。多进程 API/desktop 实际因此无分布式 lease，所谓每批 lease guard 在生产退化为永远成功。

更关键的是 claim 不是原子 ownership 转移。Prisma 先 select candidates，再按 id 无条件 `updateMany`，随后按 id 返回记录；没有在 update 条件和返回查询中限定旧状态或本次 `claimToken`（[`schedule-prisma.repository.ts:240`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:240)）。PowerSync 是同样的 select-then-update 结构（[`schedule-powersync.repository.ts:370`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:370)）。两边 `markRebuildOutboxProcessed(id, claimToken, ...)` 虽接收 token，SQL/Prisma where 却都只使用 id；超时后由新 worker 重新 claim 的记录仍可被旧 worker ack/retry/failed 覆盖（[`schedule-prisma.repository.ts:278`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:278)、[`schedule-powersync.repository.ts:418`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:418)）。

此外，worker 在 `ensureHeld()` 抛 lease-lost 后进入普通 catch，并在没有再次确认 lease ownership 的情况下写 retry/failed（[`schedule-rebuild-worker-service.ts:53`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:53)）。这不满足 claim token、lease guard 和崩溃恢复的关闭条件。

关闭条件：production roots 必须注入持久化 lease repository/coordinator；claim 必须是带 eligible-state 条件的原子抢占并只返回本 token 所有记录；ack/retry/failed 必须以 `id + claimToken + processing` 条件更新并检查 affected row；lease-lost 不得由失主继续改状态；补超时重领后旧 worker ack 的竞争测试。

### P1-2（原 P1-4）：投影列隔离已完成，但 revision-aware 语义仍错误

专用 `updateConflictProjection()` 现在只写 `hasConflict/conflictingSchedules`，不会再整聚合 save；相同值预检查也避免了普通重复应用写入。这一部分方向正确（[`schedule-prisma.repository.ts:157`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:157)、[`schedule-powersync.repository.ts:250`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:250)）。

但第一轮指出的 command revision 分叉没有修复。每个 aggregate mutation 仍各自 `version += 1`，例如 title、description 与 reschedule 会分别推进 revision（[`calendar-entry.ts:292`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:292)、[`calendar-entry.ts:318`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:318)、[`calendar-entry.ts:332`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:332)）；application service 可在一次 update command 中连续调用这些 mutation（[`schedule-event-application-service.ts:123`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:123)），而 Prisma/PowerSync CAS 都固定落库为 `expectedVersion + 1`（[`schedule-prisma.repository.ts:49`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:49)、[`schedule-powersync.repository.ts:91`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:91)）。随后 outbox 却记录内存 `schedule.version`（[`schedule-event-application-service.ts:144`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:144)）。多字段命令会再次产生 DB version=2、outbox sourceRevision=3/4/... 的分叉，使本应 stale 的投影更新被 `version <= sourceRevision` 放行。

worker 本身也没有读取或比较 `item.sourceRevision`；它只使用时间窗口重新查询，cache service 再把每个当前 schedule 自身的 `schedule.version` 当作 source revision（[`schedule-rebuild-worker-service.ts:53`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:53)、[`schedule-conflict-cache-service.ts:24`](../../packages/schedule/src/server/application/services/schedule-conflict-cache-service.ts:24)）。因此新增的 direct repository stale test 只证明一个局部 SQL 条件，不证明乱序 outbox、worker/client 并发或 revision-aware 消费。

关闭条件：一次业务命令只推进一个 canonical revision，并让 aggregate、DB 返回值、domain event 与 outbox source revision 完全一致；worker 必须显式处理 outbox `sourceRevision`；补多字段 update、旧/新 outbox 乱序、同消息重投和 worker/client 并发的真实 DB 断言。

### P1-3（原 P1-5）：delete 回滚窗口改善，但 domain event 仍不是 durable outbox

delete CAS、冲突投影刷新和 rebuild outbox 现在确实位于同一个 repository transaction；只有 callback 成功后才执行 `publishAggregateEvents()`，所以“outbox 写失败但进程事件已先外泄”的原顺序问题得到修正（[`schedule-event-application-service.ts:172`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:172)、[`schedule-event-application-service.ts:210`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:210)）。

但事务中写入的只是 conflict rebuild outbox，不是 `schedule:calendar-entry-deleted` 的 durable domain-event outbox。commit 后进程崩溃会永久丢失 delete event；event bus publish 失败时调用也没有传 `outboxWriter`，无法可靠补偿。当前 repository 的 `deleteAggregate()` 仅执行 delete，没有 event outbox write（[`schedule-prisma.repository.ts:153`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:153)、[`schedule-powersync.repository.ts:224`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:224)）。本轮 `memoflow:governance-check` 也实际被 `unflushed-events-audit` 拒绝，分别命中 Prisma/PowerSync Schedule repository。

新增故障注入的“零外部事件”断言还是无效证据：测试监听 `'ScheduleEventDeletedEvent'`，实际 domain event type 是 `'schedule:calendar-entry-deleted'`（[`schedule-w5-real-concurrency.integration.test.ts:245`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:245)、[`calendar-entry.ts:282`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:282)）。即使错误事件真的被发布，该 spy 也不会收到。

关闭条件：在删除 aggregate 的同一 DB transaction 写入 canonical domain event outbox，commit 后由可靠 publisher 投递；用正确 event type 断言 rollback 零可见事件，并覆盖 commit 后、publish 前崩溃与 publish failure 重试。`memoflow:governance-check` 必须恢复通过。

### P2-1：所谓“真实并发/回滚矩阵”仍未达到第一轮关闭条件

新增文件确实穿过真实 PostgreSQL，且 CAS 两个 promise 是并发发起，比第一轮顺序内存测试有明显提升。但测试声称的“两 distinct Prisma clients”并不成立：两次调用 `getPrisma()`，而 helper 缓存并返回同一个 `prismaPromise` singleton（[`schedule-w5-real-concurrency.integration.test.ts:196`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:196)、[`integration-helpers.ts:10`](../../packages/test-utils/src/setup/integration-helpers.ts:10)）。这能证明 PostgreSQL `updateMany` CAS 的并发结果，不能证明明确的双 client/双 transaction 交错矩阵。

PowerSync 部分是内存 SQL pattern matcher；worker“崩溃”是声明一个从未运行的 `worker1`，然后在 dequeue 前创建 `worker2`（[`schedule-w5-real-concurrency.integration.test.ts:330`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:330)）。没有覆盖 projection 后/ack 前崩溃、processing timeout 回收、旧 claim token ack、retry/backoff/max-attempt、同一 message 真重投、乱序 source revision 或 cache/base 的真实 PostgreSQL 对比。原 P2-1 保持打开。

### 已关闭：原 P2-2 app-vue fixture

`CalendarEntryServerDTO`、response schema 与 client DTO 的 `version` 已统一为 required，story fixture 也显式提供 `version: 1`（[`story-fixtures.ts:45`](../../packages/app-vue/src/modules/schedule/components/story-fixtures.ts:45)）。本轮 app-vue build/typecheck 通过，原 TS2322 不再复现。原 P2-2 关闭。

### P2-3（新增）：UI 仍允许伪造版本，HTTP/IPC contract test 缺失

`deleteCalendarEntry()` 自身仍把 `expectedVersion` 声明为 optional，并在 store 找不到 entry 时静默回退到 `1`（[`useScheduleCalendar.ts:61`](../../packages/app-vue/src/modules/schedule/composables/useScheduleCalendar.ts:61)）。这不会绕过服务端 CAS，但它不是“客户端所见 revision”的严格契约，会把客户端状态缺失伪装成 stale command。应要求调用方/已加载 entry 提供真实 version；不存在 version 时在客户端失败，而不是制造 `1`。

第一轮明确要求 HTTP 与 IPC contract test。本轮现有 route spec 仍只验证 DELETE success envelope，没有验证 DELETE body schema 对缺失 `expectedVersion` 的拒绝、controller 参数传递、409 `currentVersion`，也没有 HTTP adapter/IPC handler round-trip 测试（[`schedule-event.routes.spec.ts:155`](../../packages/schedule/src/api/schedule-event.routes.spec.ts:155)）。因此增加一个 P2 证据与客户端契约项。

### W0--W4 回归判断

- **未发现 W0--W4 核心业务实现的直接回退。** 本轮改动仍集中在 Schedule、共享 Schedule contract/http-client 类型与 app-vue Schedule UI。
- **编译/类型回归未复现。** Schedule、app-vue、desktop typecheck 通过；API 因 Nx dependency `api:prisma:generate` 调用环境中不存在的 `pnpm` 未在组合 run 执行，改用 `tsc --noEmit -p apps/api/tsconfig.typecheck.json` 后通过；app-vue build 也通过。
- **全局门禁不能宣称全绿。** governance failure 是本轮 Schedule event flush 设计直接引起，不是 W0--W4 回退，但在修复前仍是 W5 release blocker。

### 第二轮验证记录

- `schedule:test`：34 files，329/329 tests passed。
- `schedule:test:integration`：2 files，7/7 tests passed；其中 5 项来自 W5 新文件。
- `schedule:lint`：passed，53 warnings / 0 errors；W5 real integration test 包含 10 个 warning，其中 `worker1` 未使用直接暴露“崩溃”测试未运行旧 worker。
- `schedule:typecheck`：passed。
- `app-vue:build` 与 `app-vue:typecheck`：passed。
- `desktop:typecheck`：passed。
- API 直接 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`：passed；四项目 Nx run 因当前环境无 `pnpm` 导致 `api:prisma:generate` 失败，API target 未执行。
- `test:inventory --check`：passed；978 files（unit 846、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。
- `memoflow:governance-check`：**failed**；`unflushed-events-audit` 报告 Prisma/PowerSync 两个 Schedule repository 未 flush/publish domain events。其余已输出的 governance 子检查通过。

### 第二轮放行条件

1. 修复 worker production lease 注入、原子 claim、token-guarded ack/retry/failed 和 lease-lost 行为，并补真实竞争/超时回收测试。
2. 统一一次 command 一次 revision；outbox 使用数据库已提交的 canonical revision；worker 显式执行 source-revision 语义，并补多字段、乱序、重投、worker/client 并发矩阵。
3. 将 delete domain event 写入同事务 durable event outbox，修正故障测试的 event type，覆盖 commit/publish crash window，并让 governance 恢复全绿。
4. 使用真实独立 Prisma clients/transactions 与真实 PowerSync/SQLite adapter 完成 CAS、rollback、claim/retry/restart 证明；补 HTTP/IPC contract tests；移除 UI 的 version=1 fallback。
5. 重跑 Schedule unit/integration、API/app-vue/desktop typecheck/build、lint、governance 与 inventory，所有有效门禁全绿后再复审。

在上述条件关闭前，第二轮最终判定保持：**W5 不可放行，不得提交，不进入 W6。**

---

## 第三轮审查

审查日期：2026-08-11  
审查范围：第二轮遗留的 3 个 P1、2 个 P2 与 governance；只审查，未修改业务代码，未 commit/push。

### 终审结论

**W5 第三轮仍不可放行：不要提交 W5，不进入 W6。**

本轮有实质进展：Prisma production root 与 PowerSync production root 已默认注入持久化 lease coordinator；Prisma claim 已复用 eligible-state 条件并按本次 token 回读，成功 ack 也按 `id + claimToken + processing` 检查 affected row；aggregate `update()` 已收敛为一次 command 最多推进一次 version，delete/update rebuild outbox 使用命令后的 revision，worker 也把 `item.sourceRevision` 传入投影刷新；HTTP DELETE body schema 与 IPC 参数传递的实现已存在。

但当前实现与“第三轮已全部关闭”的申报不一致。PowerSync claim SQL 的参数顺序错误，且 ack/retry/failed 仍完全没有 token ownership 条件；Prisma retry/failed 虽带 token 条件，却不检查 affected row，成功 ack 的 token-lost 错误也不会被 worker 识别为 lease-lost。新增 domain-event outbox 只有 Prisma 写入端，没有可靠 publisher、投递状态或重放路径，PowerSync 根本没有同事务写入，governance 实跑仍失败。测试矩阵仍使用同一个 Prisma singleton 和手写 `Map` PowerSync 模拟器，HTTP/IPC contract test 仍缺失，UI 的 `version = 1` fallback 也仍在。

最终分级：**P0 = 0，P1 = 2，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 2 | PowerSync worker ownership/ack 仍不安全；domain event outbox 不具备 durable delivery 且 governance 失败。 |
| P2 | 2 | “真实矩阵”仍有 singleton/Map/伪重启/错误 spy 等证据缺口；UI fallback 与 HTTP/IPC contract test 未关闭。 |

### P1-1：Prisma 主路径改善，但 PowerSync claim/ack 仍不具备 token ownership

Prisma 的 claim 已把 `pending`、到期 `retry` 与超时 `processing` 统一为 `eligibleCondition`，条件同时用于候选查询和 `updateMany`，回读也限制为本次 `claimToken + processing`；成功 ack 使用 `id + claimToken + processing` 并在零行时抛错（[`schedule-prisma.repository.ts:267`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:267)、[`schedule-prisma.repository.ts:293`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:293)、[`schedule-prisma.repository.ts:305`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:305)、[`schedule-prisma.repository.ts:319`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:319)）。Requirement 6 也真实穿过 PostgreSQL，证明超时重领后旧 Prisma token 的成功 ack 被拒且记录仍归新 token（[`schedule-w5-real-concurrency.integration.test.ts:394`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:394)）。Prisma/PowerSync production root 现在都默认装配持久化 lease repository，这一部分关闭。

PowerSync 路径仍是 blocker。UPDATE SQL 中 `id IN (...)` 的占位符位于两个 eligible-time 占位符之前，但参数却按 `claimToken, now, now, threshold, threshold, ...ids` 传入；实际 id 位置收到 threshold，而尾部 id 又会落到状态时间条件，真实 SQLite 上不能按候选 id 完成 claim（[`schedule-powersync.repository.ts:387`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:387)）。即使 claim 成功，PowerSync success/retry/failed 三条 UPDATE 都只使用 `WHERE id = ?`，未使用传入的 `claimToken`、未要求 `status = 'processing'`、也未检查 `rowsAffected`；旧 worker 可以在超时重领后把新 owner 的记录 completed/retry/failed（[`schedule-powersync.repository.ts:422`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:422)）。Requirement 6 只实例化 `SchedulePrismaRepository`，完全没有覆盖 PowerSync ownership。

Prisma 的失败路径也未完整达到第二轮关闭条件：retry/failed 的 `updateMany` 已有 token/status 条件，但结果被丢弃，claim 丢失时静默 no-op（[`schedule-prisma.repository.ts:335`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:335)）。此外成功 ack 抛出的文本是 `no longer owned ... (lease lost)`，worker 只识别 `ScheduleLeaseLostError` 或包含 `lease ownership was lost` 的消息，因此该错误会落入普通失败处理，而不是直接中止本批（[`schedule-prisma.repository.ts:329`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:329)、[`schedule-rebuild-worker-service.ts:73`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:73)）。虽然第二次 token-guarded failure update 不会覆盖新 owner，但 lease-lost 语义和可观测结果仍不正确。

关闭条件：修正 PowerSync SQL 参数顺序；两 adapter 的 ack/retry/failed 一律使用 `id + claimToken + processing`、检查 affected row 并返回统一的 typed lease-lost；worker 对该错误直接退出且不再写状态；用真实 SQLite/PowerSync database 覆盖双 worker claim、超时重领、旧 token success/retry/failed 全矩阵。

### 已关闭：P1-2 canonical revision 与 source-revision CAS 主体

`CalendarEntry.update()` 现在先汇总字段变化，最后只执行一次 `version += 1`；多字段 update 不再产生内存 aggregate revision 与数据库 revision 分叉，delete 也明确推进一次 revision（[`calendar-entry.ts:292`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:292)、[`calendar-entry.ts:361`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:361)、[`calendar-entry.ts:282`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:282)）。application service 在 save/delete 后用命令后的 `schedule.version` 写 rebuild outbox，worker 显式把 `item.sourceRevision` 传给 cache refresh（[`schedule-event-application-service.ts:133`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:133)、[`schedule-event-application-service.ts:144`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:144)、[`schedule-event-application-service.ts:190`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:190)、[`schedule-rebuild-worker-service.ts:57`](../../packages/schedule/src/server/application/services/schedule-rebuild-worker-service.ts:57)）。Prisma/PowerSync 投影 UPDATE 都使用 `current.version <= sourceRevision` CAS，不会覆盖更新后的业务 revision。

因此 P1-2 实现主体可关闭。乱序、重投与 worker/client 并发的证据完整性仍计入 P2-1，不再把已修复的 command revision 问题保留为 P1。

### P1-3：新增表和事务写入不等于 durable event outbox

Prisma repository 现在会在 save/delete 同一个 transaction-scoped repository 中把 aggregate events 写入 `schedule_domain_event_outbox`，application service 也只在 transaction callback 成功后执行进程 event bus publish；真实 PostgreSQL 的 aggregate/rebuild-outbox 回滚已得到部分证明（[`schedule-prisma.repository.ts:50`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:50)、[`schedule-prisma.repository.ts:176`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:176)、[`schedule-event-application-service.ts:172`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:172)、[`schedule-event-application-service.ts:210`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:210)）。

但该表只有 `id/identityId/scheduleId/eventType/payload/createdAt`，没有状态、attempt、next-attempt、claim token、publishedAt 或 event idempotency key；仓库内也没有读取/claim/publish/ack 该表的 publisher。commit 后、application 直接 publish 前崩溃，或 publish 失败，记录虽然留在表中却永远不会被投递；直接 publish 成功后也不会把 outbox 标成已完成，未来无法安全重放（[`schedule.prisma:154`](../../packages/database/prisma/schema/schedule.prisma:154)）。这只是 durable event journal 的写入端，不是 durable delivery outbox。

PowerSync repository 的 save/deleteAggregate 则完全没有 domain-event outbox 写入（[`schedule-powersync.repository.ts:56`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:56)、[`schedule-powersync.repository.ts:224`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:224)）。本次实跑 `memoflow:governance-check` 仍被 `unflushed-events-audit` 拒绝，明确命中 `PowerSyncScheduleRepository.save()`，与“governance 通过、两个 repo 已满足审计”的申报相反。

回滚测试的外部事件断言也仍监听错误的 `'ScheduleEventDeletedEvent'`，实际 event type 是 `'schedule:calendar-entry-deleted'`；所以该 spy 不能证明 rollback 时零外部事件（[`schedule-w5-real-concurrency.integration.test.ts:245`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:245)、[`calendar-entry.ts:286`](../../packages/schedule/src/server/domain/aggregates/calendar-entry.ts:286)）。

关闭条件：为所有可启动 adapter 提供同事务 canonical event-outbox 写入；实现有 claim/retry/idempotency/ack 的可靠 publisher 与生产生命周期；覆盖 commit 后 publish 前崩溃、publish failure 重试、重复投递和正确 event type 的 rollback 零可见事件；governance 必须实跑通过。

### P2-1：PostgreSQL 用例真实，但“双连接 + 真 SQLite + 崩溃矩阵”仍未兑现

Requirement 1 的两个 promise 确实并发穿过真实 PostgreSQL，并证明一个 CAS 成功、一个返回结构化 `currentVersion`；Requirement 2/5/6 也使用真实 PostgreSQL，Requirement 6 对 Prisma timeout reclaim 是有效新增证据。本轮 `schedule:test:integration` 实跑为 2 files、8/8 passed。

但所谓两个 distinct Prisma clients 仍是两次调用缓存同一 `prismaPromise` 的 `getPrisma()`，没有显式创建两个 client/connection 或控制两个 transaction 的交错（[`schedule-w5-real-concurrency.integration.test.ts:196`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:196)、[`integration-helpers.ts:10`](../../packages/test-utils/src/setup/integration-helpers.ts:10)）。Requirement 3 的 `createInMemoryPowerSyncDb()` 仍是文件内 `Map<string, Map<...>>` 加 SQL 字符串 pattern matcher，并由手写 snapshot 模拟 rollback，不是真实 SQLite 或 PowerSync transaction（[`schedule-w5-real-concurrency.integration.test.ts:16`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:16)）。它也没有覆盖当前 PowerSync claim/ack SQL，因而没有发现 P1-1 的生产错误。

Requirement 4 仍只创建一个从未调用的 `worker1`，然后在 dequeue 前由 `worker2` 首次处理，不是 worker 在 claimed/projection-written/ack-pending 任一持久状态崩溃后的重启恢复（[`schedule-w5-real-concurrency.integration.test.ts:330`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:330)）。同 message 重投、乱序新旧 outbox、retry/backoff/max-attempt、worker/client 并发与 cache/base query 对比也仍未形成真实数据库断言。P2-1 保持打开。

### P2-3：HTTP/IPC 实现已接线，但 UI fallback 与 contract test 仍未关闭

HTTP DELETE 已声明 `DeleteScheduleRequestSchema` body，controller 会 parse 并传递 `expectedVersion`；IPC adapter/handler 也已传递 payload，这是实现进展。但 UI `deleteCalendarEntry(id, expectedVersion?)` 仍允许版本缺失，并继续执行 `expectedVersion ?? entry?.version ?? 1`（[`useScheduleCalendar.ts:61`](../../packages/app-vue/src/modules/schedule/composables/useScheduleCalendar.ts:61)）。因此“UI 不伪造版本”的申报不成立。

现有 route spec 仍只验证 DELETE 的 null success envelope，没有断言缺失/非法 `expectedVersion` 被拒绝、body 被传给 controller、409 receipt 保留 `currentVersion`；仓库中也没有 HTTP adapter 与 IPC handler/adapter 的 DELETE round-trip contract test（[`schedule-event.routes.spec.ts:155`](../../packages/schedule/src/api/schedule-event.routes.spec.ts:155)）。P2-3 保持打开。

### W0--W4 回归判断

- 未发现 W0--W4 核心业务实现的直接回退；当前变更仍集中在 Schedule、共享 Schedule contracts/http-client、数据库生成物与 app-vue Schedule UI。
- `schedule:test` 实跑 34 files、329/329 passed；`schedule:test:integration` 实跑 2 files、8/8 passed。
- `schedule:lint` 成功但来自 cache，54 warnings / 0 errors；其中 `worker1` 未使用仍直接印证 Requirement 4 不是实际崩溃测试。
- 本轮没有重跑已申报的 app-vue/api/desktop typecheck/build；代码审查未发现 W0--W4 直接回退，但不能用本轮结果独立复核这些门禁。
- 全局门禁不是全绿：`memoflow:governance-check` **实跑失败**，唯一报告到的 blocker 是 PowerSync Schedule unflushed events。这是 W5 本身的 release blocker，不是 W0--W4 回退。

### 第三轮放行条件

1. 修复 PowerSync claim 参数顺序和 token-guarded ack/retry/failed；统一两 adapter 的 affected-row/typed lease-lost 语义，并以真实 SQLite/PowerSync 双 worker 竞争证明。
2. 把 Schedule domain-event outbox 补成可 claim、重试、幂等、ack、重启恢复的可靠发布链路，并覆盖 Prisma 与 PowerSync；使用正确 event type 补回滚与 crash-window 测试，让 governance 实跑通过。
3. 用显式独立 PostgreSQL clients/transactions、真实 SQLite/PowerSync、真实 worker crash points 补齐矩阵；覆盖重投、乱序、retry/max-attempt 与 worker/client 并发。
4. 移除 UI `version = 1` fallback；补 HTTP DELETE body/409 与 IPC handler/adapter round-trip contract tests。
5. 重跑 Schedule unit/integration、app-vue/api/desktop typecheck/build、lint、inventory 与 governance；所有有效门禁全绿后再复审。

在上述条件关闭前，第三轮最终判定保持：**P0 = 0，P1 = 2，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第四轮审查

审查日期：2026-08-11  
审查范围：第三轮遗留的 governance、真实数据库矩阵、UI version fallback 与 HTTP/IPC DELETE contract；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第四轮仍不可放行：不要提交 W5，不进入 W6。**

本轮申报中有三项可以确认关闭：Requirement 1 现在确实使用共享测试 client 之外的第二个 `PrismaClient + PrismaPg`，两个 service 通过 `Promise.allSettled` 并发竞争同一 revision；Requirement 3 确实使用 `better-sqlite3`、真实 `BEGIN/COMMIT/ROLLBACK` 和真实 `schedules` 表证明 PowerSync CAS 与事务回滚；UI 也已移除 `?? 1`，无法从显式参数或 store 得到 version 时不会发送删除请求。governance 亦真实实跑通过，结果为 97 files、17 event-emitting aggregates、0 exemptions。

但第三轮的两个 P1 实现阻断没有被本轮修改关闭。PowerSync rebuild outbox 的 claim 参数顺序与 token ownership 仍是第三轮原实现，旧 worker 仍可完成、重试或失败新 owner 的记录。Schedule domain-event outbox 仍只有 Prisma 写入端，没有 claim/retry/idempotent publish/ack/runtime publisher，PowerSync 也没有同事务 domain-event outbox；新增可选 `eventBus + publishAggregateEvents` 只是满足了 governance 的文件级启发式扫描，并没有形成 durable delivery。新增 HTTP/IPC 测试还主要是 OpenAPI schema 示例和源码正则，不是实际 handler round-trip。

最终分级：**P0 = 0，P1 = 2，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 2 | PowerSync worker claim/ack ownership 仍不安全；domain event 仍无可靠、可重启的 durable delivery。 |
| P2 | 2 | worker crash/replay 等矩阵仍不真实；HTTP/IPC DELETE contract test 未执行真实 round-trip。 |

### 已关闭：Requirement 1 的独立 PostgreSQL 双连接证据

Requirement 1 的第二个连接现在显式构造新的 `PrismaClient`，并使用新的 `PrismaPg({ connectionString })`；它不再是第二次调用 `getPrisma()` 得到同一 singleton（[`schedule-w5-real-concurrency.integration.test.ts:268`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:268)）。两个 repository/service 同时以 `expectedVersion = 1` 更新，`Promise.allSettled` 断言恰好一成一败，失败 receipt 含 `currentVersion: 2`，最终数据库 revision 也是 2（[`schedule-w5-real-concurrency.integration.test.ts:288`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:288)）。本轮 `schedule:test:integration` 实跑 2 files、8/8 passed，因此第三轮关于 singleton 的证据缺口关闭。

### 已关闭：Requirement 3 的真实 SQLite CAS/回滚主体

Requirement 3 现在通过 `better-sqlite3(':memory:')` 创建真实 `schedules` 表，并把 `writeTransaction` 映射为 SQLite 的 `BEGIN/COMMIT/ROLLBACK`（[`schedule-w5-real-concurrency.integration.test.ts:18`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:18)、[`schedule-w5-real-concurrency.integration.test.ts:72`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:72)）。测试真实穿过 `PowerSyncScheduleRepository`，证明 revision 1 更新为 2、stale revision 返回结构化 conflict，并在 rebuild outbox 故障注入后恢复 title/version 2（[`schedule-w5-real-concurrency.integration.test.ts:359`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:359)）。第三轮关于 `Map` 模拟器不能证明 CAS/rollback 的这一部分关闭。

该 SQLite 用例只覆盖 schedule CAS 和事务回滚，不覆盖 PowerSync rebuild outbox claim/ack ownership，也没有断言测试中创建的 `schedule_domain_event_outbox` 得到任何写入；这些分别归入下述 P1-1 与 P1-2。

### P1-1：PowerSync claim 参数与 token ownership 仍未修复

第三轮指出的参数顺序错误原样存在。claim UPDATE 的前三个参数用于 SET，随后 SQL 先出现 `id IN (...)`，最后才是 retry/processing 的两个时间条件；实现却传入 `[claimToken, nowIso, nowIso, thresholdIso, thresholdIso, ...ids]`，因此 id 占位符收到时间，尾部 id 又进入时间条件（[`schedule-powersync.repository.ts:398`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:398)）。真实 SQLite Requirement 3 没有调用 `claimRebuildOutboxItems()`，所以 8/8 全绿没有覆盖该错误。

`markRebuildOutboxProcessed()` 也仍只按 `id` 读取和更新；success、retry、failed 都没有 `claim_token = ? AND status = 'processing'`，没有检查 `rowsAffected`，传入的 `claimToken` 实际未使用（[`schedule-powersync.repository.ts:433`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:433)）。超时重领后，旧 worker 仍可覆盖新 owner 的状态。Requirement 6 仍只覆盖 Prisma adapter（[`schedule-w5-real-concurrency.integration.test.ts:473`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:473)）。这是并发正确性 blocker，P1 保持打开。

### P1-2：governance 机械通过，但 domain event durable delivery 仍不存在

`memoflow:governance-check --skip-nx-cache` 本轮实跑成功，`unflushed-events-audit` 的确报告 97/17/0；因此“命令是否真实通过”答案是通过。但该审计只要 repository **文件任意位置**出现 `publishAggregateEvents` 等标识符，就把文件内所有相关 save 视为已 flush（[`unflushed-events.mjs:28`](../../tools/governance/lib/unflushed-events.mjs:28)、[`unflushed-events.mjs:77`](../../tools/governance/lib/unflushed-events.mjs:77)）。它不验证调用是否可达、是否发生在 commit 后、是否具备 outbox fallback 或能否重启重放。

当前 PowerSync production root 构造 repository 时没有注入 event bus（[`powersync.ts:29`](../../packages/schedule/src/server/infrastructure/powersync.ts:29)），而 `withTransaction()` 创建 transaction-scoped repository 时也没有传递 root 的 event bus（[`schedule-powersync.repository.ts:477`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:477)）。因此 application service 的正常 create/update/delete 路径仍由 `pendingEvents` 在 transaction 成功返回后统一 publish（[`schedule-event-application-service.ts:70`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:70)、[`schedule-event-application-service.ts:75`](../../packages/schedule/src/server/application/services/schedule-event-application-service.ts:75)）；production 路径当前不会双发。即使手工注入 event bus，PowerSync CAS update 分支也会在 repository publish 之前直接 `return`（[`schedule-powersync.repository.ts:71`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:71)、[`schedule-powersync.repository.ts:127`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:127)），可见新增代码并不是完整的 repository-owned flush 语义。

Prisma transaction repository 若被注入 event bus，则会继承该 bus 并在事务 callback 内 publish；`publishAggregateEvents` 随后清空 aggregate buffer，所以 application 的 post-transaction 循环不会再次发布，不会形成“双发”，但外部事件会在 commit 前可见（[`schedule-prisma.repository.ts:109`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:109)、[`schedule-prisma.repository.ts:366`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-prisma.repository.ts:366)、[`aggregate-repository.base.ts:119`](../../packages/patterns/src/repository/aggregate-repository.base.ts:119)）。这仍是错误的事务边界。

更关键的是，Prisma `ScheduleDomainEventOutbox` 仍只有 id、identity、schedule、type、payload、createdAt，没有 delivery status、attempt、nextAttemptAt、claim token、publishedAt 或 idempotency key（[`schedule.prisma:154`](../../packages/database/prisma/schema/schedule.prisma:154)）；Schedule 代码中仍没有读取、claim、publish、ack 该表的 publisher。PowerSync repository 则完全不写 domain-event outbox。application 的 post-commit direct publish 又没有传 `outboxWriter`；event bus 失败时 helper 只记日志并清空事件（[`aggregate-repository.base.ts:79`](../../packages/patterns/src/repository/aggregate-repository.base.ts:79)、[`aggregate-repository.base.ts:109`](../../packages/patterns/src/repository/aggregate-repository.base.ts:109)）。commit 后、publish 前崩溃或 publish 失败仍会永久丢失对外事件。第三轮 P1-3 因而保持为本轮 P1-2。

### P2-1：真实连接证据改善，但 worker crash/replay 矩阵仍不成立

Requirement 1 与 3 已关闭对应证据缺口；Requirement 4 仍只是声明一个从未调用的 `worker1`，在任何 claim/projection/ack 状态发生前把注释称为“crash”，随后由 `worker2` 第一次处理队列（[`schedule-w5-real-concurrency.integration.test.ts:409`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:409)）。它没有证明 claimed 后、projection 写后、ack 前崩溃的持久恢复，也没有同 message 重投、乱序 source revision、retry/backoff/max-attempt 或 PowerSync 双 worker ownership。Requirement 2 的“零外部 delete event”仍监听错误字符串 `'ScheduleEventDeletedEvent'`，而不是 domain aggregate 实际发出的 `schedule:calendar-entry-deleted`（[`schedule-w5-real-concurrency.integration.test.ts:324`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:324)）。这些仍是验证矩阵缺口，P2 保持打开。

### 已关闭 UI fallback；P2-2：HTTP/IPC contract 仍是 surface 证据

UI 已不再使用 `?? 1`：version 只能来自调用参数或 store entry，二者都缺失时进入错误处理且不会调用 `service.deleteSchedule`（[`useScheduleCalendar.ts:61`](../../packages/app-vue/src/modules/schedule/composables/useScheduleCalendar.ts:61)）。实现层的 HTTP route/controller 和 IPC handler/adapter 也已接线，未发现继续伪造 version 的路径。

新增 HTTP spec 能证明 OpenAPI DELETE body schema 拒绝缺失 `expectedVersion`，并能接受一个带 conflict context 的通用 409 error envelope（[`schedule-event.routes.spec.ts:231`](../../packages/schedule/src/api/schedule-event.routes.spec.ts:231)、[`schedule-event.routes.spec.ts:250`](../../packages/schedule/src/api/schedule-event.routes.spec.ts:250)）。但名为“passes ... to controller.delete”的测试没有执行 Express handler，也没有断言 stub 被调用；它只是第三次读取 registry schema（[`schedule-event.routes.spec.ts:287`](../../packages/schedule/src/api/schedule-event.routes.spec.ts:287)）。409 使用共享通用 error schema，也没有证明 `CONFLICT` 时 `currentVersion` 是 required。IPC spec 同样只是读取 `index.ts` 后匹配两个正则，没有注册/调用 handler 并断言 `controller.delete(id, payload, requestContext)` 的实参（[`schedule-electron.surface.spec.ts:33`](../../packages/schedule/src/electron/schedule-electron.surface.spec.ts:33)）。实现可接受，但第三轮要求的 HTTP/IPC round-trip contract 证据仍未完成，计 P2。

### W0--W4 回归与实跑结果

- 未发现 W0--W4 核心业务实现直接回退；变更范围仍集中在 Schedule、Schedule contracts/http-client、数据库 schema/generated client、PowerSync schema 与 app-vue Schedule UI。
- `schedule:test --skip-nx-cache`：34 files、333/333 passed。
- `schedule:test:integration`：2 files、8/8 passed；其中 Requirement 1 的独立 PostgreSQL client 与 Requirement 3 的 better-sqlite3 均实际执行。
- `schedule:typecheck --skip-nx-cache`：通过。
- `app-vue`、`api`、`desktop` typecheck 以 `run-many --skip-nx-cache` 实跑通过；依赖链同时成功构建 Schedule 与相关 W0--W4 packages，`app-vue:build` 也成功。
- `memoflow:governance-check --skip-nx-cache`：通过；inventory check 报告 978 files，unflushed audit 报告 97/17/0。
- 本轮未单独重跑 api/desktop production build 与全量 lint；现有实跑足以判断未见 W0--W4 回退，但不能把未执行项写成本轮独立证据。

### 第四轮放行条件

1. 修复 PowerSync claim 参数顺序；success/retry/failed 全部按 `id + claimToken + processing` 条件更新、检查 affected row，并返回统一 typed lease-lost；用本轮已引入的真实 SQLite 覆盖双 worker、超时重领以及旧 token 的 success/retry/failed。
2. 为 Prisma 与 PowerSync 提供同事务 canonical domain-event outbox，并补有 claim/retry/idempotency/ack/重启恢复的 production publisher；覆盖 commit 后 publish 前崩溃、publish failure、重复投递与正确 delete event type。不要以在文件中加入可选 `publishAggregateEvents` 作为 durable delivery 的替代。
3. 把 Requirement 4 改为真实 crash-point/replay 测试，并补乱序、重投、retry/max-attempt、worker/client 并发矩阵。
4. 补执行真实 Express DELETE handler 与 Electron IPC handler 的 contract tests，断言缺失版本被拒、numeric/payload 版本精确传给 controller、409 conflict receipt 必须保留 `currentVersion`。
5. 修复后重跑 Schedule unit/integration/typecheck、app-vue/api/desktop typecheck/build、lint、inventory 与 governance，再做第五轮终审。

在上述条件关闭前，第四轮最终判定保持：**P0 = 0，P1 = 2，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第五轮审查

审查日期：2026-08-11  
审查范围：第四轮遗留的 P1-1 PowerSync rebuild ownership、P1-2 domain-event durable delivery、P2-1 crash/replay/乱序矩阵与 P2-2 HTTP/IPC round-trip；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第五轮仍不可放行：不要提交 W5，不进入 W6。**

本轮可以关闭第四轮的 P1-1 与 P2-2，也可以确认 P2-1 中的 rebuild worker claim 后崩溃回收和乱序 source revision 已形成真实 PostgreSQL 证据。Prisma 与 PowerSync 两端现在都有 domain-event outbox 的同事务写入、claim/retry/failed/completed 状态机、token ownership、退避和 module-owned runtime，且 application service 已没有 post-commit direct publish 路径。

但 P1-2 的核心 delivery idempotency 仍未成立。publisher 先调用外部 `eventBus.publish()`，随后才在独立数据库写中把记录改为 `completed` 并写 `published_at`；进程在两者之间崩溃时，持久化记录仍是 `processing + published_at = null`，超时重领后必然再次调用 event bus。outbox 的 `idempotency_key` 只用于插入去重，没有进入发布 envelope，也没有下游 durable inbox/dedup 证据。新增 Requirement 9 实际执行出了这次重复发布但没有断言次数，Requirement 10 则人为保留了只有成功 ack 后才存在的 `published_at`，不是同一 message 在真实 crash window 的重投。因此当前链路是可重启收敛的 **at-least-once delivery**，不是申报的幂等 durable delivery。

最终分级：**P0 = 0，P1 = 1，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 1 | domain-event publisher 的 publish/ack crash window 会重复发布，`idempotency_key` 未参与交付幂等。 |
| P2 | 1 | “同 message 真重投且幂等”的测试证据不成立；worker crash 与乱序部分已关闭。 |

### P1-1 已关闭：PowerSync rebuild claim/ack ownership

PowerSync rebuild claim 的 UPDATE 参数现在按 `claimToken, now, now, ...ids, threshold, threshold` 与 SQL 占位符顺序一致；claim 后回读限制为本次 `claim_token + processing`（[`schedule-powersync.repository.ts:434`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:434)）。success、retry、failed 分支均先按 `id + claimToken + processing` 读取 ownership，条件 UPDATE 后检查 `rowsAffected`，旧 token 不能改写新 owner 的状态（[`schedule-powersync.repository.ts:491`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:491)）。Prisma 对应分支也使用 token/status 条件并检查 affected row。

Requirement 7 使用真实 `better-sqlite3` adapter：worker A claim 后人为过期，worker B 重领，worker A 的旧 token ack 被拒且记录仍保持 `processing + token B`（[`schedule-w5-real-concurrency.integration.test.ts:395`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:395)）。第四轮 P1-1 关闭。

### P1-2 保持打开：状态机完整，但 publish/ack 间隙没有交付幂等

本轮结构性补齐是有效的：Prisma schema 与 PowerSync local-only schema 都具有 status、attempts、claim token/time、next attempt、published time、last error 和 unique idempotency key；Prisma/PowerSync 的 aggregate save/delete 会在 application service 的强制 transaction callback 内写 domain-event outbox。两 adapter 的 domain-event claim、ack、retry、failed 均带 token ownership 与 affected-row 检查，publisher 由 Schedule module 默认创建并随 module `start()/dispose()` 启停（[`schedule.module.ts:280`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:280)、[`schedule.module.ts:433`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:433)）。生产代码中唯一的 Schedule domain-event publish 调用也已收敛到 publisher，没有 application/repository direct publish 泄漏。

剩余 blocker 在外部副作用与数据库 ack 的原子边界。publisher 在第 85 行先 publish，在第 88 行以后才 ack；`published_at` 也只在 ack UPDATE 中写入（[`schedule-domain-event-publisher.ts:85`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:85)、[`schedule-powersync.repository.ts:679`](../../packages/schedule/src/server/infrastructure/adapters/powersync/schedule-powersync.repository.ts:679)）。如果 publish 已成功而进程在 ack 前崩溃，重启后看不到 `published_at`，会再次进入 `publishEvent()`。更关键的是，发布出的 `IDomainEvent` 只有 eventType、payload、aggregateId、occurredAt，outbox 的 `idempotencyKey` 没有传给 event bus 或消费者（[`schedule-domain-event-publisher.ts:124`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:124)）。所以数据库 unique key 只能防止同一业务命令生成两条 outbox 记录，不能防止同一记录被投递两次。

Requirement 9 正好复现了该窗口：测试先手工 publish、保持未 ack 状态、让 claim 超时，再由 publisher 重领并再次 publish；最终只断言事件类型 `toContain`，没有断言 `eventsB` 长度为 1（[`schedule-w5-real-concurrency.integration.test.ts:530`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:530)）。Requirement 10 先完整 publish+ack，再把 status 重置为 pending 而刻意保留 `published_at`；这只能证明“已成功 ack 的 completed 行被错误重排队”不会重发，不能证明 ack 前崩溃幂等（[`schedule-w5-real-concurrency.integration.test.ts:570`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:570)）。

关闭条件：明确采用 at-least-once 语义并让稳定的 outbox `idempotency_key` 随事件 envelope 传递，在所有产生副作用的消费者入口以 durable inbox/receipt 原子去重；或者使用能够以该 key 提供幂等 publish 的 delivery adapter。测试必须在真实“publish 成功、ack 未写入”状态重启并断言同一 key 的业务副作用只发生一次，不能靠预先存在的 `published_at` 跳过发布。

### P2-1 部分关闭：真 crash 与乱序已成立，同 message 幂等重投未成立

Requirement 4 现在确实先由 worker1 通过 production Prisma repository 把 rebuild row claim 为 processing，再丢弃 worker1、老化 claim，由 worker2 超时重领、重建投影并 ack completed；这已经是持久状态上的 claim-after-crash/restart 证据（[`schedule-w5-real-concurrency.integration.test.ts:282`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:282)）。Requirement 12 也把 source revision 1 的 stale outbox 放在 revision 2 之后交给真实 worker，断言 worker 传递 revision 1 且 PostgreSQL projection CAS 不回退 title/version/conflict；乱序项关闭（[`schedule-w5-real-concurrency.integration.test.ts:631`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:631)）。

但申报的“同 message 真重投（同 idempotency_key 幂等）”没有成立。现有 rebuild unit test 仍只是第二次扫描 completed 队列得到 0；domain-event Requirement 10 是保留 ack 后 `published_at` 的人工状态，Requirement 9 的真实 ack-before-crash 重投则实际再次 publish。该证据缺口与上面的 P1 根因一致，P2-1 仍保留一个测试矩阵项。

### P2-2 已关闭：HTTP/IPC handler round-trip 真实执行

HTTP 新测试使用 Express app 与 supertest 实际执行注册后的 DELETE handler：缺少 `expectedVersion` 返回 422；数值 4 精确传入 application port；409 envelope 保留结构化 `currentVersion`（[`schedule-event-routes-roundtrip.spec.ts:43`](../../packages/schedule/src/api/schedule-event-routes-roundtrip.spec.ts:43)）。IPC 新测试 mock `ipcMain.handle`、取得实际注册的 DELETE handler 并直接调用，分别证明 numeric 与 payload-object 两种输入都转为/保留 `{ expectedVersion }` 并连同 request context 传给 controller（[`schedule-electron-delete-contract.spec.ts:80`](../../packages/schedule/src/electron/schedule-electron-delete-contract.spec.ts:80)）。第四轮 P2-2 关闭。

### W0--W4 回归与第五轮实跑结果

- 未发现 W0--W4 核心业务实现直接回退；变更仍集中在 Schedule、共享 Schedule contracts/http-client、数据库生成物、PowerSync schema 与 app-vue Schedule UI。
- `schedule:test --skip-nx-cache`：37 files，343/343 tests passed。
- `schedule:test:integration --skip-nx-cache`：2 files，14/14 tests passed；真实 PostgreSQL 与 better-sqlite3 用例均执行。
- `schedule:build` 与依赖构建通过；`app-vue:build` 通过。
- `schedule:typecheck`、`app-vue:typecheck`、`desktop:typecheck` 通过；组合 Nx run 因环境中没有 `pnpm` 导致 `api:prisma:generate` 包装 target 未启动，API 改用 `tsc --noEmit -p apps/api/tsconfig.typecheck.json` 后通过。
- `schedule:lint --skip-nx-cache`：通过，49 warnings / 0 errors。
- `memoflow:governance-check --skip-nx-cache`：通过；unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- inventory check：通过，981 files（unit 849、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。

### 第五轮剩余项

1. 关闭 publish 成功、ack 前崩溃的重复副作用窗口：把稳定 `idempotency_key` 带到 delivery envelope，并在实际消费者以 durable inbox/receipt 去重，或采用具备同等保证的幂等 delivery adapter。
2. 将 Requirement 9 改为断言相同 idempotency key 的业务副作用严格一次；删除 Requirement 10 中依赖预置 `published_at` 的伪重投前提，并补至少一个真实消费者/receipt 的重启测试。
3. 修复后重跑 Schedule unit/integration/build/typecheck、app-vue/api/desktop typecheck/build、lint、inventory 与 governance，再做第六轮终审。

在上述条件关闭前，第五轮最终判定保持：**P0 = 0，P1 = 1，P2 = 1；W5 不可放行，不得提交，不进入 W6。**

---

## 第六轮审查

审查日期：2026-08-11  
审查范围：第五轮遗留的 P1-2 publish/ack at-least-once 交付幂等与 P2-1 同 message 幂等重投；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第六轮仍不可放行：不要提交 W5，不进入 W6。**

本轮 publisher 自身的状态机语义已经改为明确的 at-least-once：每次 claim 都发布，不再按 `publishedAt` 跳过；`ScheduleDomainEventOutboxDTO.idempotencyKey` 也被放入构造出的 `IDomainEvent`。新增单测确实断言 `publishedAt = null` 的重投会形成两次 publish、两次 envelope 使用同一稳定 key，最后 ack 为 completed。这部分方向正确。

但第五轮的关闭条件要求稳定 key **到达实际消费者入口**，并由消费者以 durable receipt/inbox 将回执和副作用原子去重。当前 production composition root 仍通过 `createEventBusAdapter(eventBus)` 发布，而该 adapter 只调用 `sender.send(event.eventType, event.payload)`，把 aggregateId、occurredAt 和新增的 `idempotencyKey` 全部丢弃；下游 `CrossPlatformEventBus` handler 只接收 payload。因此单测 spy 看到的完整 envelope 并不代表生产消费者能拿到 key（[`schedule.module.ts:287`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:287)、[`events/index.ts:106`](../../packages/patterns/src/events/index.ts:106)、[`cross-platform-event-bus.ts:36`](../../packages/utils/src/domain/cross-platform-event-bus.ts:36)）。此外该 bus 对异步 handler 只 fire-and-forget，handler 的 Promise 不会被 publish/ack 链路等待；publisher 可以在 durable consumer transaction 尚未完成甚至随后失败时 ack outbox（[`cross-platform-event-bus.ts:50`](../../packages/utils/src/domain/cross-platform-event-bus.ts:50)）。这不是端到端可恢复的 at-least-once 消费边界。

Requirement 10 也没有提供申报的 durable/atomic 证据。所谓 receipt store 是测试进程内的 `Set<string>`；所谓消费逻辑依次执行 `has`、`sideEffects += 1`、`add`，没有数据库、unique constraint 或 transaction（[`schedule-w5-real-concurrency.integration.test.ts:581`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:581)）。它既不能跨真实消费者进程重启持久化，也不能阻止两个并发 delivery 同时通过 `has` 后各执行一次副作用。仓库已有 `InboxReceipt` 的 `(id, consumer)` durable 主键以及将副作用与 receipt 放在同一 Prisma transaction 的实际消费者范式，但本轮 Schedule 测试和生产消费链路均未使用它（[`reliable-messaging.prisma:36`](../../packages/database/prisma/schema/reliable-messaging.prisma:36)、[`reminder-account-closed.consumer.ts:26`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:26)）。

最终分级：**P0 = 0，P1 = 1，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 1 | 稳定 key 在 publisher 内构造成功，但生产 event-bus adapter 丢弃 envelope 元数据，且没有实际消费者 durable receipt 原子去重。 |
| P2 | 1 | Requirement 10 用进程内 Set 冒充 durable receipt，未证明重启持久化、并发原子去重或真实消费者副作用恰好一次。 |

### P1-2 保持打开：publisher 局部 at-least-once 正确，生产消费边界仍不成立

`IDomainEvent` 已新增 optional `idempotencyKey`，publisher 的 `publishEvent()` 从稳定 outbox row 复制该 key；process loop 无 `publishedAt` skip，publish 成功后再 ack（[`domain-event.interface.ts:13`](../../packages/contracts/src/shared/domain-event.interface.ts:13)、[`schedule-domain-event-publisher.ts:70`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:70)、[`schedule-domain-event-publisher.ts:116`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:116)）。单测将 row 恢复成 `pending + publishedAt null` 后重新处理，断言 bus 共收到两次、key 相等、最终 completed（[`schedule-domain-event-publisher.spec.ts:194`](../../packages/schedule/src/server/application/services/__tests__/schedule-domain-event-publisher.spec.ts:194)）。所以“publisher 不再用 publishedAt 掩盖 ack 丢失”和“publisher 内部构造稳定 key”可确认。

关闭失败发生在下一跳。默认 Schedule module 把 publisher 接到共享 `createEventBusAdapter`；adapter 明确只发送 payload，不发送 envelope。共享 bus 又不等待异步 handler，异常只在 detached Promise 上记日志。这意味着：

1. 实际订阅者无法读取 `event.idempotencyKey` 来查 durable inbox；
2. publisher 的 `eventBus.publish()` 可以在消费者副作用/receipt transaction 完成前返回并 ack；
3. ack 后进程崩溃或异步 handler 失败时，outbox 已 completed，不会再投递，甚至不能保证消费者侧 at-least-once。

因此 P1-2 不是“只差更强测试”；production delivery seam 本身还未承载本轮新契约。第五轮 P1 保持打开。

### P2-1 保持打开：同 key 重投成立，幂等副作用证据不成立

Requirement 10 通过真实 Prisma outbox 制造 `pending + publishedAt null` 并得到第二次 publish，相同 key 的断言成立（[`schedule-w5-real-concurrency.integration.test.ts:604`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:604)）。但它先让第一次 publisher 完整 ack，再手工回写 row；第二次仍使用同一个 publisher、同一个 bus、同一个进程内 Set，并不是消费者重启。更重要的是，Set 与计数器没有任何原子边界，测试只覆盖两次串行函数调用。

Requirement 9 的手工第一次 publish 仍没有放入 `item.idempotencyKey`，最终也只断言 event type `toContain`，没有把真实 publish-before-ack 窗口与同 key consumer receipt 串起来（[`schedule-w5-real-concurrency.integration.test.ts:533`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:533)）。所以“同 key 被 publisher 重投”可确认，“同 message 在 durable consumer 上副作用恰好一次”仍未确认；P2-1 保持打开。

### W0--W4 回归与第六轮实跑结果

- 未发现 W0--W4 核心业务实现直接回退；本轮代码增量集中在共享 domain-event contract、Schedule publisher 与 W5 测试。
- `schedule:test --skip-nx-cache`：37 files，343/343 tests passed。
- `schedule:test:integration --skip-nx-cache`：2 files，14/14 tests passed；Requirement 10 当前断言通过，但上述 durable/atomic 证明缺口仍存在。
- `schedule:build --skip-nx-cache` 与依赖构建通过。
- `schedule:typecheck`、`app-vue:typecheck`、`desktop:typecheck` 通过；组合 run-many 中 `api:prisma:generate` 因环境没有 `pnpm` 未启动，随后 `tsc --noEmit -p apps/api/tsconfig.typecheck.json` 直接检查通过。
- `schedule:lint --skip-nx-cache`：通过，49 warnings / 0 errors。
- `memoflow:governance-check --skip-nx-cache`：通过；unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- inventory check：通过，981 files（unit 849、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）。

### 第六轮剩余项

1. 修复 production delivery seam，使完整 envelope（至少稳定 `idempotencyKey`）真实到达消费者；publisher 的 ack 必须与可确认的消费/持久投递语义一致，不能经由丢元数据且 fire-and-forget 的 payload-only adapter 冒充可靠交付。
2. 在一个实际产生持久副作用的消费者入口使用 durable `InboxReceipt`（或等价 unique receipt），把副作用与 receipt 放在同一数据库 transaction；若副作用是外部系统，则使用该 key 的幂等 adapter 或可恢复的二级 outbox。
3. 将 Requirement 9/10 串成真实 publish-before-ack 重投：重建 publisher 和 consumer 实例，只保留数据库 receipt；断言同 key 两次到达、receipt 唯一、副作用恰好一次，并补并发双 delivery 以证明原子性。
4. 修复后重跑本轮验证矩阵，再做第七轮终审。

在上述条件关闭前，第六轮最终判定保持：**P0 = 0，P1 = 1，P2 = 1；W5 不可放行，不得提交，不进入 W6。**

---

## 第七轮审查

审查日期：2026-08-11  
审查范围：第六轮遗留的 P1 production delivery seam / durable consumer receipt 与 P2 Requirement 10；同时核验共享 patterns/utils 契约回归；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第七轮仍不可放行：不要提交 W5，不进入 W6。**

本轮确实关闭了“adapter 丢弃 envelope 元数据”这一局部问题：`EventDeliveryMetadata` 已成为 utils/domain 的共享契约，`createEventBusAdapter.publish()` 会把 `aggregateId`、`occurredAt`、`idempotencyKey` 交给 sender，`CrossPlatformEventBus.send()/invokeHandler()` 也会把同一 metadata 作为 handler 第二参数传入。Requirement 10 实际使用共享 `eventBus + createEventBusAdapter`，而非 recording spy；本轮实跑时消费者确实两次收到同一个持久 outbox key（[`events/index.ts:81`](../../packages/patterns/src/events/index.ts:81)、[`events/index.ts:110`](../../packages/patterns/src/events/index.ts:110)、[`cross-platform-event-bus.ts:43`](../../packages/utils/src/domain/cross-platform-event-bus.ts:43)、[`schedule-w5-real-concurrency.integration.test.ts:581`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:581)）。因此第六轮 P1 中的 **metadata 丢失项关闭**。

但 durable consumer 与 publisher ack 的原子边界仍未建立。共享 bus 明确保持 fire-and-forget：异步 handler Promise 被 detached，失败只记日志，`send()` 立即返回；adapter 的 async `publish()` 因而立即 resolve，publisher 随即把 outbox ack 为 completed。消费者 receipt transaction 尚未完成甚至最终失败时，生产 outbox 已可能不可重投（[`cross-platform-event-bus.ts:67`](../../packages/utils/src/domain/cross-platform-event-bus.ts:67)、[`schedule-domain-event-publisher.ts:77`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:77)）。Requirement 10 自己也在 `processOutbox()` 返回后额外等待 50ms，直接证明 publisher 没有等待 durable consumer completion（[`schedule-w5-real-concurrency.integration.test.ts:610`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:610)）。第六轮要求的“ack 必须与可确认的消费/持久投递语义一致”没有关闭。

新增 receipt 表的 `idempotencyKey @unique` 是持久化去重基础，但测试消费者没有执行其注释所称的“自身事务”：它先在事务外 `findUnique`，再单独 `create` receipt，随后增加进程内计数器。receipt 与所谓副作用不在同一数据库事务，副作用本身也不持久；并发重复投递时，一个 `create` 会因 unique 冲突抛错并被 fire-and-forget bus 吞掉，而不是形成经过断言的幂等成功结果（[`schedule.prisma:175`](../../packages/database/prisma/schema/schedule.prisma:175)、[`schedule-w5-real-concurrency.integration.test.ts:587`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:587)）。仓库中该 receipt model 的唯一消费者也是这个测试内 handler，没有 production consumer 把实际持久副作用与 receipt 原子提交。故 durable/atomic consumer 的核心 P1 仍然打开。

最终分级：**P0 = 0，P1 = 1，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 1 | metadata 已到达真实 handler，但 fire-and-forget bus 允许 publisher 在 durable consumer 完成前 ack；现有 receipt 与副作用也未在同一真实事务。 |
| P2 | 2 | Requirement 10 仍是 ack 后改库 + 同实例 + 进程内副作用的串行测试；共享 utils/patterns 测试因本轮契约变更回归。 |

### P1-1 保持打开：metadata 已贯通，但可靠消费确认与原子副作用仍不存在

生产链路的 envelope 传递已确认，不再保留第六轮“实际消费者拿不到 key”的判断。`IEventSender.send()` 的第三参数是 optional，既有两参数 sender 在类型上仍兼容；Schedule production composition root 也继续通过统一 `createEventBusAdapter(eventBus)` 接入 publisher。相关 utils、patterns、Schedule、app-vue、API、desktop typecheck 本轮直接执行均通过，未发现这项签名扩展导致编译破坏。

问题在交付确认语义，而非 metadata 类型。`CrossPlatformEventBus` 的 ADR 契约本来就是通知式、发布方不等待订阅方；新增第二参数没有改变这一点。`ScheduleDomainEventPublisherService.publishEvent()` await 的只是 adapter 立即完成的 Promise，而不是 consumer receipt/side effect 的 transaction。随后 `markDomainEventOutboxProcessed()` 可以先于异步 consumer 完成。若进程在这两者之间退出，或 handler 的 Prisma 写失败，outbox 已 completed 且错误不会返回 publisher，事件会永久丢失。用测试侧 `setTimeout(50)` 只能让断言稳定，不能成为生产 acknowledgement。

测试消费者也没有原子事务。`findUnique -> create receipt -> sideEffects += 1` 是三个分离动作，其中 `sideEffects` 是 JavaScript 局部变量。unique constraint 只保证最多一条 receipt，不保证“receipt + 业务副作用”同 commit；它无法证明 receipt 已写但副作用前崩溃、或副作用完成但 receipt 写失败时能够收敛。并且 receipt key 未包含 consumer identity；若该通用表被多个副作用消费者共享，同一 envelope 的第一个消费者会阻止其他消费者各自记录回执。

关闭条件：不要把 fire-and-forget notification bus 当成可靠消费确认边界。publisher 必须 await 一个能够确认 durable handoff/consumer transaction 的 delivery adapter，确认后才 ack；或者先把事件原子投递到每个消费者自己的 durable inbox/二级 outbox。至少实现一个 production consumer，以 `(consumer, idempotencyKey)` 唯一键在真实 `$transaction` 中同时提交 receipt 与可查询的持久业务副作用，并把 duplicate-key 视为幂等成功；覆盖 handler/transaction 失败时 publisher 不 ack 或 durable handoff 可重放。

### P2-1 保持打开：Requirement 10 不是所申报的真实 crash/restart/atomic 矩阵

Requirement 10 已从进程内 `Set` 改为 PostgreSQL receipt，这一进展成立；它也确实经真实 adapter/bus 收到两次相同 key。但用例先让第一次 `publisher.processOutbox()` 正常完成并把 row ack 为 completed，再手工把该 row 改成 `pending + publishedAt null`。它没有在 `publishEvent()` 成功与 `markDomainEventOutboxProcessed()` 之间注入崩溃，也没有保留真实 crash 后的 `processing + claimToken` 状态并通过超时重领（[`schedule-w5-real-concurrency.integration.test.ts:610`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:610)、[`schedule-w5-real-concurrency.integration.test.ts:618`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:618)）。

所谓“重启”也复用了同一个 publisher、全局 bus、同一个已注册 consumer、`seenKeys` 和 `sideEffects`；没有丢弃 publisher/consumer 实例并仅保留数据库状态。用例只串行投递两次，没有并发双 delivery；因此不会暴露 `findUnique` 后两个 handler 同时进入、其中一个 unique 冲突的路径。最后的“副作用恰好一次”断言只是同进程计数器仍为 1，不是 durable side-effect row。第五/第六轮要求的真实 publish-before-ack fault injection、实例重建、DB-only receipt/side effect 和并发原子性证据均未形成。

关闭条件：在 delivery 返回后、publisher ack 前设置确定性 fault point；丢弃 publisher、bus 与 consumer 实例，老化/回收真实 claim 后用全新实例重投；只通过数据库断言相同 key 两次到达、每个 consumer receipt 唯一且持久副作用恰好一次。另用两个并发 delivery 证明 duplicate-key 路径是幂等成功而非被日志吞掉的未观察异常。

### P2-2（新增）：共享 utils/patterns 契约测试回归

本轮直接执行 `utils:test` 得到 13 files 中 1 file failed、122 tests 中 2 failed。无 metadata 的既有 `send(eventType, payload)` 现在也以 `(payload, undefined)` 调用 handler，破坏了现有“一参数调用”的精确契约断言（[`cross-platform-event-bus.spec.ts:18`](../../packages/utils/src/domain/__tests__/cross-platform-event-bus.spec.ts:18)、[`cross-platform-event-bus.ts:68`](../../packages/utils/src/domain/cross-platform-event-bus.ts:68)）。运行时多数单参数函数会忽略额外参数，因此暂不升为 P1，但共享包门禁并非全绿。

`patterns:test` 得到 4 files 中 1 file failed、21 tests 中 1 failed；canonical adapter surface 仍断言 payload-only sender 调用，未随新 metadata 合同更新（[`create-event-bus-adapter-dual.surface.spec.ts:16`](../../packages/patterns/src/events/create-event-bus-adapter-dual.surface.spec.ts:16)）。这项主要是契约测试未同步，但同样否定“patterns/utils 契约变更对其他消费者无破坏”和完整回归验证通过的申报。

关闭条件：为 metadata-present 与 metadata-absent 两条路径定义并测试明确的向后兼容行为；修复实现或同步 canonical surface test 后，utils/patterns 全套测试必须通过。建议无 metadata 时保持旧 handler 单参数调用，避免给所有既有订阅者引入可观察的额外 `undefined` 参数。

### W0--W4 回归与第七轮实跑结果

- 未发现 W0--W4 核心业务数据路径的直接回退；本轮关键增量位于共享 event-bus 契约、Schedule integration test 与 Prisma receipt schema。
- 但共享回归门禁不是全绿：utils 2 tests failed，patterns 1 test failed，因此不能判定“W0--W4/共享契约无回退”。
- `schedule:test`（直接 Vitest）：37 files，343/343 tests passed。
- `schedule:test:integration`（直接 Vitest，真实 PostgreSQL schema sync）：2 files，14/14 tests passed。
- `schedule:typecheck`、`utils:typecheck`、`patterns:typecheck`、`app-vue:typecheck`、API `tsc --noEmit -p apps/api/tsconfig.typecheck.json`、`desktop:typecheck`：均直接执行通过。
- `utils:test`（直接 Vitest）：12 files passed、1 failed；120 tests passed、2 failed。
- `patterns:test`（直接 Vitest）：3 files passed、1 failed；20 tests passed、1 failed。
- 当前环境 `pnpm` 不在 PATH，因此不能使用仓库约定的 `pnpm nx ...` 入口；本轮改由 `node node_modules/nx/dist/bin/nx.js run memoflow:governance-check --skip-nx-cache` 实跑并通过，依赖的 inventory 为 981 files（unit 849、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30），unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。build 与 lint 未在本轮独立重跑。`git diff --check` 另报告 Prisma 生成物中的既有/本轮 trailing whitespace 与 schema 文件末尾空行。

### 第七轮剩余项

1. 将 Schedule reliable delivery 从 fire-and-forget notification seam 移到可 await 的 durable handoff/consumer confirmation seam，确保 consumer 失败时 publisher 不会提前 completed。
2. 实现 production durable consumer：在真实事务中原子提交 consumer-specific receipt 与持久副作用，并正确处理并发 duplicate-key。
3. 重写 Requirement 10 为真实 publish-before-ack fault injection、publisher/consumer 实例重建、DB-only 断言及并发双 delivery；不得用 ack 完成后改库和进程内计数器替代。
4. 修复 utils/patterns 共享测试回归，重跑完整 Schedule、共享包、app-vue/API/desktop、build、typecheck、lint、governance 与 inventory 门禁。

在上述条件关闭前，第七轮最终判定保持：**P0 = 0，P1 = 1，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第八轮审查

审查日期：2026-08-11  
审查范围：第七轮遗留的 fire-and-forget ack 竞态、consumer receipt/effect 原子性、Requirement 10 重启语义及 utils/patterns 回归；同时复核 W0--W4 与申报验证门禁；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第八轮仍不可放行：不要提交 W5，不进入 W6。**

本轮实现已经把“ack 的时间顺序”向前推进了一步：`CrossPlatformEventBus` 会追踪异步 handler，`createEventBusAdapter.publish()/send()` 在 sender 提供 `awaitDrain()` 时等待 in-flight 集合清空；Schedule canonical composition root 确实使用 `createEventBusAdapter(eventBus)` 构造 production publisher。因此正常成功路径上，publisher 不再像第七轮那样在 consumer Promise 尚未结束时立即 ack（[`cross-platform-event-bus.ts:29`](../../packages/utils/src/domain/cross-platform-event-bus.ts:29)、[`events/index.ts:112`](../../packages/patterns/src/events/index.ts:112)、[`schedule.module.ts:287`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:287)）。utils 的 metadata-absent 路径也恢复为一参数调用，本轮实跑 utils 122/122、patterns 21/21，原 P2-2 共享契约回归关闭。

但这还不是可靠消费确认。异步 handler Promise 在加入 `inFlight` 前已经 `.catch()` 并把拒绝转换为 fulfilled，`awaitDrain()` 又使用 `Promise.allSettled()` 且不检查 rejected；同步 handler 抛错同样只记日志、不进入 in-flight。结果是 consumer transaction 失败时 adapter 仍正常 resolve，`ScheduleDomainEventPublisherService` 随后仍可把 durable outbox 标为 completed（[`cross-platform-event-bus.ts:40`](../../packages/utils/src/domain/cross-platform-event-bus.ts:40)、[`cross-platform-event-bus.ts:80`](../../packages/utils/src/domain/cross-platform-event-bus.ts:80)、[`schedule-domain-event-publisher.ts:68`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:68)）。第七轮 P1 的关闭条件要求“consumer 失败时 publisher 不 ack 或 durable handoff 可重放”，这一生产故障路径仍未关闭。

Requirement 10 的 `$transaction` 与数据库断言比第七轮改善，但证据仍不满足原关闭条件。事务内的 `findUnique + create` 把 receipt 与同一 receipt 行上的 `effectCount = 1` 一起提交，唯一约束可以保证该行最多一条；然而它没有独立的持久业务副作用，production 代码也没有任何 consumer 使用该 receipt model。并发两个首次 delivery 时，两边都可能先读到不存在，loser 的 `create` 会报唯一冲突；实现没有把该冲突转换为幂等成功，而上述 bus 又会吞掉失败并允许 ack。用例本身只串行重投，没有执行或断言这一并发路径（[`schedule-w5-real-concurrency.integration.test.ts:589`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:589)、[`schedule.prisma:179`](../../packages/database/prisma/schema/schedule.prisma:179)）。

最终分级：**P0 = 0，P1 = 1，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 1 | `awaitDrain()` 只等待结束却吞掉同步/异步 consumer 失败，production publisher 仍可在 durable transaction 失败后 ack；现有 receipt 用例也没有形成可确认的 production 副作用消费。 |
| P2 | 2 | Requirement 10 仍是 ack 后手工回写状态、仅重建 publisher、串行重投；申报的完整 lint 0 errors 无法复现。 |

### P1-1 保持打开：drain 后置了 ack，但没有建立成功确认语义

生产接线本身成立：event sender 的 `awaitDrain?` 保持可选兼容，adapter 在 publish/send 后调用它，Schedule module 的默认 publisher 使用该 adapter。对一个正常 resolve 的 async handler，`processOutbox()` 现在会先等待 consumer transaction，再执行 `markDomainEventOutboxProcessed()`；第七轮指出的纯时间竞态在 success path 已关闭。

可靠交付需要的却是“成功才 ack”，而不是“无论成功失败都等到结束再 ack”。当前 `tracked` Promise 的 catch 只写日志且不重新抛出，`awaitDrain()` 无法看到 rejection；同步异常更是在 `invokeHandler()` 内直接吞掉。若 Requirement 10 的 Prisma transaction 因数据库中断、约束错误或业务写失败而 reject，publisher 得到的仍是 resolved `eventBus.publish()`，随后把 outbox completed，事件不能再由 durable outbox 重投。这与代码注释声称的 at-least-once 交付边界相冲突，也没有任何测试覆盖“真实 bus handler reject -> publisher retry/failed、outbox 不 completed”。

receipt/effect 用例也只在 integration test 中注册临时 consumer；仓库内 `ScheduleEventConsumerReceipt` 的唯一读写方仍是 Requirement 10。把 `effectCount` 放在 receipt 自身的 create 中，只证明一行插入原子，不证明 receipt 与一个可查询、具有业务含义的独立持久副作用同 commit。并发 duplicate-key loser 未被识别为幂等成功，恰好会触发上述异常吞噬路径。因此第七轮 P1 不能关闭。

关闭条件：可靠 publisher 使用的 delivery seam 必须把至少一个 consumer 的失败反馈给 `publish()`，使 outbox 进入 retry/failed 而非 completed，同时仍允许通知式 bus 保持原有隔离语义；实现一个 production consumer，在同一真实事务中写 consumer-specific receipt 与独立持久业务副作用，显式把并发 unique conflict 视为幂等成功；补真实 bus 的 success、sync throw、async reject、transaction rollback 与并发 duplicate-key 测试，逐项断言 outbox 状态和数据库副作用。

### P2-1 保持打开：Requirement 10 仍不是真实 crash/restart/concurrency 证据

本轮确实新建了第二个 `ScheduleDomainEventPublisherService`，并改用数据库 receipt/effectCount 断言；这两点相对第七轮有进展。但第一次 `publisher.processOutbox()` 已经正常 publish 并 ack completed，测试随后通过直接 `updateMany` 把行改回 `pending`。这不是 publish 返回后、ack 写入前的确定性 fault point，也没有留下并老化真实 `processing + claimToken` 状态（[`schedule-w5-real-concurrency.integration.test.ts:609`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:609)、[`schedule-w5-real-concurrency.integration.test.ts:619`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:619)）。

所谓重启只替换 publisher；repository、全局 bus、adapter、已注册 consumer、`seenKeys` 与 Prisma client 全部复用。测试注释所称“process restart”与实际实例边界不符。两次 delivery 又是串行执行，没有并发首次投递，不能证明 `findUnique -> create` 的 unique-conflict loser 会幂等成功。数据库最终 receipt count/effectCount 为 1 是有效的串行去重证据，但不足以关闭第七轮要求的真实 fault/restart/concurrent atomic 矩阵。

关闭条件：在 `publishEvent()` 成功与 outbox ack 之间加入确定性 fault injection；丢弃 publisher、bus、adapter、consumer、repository/client 等进程内对象，仅保留数据库状态，老化真实 claim 后用全新组合重启；再用两个并发 delivery 覆盖首次 duplicate-key 竞争，并只以 outbox、consumer receipt 和独立业务副作用数据库状态判定收敛。

### P2-2：完整 lint 0 errors 的申报无法复现

本轮对 `schedule,utils,patterns,app-vue,api,desktop` 执行 Nx lint（排除 target dependencies）时，patterns 通过，utils/schedule/app-vue/desktop 均为 0 errors、仅既有 warnings；但 `api:lint` 得到 11 errors、1 warning，整条 lint 命令 exit 1。错误包括 `apps/api/src/main.ts` 的未使用 `AccountApiModule`、多处 Nx lazy/static module-boundary 违规，以及 AI adapter 测试的 `no-explicit-any`。`apps/api` 在当前 W5 working diff 中没有任何修改，因此没有证据把这些错误归因于 W5，也不据此判定 W0--W4 被本轮实现直接回退；但根脚本 `lint` 是 `run-many -t lint --all`，所以“lint 0 errors/完整门禁全绿”的申报不成立，不能作为放行证据。

关闭条件：使用仓库 canonical lint 入口在可用 pnpm 环境中实跑全量 lint 并达到 0 errors；若 API 错误确属既有基线，应在 W5 外先修复或提供已批准、可审计的 baseline，而不能把局部 W5-scope lint 结果表述为完整 lint 通过。

### 已关闭：utils/patterns 回归与 metadata-absent 兼容

`invokeHandler()` 现在仅当 metadata 有定义时传第二参数，无 metadata 的既有 `send(eventType, payload)` 保持单参数 handler 调用。patterns canonical surface 也已更新为 envelope metadata 契约，同时保留 payload-only `send` 路径。本轮直接实跑 `utils:test` 为 13 files、122/122 passed，`patterns:test` 为 4 files、21/21 passed；第七轮 P2-2 关闭。

### W0--W4 回归与第八轮实跑结果

- 未发现 W5 working diff 直接修改 W0--W4 核心模块；共享 event-bus 的 metadata-absent 行为已恢复，Schedule 与共享包测试、六项目 typecheck 均通过，未见由 W5 引入的 W0--W4 类型或单测回退。
- `schedule:test`：37 files、343/343 passed；`schedule:test:integration`：2 files、14/14 passed，真实 PostgreSQL schema sync 实际执行。
- `utils:test`：13 files、122/122 passed；`patterns:test`：4 files、21/21 passed。
- schedule/utils/patterns/app-vue/API/desktop 的底层 `tsc`/`vue-tsc` typecheck 全部 exit 0；Schedule production build 通过。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory 为 981 files（unit 849、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30），unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- 当前环境 `pnpm` 不在 PATH，首次 Nx run-many typecheck 在 API 的 `prisma:generate` 依赖调用 `pnpm` 时因命令不存在退出；随后按各 project target 声明直接执行底层 typecheck 均通过。该环境问题不计代码缺陷。
- 六项目 lint 不是全绿：API 11 errors；相关 API 文件不在 W5 diff，故记录为未复现申报/既有门禁问题，而不是 W5 直接回退。
- `git diff --check` 仍报告 `packages/database/prisma/schema/schedule.prisma` 文件末尾多余空行；不单独升级分级，但应在提交前清理。

### 第八轮剩余项

1. 让 reliable delivery seam 保留 consumer failure，并在 sync throw、async reject、transaction rollback 时阻止 outbox completed；不要以吞错后的 drain 作为 acknowledgement。
2. 提供 production consumer-specific receipt + 独立持久副作用的同事务实现，并把并发 duplicate-key 明确处理为幂等成功。
3. 将 Requirement 10 改为真实 publish-before-ack fault、完整实例重建、真实 claim 回收和并发双 delivery；仅依赖数据库断言收敛。
4. 在 canonical 全量 lint 上达到 0 errors，并重跑本轮完整验证矩阵。

在上述条件关闭前，第八轮最终判定保持：**P0 = 0，P1 = 1，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第九轮审查

审查日期：2026-08-12  
审查范围：第八轮遗留的 production consumer receipt/副作用原子性、并发 duplicate-key、Requirement 10 publish-before-ack/restart/reclaim 语义、`awaitDrain` 生产路径与 canonical lint；同时复核 W0--W4 回归；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第九轮仍不可放行：不要提交 W5，不进入 W6。**

R13 相比第八轮新增了真实 PostgreSQL 并发：两个 adapter 的 `publish()` 通过 `Promise.allSettled` 同时投递同一个 key，两个 handler transaction 会竞争 `ScheduleEventConsumerReceipt.idempotencyKey` 唯一约束，最终数据库确实只有一条 receipt，`effectCount` 合计也为 1（[`schedule-w5-real-concurrency.integration.test.ts:727`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:727)、[`schedule.prisma:179`](../../packages/database/prisma/schema/schedule.prisma:179)）。但 loser 的 `findUnique -> create` unique violation 没有被 consumer 转换为幂等成功，而是被共享 bus 吞掉；`effectCount` 仍是 receipt 自身字段，不是独立持久业务副作用；该 consumer 也仍只存在于 integration test。故第八轮的 production 原子消费与并发幂等关闭条件没有满足。

Requirement 10 的实现与第八轮所审版本实质相同：第一次 `processOutbox()` 已正常 publish 并 ack completed，随后测试手工把 outbox 改回 `pending`；所谓重启只新建 publisher，仍复用 repository、Prisma client、全局 bus、adapter、consumer 与 `seenKeys`。它没有 publish 成功/ack 前的确定性 fault injection，没有保留并超时回收真实 `processing + claimToken`，也没有在该场景执行并发双 delivery（[`schedule-w5-real-concurrency.integration.test.ts:607`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:607)）。因此“完整实例重建、真实 claim 回收、仅 DB 断言”的申报不能成立。

最终分级：**P0 = 0，P1 = 1，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 1 | production reliable-delivery seam 仍吞掉 consumer 失败；没有 production consumer-specific receipt + 独立持久副作用的同事务实现，并发 duplicate-key loser 也不是显式幂等成功。 |
| P2 | 2 | Requirement 10/R13 仍未形成申报的 fault/restart/reclaim/concurrent atomic 完整证据；canonical 全量 lint 仍失败且实际为 16 errors，不只 API 11 errors。 |

### P1-1 保持打开：R13 证明唯一约束获胜者，但没有可靠的 production 幂等消费

R13 的并发本身是真实的：两个 `createEventBusAdapter(eventBus)` 并发发布同 key，handler 内使用真实 `prisma.$transaction()`，最终唯一约束保证最多一条 receipt。该测试关闭了“是否真实并发触碰数据库唯一约束”这一狭窄证据缺口。

它没有关闭第八轮 P1。consumer 先 `findUnique`，不存在时再 `create`；并发首次 delivery 可以都读到不存在，loser 的 `create` 抛 Prisma unique violation。consumer 没有捕获该错误并在确认 winner 已提交后返回幂等成功（[`schedule-w5-real-concurrency.integration.test.ts:734`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:734)）。`CrossPlatformEventBus` 又在 async handler 的 `.catch()` 中只记录错误并把 tracked Promise 转成 fulfilled，`awaitDrain()` 使用 `Promise.allSettled()` 且不检查 rejection；同步 throw 同样只记录日志（[`cross-platform-event-bus.ts:40`](../../packages/utils/src/domain/cross-platform-event-bus.ts:40)、[`cross-platform-event-bus.ts:80`](../../packages/utils/src/domain/cross-platform-event-bus.ts:80)）。所以两个 `adapter.publish()` 都可 resolve，publisher 仍可 ack completed；R13 的 `Promise.allSettled` 也没有断言 delivery 结果，测试绿灯掩盖了 loser 失败。

原子副作用仍未实现。schema 只有 `ScheduleEventConsumerReceipt`，`effectCount` 就在 receipt 同一行；测试所谓 side effect 只是创建 receipt 时给该字段赋 1，并没有写一个独立、可查询且有业务含义的持久 projection/effect 表（[`schedule.prisma:175`](../../packages/database/prisma/schema/schedule.prisma:175)）。仓库内 `scheduleEventConsumerReceipt` 的运行时读写也只出现在 W5 integration test，没有 production consumer 被 module/runtime 装配。`effectWins` 是未断言的进程内计数器，且 `schedule:lint` 也把它报告为 unused（[`schedule-w5-real-concurrency.integration.test.ts:733`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:733)）。

生产 publisher 的接线路径仍成立：Schedule module 默认以 `createEventBusAdapter(eventBus)` 创建 publisher，adapter 在 sender 支持时调用 `awaitDrain()`（[`schedule.module.ts:287`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:287)、[`events/index.ts:112`](../../packages/patterns/src/events/index.ts:112)）。但这只证明“等待 handler 结束”，没有证明“handler 成功才 ack”。因此第八轮 P1-1 保持打开。

关闭条件：实现并装配至少一个 production consumer；在同一个真实事务中写 consumer-specific receipt 与独立持久业务副作用；并发 unique conflict 必须被显式识别为已消费成功，同时非 duplicate 的 transaction failure 必须反馈给 reliable publisher、阻止 completed 并进入 retry/failed。测试需覆盖 success、sync throw、async reject、transaction rollback、并发 duplicate loser 以及 outbox 状态。

### P2-1 保持打开：Requirement 10 仍是手工改库与局部实例替换，R13 不能代替 crash/restart 矩阵

Requirement 10 第一次调用 publisher 后已经断言 `publishedCount = 1`，即 publish 与 ack 都正常完成；接着直接 `updateMany` 把行重置为 `pending/claimToken=null/publishedAt=null`。这不是 publish 返回后、ack 写入前的 fault point，也绕过了 production claim 超时回收状态机（[`schedule-w5-real-concurrency.integration.test.ts:609`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:609)、[`schedule-w5-real-concurrency.integration.test.ts:619`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:619)）。

第二次仅构造新的 `ScheduleDomainEventPublisherService`，仍复用 `repo`、`passThroughLease`、`adapter`、全局 `eventBus`、已注册 consumer、`prisma` 和 `seenKeys`；这不是丢弃所有进程对象后从数据库恢复的完整实例重建。该用例仍以 `seenKeys` 长度和内容作关键 delivery 断言，故“无进程内计数器/仅 DB 断言”也不准确（[`schedule-w5-real-concurrency.integration.test.ts:628`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:628)）。

R13 确实补了一个独立的并发双 delivery 测试，但它没有与真实 outbox claim、publish-before-ack fault 和重启恢复组合，且如 P1-1 所述吞掉了 duplicate loser 的异常。故 Requirement 10 的原关闭条件不能由 R13 拼接替代，P2-1 保持打开。

关闭条件：在 publisher 的 `publishEvent()` 成功返回与 `markDomainEventOutboxProcessed()` 之间提供确定性 fault injection；保留真实 `processing + claimToken` 持久状态并经 timeout reclaim；销毁并重建 publisher、repository/client、bus/adapter、consumer 和 lease 相关实例；在恢复路径执行并发双 delivery，只用 outbox、receipt 与独立 effect 表断言最终收敛。

### P2-2 保持打开：API 错误不是 W5 引入，但 canonical lint 仍未达到 0 errors

本轮用仓库 canonical target 的等价直接入口 `nx run-many -t lint --all --parallel=5 --skip-nx-cache` 实跑 40 个项目，exit 1。失败并非只有 API：`api:lint` 为 11 errors / 1 warning，`account:lint` 为 3 errors / 13 warnings，`reminder:lint` 为 2 errors / 70 warnings，合计 16 errors。`git diff HEAD -- apps/api/src`、`git diff HEAD -- packages/account`、`git diff HEAD -- packages/reminder` 均为空，所以这些 error 可确认不由当前 W5 working diff 引入；“API 11 errors 属于既有文件”的归因成立，但“canonical 全量 lint 0”以及“全量失败只有 API”的申报不成立。

既有缺陷是否由另一个 cleanup 提交修复是工作流选择，但不能由本轮审查单方面把一个明确为 0 errors 的 canonical gate 改成 scope-only gate。仓库目前也没有提供已批准、可审计的 lint baseline/exemption。因此它不构成 W0--W4 被 W5 回退的证据，却仍不能作为 W5 放行绿灯；可在 W5 外先合入 cleanup，或由治理决策正式建立 baseline 后再复审。

### W0--W4 回归与第九轮实跑结果

- 未发现 W5 working diff 修改 W0--W4 的 Account/Reminder/Goal/Task/Notification 核心实现；API、Account、Reminder lint errors 所在 scope 相对 HEAD 均无 diff，因此不是 W5 直接回退。
- `schedule:test`：37 files、343/343 passed；`schedule:test:integration`：2 files、15/15 passed，真实 PostgreSQL schema sync 执行。新增 R13 会实际竞争唯一约束，但共享 bus 吞错使其绿灯不能证明 delivery 成功语义。
- `utils:test`：13 files、122/122 passed；`patterns:test`：4 files、21/21 passed。
- schedule/utils/patterns/app-vue/API/desktop 六项目 typecheck 全部通过。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory 为 981 files（unit 849、integration 19、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30），unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- canonical 全量 lint 失败：API 11、Account 3、Reminder 2，共 16 errors；Schedule 为 0 errors / 50 warnings，其中 R13 的 `effectWins` 未使用是本轮新增 warning。
- `git diff --check` 失败：`schedule.prisma` 文件末尾仍有多余空行，生成的 `packages/database/src/generated/prisma/index.d.ts` 还有多处 trailing whitespace。它们不是本轮主 blocker，但提交前必须清理或以可复现生成策略处理。
- 当前环境 `pnpm` 不在 PATH，因此上述命令通过仓库现有 `node_modules/.bin/nx` 直接执行相同 Nx targets；没有修改依赖或业务代码。

### 第九轮最小剩余项

1. 将 reliable delivery 与通知式 bus 的错误隔离语义分开：reliable seam 必须把 sync/async consumer failure 返回 publisher，阻止错误 ack；保留普通通知 bus 的既有隔离语义。
2. 实现并生产装配 consumer-specific receipt + 独立持久副作用的同事务消费；显式处理并发 duplicate-key loser 为幂等成功，并补 outbox/receipt/effect 的故障断言。
3. 重写 Requirement 10：确定性 publish-before-ack fault、真实 processing claim 超时回收、完整实例销毁重建、恢复路径并发双 delivery，最终只以数据库状态判断收敛。
4. 使 canonical 全量 lint 达到 0 errors，或先取得正式、可审计的 baseline/exemption；同时清理 `git diff --check` 问题并重跑完整门禁。

在上述条件关闭前，第九轮最终判定保持：**P0 = 0，P1 = 1，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第十轮审查

审查日期：2026-08-12  
审查范围：第九轮遗留的 production consumer 原子幂等、consumer failure 反馈、Requirement 10 crash/restart/reclaim/concurrent delivery 与 canonical 全量 lint；同时复核 W0--W4 回归；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第十轮仍不可放行：不要提交 W5，不进入 W6。**

本轮已经实质关闭第九轮 P1-1。Prisma production 组合根默认创建 `ScheduleEventDeliveryLogConsumer`，Schedule module 把它作为 module-owned runtime 随模块启停（[`prisma.ts:39`](../../packages/schedule/src/server/infrastructure/prisma.ts:39)、[`schedule.module.ts:302`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:302)、[`schedule.module.ts:443`](../../packages/schedule/src/server/infrastructure/schedule.module.ts:443)）。consumer 在同一个真实 `$transaction` 中写 `ScheduleEventConsumerReceipt` 与独立的 `ScheduleEventDeliveryLog`；两张表分别以 `idempotencyKey` 唯一约束收敛，串行重复与并发 unique loser 都显式返回成功，其他错误继续抛出（[`schedule-event-delivery-log.consumer.ts:28`](../../packages/schedule/src/server/infrastructure/consumers/schedule-event-delivery-log.consumer.ts:28)、[`schedule.prisma:179`](../../packages/database/prisma/schema/schedule.prisma:179)、[`schedule.prisma:193`](../../packages/database/prisma/schema/schedule.prisma:193)）。共享 bus 现在保留 sync throw 和 async reject，`awaitDrain()` 抛错后 adapter/publisher 不会 ack completed，而是进入既有 retry/failed 状态机（[`cross-platform-event-bus.ts:46`](../../packages/utils/src/domain/cross-platform-event-bus.ts:46)、[`events/index.ts:112`](../../packages/patterns/src/events/index.ts:112)、[`schedule-domain-event-publisher.ts:116`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:116)）。本轮直接实跑 production consumer integration matrix，success、sync throw、async reject、receipt/effect rollback、并发 duplicate loser 和 outbox 状态均通过。

第九轮 P2-2 也已关闭。canonical 全量 Nx lint 对 40 个项目执行成功；API、Account、Reminder 分别从原 11/3/2 errors 降到 0 errors，整个 workspace 为 **0 errors**（仍有不阻断的 warnings）。

但第九轮 P2-1 没有完全关闭。fault point 的位置、真实 `processing + claimToken` 保留、claim 老化后的 timeout reclaim、新 Prisma client/repository/bus/adapter/consumer/publisher 以及恢复路径并发双 delivery 都已形成；最终收敛也只依赖 outbox、receipt 与独立 delivery-log 数据库状态（[`schedule-domain-event-publisher.ts:99`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:99)、[`schedule-event-delivery-log-consumer.integration.test.ts:223`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:223)）。然而用例声称“完整实例重建”时，进程 1 和进程 2 都复用了文件级同一个 `passThroughLease`，publisher 2 仍以该对象构造，没有新建真实 `ScheduleLeaseCoordinator` 或持久化 lease repository（[`schedule-event-delivery-log-consumer.integration.test.ts:18`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:18)、[`schedule-event-delivery-log-consumer.integration.test.ts:240`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:240)、[`schedule-event-delivery-log-consumer.integration.test.ts:275`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:275)）。原 `schedule-w5-real-concurrency.integration.test.ts` 的 Requirement 10 同样只新建 publisher，并继续复用 repository、bus、adapter、consumer 和 `passThroughLease`（[`schedule-w5-real-concurrency.integration.test.ts:575`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:575)、[`schedule-w5-real-concurrency.integration.test.ts:610`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:610)）。因此本轮证明了 outbox claim reclaim，却没有证明生产 lease owner/lease repository 在进程重启后的重新获取与恢复；这与第九轮明确要求重建 lease 相关实例、以及本轮“新 publisher/repo/bus/adapter/consumer/lease”的申报不一致。

最终分级：**P0 = 0，P1 = 0，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 0 | production consumer、receipt/effect 原子提交、并发幂等成功及失败反馈均已关闭。 |
| P2 | 1 | crash/restart 用例仍复用假的 `passThroughLease`，未完成申报的 lease 实例与持久化 lease 协调器重建。 |

### P1-1 已关闭：production 幂等消费与失败反馈成立

- `createSchedulePrismaModule()` 默认装配 delivery-log consumer，API canonical module 会调用 `scheduleModule.start()`，consumer 的 start/stop 生命周期真实进入 production composition root（[`api/module.ts:73`](../../packages/schedule/src/api/module.ts:73)、[`prisma.ts:47`](../../packages/schedule/src/server/infrastructure/prisma.ts:47)）。
- consumer 缺少 key 会 fail-fast；receipt 与独立 effect 在一个 `$transaction` 内写入；已有 receipt 与 P2002 duplicate 均返回成功，非 duplicate 错误抛出。真实 PostgreSQL 并发用例断言两个 publish 均 fulfilled，最终各一条 receipt/effect（[`schedule-event-delivery-log.consumer.ts:33`](../../packages/schedule/src/server/infrastructure/consumers/schedule-event-delivery-log.consumer.ts:33)、[`schedule-event-delivery-log-consumer.integration.test.ts:193`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:193)）。
- `CrossPlatformEventBus.awaitDrain()` 收集同步错误和 rejected in-flight Promise；adapter await drain；publisher 只有 publish 成功才 ack，普通错误调用带 claim token 的 `markDomainEventOutboxProcessed()`，从而进入 retry/failed。utils 单测、publisher 单测及真实 Prisma integration test 分别覆盖 sync/async failure、最大尝试 failed、事务回滚和 outbox retry（[`cross-platform-event-bus.spec.ts:37`](../../packages/utils/src/domain/__tests__/cross-platform-event-bus.spec.ts:37)、[`schedule-domain-event-publisher.spec.ts:220`](../../packages/schedule/src/server/application/services/__tests__/schedule-domain-event-publisher.spec.ts:220)、[`schedule-event-delivery-log-consumer.integration.test.ts:84`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:84)）。

### P2-1 保持打开：delivery/reclaim 已证明，production lease restart 尚未证明

新用例相对第九轮有明确进展：`failAfterPublishBeforeAck` 在 `publishEvent()` 返回后、ack 前抛 `PublishBeforeAckFaultError`，该异常绕过 retry/failed catch，使 durable row 保持 `processing + claimToken + publishedAt null`；测试老化 `claimedAt` 后由新 client/repository/publisher 回收，并与第二 adapter 并发重投相同 key，最终数据库只有一个 receipt 和一个 delivery-log effect。这些子条件均可确认。

剩余缺口很窄但与申报直接冲突：恢复 publisher 仍拿到同一个无状态 `passThroughLease`，它的 `execute()` 永远返回 `acquired: true`，完全没有经过 `ScheduleLeaseCoordinator`、lease owner token、持久 lease row 或重启后的重新 acquire。由此无法证明真实生产 publisher 在旧 owner 崩溃后能重新获得 lease 并进入 claim timeout reclaim 路径。测试注释“rebuild ... lease”不能替代实例边界。

关闭条件：Requirement 10 的进程 1 与进程 2 分别新建独立 `ScheduleLeaseCoordinator` 和独立 Prisma-backed lease repository（连同各自 client），让进程 1 留下或释放符合崩溃模型的真实 lease 状态，并证明进程 2 按生产 lease 语义重新 acquire 后完成 outbox timeout reclaim；不得再复用 `passThroughLease`。最终继续只以 outbox、receipt、delivery-log 数据库状态判断业务收敛。

### P2-2 已关闭：canonical 全量 lint 为 0 errors

本轮使用仓库 canonical target 的直接 Nx 入口执行 `run-many -t lint --all --parallel=5 --skip-nx-cache`，40 个项目全部成功。重点复核结果为 API 0 errors / 1 warning、Account 0 errors / 13 warnings、Reminder 0 errors / 70 warnings；Schedule 0 errors / 49 warnings。warnings 不阻断当前 lint target，故第九轮 P2-2 关闭。

### W0--W4 回归与第十轮实跑结果

- `schedule:test --skip-nx-cache`：37 files、344/344 passed。
- `schedule:test:integration --skip-nx-cache`：3 files、21/21 passed；真实 PostgreSQL schema sync 执行。
- `utils:test`：13 files、126/126 passed；`patterns:test`：4 files、21/21 passed。
- W0--W4 相关 Account、Goal、Task、Reminder、Notification 单测均通过：分别 171、454、793、378、235 tests passed；未发现核心实现回退。
- canonical 全量 lint：40/40 projects 成功，0 errors。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory check 通过，982 files（unit 849、integration 20、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- `git diff --check` 仍因 Prisma 生成的 `packages/database/src/generated/prisma/index.d.ts` 存在 trailing whitespace 而失败。该生成物问题不升级为本轮 P2，但提交前应按可复现生成策略清理。
- 当前环境仍没有 `pnpm` 可执行文件，因此本轮通过 `node node_modules/nx/dist/bin/nx.js` 直接执行同一 Nx targets；未修改依赖或业务代码。

### 第十轮最小剩余项

1. 用两个全新 Prisma-backed `ScheduleLeaseCoordinator` / lease repository 替换 Requirement 10 中复用的 `passThroughLease`，真实证明 lease crash/restart/reacquire 后才进入 outbox timeout reclaim。
2. 重跑该 integration target、canonical lint、治理与 inventory，并清理提交前 `git diff --check` 问题。

在上述 P2-1 关闭前，第十轮最终判定为：**P0 = 0，P1 = 0，P2 = 1；W5 不可放行，不得提交，不进入 W6。**

---

## 第十一轮审查

审查日期：2026-08-12  
审查范围：第十轮唯一剩余项——Requirement 10 的真实 Prisma-backed lease crash/restart/reacquire 证据；同时复核已关闭的 production 幂等消费、fault matrix、canonical lint 与 W0--W4 回归；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第十一轮仍不可放行：不要提交 W5，不进入 W6。**

本轮确实在旧的 `schedule-w5-real-concurrency.integration.test.ts` Requirement 10 中把两个 publisher 的 lease 参数分别改成了新的 `ScheduleLeaseCoordinator(createScheduleLeasePrismaRepository(prisma))`，恢复调用也新增 `second.leaseAcquired === true`；实跑时该用例随 integration suite 通过（[`schedule-w5-real-concurrency.integration.test.ts:591`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:591)、[`schedule-w5-real-concurrency.integration.test.ts:613`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:613)）。这证明两个新 coordinator 均能经 Prisma repository 获取一个数据库 lease。

但第十轮的唯一关闭条件仍未满足。第十轮据以验收完整 publish-before-ack fault、真实 claim timeout reclaim、全实例重建、恢复并发双 delivery 与 DB-only 收敛的 `schedule-event-delivery-log-consumer.integration.test.ts` Requirement 10 完全未接入上述改动：process 1 与 process 2 仍都使用同一个文件级 `passThroughLease`，尽管注释继续声称重建了 lease（[`schedule-event-delivery-log-consumer.integration.test.ts:240`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:240)、[`schedule-event-delivery-log-consumer.integration.test.ts:265`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:265)、[`schedule-event-delivery-log-consumer.integration.test.ts:275`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:275)）。新增真实 lease 的旧用例又继续复用同一个 Prisma client、repository、bus、adapter 与 consumer，不是第十轮要求的完整进程实例重建。

此外，旧用例的 fault 并不会留下 crash 时的持久 lease 状态。`ScheduleLeaseCoordinator.execute()` 无论 task 成功还是抛出 `PublishBeforeAckFaultError`，都会在 `finally` 中调用 repository `release()` 删除当前 owner 的 lease（[`schedule-lease-coordinator.ts:106`](../../packages/schedule/src/server/infrastructure/lease/schedule-lease-coordinator.ts:106)）。因此第二次的 `leaseAcquired === true` 发生在第一个 lease 已被正常释放之后，只证明正常 release 后可重新 acquire；测试没有断言旧 owner lease row、没有老化 lease expiry，也没有证明新 owner能按 crash/TTL 语义接管。outbox claim 的 crash/reclaim 证据仍然成立，但不能替代缺失的 lease crash/restart/reacquire 证据。

最终分级：**P0 = 0，P1 = 0，P2 = 1。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 0 | production consumer、receipt/effect 原子提交、并发幂等成功及失败反馈继续维持关闭。 |
| P2 | 1 | 真实 lease 改到了不具备完整重启矩阵的旧 Requirement 10；完整矩阵仍复用 `passThroughLease`，且新增用例只覆盖正常释放后的再次获取。 |

### P2-1 保持打开：新增 Prisma lease 不是完整 crash/restart/reacquire 证据

- 有效增量：旧 Requirement 10 的两个 publisher 各自持有新的 coordinator 与 Prisma repository wrapper，恢复结果也明确断言 `leaseAcquired`，不再是该用例内的 `passThroughLease`。
- 未关闭项一：承担完整 fault/restart/concurrent/DB-only 矩阵的 production consumer integration Requirement 10 仍在两个进程位置传入同一个 `passThroughLease`。
- 未关闭项二：旧用例只重建 coordinator/publisher，仍复用 client/repository/bus/adapter/consumer，不符合“仅保留数据库状态”的进程重启边界。
- 未关闭项三：fault exception 会经过 coordinator 的 `finally` 正常释放 lease。恢复 acquire 没有与旧 lease 的 crash 遗留、TTL 老化或过期抢占发生关系。

关闭条件：在 `schedule-event-delivery-log-consumer.integration.test.ts` 的完整 Requirement 10 矩阵中，为 process 1 和 process 2 分别创建独立 Prisma client、lease repository 与 `ScheduleLeaseCoordinator`；模拟 crash 时必须保留旧 owner 的真实 lease row（不能走正常 `finally release`），断言新 owner 在未过期前获取失败、老化/推进 expiry 后获取成功，再由成功获取 lease 的新 publisher 完成 outbox timeout reclaim 与既有并发双 delivery，最终仍仅按 outbox、receipt、delivery-log 数据库状态判断业务收敛。

### 已关闭项维持情况

- **P1-1 维持关闭**：production Prisma composition root 默认装配 delivery-log consumer；receipt 与独立 effect 同事务提交；并发 duplicate loser 为显式幂等成功；sync/async/transaction failure 会反馈 publisher 并阻止错误 ack。本轮相关 production consumer integration matrix 继续通过。
- **原 P2 fault 矩阵主体维持关闭**：publish-before-ack 确定性 fault、真实 `processing + claimToken`、outbox claim timeout reclaim、consumer/repository/bus 重建、恢复并发双 delivery与 DB-only receipt/effect 收敛仍存在；只剩 lease crash/reacquire 子项。
- **P2-2 维持关闭**：canonical workspace lint 40/40 projects 成功，0 errors；warnings 按当前配置不阻断。

### W0--W4 回归与第十一轮实跑结果

- `schedule:test --skip-nx-cache`：37 files、344/344 passed。
- `schedule:test:integration --skip-nx-cache`：3 files、21/21 passed，真实 PostgreSQL schema sync；两个同名 Requirement 10 均通过，但测试通过不弥补上述 lease 语义缺口。
- `schedule:typecheck`、`schedule:build` 通过；`schedule:lint` 为 0 errors、49 warnings。
- canonical 全量 lint：40/40 projects 成功，0 errors。
- W0--W4 相关 Account、Task、Reminder、Notification 单测分别为 171/171、793/793、378/378、235/235 passed。Goal 在五项目并行首跑时一个动态 import 用例超过 10 秒，453/454、Nx 标记 flaky；隔离重跑 `goal:test` 后 454/454 passed，未形成可复现回归。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory check 通过，982 files（unit 849、integration 20、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- `git diff --check` 仍因 Prisma 生成物 `packages/database/src/generated/prisma/index.d.ts` 的 trailing whitespace 失败，与第十轮相同；未升级为新的 P2，但提交前仍应清理。
- 当前环境没有 `pnpm` 可执行文件，本轮通过 `node node_modules/nx/dist/bin/nx.js` 直接执行相同 Nx targets；未修改依赖或业务代码。

在上述 lease crash/restart/reacquire 证据补齐前，第十一轮最终判定为：**P0 = 0，P1 = 0，P2 = 1；W5 不可放行，不得提交，不进入 W6。**

---

## 第十二轮审查

审查日期：2026-08-12  
审查范围：第十一轮遗留的 crash lease 未过期阻挡/过期接管、Requirement 10 完整实例重建与重复 R10 澄清；同时复核 publish-before-ack fault、DB-only 收敛、canonical lint、治理门禁及 W0--W4 回归；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第十二轮仍不可放行：不要提交 W5，不进入 W6。**

本轮真实 lease 增量成立。新的 real-concurrency Requirement 10 在 publisher A 的 coordinator 经 `finally` 正常释放后，直接通过 Prisma-backed lease repository 写入 `dead-owner-crash` 的未过期 lease；重启 publisher 第一次执行明确得到 `leaseAcquired === false`，随后只老化该 owner 的 `expiresAt`，第二次执行得到 `leaseAcquired === true` 且 `publishedCount === 1`。这条路径确实证明了 crash 遗留 lease 的未过期阻挡与 TTL 过期接管，不再把“正常释放后的再次 acquire”冒充 crash takeover（[`schedule-w5-real-concurrency.integration.test.ts:604`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:604)、[`schedule-w5-real-concurrency.integration.test.ts:626`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:626)、[`schedule-w5-real-concurrency.integration.test.ts:631`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:631)）。第十一轮关于 crash 遗留 lease 本身的缺口关闭。

publish-before-ack 与数据库收敛也成立。fault 在 `await this.publishEvent(item)` 成功返回后、`markDomainEventOutboxProcessed()` 之前确定性抛出，并被专门分支原样重新抛出；测试观察到真实 `processing + claimToken`，没有手工改回 pending。第一次消费已提交 receipt 与独立 delivery log，各一条；恢复后最终只按数据库确认 receipt 1、delivery log 1、outbox completed，没有用 recording counter 作为收敛依据（[`schedule-domain-event-publisher.ts:97`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:97)、[`schedule-domain-event-publisher.ts:102`](../../packages/schedule/src/server/application/services/schedule-domain-event-publisher.ts:102)、[`schedule-w5-real-concurrency.integration.test.ts:594`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:594)、[`schedule-w5-real-concurrency.integration.test.ts:646`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:646)）。

但“完整重建”与“唯一 Requirement 10”两项申报均与当前代码不符。最终分级：**P0 = 0，P1 = 0，P2 = 2。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 0 | production consumer、receipt/effect 原子提交、并发幂等成功、失败反馈与真实 lease TTL 接管均维持成立。 |
| P2 | 2 | 新 R10 恢复链复用全局 bus 并保留旧 consumer；仓库仍有两个同名 R10，旧用例继续使用 `passThroughLease`。 |

### P2-1 保持打开：所谓 fully rebuilt chain 仍复用全局 bus 与崩溃前 consumer

注释声称新建了 client/repo/bus/adapter/consumer/lease，但实现把 `busA` 和 `busB` 都赋为同一个 imported global `eventBus`。更关键的是，`consumerA` 在整个恢复发布期间仍注册在这个 bus 上，直到所有恢复断言之后才 `stop()`；因此第二次发布同时进入崩溃前 consumer 与新 consumer，旧 Prisma client/consumer 并未从恢复边界消失（[`schedule-w5-real-concurrency.integration.test.ts:585`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:585)、[`schedule-w5-real-concurrency.integration.test.ts:614`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:614)、[`schedule-w5-real-concurrency.integration.test.ts:618`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:618)、[`schedule-w5-real-concurrency.integration.test.ts:654`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:654)）。最终 DB 行数可以证明全局共享订阅下的幂等收敛，却不能证明只保留数据库状态后由全新 bus/consumer 链完成恢复。

该用例还没有停止 `consumerB` 就断开 `rebuiltClient`；因为 `consumerB` 注册在文件级全局 bus 上，这会把引用已断开 client 的 handler 留给后续用例。当前 suite 仍通过，但这种生命周期泄漏进一步说明恢复实例没有被建立为隔离、可销毁的进程边界（[`schedule-w5-real-concurrency.integration.test.ts:620`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:620)、[`schedule-w5-real-concurrency.integration.test.ts:651`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:651)）。

关闭条件：publisher A 与 publisher B 分别使用独立 `CrossPlatformEventBus`、adapter、consumer、Prisma client/repository 和 Prisma-backed lease coordinator；第一次 fault 后停止/丢弃 A 链，再启动 B 链；恢复发布期间不得保留 A consumer 订阅；两条链均在 `finally` 中正确停止 consumer 并断开各自 client。现有 lease 未过期阻挡、过期接管、真实 processing claim 回收与 DB-only 收敛断言应保留。

### P2-2 保持打开：仓库并非“唯一 Requirement 10”，旧 R10 仍保留不完整的关闭声明

全仓搜索仍得到两个名为 `Requirement 10` 的 integration tests。`schedule-event-delivery-log-consumer.integration.test.ts` 的旧 R10 仍声称 `full instance rebuild`，却在 process 1 和 process 2 都传入文件级 `passThroughLease`；新的 real-concurrency R10 则补了真实 lease，却存在上述 bus/consumer 复用。当前不是一个完整 R10，而是两个同名用例分别覆盖部分条件，正是第十一轮要求消除的验收歧义（[`schedule-event-delivery-log-consumer.integration.test.ts:223`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:223)、[`schedule-event-delivery-log-consumer.integration.test.ts:240`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:240)、[`schedule-event-delivery-log-consumer.integration.test.ts:275`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:275)、[`schedule-w5-real-concurrency.integration.test.ts:577`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:577)）。

关闭条件：只保留一个 canonical Requirement 10，并让它在同一用例中覆盖 publish-before-ack fault、真实 processing claim、crash lease 未过期阻挡/过期接管、全 client/repo/bus/adapter/consumer/lease 重建、同 key 重投与 DB-only 收敛；旧用例若仍有独立价值，应按其实际职责重新编号/命名并删除 `full instance rebuild` 等超出证据的声明。

### 已关闭项维持情况

- **第十一轮 lease 缺口部分关闭**：直接创建的未过期 crash lease 会阻挡新 owner；老化 expiry 后由新 Prisma-backed coordinator 接管，且只有成功 acquire 后才进入 outbox reclaim。
- **fault 与收敛维持关闭**：fault point 确定地位于 publish 成功/ack 前；outbox 保持真实 processing claim；最终以 receipt、独立 delivery log 与 outbox 数据库状态判断收敛。
- **P1-1 维持关闭**：production consumer 的 receipt/effect 同事务、并发 duplicate 幂等成功和消费失败阻止错误 ack 的既有证据未回退。
- **lint 与 W0--W4 无回退**：本轮实跑 canonical lint、五个前置 wave 模块测试均通过。

### W0--W4 回归与第十二轮实跑结果

- `schedule:test --skip-nx-cache`：37 files、344/344 passed。
- `schedule:test:integration --skip-nx-cache`：3 files、21/21 passed，真实 PostgreSQL schema sync；两个同名 Requirement 10 均通过，但 passing tests 不补足上述测试边界与命名缺口。
- `schedule:typecheck`、`schedule:build` 通过；`schedule:lint` 为 0 errors、49 warnings。
- canonical 全量 lint：40/40 projects 成功，0 errors。
- W0--W4 相关 Account、Task、Reminder、Notification、Goal 单测分别为 171/171、793/793、378/378、235/235、454/454 passed；Nx 仍把 `goal:test` 标记为 flaky，但本轮没有失败或重试后缺项。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory check 通过，982 files（unit 849、integration 20、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30）；unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- `git diff --check` 仍因 Prisma 生成物 `packages/database/src/generated/prisma/index.d.ts` 的 trailing whitespace 失败，与前两轮相同；未升级为本轮新 P2，但提交前仍应按可复现生成策略清理。
- 当前环境没有 `pnpm` 可执行文件，本轮通过 `node node_modules/nx/dist/bin/nx.js` 直接执行相同 Nx targets；未修改依赖或业务代码。

在完整进程重建与重复 Requirement 10 两项关闭前，第十二轮最终判定为：**P0 = 0，P1 = 0，P2 = 2；W5 不可放行，不得提交，不进入 W6。**

---

## 第十三轮审查

审查日期：2026-08-12  
审查范围：第十二轮仅余的两个 P2——real-concurrency Requirement 10 的完整恢复实例边界，以及两条 R10 的命名/职责歧义；同时复核 W0--W4 回归、canonical lint、typecheck、build 与治理门禁；只审查并追加本文档，未修改业务代码，未 commit/push。

### 终审结论

**W5 第十三轮可放行：可以提交 W5，并进入 W6。**

第十二轮两个 P2 均已关闭。本轮未发现新的 P0、P1 或 P2。最终分级：**P0 = 0，P1 = 0，P2 = 0。**

| 级别 | 数量 | 摘要 |
| --- | ---: | --- |
| P0 | 0 | 未发现已经发生的不可逆数据破坏或生产安全事故。 |
| P1 | 0 | production consumer、原子 receipt/effect、失败反馈、并发幂等与 durable publisher 语义维持成立。 |
| P2 | 0 | real-concurrency R10 已形成隔离的 A/B 恢复链；两条 R10 已以 suite qualifier 明确区分。 |

### P2-1 已关闭：real-concurrency R10 的恢复链完成全量重建

- publisher A 使用独立 `new CrossPlatformEventBus()`、adapter、delivery-log consumer、Prisma repository 与 Prisma-backed `ScheduleLeaseCoordinator`；不再引用文件级全局 `eventBus`（[`schedule-w5-real-concurrency.integration.test.ts:585`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:585)、[`schedule-w5-real-concurrency.integration.test.ts:590`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:590)）。
- publish 成功、ack 前 fault 后，测试在创建恢复链之前先执行 `consumerA.stop()`；因此 A consumer 不再订阅恢复发布，上一轮“旧 consumer 与新 consumer 同时消费”的核心缺口消失（[`schedule-w5-real-concurrency.integration.test.ts:594`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:594)、[`schedule-w5-real-concurrency.integration.test.ts:614`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:614)）。
- publisher B 由新的 `PrismaClient`、`SchedulePrismaRepository`、独立 `new CrossPlatformEventBus()`、adapter、delivery-log consumer、Prisma-backed lease repository/coordinator 与 publisher 组成；恢复发布期间不复用 A 的 client/repository/bus/adapter/consumer/lease（[`schedule-w5-real-concurrency.integration.test.ts:617`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:617)、[`schedule-w5-real-concurrency.integration.test.ts:619`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:619)、[`schedule-w5-real-concurrency.integration.test.ts:624`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:624)、[`schedule-w5-real-concurrency.integration.test.ts:626`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:626)）。
- crash 遗留的未过期真实 lease 仍先阻挡新 owner；只老化 lease expiry 和真实 outbox claim 后，新 coordinator 才接管并重投同一事件。最终继续只以数据库中的 receipt 1、独立 delivery-log 1、outbox completed 判定收敛（[`schedule-w5-real-concurrency.integration.test.ts:628`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:628)、[`schedule-w5-real-concurrency.integration.test.ts:633`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:633)、[`schedule-w5-real-concurrency.integration.test.ts:644`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:644)、[`schedule-w5-real-concurrency.integration.test.ts:648`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:648)）。故 fault/restart/reclaim/at-least-once/DB-only 证据没有因重建而丢失。

静态清理观察项：恢复用例在断开 `rebuiltClient` 前没有显式 `consumerB.stop()`，并在末尾重复调用已停止的 `consumerA.stop()`（[`schedule-w5-real-concurrency.integration.test.ts:653`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:653)、[`schedule-w5-real-concurrency.integration.test.ts:656`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:656)）。这与上一轮不同：`consumerB` 仅注册在用例局部 bus，bus 不被全局单例或后续用例持有，恢复断言完成后也不再发布，因此不污染后续测试、不削弱恢复边界证据，不列为 P2；后续维护仍宜按 `consumerB.stop()` 再 `$disconnect()` 的顺序对称清理。

### P2-2 已关闭：两条 R10 已按 suite 职责区分

全仓精确搜索仍有两条含 `Requirement 10` 的测试，但名称不再相同：canonical real-concurrency 用例保持 `Requirement 10: Prisma publisher at-least-once ...`；另一条改为 `Requirement 10 (delivery-log suite, full-rebuild matrix): ...`（[`schedule-w5-real-concurrency.integration.test.ts:577`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-w5-real-concurrency.integration.test.ts:577)、[`schedule-event-delivery-log-consumer.integration.test.ts:223`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:223)）。

后者仍是有独立价值的 delivery-log consumer 矩阵：使用 fresh bus，重建 client/repository/bus/adapter/consumer，并在 timeout recovery 路径并发执行 publisher redelivery 与第二 adapter 的同 key delivery，断言两次 delivery 均成功且数据库只保留一条 receipt/effect（[`schedule-event-delivery-log-consumer.integration.test.ts:265`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:265)、[`schedule-event-delivery-log-consumer.integration.test.ts:277`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:277)、[`schedule-event-delivery-log-consumer.integration.test.ts:291`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:291)、[`schedule-event-delivery-log-consumer.integration.test.ts:297`](../../packages/schedule/src/server/infrastructure/adapters/prisma/schedule-event-delivery-log-consumer.integration.test.ts:297)）。它的 `passThroughLease` 只表示该 suite 不承担真实 lease takeover 的 canonical 证明；真实 lease 未过期阻挡/过期接管由已完成全量重建的 real-concurrency R10 单独且完整证明。名称 qualifier 已消除把两条局部证据误认成两个同名 canonical 验收项的歧义。

### W0--W4 回归与第十三轮实跑结果

- `schedule:test --skip-nx-cache`：37 files、344/344 passed。
- `schedule:test:integration --skip-nx-cache`：3 files、21/21 passed，真实 PostgreSQL schema sync；两条已区分名称的 R10 均实际执行通过。
- `schedule:typecheck`、`schedule:build` 依次重跑通过；最初并行启动二者时曾竞争同一个 `contracts/dist` 清理目录并出现 `ENOENT unlink`，顺序重跑后通过，确认是验证命令间的输出目录竞争，不是代码失败。
- `schedule:lint`：0 errors、49 warnings；canonical `run-many -t lint --all --parallel=5 --skip-nx-cache`：40/40 projects 成功，0 errors。
- W0--W4 相关 Account、Task、Reminder、Notification 测试分别为 171/171、793/793、378/378、235/235 passed。Goal 在五项目并行首跑时有一个动态 import 用例超过 10 秒超时，Nx 标记为 flaky；随后单独无缓存重跑为 86 files、454/454 passed。因此未发现 W0--W4 功能回退。
- `memoflow:governance-check --skip-nx-cache` 通过；inventory check 为 982 files（unit 849、integration 20、smoke 2、boundary-ipc 9、boundary-main 5、e2e 63、perf 4、governance 30），unflushed audit 为 97 files、17 event-emitting aggregates、0 exemptions。
- `git diff --check` 仍因既有 Prisma 生成物 `packages/database/src/generated/prisma/index.d.ts` 的 trailing whitespace 失败，与第十二轮相同；不是 r13 引入，也不重新打开 W5 P2。提交前仍建议按仓库可复现生成策略处理，避免提交脏白空格。
- 当前环境没有 `pnpm` 可执行文件，本轮使用 `node node_modules/nx/dist/bin/nx.js` 直接执行相同 Nx targets；未修改依赖或业务代码。

第十三轮最终判定：**P0 = 0，P1 = 0，P2 = 0；W5 可放行，可以提交 W5，并进入 W6。**
