---
tags:
  - audit
  - account
  - cloud-auth
  - w3
  - saga
description: W3 第一轮 AccountClosureCoordinator 骨架审查
created: 2026-08-10T00:00:00Z
---

# W3 第一轮审查：AccountClosureCoordinator 骨架

本次审查对照 [`2026-08-09-business-p0-elegant-fix-plan.md`](../plan/active/2026-08-09-business-p0-elegant-fix-plan.md) 的 W3、小节所列第一轮范围以及 W0--W2 审查标准，复核当前工作区未提交实现。审查只新增本文档；未修改业务代码，未 commit/push。

## 结论

**W3 第一轮不可放行，不能进入第二轮。**

当前产物具备 operation schema、Coordinator/port/repository 类型、顺序阶段推进、失败后按已保存 phase 重试、CloseAccountUseCase 委托以及 Cloud Auth 批量删除 session/device code 的局部骨架。account 168 个单测、2 个名为 integration 的用例、build/typecheck，以及 cloud-auth 16 个单测/typecheck 均通过。

但生产 API 与 Desktop 的 canonical account module factory 都没有注入 Coordinator 所需依赖，注册时会直接抛错；Cloud Auth 的批量撤销方法没有适配到 Account port。Coordinator 的仓储契约只有 find + 无条件 upsert，没有 owner/lease/version/条件阶段转换；并发执行会重复撤销、关户和发布事件，同一 idempotency key 还可跨 identity 返回他人的 receipt。`requested` 阶段并未实际阻断登录/refresh，cloud user/provider account 也未禁用或删除。account-closed 事件没有生产 publisher/outbox/consumer，Reminder、Notification、Repository pending work 不会因闭户取消。绿测未触达上述生产路径。

### 双视角判定

- **PM 视角**：用户点击注销后，当前 API/桌面模块可能在注册阶段不可用；即使绕过 wiring，用户仍可重新登录，关闭 receipt 也被 transport 丢弃，不能兑现“安全注销、明确退出、可查询进度”的产品承诺。
- **项目经理视角**：第一轮基础不变量尚未成立，第二轮若直接接 account-closed consumer，会建立在可重复发布、无 durable outbox、无生产 publisher 的不稳定接口上，扩大 W1/W2 pending work 误取消或漏取消风险。必须先关闭本轮 P0，再进入第二轮。

## 根因判定

根因不是缺少更多 `try/catch` 或测试数量，而是**把单进程内的顺序流程当成了可恢复 saga，却没有把并发所有权、条件状态转换、生产依赖装配和外部副作用幂等纳入持久化契约**：

1. operation repository 只暴露读取和整行保存（[`i-account-closure-operation-repository.ts:25`](../../packages/account/src/server/domain/repositories/i-account-closure-operation-repository.ts:25)），无法表达 `requested -> revoking`、owner claim、stale owner 拒绝或 publish completion CAS。
2. Prisma adapter 使用按 idempotency key 的无条件 `upsert`（[`account-closure-operation-prisma.repository.ts:45`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:45)），并发调用者可以互相覆盖并同时执行外部副作用。
3. Account、Cloud Auth、event publisher 分属不同 composition root，但没有一个生产根同时持有并装配它们；当前只在测试中手工拼装。
4. “闭户开始即阻断认证”和“闭户完成后取消异步投递”没有成为认证中间件/worker 的共享 durable policy，只存在注释或未接线 port。

因此这是架构与接线根因，不是局部实现遗漏。

## Findings

### P0-1：生产 composition root 未接线，API 与 Desktop account module 注册必然失败

`AccountModuleDependencies` 把 closure repository、revocation port、event publisher 和 coordinator 全部声明为可选（[`account.module.ts:17`](../../packages/account/src/server/infrastructure/account.module.ts:17)），`createAccountUseCases()` 在缺失这些依赖时抛错（[`account.module.ts:61`](../../packages/account/src/server/infrastructure/account.module.ts:61)、[`account.module.ts:75`](../../packages/account/src/server/infrastructure/account.module.ts:75)）。这能防止静默降级，但不是生产接线完成。

真实 API factory 仍只传 `accountRepository` 和 runtime contributions（[`prisma.ts:24`](../../packages/account/src/server/infrastructure/prisma.ts:24)、[`prisma.ts:30`](../../packages/account/src/server/infrastructure/prisma.ts:30)），`AccountApiModule.register()` 无额外依赖地调用它（[`module.ts:36`](../../packages/account/src/api/module.ts:36)、[`module.ts:39`](../../packages/account/src/api/module.ts:39)）。Desktop factory 同样只传 PowerSync account repository（[`powersync.ts:15`](../../packages/account/src/server/infrastructure/powersync.ts:15)、[`powersync.ts:21`](../../packages/account/src/server/infrastructure/powersync.ts:21)），Electron 注册直接调用该 factory（[`electron/index.ts:55`](../../packages/account/src/electron/index.ts:55)）。所以两个 canonical runtime 都会在创建 use cases 时抛出第 77 行错误。

全仓生产调用点只有 `createCloudAuth()` 返回的 `revokeAllSessions()`（[`cloud-auth.ts:44`](../../packages/cloud-auth/src/server/cloud-auth.ts:44)、[`cloud-auth.ts:176`](../../packages/cloud-auth/src/server/cloud-auth.ts:176)），没有将其包装为 `CloudAuthRevocationPort.revokeAll()` 并传给 account module，也没有 `AccountClosureEventPublisher` 的生产实现。当前只有测试手工构造这三个依赖。

**影响**：不是“关闭账号功能未启用”，而是 Account API/Desktop 模块整体无法注册，属于直接发布阻塞。

### P0-2：不是并发安全的可恢复 saga；幂等键也未绑定 identity

Coordinator 先按裸 idempotency key 查记录（[`account-closure-coordinator.ts:55`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:55)），若查到 succeeded/running 就直接返回该记录（[`account-closure-coordinator.ts:58`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:58)），从未校验 `existing.identityId === identityId`。数据库唯一约束也只是全局 `idempotencyKey @unique`（[`reliable-messaging.prisma:62`](../../packages/database/prisma/schema/reliable-messaging.prisma:62)）。不同 identity 使用同一 key 时，后者会收到前者包含 identityId/operationId 的 receipt，形成跨租户语义错误和信息泄露。

两个并发首次请求可同时 `find -> null`，随后都保存 running。Prisma `upsert` 的冲突方会更新同一行而不是失去执行权；两个调用者仍会继续调用 `revokeAll()`、`accountRepository.save()` 和 publisher。所有 phase 更新都是内存改字段后整行 save（[`account-closure-coordinator.ts:95`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:95) 至 [`account-closure-coordinator.ts:154`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:154)），没有 expected phase/status/version/owner 条件，也没有 lease expiry/heartbeat/fencing。

默认 key 由 identityId 和用户填写的 reason 拼接（[`close-account.use-case.ts:26`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:26)），而公开 schema 根本不接受 idempotency key（[`account-lifecycle.dto.ts:3`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:3)）。同一用户换一个注销原因就会创建第二个 operation；客户端重试也不能显式复用请求级 key。这不是严格的闭户幂等语义。

失败恢复虽能从 `revoking`/`closing` 继续，但有关键不确定窗口：外部撤销完成后、保存 `revoked` 前崩溃会重复撤销；account save 完成后、保存 `closed` 前崩溃会重读已关闭 account；publish 完成后、保存 succeeded receipt 前崩溃会重复发布。重复撤销本身可设计为幂等，但当前 publisher/downstream 没有稳定 event id/inbox 证明，整体仍不是可恢复 saga。

### P0-3：认证关闭生命周期未成立，`requested` 不阻断新登录/refresh，身份主体也未禁用

Coordinator 在 requested 阶段只把 phase 改为 revoking；“CloudAuth adapter checks closure operation state”只是注释（[`account-closure-coordinator.ts:95`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:95)），Cloud Auth options、hooks、session resolution 和 Better Auth route 中均没有 closure operation/account status guard（[`cloud-auth.ts:30`](../../packages/cloud-auth/src/server/cloud-auth.ts:30)、[`cloud-auth.ts:53`](../../packages/cloud-auth/src/server/cloud-auth.ts:53)、[`cloud-auth.ts:145`](../../packages/cloud-auth/src/server/cloud-auth.ts:145)）。认证路由在业务 auth middleware 之前直接挂载（[`bootstrap.ts:81`](../../apps/api/src/bootstrap.ts:81)、[`bootstrap.ts:102`](../../apps/api/src/bootstrap.ts:102)），所以 account status middleware 不能阻止 sign-in/refresh endpoint 自身创建新 session。

`revokeAllSessions()` 只删除 `cloudAuthSession` 和 `cloudAuthDeviceCode`（[`cloud-auth.ts:176`](../../packages/cloud-auth/src/server/cloud-auth.ts:176)）；没有禁用/删除 `cloudAuthUser` 或 `cloudAuthProviderAccount`，也没有 tombstone/denylist。用户凭据仍存在，可再次登录。`CloudAuthRevocationPort` 的名字和注释也只承诺 session（[`cloud-auth-revocation.port.ts:5`](../../packages/account/src/server/application/ports/cloud-auth-revocation.port.ts:5)），没有表达“撤销认证能力”与“删除用户数据/PII”的两个独立政策动作。

API 业务请求在 account 真正变为非 Active 后确实会被 middleware 拒绝（[`auth-middleware.ts:34`](../../apps/api/src/shared/infrastructure/http/middlewares/auth-middleware.ts:34) 至 [`auth-middleware.ts:41`](../../apps/api/src/shared/infrastructure/http/middlewares/auth-middleware.ts:41)），这是局部 fail-closed；但它发生在 closing 之后，不能覆盖 requested/revoking 窗口，也不能阻止认证端点重新发 session。当前会话客户端也没有明确 signed-out receipt/event 接线。

### P0-4：account-closed 没有 durable 生产发布与 consumer，异步读取/新投递未 fail-closed

Coordinator 依赖抽象 `AccountClosureEventPublisher`（[`account-closure-event-publisher.port.ts:11`](../../packages/account/src/server/application/ports/account-closure-event-publisher.port.ts:11)），但全仓没有生产实现或装配。与此同时，`Account.close()` 已产生 `account:closed` domain event（[`account.ts:164`](../../packages/account/src/server/domain/aggregates/account.ts:164)、[`account.ts:175`](../../packages/account/src/server/domain/aggregates/account.ts:175)），Prisma repository 保存后立即发布进程内 aggregate events（[`account-prisma.repository.ts:99`](../../packages/account/src/server/infrastructure/adapters/prisma/account-prisma.repository.ts:99)），Coordinator 又在 closed phase 调用另一 publisher（[`account-closure-coordinator.ts:134`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:134)）。若未来把该 port 直接接到同一 event bus，会双发；若进程在 publish 后、operation succeeded 保存前崩溃，重试还会再次发布。

全仓没有 Reminder、Notification、Repository 对 `account:closed` 的生产订阅/Inbox receipt，也没有 worker 在 claim/投递前查询 account/closure operation。当前这些模块只按自身 Active/pending 状态工作，已排队 reminder/notification/repository work 会继续执行。第二轮要求的 pending work 取消消费尚未实现，第一轮也没有提供可安全消费的 durable、唯一事件。

**影响**：闭户后 HTTP 业务访问的局部门禁不能替代后台 worker fail-closed；用户仍可能收到关闭后的通知或触发 repository work。

### P1-1：receipt 没有贯穿 transport，running 被当作成功且客户端看不到 operation

CloseAccountUseCase 对 `running` receipt 直接返回 `ok(receipt)`（[`close-account.use-case.ts:29`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:29) 至 [`close-account.use-case.ts:40`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:40)）。Controller 随后丢弃 receipt 并固定序列化 `data: null`（[`account.controller.ts:79`](../../packages/account/src/server/transport/account.controller.ts:79) 至 [`account.controller.ts:91`](../../packages/account/src/server/transport/account.controller.ts:91)），契约也仍定义 `CloseAccountRes = null`（[`account-lifecycle.dto.ts:9`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:9)），client service 返回 `Result<void>`（[`account-client-service.ts:97`](../../packages/account/src/application-client/services/account-client-service.ts:97)）。

因此“running -> 进行中”和 retryable receipt 只存在服务端内部，客户端无法区分已完成、进行中或拿 operationId 轮询；也没有当前会话 signed-out 状态。测试只直接调用 use case，未经过 controller/HTTP/client contract。

### P1-2：所谓 integration 测试全部是内存 fake，未驱动真实仓储/Cloud Auth/组合根

`account-closure-coordinator.integration.spec.ts` 自建 `InMemoryAccountRepository`（[`account-closure-coordinator.integration.spec.ts:11`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:11)）、使用 `InMemoryAccountClosureOperationRepository`（[`account-closure-coordinator.integration.spec.ts:52`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:52)），revocation port 只是递增本地变量（[`account-closure-coordinator.integration.spec.ts:61`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:61)），publisher 只是 push 数组（[`account-closure-coordinator.integration.spec.ts:72`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:72)）。第二个恢复用例仍是同一组 fake。

它没有触达 `PrismaAccountClosureOperationRepository`、数据库唯一约束/并发 upsert、真实 `cloudAuthSession.deleteMany()`、Better Auth login/refresh、`AccountApiModule`、Electron module、HTTP middleware、真实 event bus/outbox 或跨进程恢复。unit 测试也只验证 mock 被调用（[`account-closure-coordinator.spec.ts:61`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.spec.ts:61)）。2/2 通过不能作为生产 saga 接线证据。

缺失的关键测试至少包括：同 key 双并发 only-one-owner；同 key 不同 identity 拒绝；Prisma 中间 phase 崩溃与重启恢复；publish 后 crash 不重复消费；真实多 session/bearer/device code 失效；闭户后 login/refresh 拒绝；API/Desktop production composition 启动；后台 pending work fail-closed。

### P1-3：撤销认证与删除/保留数据没有独立审计政策

operation 只记录 phase/status/error/receipt（[`reliable-messaging.prisma:61`](../../packages/database/prisma/schema/reliable-messaging.prisma:61)），receipt 不记录 revoked session 数、认证主体动作、PII 清理结果、retention policy/version 或审计 actor。Coordinator 把事件 reason 固定成 `User requested closure`（[`account-closure-coordinator.ts:140`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:140)），没有使用请求中的 reason/feedback，也没有明确哪些数据保留、匿名化或删除。

Cloud Auth 实现只做 session/device code 删除，业务 Account 只改 `Deactivated`；这可以是合理的分阶段策略，但代码和 receipt 没有把“立即安全撤权”与“受政策约束的数据删除/保留”建模成两个可审计阶段，无法核验隐私政策承诺。

### P2-1：phase/status 为自由字符串，缺少数据库级状态约束与恢复元数据

Prisma schema 的 phase/status 是任意 String（[`reliable-messaging.prisma:66`](../../packages/database/prisma/schema/reliable-messaging.prisma:66)），没有 enum/check、version、owner token、lease expiry、next retry/dead-letter 或 event id。TypeScript union（[`i-account-closure-operation-repository.ts:1`](../../packages/account/src/server/domain/repositories/i-account-closure-operation-repository.ts:1)）不能保护数据库被旧进程、手工操作或并发写入非法组合。该问题本身为 P2，但缺少 owner/CAS 已在 P0-2 构成放行阻塞。

### P2-2：默认幂等键包含自由文本 reason，协议和隐私边界不稳健

默认 key 直接拼接用户 reason（[`close-account.use-case.ts:26`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:26)），既可能包含 PII/敏感自由文本并长期落在 unique index，也受空白、语言、文案变化影响。operation key 应是稳定、不透明、由协议明确携带或按 identity/closure generation 规范化生成的标识；reason 应进入独立受政策管理的审计字段，而不是兼任幂等键组成部分。

## 审查要点逐条核验

| 审查项 | 当前证据与判断 | 结论 |
| --- | --- | --- |
| 可恢复 saga：阶段条件写 + 恢复点 | 有阶段保存和 failed 后重试；无条件 transition、owner/lease/fencing，外部副作用后均有 crash 窗口。 | **未满足（P0）** |
| 幂等键严格语义 | DB key unique；但不校验 identity、并发首次执行可双跑、公开请求不携带 key、默认 key 受 reason 影响。 | **未满足（P0）** |
| 撤销认证 vs 删除数据 | 只删除 session/device code；没有 user/provider 禁用、denylist、PII 删除/保留阶段或审计 receipt。 | **未满足（P0/P1）** |
| production composition root | API/Desktop factory 都不传所需依赖并会抛错；Cloud Auth 方法无 port adapter；publisher 无实现。 | **未满足（P0）** |
| CloseAccountUseCase 走 Coordinator | use case 确实只依赖并调用 Coordinator；但 production 无法构造，transport 丢弃 receipt。 | **局部满足，端到端未满足** |
| 闭户后业务读取 fail-closed | API middleware 在 Account 非 Active 后拒绝业务请求；requested/revoking 窗口和 auth routes 不受保护，模块当前还无法启动。 | **部分满足，核心未满足（P0）** |
| 全会话撤销 | Cloud Auth 方法会 deleteMany session/device code；没有生产接线，没有真实多会话测试，新登录仍可恢复 session。 | **局部实现，未满足（P0）** |
| 新投递/pending work fail-closed | 无 durable account-closed publisher/consumer，无 worker closure guard。 | **未满足（P0）** |
| 测试驱动真实仓储路径 | 新增 2 个 integration 用例均为内存仓储和手写 fake port。 | **未满足（P1）** |
| account 模块既有测试不回退 | account 24 files / 168 tests 全通过，build/typecheck 通过。 | **测试回归未见回退；不能覆盖 production wiring** |

## 本地验证

当前 shell 没有 `pnpm` 可执行文件，因此使用仓库本地 Nx CLI 等价执行：

- `node node_modules/nx/dist/bin/nx.js run account:test --skip-nx-cache`：**24 files / 168 tests passed**。
- `node node_modules/nx/dist/bin/nx.js run account:test:integration --skip-nx-cache`：**1 file / 2 tests passed**；两项均为内存 fake，不是 Prisma/Cloud Auth 集成。
- `node node_modules/nx/dist/bin/nx.js run account:typecheck --skip-nx-cache`：**passed**。
- `node node_modules/nx/dist/bin/nx.js run account:build --skip-nx-cache`：**passed**。
- `node node_modules/nx/dist/bin/nx.js run cloud-auth:test --skip-nx-cache`：**4 files / 16 tests passed**。
- `node node_modules/nx/dist/bin/nx.js run cloud-auth:typecheck --skip-nx-cache`：**passed**。

首次并行执行 cloud-auth typecheck 时，多个 Nx target 同时清理/生成 `contracts/dist`，出现一次 `ENOENT unlink`；改为串行重跑后通过。Nx 将 `contracts:build` 标记为 flaky。该并行构建竞争不作为 W3 finding，也不改变上述静态生产接线结论。

## 分级与放行决定

| 级别 | 数量 | 阻塞摘要 |
| --- | ---: | --- |
| P0 | 4 | API/Desktop composition root 不可启动且无真实 port wiring；saga 无条件写/并发所有权且 key 可跨 identity；requested 不阻断认证且 cloud identity 可重新登录；无 durable account-closed 发布/消费，后台 pending work 不 fail-closed。 |
| P1 | 3 | receipt 未贯穿 transport/客户端；integration 测试全部为内存 fake；认证撤权与数据删除/保留缺少独立审计政策。 |
| P2 | 2 | schema 状态与恢复元数据约束不足；默认 key 包含自由文本 reason。 |

**终审判定：W3 第一轮不可进入第二轮。**

进入第二轮前至少应完成：

1. 由真实 API composition root 同时装配 Prisma closure repository、Cloud Auth revocation/disable adapter 和 durable event publisher；Desktop 若不拥有 cloud closure capability，应明确走远端关闭 port，而不是构造缺依赖的本地 Coordinator。
2. 将 repository 契约改为 identity-bound create/claim 和 expected-phase/version/owner 的条件转换，定义 running 接管/超时策略；同 key 不同 identity 必须拒绝，并用真实数据库并发测试证明 only-one-owner。
3. 在 requested 落库后立即让 sign-in、refresh、bearer/device flow 共享 closure deny policy；撤销全部 session/device code，并按明确政策禁用 cloud user/provider credential，防止重新登录。
4. 让 account close、operation phase 和 account-closed outbox 具备可证明的事务/幂等边界；事件携带稳定 eventId/operationId，下游使用 Inbox receipt，避免 domain event + publisher 双轨。
5. 让 closure receipt/idempotency key 进入公开契约，客户端可得到 running/succeeded/failed 与 signed-out 结果；补 API/Desktop composition、Prisma recovery、真实 Cloud Auth 多会话和 login/refresh 负向集成测试。

完成上述第一轮基础后，再实施第二轮的 `account-closed` consumer，并分别对 W1 Reminder、W2 Notification、Repository pending work 做 identity-scoped 条件取消、重复消费幂等和关闭后新 claim 拒绝。当前不应在非 durable publisher 上继续堆 consumer。

## 第二轮审查

本轮对照上文第一轮 P0/P1/P2 逐项复核当前工作区实现，并把“代码存在”“生产接线”“真实数据库测试”分开判断。结论是：第二轮补齐了 API lane 装配、identity-bound 唯一键、首个阶段 CAS、Cloud Auth 用户禁用、HTTP receipt 和 durable 表结构等重要基础，但第一轮 4 个 P0 均未完整关闭，当前仍不能把 W3 判定为完成，也不能按计划进入 W4。

### 核验表

| 第一轮项 | 第二轮生产证据 | 核验结论 |
| --- | --- | --- |
| **P0-1 production composition root** | API 组合根创建 Prisma closure repo，并把同一 repo 作为 `closureChecker` 注入 Cloud Auth（[`main.ts:116`](../../apps/api/src/main.ts:116)、[`main.ts:117`](../../apps/api/src/main.ts:117)、[`main.ts:133`](../../apps/api/src/main.ts:133)）；Account API factory 收到真实 `cloudAuth`（[`main.ts:200`](../../apps/api/src/main.ts:200)），Prisma factory 默认装配 repo、revocation adapter、outbox publisher（[`prisma.ts:47`](../../packages/account/src/server/infrastructure/prisma.ts:47) 至 [`prisma.ts:58`](../../packages/account/src/server/infrastructure/prisma.ts:58)）。但 Desktop 先调用远端关闭，再继续调用本地 PowerSync module 的 close use case（[`electron/index.ts:121`](../../packages/account/src/electron/index.ts:121) 至 [`electron/index.ts:126`](../../packages/account/src/electron/index.ts:126)），而该本地 coordinator 明确只会抛“必须通过 Cloud API”（[`account.module.ts:80`](../../packages/account/src/server/infrastructure/account.module.ts:80) 至 [`account.module.ts:87`](../../packages/account/src/server/infrastructure/account.module.ts:87)）；测试用 mock 把这条真实失败路径替换成成功（[`electron/index.spec.ts:139`](../../packages/account/src/electron/index.spec.ts:139) 至 [`electron/index.spec.ts:169`](../../packages/account/src/electron/index.spec.ts:169)）。此外 `account:build` 因 worker 导入不存在的 `@memoflow/reminder/server`、`@memoflow/notification/server` 导出而失败（源引用见 [`account-closed.worker.ts:2`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:2)、[`account-closed.worker.ts:3`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:3)）。 | **部分修复，仍为 P0**：API lane 已真实接线；Desktop closure 生产路径自相矛盾且 account 包不能构建。 |
| **P0-2 并发安全 saga / identity-bound 幂等** | 联合唯一键已变为 `@@unique([identityId, idempotencyKey])`（[`reliable-messaging.prisma:84`](../../packages/database/prisma/schema/reliable-messaging.prisma:84)），lookup 也使用联合键（[`account-closure-operation-prisma.repository.ts:69`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:69) 至 [`account-closure-operation-prisma.repository.ts:81`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:81)）；`updatePhaseCAS()` 使用 `updateMany` 的 `id + identityId + expectedPhase` 条件并以 `result.count > 0` 判定（[`account-closure-operation-prisma.repository.ts:144`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:144) 至 [`account-closure-operation-prisma.repository.ts:167`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:167)），首个 `requested -> revoking` 冲突也会重取（[`account-closure-coordinator.ts:134`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:134) 至 [`account-closure-coordinator.ts:153`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:153)）。但 failed retry 先用无条件 `save()` 把记录改回 running（[`account-closure-coordinator.ts:85`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:85) 至 [`account-closure-coordinator.ts:94`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:94)；Prisma 实现为无条件 upsert，[`account-closure-operation-prisma.repository.ts:170`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:170) 至 [`account-closure-operation-prisma.repository.ts:219`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:219)），两个失败重试者可同时进入外部撤销。`revoked -> closing` 和 `closing -> closed` 又忽略 CAS 返回值后继续关闭账户（[`account-closure-coordinator.ts:198`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:198) 至 [`account-closure-coordinator.ts:226`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:226)）；owner/lease 字段虽存在，但 coordinator 从不设置或校验（初始值始终为 null，见 [`account-closure-coordinator.ts:103`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:103) 至 [`account-closure-coordinator.ts:107`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:107)）。 | **identity 泄漏已关闭；并发 P0 未关闭**：首段 CAS 原子，但完整 saga 没有 owner/lease/fencing，失败重试和后续阶段仍可重复副作用。 |
| **P0-3 requested 后阻断 sign-in/refresh，撤销全部认证能力** | API 确实向 Cloud Auth 注入 durable closure query（[`main.ts:133`](../../apps/api/src/main.ts:133)）。Better Auth 的 session create hook 会在创建新 session 前检查 closure/user disabled 状态（[`cloud-auth.ts:55`](../../packages/cloud-auth/src/server/cloud-auth.ts:55) 至 [`cloud-auth.ts:67`](../../packages/cloud-auth/src/server/cloud-auth.ts:67)、[`cloud-auth.ts:144`](../../packages/cloud-auth/src/server/cloud-auth.ts:144) 至 [`cloud-auth.ts:151`](../../packages/cloud-auth/src/server/cloud-auth.ts:151)），所以 email/social/device 最终创建 session 的 sign-in 路径有真实阻断；业务 principal resolution 也二次检查（[`cloud-auth.ts:171`](../../packages/cloud-auth/src/server/cloud-auth.ts:171) 至 [`cloud-auth.ts:193`](../../packages/cloud-auth/src/server/cloud-auth.ts:193)）。`revokeAllSessions()` 删除 session/device code 并禁用 CloudAuthUser（[`cloud-auth.ts:201`](../../packages/cloud-auth/src/server/cloud-auth.ts:201) 至 [`cloud-auth.ts:215`](../../packages/cloud-auth/src/server/cloud-auth.ts:215)）。但原始 `/api/auth/*` 仍直接进入 Better Auth handler（[`bootstrap.ts:102`](../../apps/api/src/bootstrap.ts:102)），closure check 没有包裹 handler，也没有 session update/refresh hook；现有负向测试只调用 `resolvePrincipal()`（[`cloud-auth.spec.ts:176`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:176) 至 [`cloud-auth.spec.ts:217`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:217)），没有证明 sign-in endpoint 或已有 session refresh/get-session endpoint 在 requested/revocation 失败窗口被拒绝。 | **部分修复，仍为 P0**：新 session 创建和业务 principal 已阻断，用户也会被禁用；原始 auth refresh/get-session 路径缺少 closure guard 与真实负向测试。 |
| **P0-4 durable 发布、幂等消费、闭户后 fail-closed** | publisher 已写 Prisma `OutboxMessage`，不再是内存 push（[`account-closure-outbox-event-publisher.ts:10`](../../packages/account/src/server/infrastructure/adapters/outbox/account-closure-outbox-event-publisher.ts:10) 至 [`account-closure-outbox-event-publisher.ts:29`](../../packages/account/src/server/infrastructure/adapters/outbox/account-closure-outbox-event-publisher.ts:29)）；Reminder/Notification consumer 在各自事务内做 identity-scoped 条件取消并写联合主键 InboxReceipt（[`reminder-account-closed.consumer.ts:26`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:26) 至 [`reminder-account-closed.consumer.ts:61`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:61)、[`notification-account-closed.consumer.ts:26`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:26) 至 [`notification-account-closed.consumer.ts:49`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:49)）。但 publisher 的唯一键使用每次重试重新生成的 `closedAt`（[`account-closure-outbox-event-publisher.ts:11`](../../packages/account/src/server/infrastructure/adapters/outbox/account-closure-outbox-event-publisher.ts:11)），coordinator 生成的稳定 `eventId` 没有进入 event payload（[`account-closure-coordinator.ts:237`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:237) 至 [`account-closure-coordinator.ts:245`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:245)），publish 后崩溃重试可插入第二条 outbox。worker 只 `findMany(status=pending)`，没有 claim/owner/lease/fencing（[`account-closed.worker.ts:14`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:14) 至 [`account-closed.worker.ts:21`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:21)），且全仓没有实例化或 scheduler 注册该 worker；当前仅导出类。worker 还导致 account build 失败。没有 Repository consumer，也没有 Reminder/Notification 在新 claim/新创建时查询 closure 状态；一次性取消不能封闭 consumer 执行期间或之后新写入的 work。 | **仍为 P0**：持久化表与 consumer 雏形存在，但事件不稳定幂等、worker 无 lease 且未生产运行、Repository 与关闭后新 work fail-closed 均缺失。 |
| **P1-1 receipt 全链路** | HTTP contract、OpenAPI、controller、HTTP adapter、client service 已改为 `AccountClosureReceiptDTO`（[`account-lifecycle.dto.ts:24`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:24) 至 [`account-lifecycle.dto.ts:41`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:41)、[`routes.ts:122`](../../packages/account/src/api/routes.ts:122) 至 [`routes.ts:135`](../../packages/account/src/api/routes.ts:135)、[`account.controller.ts:79`](../../packages/account/src/server/transport/account.controller.ts:79) 至 [`account.controller.ts:89`](../../packages/account/src/server/transport/account.controller.ts:89)、[`account-http.adapter.ts:45`](../../packages/account/src/infrastructure-client/adapters/http/account-http.adapter.ts:45) 至 [`account-http.adapter.ts:47`](../../packages/account/src/infrastructure-client/adapters/http/account-http.adapter.ts:47)）。但 Desktop `closeCloudAccount` 把 HTTP receipt 丢成 void（[`main.ts:230`](../../apps/desktop/src/main/main.ts:230) 至 [`main.ts:245`](../../apps/desktop/src/main/main.ts:245)），IPC handler最终固定 `ok(null)`（[`electron/index.ts:121`](../../packages/account/src/electron/index.ts:121) 至 [`electron/index.ts:126`](../../packages/account/src/electron/index.ts:126)），与 `CloseAccountRes` 冲突。DTO 也没有 coordinator 已记录的 `revokedSessions`/`piiCleanupStatus` 字段。 | **部分修复，P1 未关闭**：HTTP/API client 可见 receipt；Desktop/IPC 仍丢 receipt 且真实路径失败。 |
| **P1-2 测试真实性** | 新 Prisma integration 确实连接 5433 测试库并验证联合唯一与单次 CAS（[`account-closure-operation-prisma.repository.integration.test.ts:6`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-operation-prisma.repository.integration.test.ts:6) 至 [`account-closure-operation-prisma.repository.integration.test.ts:121`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-operation-prisma.repository.integration.test.ts:121)）。但所谓 coordinator integration 仍使用内存 Account/closure repo 与手写 revocation/publisher（[`account-closure-coordinator.integration.spec.ts:12`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:12) 至 [`account-closure-coordinator.integration.spec.ts:88`](../../packages/account/src/server/application/services/__tests__/account-closure-coordinator.integration.spec.ts:88)），没有真实 DB coordinator 双并发、failed retry 双并发、崩溃恢复、outbox worker、真实 sign-in/refresh 或 production composition 启动测试。 | **仍为 P1**：真实 repo 单元集成已补，第二轮声称的真实 coordinator/并发/恢复点证据不存在。 |
| **P1-3 撤权/PII/retention 审计** | operation 新增 `reason`、`revokedSessions`、`piiCleanupStatus` 并进入内部 receipt（[`reliable-messaging.prisma:75`](../../packages/database/prisma/schema/reliable-messaging.prisma:75) 至 [`reliable-messaging.prisma:79`](../../packages/database/prisma/schema/reliable-messaging.prisma:79)、[`account-closure-coordinator.ts:290`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:290) 至 [`account-closure-coordinator.ts:304`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:304)），port 也拆成撤销认证与删除数据（[`cloud-auth-revocation.port.ts:11`](../../packages/account/src/server/application/ports/cloud-auth-revocation.port.ts:11) 至 [`cloud-auth-revocation.port.ts:24`](../../packages/account/src/server/application/ports/cloud-auth-revocation.port.ts:24)）。但 Prisma adapter 的 `deleteUserData()` 没删除任何数据，却返回 `piiCleanupStatus: 'completed'`（[`cloud-auth-revocation.adapter.ts:48`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:48) 至 [`cloud-auth-revocation.adapter.ts:53`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:53)）；没有 retention policy/version、audit actor、删除明细或 provider credential 处理记录，公开 DTO 也不携带这两个审计字段。 | **仍为 P1**：模型拆分方向正确，但当前产生虚假的 PII completed 审计结果。 |
| **P2-1 schema enum/version/owner/lease/dead-letter/eventId** | version、owner、lease、retry、dead-letter、eventId 列已添加（[`reliable-messaging.prisma:69`](../../packages/database/prisma/schema/reliable-messaging.prisma:69) 至 [`reliable-messaging.prisma:74`](../../packages/database/prisma/schema/reliable-messaging.prisma:74)）。但 phase/status 仍是自由 `String`（[`reliable-messaging.prisma:66`](../../packages/database/prisma/schema/reliable-messaging.prisma:66)、[`reliable-messaging.prisma:67`](../../packages/database/prisma/schema/reliable-messaging.prisma:67)），owner/lease/retry/dead-letter 未参与任何 claim/recovery 算法，eventId 也未绑定 outbox。 | **部分修复，P2 未关闭**。 |
| **P2-2 幂等键不使用用户 reason** | 请求可显式传 `idempotencyKey`（[`account-lifecycle.dto.ts:3`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:3) 至 [`account-lifecycle.dto.ts:7`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:7)），默认键固定为 identity-scoped `close:${identityId}`，reason 只作为独立审计输入（[`close-account.use-case.ts:26`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:26) 至 [`close-account.use-case.ts:33`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:33)）。 | **已关闭**。 |

### 重点问题结论

1. **P0-3 sign-in/refresh**：不是“只在 coordinator 内调用”；API 的 closure repo 已真实注入 Cloud Auth，session create hook 能阻断新登录，principal resolution 也 fail-closed。但 `/api/auth/*` 原始 handler 没有统一 closure guard，缺少 session update/refresh hook；现有测试没有请求 sign-in/refresh endpoint。因此只能确认 sign-in 的 session-create 边界，不能确认 refresh/get-session 在 requested 窗口被阻断。
2. **P0-4 durable/lease/idempotency**：publish 确实落数据库，但 `closedAt` 不是稳定 event id；worker 没有 claim/lease/fencing、没有生产启动点。consumer 的 cancellation update 本身可重复执行，InboxReceipt 也有数据库唯一键，但这不能弥补 worker 双领、事件双写以及关闭后新 work 无门禁。
3. **P0-2 原子条件写**：repository 的单次 CAS 是真实原子写，并正确检查 affected 行数；问题在 coordinator 只对部分转换处理冲突，且 failed retry 仍以无条件 save 抢占。不能用“存在 CAS 方法”推导整个 saga only-one-owner。

### W0 冻结面与契约演进

`CloseAccountRes: null -> AccountClosureReceiptDTO` 是合理且必要的产品/API 演进，因为客户端需要 operationId、running/succeeded/failed 与 signedOut；该方向本身不构成回退。但当前改动没有记录为 W0 contract revision，也没有复用 W0 已冻结的 `AccountClosureReliableOperationPort -> BusinessOperationReceipt`（[`ports.ts:152`](../../packages/contracts/src/modules/reliable-messaging/ports.ts:152) 至 [`ports.ts:171`](../../packages/contracts/src/modules/reliable-messaging/ports.ts:171)）。W0 receipt 要求 schemaVersion/source/occurrenceKey、canonical idempotency key、lease、nextRetryAt/deadLetterAt 和统一状态不变量（[`operation-receipt.ts:270`](../../packages/contracts/src/modules/reliable-messaging/operation-receipt.ts:270) 至 [`operation-receipt.ts:339`](../../packages/contracts/src/modules/reliable-messaging/operation-receipt.ts:339)），而新的 Account DTO 使用另一套 phase/status/attempts/timestamp 结构。故本轮应记录为**合理但尚未治理闭环的破坏性契约演进**：在 W3 收尾时要么正式 version/ADR 并定义与 W0 receipt 的映射，要么直接收敛到 W0 receipt；不能同时宣称 W0 frozen contract 与 Account 私有 receipt 都是 canonical。

### 本地验证（第二轮）

- `node node_modules/nx/dist/bin/nx.js run-many -t test -p account,cloud-auth,reminder,notification --skip-nx-cache --parallel=1`：**通过**。Account 24 files / 168 tests；Cloud Auth 4 / 17；Reminder 47 / 372；Notification 34 / 229。
- `node node_modules/nx/dist/bin/nx.js run account:test:integration --skip-nx-cache`：**通过**，2 files / 6 tests；其中只有 1 file / 2 tests 驱动真实 Prisma closure repository，另 1 file / 4 tests 仍为内存 coordinator。
- `account:typecheck`、`cloud-auth:typecheck`、`reminder:typecheck`、`notification:typecheck`：**通过**。
- `account:build`：**失败**，TypeScript 无法解析 `@memoflow/reminder/server` 与 `@memoflow/notification/server`。
- `api:typecheck`：**未执行到**；其依赖 `api:prisma:generate` 使用环境中不存在的 `pnpm` 而失败。该环境问题不替代上面的 account build 代码失败，也不作为 API 通过证据。

绿测说明 Account、Reminder、Notification 的既有单测集合未见回退，但新增的 account-closed consumer/worker 没有自身测试且 worker 未接线；因此不能据此宣称跨模块闭户回归通过。

### 第二轮终审判定

| 级别 | 第一轮项关闭情况 | 当前判定 |
| --- | --- | --- |
| P0 | 0/4 完整关闭；P0-1/P0-3 为部分修复，P0-2/P0-4 仍缺核心并发/运行时边界 | **4 项仍阻塞** |
| P1 | 0/3 完整关闭；HTTP receipt、真实 repo CAS、审计字段为局部进展 | **3 项仍需关闭** |
| P2 | P2-2 已关闭；P2-1 仅加列未形成状态/租约约束 | **1 项关闭，1 项保留** |

**终审：W3 第二轮不可放行进入 W4，也不能把第一轮 4 个 P0 标记为关闭。**

可以继续进入“第三轮”作为 **W3 修复/收尾轮**，但这不是 W3 放行。第三轮至少应：修通并构建 Desktop/API closure composition；把所有阶段和 failed retry 纳入 owner/lease/fencing CAS；在 Better Auth 原始 sign-in/refresh/get-session 路径做端到端 closure deny；用稳定 eventId 写 outbox，接入带 lease 的生产 worker并覆盖 Repository 与新 work fail-closed；让 PII 状态反映真实政策动作；补真实 DB 双并发、崩溃恢复、auth endpoint 与 account-closed consumer/worker 集成测试。上述 P0 清零、account build/API typecheck/governance 门禁通过后，才可重新判断进入 W4。

## 第三轮审查

本轮继续复核同一工作区的未提交实现，只追加审查结论，没有修改业务代码，也没有 commit/push。第三轮确实补上了 structured receipt、identity-scoped idempotency key、稳定 eventId/outbox、真实 Reminder/Notification consumer、worker lease claim、API closure checker 注入和 account build；但第二轮终审要求仍未全部成立，而且新增的 Cloud Auth endpoint 测试和 API 核心 typecheck 直接暴露了两个可复现发布阻塞。

### 核验表

| 第二轮终审要求 | 第三轮证据 | 终审核验 |
| --- | --- | --- |
| 所有阶段与 failed retry 纳入 owner/lease/fencing CAS | Coordinator 首次创建即写 owner/lease（[`account-closure-coordinator.ts:139`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:139) 至 [`account-closure-coordinator.ts:163`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:163)）；running/failed 都先 claim（[`account-closure-coordinator.ts:87`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:87) 至 [`account-closure-coordinator.ts:134`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:134)）；failed claim 的 Prisma `UPDATE ... WHERE status='failed'` 会原子地把首个重试者改为 running 并递增 attempts（[`account-closure-operation-prisma.repository.ts:158`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:158) 至 [`account-closure-operation-prisma.repository.ts:179`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:179)），故**狭义的两个 failed retrier 只有一个 claim 成功**。但 phase CAS 允许 `leaseExpiresAt < now` 时忽略 owner mismatch（[`account-closure-operation-prisma.repository.ts:192`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:192) 至 [`account-closure-operation-prisma.repository.ts:197`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:197)；内存实现同样如此：[`account-closure-operation-in-memory.repository.ts:86`](../../packages/account/src/server/infrastructure/adapters/in-memory/account-closure-operation-in-memory.repository.ts:86) 至 [`account-closure-operation-in-memory.repository.ts:91`](../../packages/account/src/server/infrastructure/adapters/in-memory/account-closure-operation-in-memory.repository.ts:91)），旧 owner 可在新 owner 的 lease 再次过期后不重新 claim 就推进阶段；30 秒 lease 在外部副作用执行期间没有 heartbeat，超时后第二 owner 可重复进入 revocation/close；catch 中的 failed CAS 返回值也被忽略（[`account-closure-coordinator.ts:383`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:383) 至 [`account-closure-coordinator.ts:402`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:402)）。 | **部分满足，整体未满足（P0）** |
| Better Auth 原始 sign-in/refresh/get-session closure deny | session create hook 在 Better Auth 内部阻止新 session（[`cloud-auth.ts:116`](../../packages/cloud-auth/src/server/cloud-auth.ts:116) 至 [`cloud-auth.ts:153`](../../packages/cloud-auth/src/server/cloud-auth.ts:153)），sign-in email wrapper 也在 raw handler 前按 email 拒绝（[`cloud-auth.ts:221`](../../packages/cloud-auth/src/server/cloud-auth.ts:221) 至 [`cloud-auth.ts:250`](../../packages/cloud-auth/src/server/cloud-auth.ts:250)）。但 `resolvePrincipal()` 在 closure blocked 时返回 null（[`cloud-auth.ts:171`](../../packages/cloud-auth/src/server/cloud-auth.ts:171) 至 [`cloud-auth.ts:175`](../../packages/cloud-auth/src/server/cloud-auth.ts:175)），外层 handler 随即无法区分“被 closure 拒绝”和“无 principal”，继续调用原始 handler；新增 raw get-session 测试实际得到 200 而非 403（测试断言：[`cloud-auth.spec.ts:220`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:220) 至 [`cloud-auth.spec.ts:277`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:277)）。`catch { proceed to raw handler }` 还会在 checker/DB 异常时 fail-open（[`cloud-auth.ts:247`](../../packages/cloud-auth/src/server/cloud-auth.ts:247) 至 [`cloud-auth.ts:250`](../../packages/cloud-auth/src/server/cloud-auth.ts:250)）。没有独立 refresh endpoint closure 测试。 | **未满足（P0）** |
| 稳定 eventId + lease worker 生产接线 | eventId 由 operation id 稳定导出（[`account-closure-coordinator.ts:339`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:339) 至 [`account-closure-coordinator.ts:349`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:349)），outbox 按该 idempotency key upsert（[`account-closure-outbox-event-publisher.ts:10`](../../packages/account/src/server/infrastructure/adapters/outbox/account-closure-outbox-event-publisher.ts:10) 至 [`account-closure-outbox-event-publisher.ts:30`](../../packages/account/src/server/infrastructure/adapters/outbox/account-closure-outbox-event-publisher.ts:30)）。worker 用 pending + owner/expired lease 条件 `updateMany` 原子 claim（[`account-closed.worker.ts:24`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:24) 至 [`account-closed.worker.ts:70`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:70)），构造器强制两个 consumer（[`account-closed.worker.ts:9`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:9) 至 [`account-closed.worker.ts:22`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:22)）。但成功/失败落库使用仅按 id 的无 owner-token CAS update（[`account-closed.worker.ts:75`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:75) 至 [`account-closed.worker.ts:105`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:105)），长消费超 lease 后旧 worker 仍可提交；API main 虽写了真实 consumer 构造（[`main.ts:230`](../../apps/api/src/main.ts:230) 至 [`main.ts:238`](../../apps/api/src/main.ts:238)），当前却无法通过 typecheck，故不是可运行的生产接线。 | **durable 核心部分满足，生产接线未满足（P0/P1）** |
| PII 状态真实且可审计 | revocation 与 deleteUserData 已拆开；无 delete capability 时返回 `not_performed`，没有虚报 completed（[`cloud-auth-revocation.adapter.ts:48`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:48) 至 [`cloud-auth-revocation.adapter.ts:59`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:59)）。但第二轮要求的“not_performed + 原因”没有实现：adapter 不返回 reason，operation/receipt 也只保存 `piiCleanupStatus`（[`account-closure-coordinator.ts:240`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:240) 至 [`account-closure-coordinator.ts:254`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:254)、[`account-lifecycle.dto.ts:24`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:24) 至 [`account-lifecycle.dto.ts:38`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:38)）。 | **状态真实性满足，审计原因未满足（P1）** |
| 真实 DB 双并发、崩溃恢复、auth endpoint、consumer 集成测试 | DB 双首次调用测试存在并通过（[`account-closure-coordinator-db.integration.test.ts:25`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:25) 至 [`account-closure-coordinator-db.integration.test.ts:62`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:62)），但只断言同 operation/final succeeded，没有观测 only-one-side-effect，也没有 failed 双重试或进程崩溃/lease takeover 测试。DB worker 测试仍注入 consumer stubs（[`account-closure-coordinator-db.integration.test.ts:117`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:117) 至 [`account-closure-coordinator-db.integration.test.ts:135`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:135)）。apps/api 集成测试确实实例化真实 Reminder/Notification consumer 并通过（[`account-closure-consumers.integration.test.ts:79`](../../apps/api/src/__tests__/integration/account-closure-consumers.integration.test.ts:79) 至 [`account-closure-consumers.integration.test.ts:105`](../../apps/api/src/__tests__/integration/account-closure-consumers.integration.test.ts:105)），但直接手工拼装 module/worker，不启动 main/scheduler；只 seed/assert Reminder，没有 seed Notification dispatch，也没有断言 notification inbox receipt。auth endpoint 测试存在但红灯。 | **部分满足（P1；auth 红灯构成 P0）** |
| account build/API typecheck/governance 门禁 | account typecheck/build/lint 均通过；governance-check 与 inventory 969 通过。API 标准 target 仍受环境无 pnpm 影响；在依赖已生成/构建后直接运行其核心 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`，得到 8 个代码错误：错误导入 Account API/worker、缺少 Prisma closure repo、provisioner、Reminder/Notification consumer 符号，以及 CloudAuth stub 缺少 `revokeAllSessions`。对应源码包括 [`main.ts:29`](../../apps/api/src/main.ts:29) 至 [`main.ts:35`](../../apps/api/src/main.ts:35)、[`main.ts:115`](../../apps/api/src/main.ts:115)、[`main.ts:130`](../../apps/api/src/main.ts:130)、[`main.ts:233`](../../apps/api/src/main.ts:233) 至 [`main.ts:237`](../../apps/api/src/main.ts:237)、[`cloud-auth.stub.ts:3`](../../apps/api/src/test/cloud-auth.stub.ts:3) 至 [`cloud-auth.stub.ts:12`](../../apps/api/src/test/cloud-auth.stub.ts:12)。 | **未满足（P0）** |
| Desktop/API closure composition | Desktop 通过 remote Cloud API close 并在成功后断开当前云连接（[`main.ts:230`](../../apps/desktop/src/main/main.ts:230) 至 [`main.ts:251`](../../apps/desktop/src/main/main.ts:251)），本轮直接 Desktop core typecheck 通过。API module factory 默认装配 Prisma closure repo、revocation adapter 和 outbox publisher（[`prisma.ts:41`](../../packages/account/src/server/infrastructure/prisma.ts:41) 至 [`prisma.ts:64`](../../packages/account/src/server/infrastructure/prisma.ts:64)），但 API host main 自身不能编译。 | **Desktop 满足，API 未满足（P0）** |
| Repository 与新 work fail-closed | 新增 consumer 只覆盖 Reminder pending occurrence/template（[`reminder-account-closed.consumer.ts:26`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:26) 至 [`reminder-account-closed.consumer.ts:60`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:60)）和 Notification dispatch outbox（[`notification-account-closed.consumer.ts:26`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:26) 至 [`notification-account-closed.consumer.ts:48`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:48)）。Repository 没有 account-closed consumer；Reminder/Notification/Repository 新建工作路径也没有 closure/account-active guard。 | **未满足（P0）** |

### 重点问题结论

#### failed retry 的 CAS 接管

`status='failed'` 是有效的单语句原子守卫：两个 retrier 同时执行 `updateMany where status='failed'` 时，第一个把状态改为 running，第二个 count 为 0。因此第三轮描述的“两个 failed retry 只有一个 claim 成功”在 Prisma 语义上成立。

但这不能等同于 saga 已具备完整 fencing。phase CAS 把“当前 lease 已过期”也视为写权限，未要求调用者先成功 claim；外部副作用执行期间没有 heartbeat 或 phase-scoped claim；failure transition 也未检查 CAS 结果。真实 DB 测试没有覆盖 failed 双重试、lease 过期后旧 owner、慢副作用跨 lease 或 crash/restart。故第二轮的并发终审要求仍是 P0 未关闭。

#### Better Auth 原始路径

这轮确实不再只依赖业务 middleware：session create database hook 和 raw auth handler wrapper 都位于 `createCloudAuth()` 内。但 raw get-session closure deny 的新增测试实际失败，说明“进入 Better Auth 原始路径”不等于“原始路径已闭环”。当前 get-session 可返回被 closureChecker 判定为 closing 的有效 session，refresh 也没有独立 endpoint 证据；这是直接的认证 P0。

#### worker claim 与真实 consumer

worker 的 claim 本身是数据库 CAS，两个正常 worker 同时抢同一 pending row 时只有一个 token 能获得记录；稳定 eventId + consumer inbox transaction 也使重复消费有幂等基础。apps/api 新测试确实驱动了真实 Reminder/Notification consumer 类，而非纯 stub，因此这部分是有效进展。

不过测试没有启动生产 main/scheduler，Notification 路径未 seed/断言，Repository 完全缺席；worker completion/error write 也没有 owner fencing。最关键的是 main 当前 typecheck 不通过，所谓“生产接线”无法形成可发布产物。

### 契约与门禁

`CloseAccountRes` 从 null 改为 structured receipt，并已贯穿 controller、HTTP/IPC adapter 和 client port（[`account-lifecycle.dto.ts:24`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:24) 至 [`account-lifecycle.dto.ts:43`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:43)、[`account.controller.ts:79`](../../packages/account/src/server/transport/account.controller.ts:79) 至 [`account.controller.ts:89`](../../packages/account/src/server/transport/account.controller.ts:89)、[`account-api-client.port.ts:17`](../../packages/account/src/application-client/ports/account-api-client.port.ts:17) 至 [`account-api-client.port.ts:23`](../../packages/account/src/application-client/ports/account-api-client.port.ts:23)），成功/running receipt 不再被 transport 丢弃，这是合理修复。残余问题有两项：

1. Use case 将 failed receipt 转为普通 `Result` error（[`close-account.use-case.ts:35`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:35) 至 [`close-account.use-case.ts:41`](../../packages/account/src/server/application/use-cases/commands/close-account.use-case.ts:41)），客户端仍拿不到 failed operationId/attempts/retryable/PII 状态。
2. Account 私有 receipt 仍没有版本化或映射到 W0 `BusinessOperationReceipt`，第二轮指出的 frozen contract 分叉没有关闭。

Reminder/Notification 增加 `./server` export 后，tsup 确实产出对应 server 文件，package-export-audit 与完整 governance-check 也通过；因此白名单改动在现有规则下是合法的。但两个 `server/index.ts` 都 `export *` 整个 infrastructure/domain，并非只暴露 account-closed consumer（[`server/index.ts:1`](../../packages/reminder/src/server/index.ts:1) 至 [`server/index.ts:7`](../../packages/reminder/src/server/index.ts:7)、[`server/index.ts:1`](../../packages/notification/src/server/index.ts:1) 至 [`server/index.ts:7`](../../packages/notification/src/server/index.ts:7)），属于为一个 composition symbol 扩大完整服务端公共面；而 account 自己没有 `./server` package export/tsup entry，API main 又错误地从 root 导入 API module/worker。结论是：**白名单更新本身可接受，但当前 package surface 组合不完整且过宽，不能用 governance 绿灯替代 API build/typecheck。**

### 回归与本地验证（第三轮）

- `account:test`：**通过**，24 files / 168 tests。
- `account:test:integration`：**通过**，3 files / 8 tests；覆盖真实 Prisma 基础路径，但未覆盖上述 failed retry/crash/fencing 矩阵。
- apps/api 指定 `account-closure-consumers.integration.test.ts`：**通过**，1/1，627ms；真实 Reminder/Notification consumer 类被调用，但断言只覆盖 Reminder。
- `cloud-auth:test`：**失败**，4 files 中 1 file failed；18 tests 中 17 passed / 1 failed。失败项为 closing user 的 raw `/api/auth/get-session` 期望 403、实际 200。
- `account:typecheck`、`account:build`：**通过**。
- `account:lint`：**通过**，0 error / 7 warnings（含新增 integration 文件的未使用 import）。
- API 核心 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`：**失败**，8 errors。标准 `api:typecheck` target 另受环境无 `pnpm` 阻断，不能宣称通过。
- Desktop 核心 `tsc --noEmit -p apps/desktop/tsconfig.typecheck.json`：**通过**。
- Reminder：47 files / 372 tests **通过**；Notification：34 files / 229 tests **通过**。W1/W2 既有单测集合未见回退。
- `memoflow:governance-check`：**通过**；inventory **969**（unit 840 / integration 16 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。

### 第三轮终审判定

| 级别 | 第三轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | 认证 raw get-session closure deny 实测失败；API main/core typecheck 8 errors；saga fencing/lease heartbeat 与 failure CAS 未闭环；Repository 和新 work fail-closed 缺失 | **4 项发布阻塞** |
| P1 | worker completion 无 owner fencing；真实 DB crash/failed-retry/auth/notification assertions 不完整；PII not_performed 无原因；failed receipt 不贯穿客户端；W0 receipt 映射未关闭 | **5 项需关闭** |
| P2 | Reminder/Notification 为单个 consumer 扩大整个 `./server` 公共面；phase/status/PII status 仍为数据库自由字符串 | **2 项保留** |

**终审：W3 第三轮仍不可放行，不应提交 W3，也不能进入 W4。**

放行前至少必须同时满足：修复并跑绿 Cloud Auth 原始 get-session/refresh/sign-in endpoint closure 测试；修通 API main imports/exports/stub 并让标准 `api:typecheck` 或等价核心门禁无代码错误；phase/failure/worker completion 只允许当前 owner token 写入并补 lease heartbeat/超时竞争测试；补真实 failed 双重试与 crash/restart；接入 Repository account-closed consumer并在 Reminder/Notification/Repository 新建工作入口 fail-closed；让 failed/PII receipt 携带可审计原因并收敛 W0 receipt 契约。account build、governance 和现有模块绿测是必要条件，但不足以覆盖上述 P0。

## 第四轮审查

本轮只复核三审四项发布阻塞及其直接回归面。审查未修改业务代码，未 commit/push。

### 结论

**W3 第四轮仍不可放行，不应提交 W3，也不能进入 W4。**

第四轮关闭了两项明确子问题：Fetch `handler` 的 raw get-session 现在会在调用 Better Auth 原始 handler 前返回 403，Cloud Auth 18/18 通过；API 核心 typecheck 已无代码错误。phase/failure/worker completion 的最终状态写也都增加了 owner-token 条件，failure CAS 返回值不再被忽略。Repository consumer 已实现并接入 API worker，真实 consumer-chain 测试能取消 Reminder、Notification、Repository 三类已排队工作。

但三项发布阻塞仍在：生产实际使用的 Express raw sign-in 路径在 JSON body parser 之前挂载，wrapper 看不到 email，现有 403 测试只调用 Fetch `handler` 而没有覆盖部署路径；Coordinator/worker 没有在慢副作用或慢 consumer 期间持续 heartbeat，所谓慢副作用测试只直接操作 repository CAS；Reminder/Notification/Repository 的 closure checker 均为可选且没有进入生产 composition root，Repository 新增的拒绝测试本轮实跑还直接失败。PII cleanup adapter 虽返回原因，Coordinator/receipt 仍丢弃该原因。

### 三审四项阻塞逐条核验

| 三审阻塞 | 第四轮证据与判断 | 结论 |
| --- | --- | --- |
| raw auth endpoint closure deny | `checkRequestClosure()` 会先用原始 session 解析并在命中 closure 时返回 403（[`cloud-auth.ts:199`](../../packages/cloud-auth/src/server/cloud-auth.ts:199) 至 [`cloud-auth.ts:211`](../../packages/cloud-auth/src/server/cloud-auth.ts:211)），Fetch `handler` 与 Express `expressHandler` 都在调用 Better Auth raw handler 前执行该检查（[`cloud-auth.ts:239`](../../packages/cloud-auth/src/server/cloud-auth.ts:239) 至 [`cloud-auth.ts:263`](../../packages/cloud-auth/src/server/cloud-auth.ts:263)），故 get-session/bearer 路径已真实关闭。sign-in 检查依赖已解析的 `body.email/identifier`（[`cloud-auth.ts:214`](../../packages/cloud-auth/src/server/cloud-auth.ts:214) 至 [`cloud-auth.ts:233`](../../packages/cloud-auth/src/server/cloud-auth.ts:233)）；但生产把 `expressHandler` 挂在 body parser 之前（[`bootstrap.ts:81`](../../apps/api/src/bootstrap.ts:81) 至 [`bootstrap.ts:103`](../../apps/api/src/bootstrap.ts:103)、[`global.ts:54`](../../apps/api/src/shared/infrastructure/middleware/global.ts:54) 至 [`global.ts:68`](../../apps/api/src/shared/infrastructure/middleware/global.ts:68)），此时 `req.body` 未解析。现有 403 测试只调用 Fetch `auth.handler`（[`cloud-auth.spec.ts:257`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:257) 至 [`cloud-auth.spec.ts:277`](../../packages/cloud-auth/src/server/cloud-auth.spec.ts:277)），没有覆盖生产 Express sign-in/refresh。session create hook 能阻止创建 session，但不能证明 raw endpoint 返回约定的 closure 403。 | **部分关闭；生产 raw sign-in 403 仍为 P0** |
| API typecheck | `main.ts` 已从可用 package surface 导入 account factory/worker/consumer（[`main.ts:29`](../../apps/api/src/main.ts:29) 至 [`main.ts:43`](../../apps/api/src/main.ts:43)），Cloud Auth stub 也补齐 `revokeAllSessions`。本轮直接运行 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`，0 error。标准 Nx target 因环境没有 `pnpm` 不能启动，但核心编译门禁已无代码错误。 | **已关闭** |
| phase/failure/worker completion 当前 owner + heartbeat/超时竞争 | Prisma phase CAS 现在把 `ownerToken` 放入 `UPDATE ... WHERE`（[`account-closure-operation-prisma.repository.ts:182`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:182) 至 [`account-closure-operation-prisma.repository.ts:213`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:213)）；Coordinator 的 failure CAS 检查结果并在失败时重读（[`account-closure-coordinator.ts:404`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:404) 至 [`account-closure-coordinator.ts:432`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:432)）；worker completion/error 也按当前 worker token 更新（[`account-closed.worker.ts:89`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:89) 至 [`account-closed.worker.ts:112`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:112)）。但所谓 heartbeat 只是副作用前调用一次 `claimOwnership()` 把 lease 延长 30 秒（[`account-closure-coordinator.ts:227`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:227) 至 [`account-closure-coordinator.ts:266`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:266)、[`account-closure-coordinator.ts:436`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:436) 至 [`account-closure-coordinator.ts:450`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:450)），没有定时续租；worker 从 claim 到三个 consumer 完成之间同样没有 heartbeat（[`account-closed.worker.ts:27`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:27) 至 [`account-closed.worker.ts:97`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:97)）。schema 也没有所宣称的 `lastHeartbeatAt`（[`reliable-messaging.prisma:9`](../../packages/database/prisma/schema/reliable-messaging.prisma:9) 至 [`reliable-messaging.prisma:32`](../../packages/database/prisma/schema/reliable-messaging.prisma:32)、[`reliable-messaging.prisma:62`](../../packages/database/prisma/schema/reliable-messaging.prisma:62) 至 [`reliable-messaging.prisma:87`](../../packages/database/prisma/schema/reliable-messaging.prisma:87)）。慢操作超过 30 秒时新 owner 仍可接管并重复外部副作用，旧 owner 只是在最终写入时被 fenced。 | **owner fencing 部分关闭；持续 heartbeat/慢副作用不重复仍为 P0** |
| Repository consumer + 新工作 fail-closed + receipt 原因 | Repository consumer 会事务性取消 pending write、归档 repository 并写 inbox receipt（[`repository-account-closed.consumer.ts:11`](../../packages/repository/src/server/infrastructure/consumers/repository-account-closed.consumer.ts:11) 至 [`repository-account-closed.consumer.ts:62`](../../packages/repository/src/server/infrastructure/consumers/repository-account-closed.consumer.ts:62)），API main 注入三个真实 consumer（[`main.ts:238`](../../apps/api/src/main.ts:238) 至 [`main.ts:247`](../../apps/api/src/main.ts:247)），consumer-chain 也 seed/assert 三类 work 和三个 receipt（[`account-closure-consumers.integration.test.ts:80`](../../apps/api/src/__tests__/integration/account-closure-consumers.integration.test.ts:80) 至 [`account-closure-consumers.integration.test.ts:192`](../../apps/api/src/__tests__/integration/account-closure-consumers.integration.test.ts:192)）。但新工作 guard 都是可选条件：Reminder（[`create-reminder-template.use-case.ts:26`](../../packages/reminder/src/server/application/use-cases/commands/create-reminder-template.use-case.ts:26) 至 [`create-reminder-template.use-case.ts:45`](../../packages/reminder/src/server/application/use-cases/commands/create-reminder-template.use-case.ts:45)）、Notification（[`create-notification.use-case.ts:44`](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:44) 至 [`create-notification.use-case.ts:68`](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:68)）、Repository（[`knowledge-note-commit.service.ts:36`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:36) 至 [`knowledge-note-commit.service.ts:46`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:46)、[`knowledge-note-commit.service.ts:128`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:128) 至 [`knowledge-note-commit.service.ts:139`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:139)）。三个 Prisma/module factory 均未注入 checker（[`reminder.module.ts:95`](../../packages/reminder/src/server/infrastructure/reminder.module.ts:95) 至 [`reminder.module.ts:101`](../../packages/reminder/src/server/infrastructure/reminder.module.ts:101)、[`notification.module.ts:83`](../../packages/notification/src/server/infrastructure/notification.module.ts:83) 至 [`notification.module.ts:99`](../../packages/notification/src/server/infrastructure/notification.module.ts:99)、[`prisma.ts:109`](../../packages/repository/src/server/infrastructure/prisma.ts:109) 至 [`prisma.ts:118`](../../packages/repository/src/server/infrastructure/prisma.ts:118)），所以 canonical production path 是 fail-open。Repository 新测试的 helper 也没有接受/转交 checker（[`knowledge-note-commit.service.spec.ts:260`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.spec.ts:260) 至 [`knowledge-note-commit.service.spec.ts:292`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.spec.ts:292)），实跑得到成功而非期望的 `FORBIDDEN`。此外 adapter 返回的 cleanup `reason`（[`cloud-auth-revocation.adapter.ts:48`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:48) 至 [`cloud-auth-revocation.adapter.ts:61`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:61)）只被 Coordinator 收敛成 status 字符串（[`account-closure-coordinator.ts:240`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:240) 至 [`account-closure-coordinator.ts:266`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:266)），receipt schema 没有 reason（[`account-lifecycle.dto.ts:24`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:24) 至 [`account-lifecycle.dto.ts:38`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:38)）。 | **consumer 已关闭；新工作 fail-closed 为 P0，receipt 原因为 P1** |

### 真实 DB 测试真实性

`account:test:integration` 的确连接 `127.0.0.1:5433/memoflow_test`，并驱动真实 Prisma repository/schema，3 files / 12 tests 通过；不能再称为纯内存 fake。但四个新增标题与实际故障模型仍有差距：

1. failed 双重试只并发调用 `closureOpRepo.claimOwnership()`，验证数据库 status CAS 和 attempts 递增（[`account-closure-coordinator-db.integration.test.ts:137`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:137) 至 [`account-closure-coordinator-db.integration.test.ts:187`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:187)）；没有让两个 Coordinator retrier 同时执行并观测 revoke/close/publish 只发生一次。
2. lease 过期旧 owner 测试直接 claim 后直接调用 phase CAS（[`account-closure-coordinator-db.integration.test.ts:189`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:189) 至 [`account-closure-coordinator-db.integration.test.ts:232`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:232)），有效证明 stale token 写不进去。
3. “慢副作用跨 lease”没有启动慢 revocation/close/consumer，也没有等待 lease 过期或观测 heartbeat；它预先 seed 过期 lease，让 worker B claim，再直接断言 worker A CAS 失败（[`account-closure-coordinator-db.integration.test.ts:234`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:234) 至 [`account-closure-coordinator-db.integration.test.ts:277`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:277)）。它证明 completion fencing，不证明慢副作用期间续租或副作用 only-once。
4. crash/restart 测试把中间 phase/过期 owner 直接 seed 到 DB，再由同一进程中的既有 Coordinator 恢复（[`account-closure-coordinator-db.integration.test.ts:279`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:279) 至 [`account-closure-coordinator-db.integration.test.ts:325`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:325)）。这是有效的 persisted-state recovery 测试，但不是实际进程 crash/restart，也未覆盖副作用后、phase 保存前的真实 crash window。

因此本轮可以认定“真实 DB 仓储路径存在且基础 CAS/fencing 绿”，不能认定三审要求的真实 failed 双重试、慢副作用 heartbeat 与 crash-window 矩阵已完整关闭。

### 回归与本地验证（第四轮）

- 环境没有 `pnpm`，标准 `pnpm nx run ...` 入口无法启动；以下均使用对应 target 的直接 Node 命令并禁用缓存等价核验。
- Cloud Auth：4 files / 18 tests **通过**。
- API 核心 `tsc --noEmit -p apps/api/tsconfig.typecheck.json`：**通过，0 error**。
- Account unit：24 files / 168 tests **通过**。
- Account integration：3 files / 12 tests **通过**，真实 PostgreSQL/Prisma；真实性边界如上。
- apps/api consumer-chain：1 file / 1 test **通过**；三类 pending work 与三个 inbox receipt 均有断言。
- Reminder：47 files / 373 tests **通过**；Notification：34 files / 230 tests **通过**。W1/W2 既有集合未见回退。
- Repository 指定 `knowledge-note-commit.service.spec.ts`：**失败**，12 tests 中 11 passed / 1 failed；闭户拒绝用例期望 `FORBIDDEN`，实际得到成功。
- `memoflow:governance-check`：**通过**；inventory **969**（unit 840 / integration 16 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。

### 第四轮终审判定

| 级别 | 第四轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | 生产 Express raw sign-in 没有 closure 403 证据且 wrapper 在 body parser 前看不到 email；Coordinator/worker 无持续 heartbeat，慢副作用测试未驱动真实慢操作；Reminder/Notification/Repository production composition 未注入 closure checker，Repository 新测试红灯 | **3 项发布阻塞** |
| P1 | failed 双重试与 crash 测试只覆盖仓储 CAS/seeded recovery，未覆盖两个 Coordinator 与真实 crash window；PII cleanup reason 未进入 operation/receipt；failed receipt 仍被 use case 转成普通 error；worker 的 Repository consumer 仍为可选依赖，缺失时不 fail-fast | **4 项需关闭** |
| P2 | phase/status/PII status 仍为数据库自由字符串；Reminder/Notification/Repository 为 composition symbol 暴露较宽 `./server` surface | **2 项保留** |

**终审：W3 第四轮不可放行。当前不能提交 W3，也不能进入 W4。**

放行前至少必须：让部署中的 Express raw sign-in/refresh/get-session 全部以 endpoint 级测试证明 closure 命中返回 403；给 Coordinator 和 AccountClosedWorker 的长执行区间增加真正的周期 heartbeat/续租，并用两个真实执行者驱动慢副作用跨 lease、证明不会重复副作用且旧 owner 不能完成/失败写；把非可选 account-active/closure policy 注入 Reminder、Notification、Repository 所有 canonical 新工作 composition root 并跑绿 Repository 拒绝测试；把 PII `not_performed`/retention 原因持久化并贯穿 receipt。API typecheck、consumer 取消链和基础 owner-token CAS 已关闭，不需要回退。

## 第五轮审查

本轮只审查四审要求关闭的 3 项 P0、PII/retention reason 这一项 P1，以及它们对 W0--W4 已放行面的直接回归；未修改业务代码，未 commit/push。

### 结论

**W3 第五轮仍不可放行，不能提交 W3，也不能进入 W4。**

第五轮真实关闭了 Express 部署路径的 sign-in/get-session/refresh 403、Repository `FORBIDDEN` 拒绝测试，以及 PII/retention reason 的持久化和 receipt 映射。Coordinator 与当前正在执行的单条 outbox message 也确实新增了周期 heartbeat，不再只是副作用前一次性延长 lease。

但两个发布阻塞没有关闭：第一，所宣称的“真实双执行者慢副作用测试”不存在，现有 account integration 仍是四审时的 3 files / 12 tests，所谓 slow-side-effect 用例仍只 seed 过期 lease 后直接调用 repository claim/CAS；worker 还一次领取一批消息、却只在每条消息轮到执行时才启动 heartbeat，排队中的已领取消息可以先过期并被第二 worker 再领取。第二，Notification 与 Repository API factory 仍把 checker 声明为可选并回退到 `async () => false`，Notification schedule port 也保留同类默认值；AI reminder adapter 只查 account status，无法阻断 requested/revoking 阶段仍为 Active 的账户。这与“production 必填、无 no-op、closure in progress 即拒绝”直接冲突。

### 四审 3 P0 + 1 P1 逐条核验

| 四审关闭项 | 第五轮证据与判断 | 结论 |
| --- | --- | --- |
| Express raw sign-in/refresh/get-session 端点级 403 | `expressHandler` 会在 raw Better Auth handler 前自行读取尚未解析的请求体，再执行 closure 检查（[`cloud-auth.ts:249`](../../packages/cloud-auth/src/server/cloud-auth.ts:249) 至 [`cloud-auth.ts:303`](../../packages/cloud-auth/src/server/cloud-auth.ts:303)）；session 先走 Better Auth session resolution，Bearer token 再直接查 session token（[`cloud-auth.ts:199`](../../packages/cloud-auth/src/server/cloud-auth.ts:199) 至 [`cloud-auth.ts:221`](../../packages/cloud-auth/src/server/cloud-auth.ts:221)），sign-in 则按 email/identifier 反查 user（[`cloud-auth.ts:224`](../../packages/cloud-auth/src/server/cloud-auth.ts:224) 至 [`cloud-auth.ts:244`](../../packages/cloud-auth/src/server/cloud-auth.ts:244)）。测试通过真实 `ApiBootstrapper`，而该 bootstrapper 正是在 body parser 前挂载 `cloudAuth.expressHandler`（[`bootstrap.ts:81`](../../apps/api/src/bootstrap.ts:81) 至 [`bootstrap.ts:103`](../../apps/api/src/bootstrap.ts:103)）；四个用例覆盖 closed sign-in/get-session/refresh 403 和 active sign-in 非 403（[`express-auth-closure.integration.test.ts:85`](../../apps/api/src/__tests__/integration/express-auth-closure.integration.test.ts:85) 至 [`express-auth-closure.integration.test.ts:141`](../../apps/api/src/__tests__/integration/express-auth-closure.integration.test.ts:141)）。按说明先 rebuild `cloud-auth` 后，本轮实跑 apps/api integration 3 files / 6 tests 全绿。 | **已关闭** |
| Coordinator/worker 周期 heartbeat + 真实双执行者慢副作用 only-once | Coordinator 在取得 owner 后按 `leaseDuration / 3` 启动 `setInterval`，每次以 id/identity/owner token CAS 更新 lease（[`account-closure-coordinator.ts:207`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:207) 至 [`account-closure-coordinator.ts:232`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:232)、[`account-closure-operation-prisma.repository.ts:189`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:189) 至 [`account-closure-operation-prisma.repository.ts:207`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:207)）；worker 对当前消息也按 owner token 周期更新 `leaseExpiresAt/lastHeartbeatAt`（[`account-closed.worker.ts:78`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:78) 至 [`account-closed.worker.ts:104`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:104)）。但测试没有新增真实慢执行：标题为 slow side-effect 的用例仍只预置过期 owner、让 B 直接 claim、再断言 A 的 phase CAS 失败（[`account-closure-coordinator-db.integration.test.ts:234`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:234) 至 [`account-closure-coordinator-db.integration.test.ts:277`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:277)），没有两个 Coordinator/worker、没有慢 revocation/close/consumer、没有跨真实 lease 等待，也没有副作用计数 only-once。另有实现缺口：worker 在第 49--74 行一次 claim 整批消息，却到 for-loop 当前项才为该项启动 heartbeat；前一项慢于 30 秒时，后续已领取项 lease 会过期并可被第二 worker claim，而旧 worker仍会继续执行其本地 `claimedMessages`（[`account-closed.worker.ts:49`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:49) 至 [`account-closed.worker.ts:78`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:78)、[`account-closed.worker.ts:106`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:106) 至 [`account-closed.worker.ts:127`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:127)）。 | **周期续租部分关闭；真实双执行者证明与 worker 批量租约仍为 P0** |
| Reminder/Notification/Repository production checker 必填且 Repository 拒绝测试为 `FORBIDDEN` | Reminder API options 和 Prisma factory 已改为必填/fail-fast（[`module.ts:33`](../../packages/reminder/src/api/module.ts:33) 至 [`module.ts:48`](../../packages/reminder/src/api/module.ts:48)、[`prisma.ts:38`](../../packages/reminder/src/server/infrastructure/prisma.ts:38) 至 [`prisma.ts:48`](../../packages/reminder/src/server/infrastructure/prisma.ts:48)）；Notification/Repository 底层 service/factory 也有 fail-fast，API main 明确把 closure-operation checker 注入三模块（[`main.ts:123`](../../apps/api/src/main.ts:123) 至 [`main.ts:125`](../../apps/api/src/main.ts:125)、[`main.ts:179`](../../apps/api/src/main.ts:179) 至 [`main.ts:183`](../../apps/api/src/main.ts:183)、[`main.ts:213`](../../apps/api/src/main.ts:213) 至 [`main.ts:226`](../../apps/api/src/main.ts:226)）。Repository service 在远端写入前返回 `FORBIDDEN`（[`knowledge-note-commit.service.ts:131`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:131) 至 [`knowledge-note-commit.service.ts:142`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.ts:142)），本轮全量 180/180 通过，拒绝用例已修绿。但 canonical API factory 仍不是必填：Notification options/context 为可选、factory 有默认空 options 和 `async () => false` fallback，并继续导出无参数 singleton（[`module.ts:45`](../../packages/notification/src/api/module.ts:45) 至 [`module.ts:80`](../../packages/notification/src/api/module.ts:80)、[`module.ts:108`](../../packages/notification/src/api/module.ts:108)）；Repository 同样保留可选 options/context、默认空 options、no-op fallback 和无参数 singleton（[`module.ts:45`](../../packages/repository/src/api/module.ts:45) 至 [`module.ts:93`](../../packages/repository/src/api/module.ts:93)、[`module.ts:119`](../../packages/repository/src/api/module.ts:119)）。Notification 的 schedule new-work port 也默认 `async () => false`（[`prisma.ts:86`](../../packages/notification/src/server/infrastructure/prisma.ts:86) 至 [`prisma.ts:96`](../../packages/notification/src/server/infrastructure/prisma.ts:96)）。此外 API AI reminder adapter 的 checker 只读取 account status（[`backend-automation-tool-executor.adapter.ts:41`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.ts:41) 至 [`backend-automation-tool-executor.adapter.ts:52`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.ts:52)），在 closure operation 已 requested/revoking、account 尚未 close 的窗口仍会放行。 | **Repository 拒绝测试已关闭；production 必填/no-op 与 closure-in-progress 旁路仍为 P0** |
| PII/retention reason 贯穿 receipt | Cloud Auth revocation adapter 为 retained/not-performed 返回可审计原因（[`cloud-auth-revocation.adapter.ts:48`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:48) 至 [`cloud-auth-revocation.adapter.ts:61`](../../packages/account/src/server/infrastructure/adapters/cloud-auth/cloud-auth-revocation.adapter.ts:61)）；Coordinator 接收 reason、随 revoking -> revoked CAS 保存并把缺省归一化为 null（[`account-closure-coordinator.ts:274`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:274) 至 [`account-closure-coordinator.ts:310`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:310)），Prisma record/schema 均有 `piiReason`，最终 receipt 显式输出 null 或字符串（[`account-closure-operation-prisma.repository.ts:223`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:223) 至 [`account-closure-operation-prisma.repository.ts:240`](../../packages/account/src/server/infrastructure/adapters/prisma/account-closure-operation-prisma.repository.ts:240)、[`account-closure-coordinator.ts:495`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:495) 至 [`account-closure-coordinator.ts:517`](../../packages/account/src/server/application/services/account-closure-coordinator.ts:517)、[`account-lifecycle.dto.ts:24`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:24) 至 [`account-lifecycle.dto.ts:39`](../../packages/contracts/src/modules/account/api/account-lifecycle.dto.ts:39)）。 | **已关闭** |

### 真实双执行者测试真实性

第五轮没有提供所宣称的新测试。`account:test:integration` 仍只有 3 files / 12 tests，与第四轮完全相同；inventory 从 969 增至 970 的新增 integration 文件是 apps/api Express auth 测试，不是慢副作用测试。

现有并发覆盖只能证明 repository CAS/fencing：首次并发用例确实并发调用两个 Coordinator，但没有慢副作用、短 lease、第二实例接管尝试或 revoke/close/publish 精确计数（[`account-closure-coordinator-db.integration.test.ts:25`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:25) 至 [`account-closure-coordinator-db.integration.test.ts:62`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:62)）；failed retrier 用例仍只并发调用 repository `claimOwnership()`（[`account-closure-coordinator-db.integration.test.ts:137`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:137) 至 [`account-closure-coordinator-db.integration.test.ts:187`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:187)）；slow-side-effect 用例如上仍是人工 seed/CAS。故不能据此证明 heartbeat 跨 lease 时真实副作用 only-once，也不能证明 AccountClosedWorker 两实例慢 consumer 场景。

### 回归与本地验证（第五轮）

- 环境仍无 `pnpm`；使用项目 target 中对应的直接 Node 命令。由于 apps/api 从 `node_modules` package dist 加载 Cloud Auth，先重建 `packages/cloud-auth/dist` 再运行 Express integration。
- apps/api integration：**通过**，3 files / 6 tests；包含 Express sign-in/get-session/refresh closure 403、active 放行以及既有 consumer-chain。
- Cloud Auth：4 files / 18 tests **通过**。
- Account unit：24 files / 168 tests **通过**；Account integration：3 files / 12 tests **通过**，但真实性边界如上。
- Reminder：47 files / 373 tests **通过**；Notification：34 files / 230 tests **通过**；Repository：28 files / 180 tests **通过**，闭户拒绝测试实际返回 `FORBIDDEN`。
- Account、Cloud Auth、Reminder、Notification、Repository、apps/api 六个核心 typecheck **通过**。
- inventory **970**（unit 840 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。
- governance-check 的直接治理审计 **通过**。Nx 聚合入口在当前无 `pnpm` 环境下无法作为标准入口复现；不影响上述代码阻塞判定。

W0--W2 既有 account/reminder/notification/repository 测试集合未见数量下降或断言回退；Express 修复也没有回退 Fetch Cloud Auth 18/18。W3 仍未放行，故不存在可据此宣称的 W4 已放行实现。

### 第五轮终审判定

| 级别 | 第五轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | 真实双执行者慢副作用/慢 consumer heartbeat only-once 测试缺失，worker 批量预领取消息存在未续租窗口；Notification/Repository API factory 与 Notification schedule port 仍有 no-op fallback，AI reminder checker 不覆盖 requested/revoking | **2 项发布阻塞** |
| P1 | PII/retention reason 已关闭；四审既有 failed 双 Coordinator retry/crash-window 证明、worker Repository consumer 必填、failed receipt transport 语义仍未见新关闭证据 | **3 项保留** |
| P2 | phase/status/PII status 仍为数据库自由字符串；较宽 `./server` public surface 未收敛 | **2 项保留** |

**终审：W3 第五轮不可放行。当前不能提交 W3，也不能进入 W4。**

放行前至少必须：删除 Notification/Repository/Notification schedule 等 production new-work composition 的 no-op checker fallback并让类型/启动路径 fail-fast；让 AI/desktop 等旁路使用能识别 closure operation requested/revoking 的同一 durable policy；修正 AccountClosedWorker 对整批已领取消息的租约管理；新增两个真实 Coordinator 与两个真实 Worker 的慢副作用/慢 consumer 跨 lease 测试，实际等待 heartbeat、尝试第二执行者 claim，并分别断言 revoke/close/publish 与三类 consumer 副作用 only-once。现有 Express、Repository `FORBIDDEN` 和 PII reason 修复应保留。

## 第六轮审查

本轮只复核五审遗留的 2 项 P0 及其直接回归面。审查只追加本文档，未修改业务代码，未 commit/push。

### 结论

**W3 第六轮仍不可放行，不能提交 W3，也不能进入 W4。**

第六轮真实关闭了并发测试与 worker 批量租约窗口：Account integration 现在有两个真实 Coordinator 的慢 revocation 跨 lease 用例，以及两个真实 AccountClosedWorker 的慢 consumer、lease 到期接管和三类真实 consumer 幂等消费用例；worker 也已改成循环内逐条查找、逐条 CAS claim，claim 成功后立即为当前消息启动 heartbeat，不再预领整批消息。

Notification/Repository API factory 的 production no-op fallback 已删除，缺 checker 时会在 register 阶段 fail-fast；Notification schedule port 和 Reminder API factory 也已成为必填依赖，三个 API singleton 导出均已移除。服务端 AI reminder 旁路会同时检查 account 状态和 durable closure operation 的 `requested`/`revoking`/`closing` phase。

但五审 P0 的 Desktop 部分没有关闭。Desktop 的 Reminder/Notification PowerSync factory 仍只查询本地 `accounts.status`，Desktop AI 又直接使用同一个 Reminder PowerSync factory；它们没有读取 durable closure operation，也没有一个在远端 close 请求开始时立即关闭本地新工作入口的等价 policy。Desktop 只在远端 close 请求成功返回后才调用 `closeCurrentCloudConnection()`，再停止 scheduler、销毁 profile modules 和 PowerSync。因此在云端 operation 已进入 requested/revoking、业务 Account 尚为 Active 且 HTTP 请求仍等待的窗口，本地 AI/scheduler/new-work 路径仍可放行。这正是五审要求收敛的 Desktop 旁路，属于剩余发布阻塞。

### 五审 2 P0 逐条核验

| 五审阻塞 | 第六轮证据与判断 | 结论 |
| --- | --- | --- |
| 真实双 Coordinator/双 Worker 慢副作用 only-once + worker 批量租约窗口 | Worker 现在在 `for (i < limit)` 内逐条生成 token、查一条 candidate 并用 `id + pending + unowned/expired lease` 做条件 claim（[`account-closed.worker.ts:33`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:33) 至 [`account-closed.worker.ts:75`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:75)）；claim 后立即读取当前消息并启动当前消息 heartbeat（[`account-closed.worker.ts:77`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:77) 至 [`account-closed.worker.ts:111`](../../packages/account/src/server/infrastructure/workers/account-closed.worker.ts:111)），不存在先领取整批、排队消息尚未续租的窗口。双 Coordinator 测试创建 `coordA`/`coordB`，A 的真实 Cloud Auth revoke 后额外等待 2000ms，lease 为 1500ms且 heartbeat 开启；测试等待 1600ms 后让 B 调用同一 operation，并断言同一 operation、A succeeded、B running/succeeded、revoke 精确为 1（[`account-closure-coordinator-db.integration.test.ts:68`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:68) 至 [`account-closure-coordinator-db.integration.test.ts:152`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:152)）。双 Worker 测试 seed 三类真实 pending work 和一条 outbox，worker1 的真实 Reminder consumer 完成后等待 2000ms且关闭 heartbeat，1600ms 后 worker2 对同一消息实际接管并完成；最终三类目标状态、恰好三个分 consumer Inbox receipt 和 outbox dispatched 均有断言（[`account-closure-coordinator-db.integration.test.ts:226`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:226) 至 [`account-closure-coordinator-db.integration.test.ts:427`](../../packages/account/src/server/infrastructure/adapters/prisma/__tests__/account-closure-coordinator-db.integration.test.ts:427)）。本轮实跑真实 `127.0.0.1:5433/memoflow_test`，3 files / 14 tests 全绿。 | **已关闭** |
| production checker no-op 清除 + AI/Desktop closure-in-progress policy | Notification API register 只接受 `options.closureChecker ?? context.closureChecker`，缺失即抛错，且无 singleton 导出（[`module.ts:71`](../../packages/notification/src/api/module.ts:71) 至 [`module.ts:108`](../../packages/notification/src/api/module.ts:108)）；Repository 同理（[`module.ts:77`](../../packages/repository/src/api/module.ts:77) 至 [`module.ts:120`](../../packages/repository/src/api/module.ts:120)）。Notification Prisma factory 与 schedule port 都要求 checker 并显式 fail-fast（[`prisma.ts:34`](../../packages/notification/src/server/infrastructure/prisma.ts:34) 至 [`prisma.ts:42`](../../packages/notification/src/server/infrastructure/prisma.ts:42)、[`prisma.ts:86`](../../packages/notification/src/server/infrastructure/prisma.ts:86) 至 [`prisma.ts:99`](../../packages/notification/src/server/infrastructure/prisma.ts:99)）；Reminder API options 为必填且不再导出 singleton（[`module.ts:33`](../../packages/reminder/src/api/module.ts:33) 至 [`module.ts:48`](../../packages/reminder/src/api/module.ts:48)、[`module.ts:67`](../../packages/reminder/src/api/module.ts:67) 至 [`module.ts:75`](../../packages/reminder/src/api/module.ts:75)）。API AI checker 查询 Account 后继续查询 operation phase `requested/revoking/closing`（[`backend-automation-tool-executor.adapter.ts:41`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.ts:41) 至 [`backend-automation-tool-executor.adapter.ts:61`](../../apps/api/src/modules/ai/backend-automation-tool-executor.adapter.ts:61)），对应单测也验证该 query 被注入。可是 Desktop Reminder/Notification PowerSync checker 仍只查 `accounts.status`（[`powersync.ts:34`](../../packages/reminder/src/server/infrastructure/powersync.ts:34) 至 [`powersync.ts:58`](../../packages/reminder/src/server/infrastructure/powersync.ts:58)、[`powersync.ts:337`](../../packages/notification/src/server/infrastructure/powersync.ts:337) 至 [`powersync.ts:357`](../../packages/notification/src/server/infrastructure/powersync.ts:357)），Desktop AI 直接创建该 Reminder PowerSync module（[`desktop-automation-tool-executor.adapter.ts:33`](../../apps/desktop/src/main/modules/ai/desktop-automation-tool-executor.adapter.ts:33) 至 [`desktop-automation-tool-executor.adapter.ts:45`](../../apps/desktop/src/main/modules/ai/desktop-automation-tool-executor.adapter.ts:45)）。远端 close 成功返回后才断开当前 cloud connection（[`main.ts:230`](../../apps/desktop/src/main/main.ts:230) 至 [`main.ts:251`](../../apps/desktop/src/main/main.ts:251)），profile deactivate 才停止 scheduler/销毁模块（[`desktop-profile-runtime-manager.ts:324`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:324) 至 [`desktop-profile-runtime-manager.ts:330`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:330)）。 | **服务端 no-op 与 AI 已关闭；Desktop requested/revoking 旁路仍为 P0** |

### no-op 全仓扫描

对 `packages/notification`、`packages/repository`、`packages/reminder`、`packages/account`、`packages/cloud-auth` 和 `apps/api/src` 扫描 `async (...) => false`、checker fallback 与无参 API module 创建点后，**production 代码没有残留 closure checker no-op fallback**。

搜索仍命中测试中的显式“账户未关闭”stub：Notification API/Prisma/PowerSync 测试、Reminder use-case 测试，以及 Repository service 测试 helper 的 `overrides.closureChecker ?? (async () => false)`（[`knowledge-note-commit.service.spec.ts:293`](../../packages/repository/src/server/application/services/knowledge-note-commit.service.spec.ts:293)）。这些只存在于测试装配，不会进入 production composition；Notification 缺 checker 的测试还明确断言 register fail-fast（[`notification-lane-bootstrap.spec.ts:80`](../../packages/notification/src/api/__tests__/notification-lane-bootstrap.spec.ts:80) 至 [`notification-lane-bootstrap.spec.ts:85`](../../packages/notification/src/api/__tests__/notification-lane-bootstrap.spec.ts:85)）。因此不能把测试 double 误报为生产 fallback。

### 测试真实性边界

- 双 Coordinator 用例确实驱动两个 Coordinator 和真实 Prisma/Cloud Auth adapter；它直接计数的是 revocation，Account close/publish 由最终 closed/succeeded operation 间接覆盖，没有独立的 close/publish spy 计数。
- 双 Worker 用例确实驱动两个 Worker 和三类真实 consumer。worker1 故意关闭 heartbeat，使 lease 真正到期并允许 worker2 接管；这与 Coordinator 用例“heartbeat 保持所有权、第二执行者 claim 失败”是两个互补故障模型。三类 only-once 通过 consumer 的事务性 Inbox 唯一 receipt、目标状态和最终 dispatched 状态证明，而不是通过 wrapper 函数调用次数证明。
- 三个 consumer 都先查 Inbox receipt，未命中时在同一事务内执行取消/归档并创建 receipt（Reminder：[`reminder-account-closed.consumer.ts:11`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:11) 至 [`reminder-account-closed.consumer.ts:61`](../../packages/reminder/src/server/infrastructure/consumers/reminder-account-closed.consumer.ts:61)；Notification：[`notification-account-closed.consumer.ts:11`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:11) 至 [`notification-account-closed.consumer.ts:49`](../../packages/notification/src/server/infrastructure/consumers/notification-account-closed.consumer.ts:49)；Repository：[`repository-account-closed.consumer.ts:11`](../../packages/repository/src/server/infrastructure/consumers/repository-account-closed.consumer.ts:11) 至 [`repository-account-closed.consumer.ts:62`](../../packages/repository/src/server/infrastructure/consumers/repository-account-closed.consumer.ts:62)），所以本用例能证明每类事务副作用只成功提交一次。

### W0--W2 回归与本地验证（第六轮）

- Account unit：24 files / 168 tests **通过**。
- Account integration：3 files / 14 tests **通过**，真实 PostgreSQL/Prisma（`127.0.0.1:5433/memoflow_test`），包含新增双 Coordinator 与双 Worker 用例。
- Notification：34 files / 231 tests **通过**；Reminder：47 files / 373 tests **通过**；Repository：28 files / 180 tests **通过**；Cloud Auth：4 files / 18 tests **通过**。
- apps/api integration：3 files / 6 tests **通过**；AI checker 指定单测：1 file / 3 tests **通过**。
- Account、Notification、Reminder、Repository、Cloud Auth、apps/api 六个核心 typecheck **通过**；Account build **通过**。
- inventory `--check` **通过**：970 files（unit 840 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。
- 标准 `pnpm nx run memoflow:governance-check` 无法启动，因为当前环境没有可执行 `pnpm`，且本地 `node_modules/.bin/nx` 是指向缺失 `node_modules/nx/dist/bin/nx.js` 的断链；按该 target 的完整 command 直接运行 21 项 docs/governance audit，**全部通过**。

W0--W2 的 Account/Reminder/Notification/Repository/Cloud Auth 既有测试数量未下降且全绿；Account build、API integration 与六个 typecheck 也未见回退。Repository 新增 `./server` export 与 tsup server entry 分别存在于 [`package.json:29`](../../packages/repository/package.json:29) 至 [`package.json:32`](../../packages/repository/package.json:32)、[`tsup.config.ts:4`](../../packages/repository/tsup.config.ts:4) 至 [`tsup.config.ts:10`](../../packages/repository/tsup.config.ts:10)，Account 已声明 `@memoflow/repository` 依赖（[`package.json:31`](../../packages/account/package.json:31) 至 [`package.json:46`](../../packages/account/package.json:46)），没有复现五审前的 package surface/build 问题。

### 第六轮终审判定

| 级别 | 第六轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | 真实双执行者与 worker 单条 claim 已关闭；production no-op 与服务端 AI 已关闭；Desktop PowerSync/AI 在 requested/revoking 窗口仍只按 Active account 放行 | **1 项发布阻塞** |
| P1 | 五审保留项未见新关闭证据：failed 双 Coordinator retry/真实 crash-window 证明不足；worker 的 Repository consumer 仍为可选；failed receipt transport 语义仍未收敛 | **3 项保留** |
| P2 | phase/status/PII status 仍为数据库自由字符串；较宽 `./server` public surface 未收敛 | **2 项保留** |

**终审：W3 第六轮仍不可放行。当前不能提交 W3，也不能进入 W4。**

放行前必须让 Desktop 的 Reminder、Notification、schedule notification 与 AI reminder 等本地新工作入口，在远端 operation 已 requested/revoking、Account 尚未 Deactivated 的窗口使用同一 durable closure policy，或在发起 close 后立即以等价的 durable 本地状态停掉这些入口；同时补一个覆盖该窗口的 Desktop/PowerSync 负向测试。无需回退本轮已通过的单条 claim、双执行者、production fail-fast 与服务端 AI 修复。

## 第七轮审查

本轮只复核六审遗留的 Desktop closure-in-progress 旁路及其直接回归面。审查只追加本文档，未修改业务代码，未 commit/push。

### 结论

**W3 第七轮仍不可放行，不能提交 W3，也不能进入 W4。**

第七轮建立了正确的局部机制：PowerSync schema 已加入 local-only `account_closure_requested`；canonical Desktop close handler 的确先等待 `markAccountClosing()`、再发起云端 POST；Reminder 与 Notification PowerSync checker 都会在 Account 缺失、非 Active、marker 存在或查询异常时 fail-closed，两个 PowerSync module factory 也把 checker 注入 create use case。对应 Account 顺序测试、Reminder 5 项 checker 测试和 Notification 4 项 checker 测试均通过。

但端到端 closure policy 仍未成立。云端关闭成功后，代码先删除 marker，再只移除 token 和关闭 PowerSync 云同步；该流程不会停掉 scheduler、销毁模块或停用本地 Profile，且本地 Account 在断连前没有保证已同步为 Deactivated。于是 marker 删除后，只要本地 Account 仍为 Active，同一 checker 会重新返回未关闭，并且断连后该 Active 投影可能不再得到远端修正。此外，Desktop scheduler 的 schedule-notification port 仍无 checker 构造 `CreateNotificationUseCase`，会在 Profile 业务模块注册阶段直接抛错。`markAccountClosing`/`afterCloudAccountClosed` 也仍是可选依赖并以可选链调用，canonical marker writer 在取不到 identity 时静默返回；这保留了六审明确禁止的 no-op 旁路。

### 六审 P0 逐项核验

| 六审要求 | 第七轮证据与判断 | 结论 |
| --- | --- | --- |
| 云端 close 请求开始前立即阻断本地新工作 | CLOSE handler 在云端调用前 `await syncOptions.markAccountClosing?.()`，随后才调用 `closeCloudAccount()`（[`index.ts:100`](../../packages/account/src/electron/index.ts:100) 至 [`index.ts:130`](../../packages/account/src/electron/index.ts:130)）；新增顺序测试也用 invocation order 证明 mark 先于 close。canonical writer 使用同一 profile DB 插入 marker（[`main.ts:249`](../../apps/desktop/src/main/main.ts:249) 至 [`main.ts:256`](../../apps/desktop/src/main/main.ts:256)）。 | **局部满足；no-op 契约使其未成为强不变量** |
| requested/revoking 窗口的 Reminder/Notification/AI 门禁 | Reminder checker 先验证 Account 必须为 Active，再查询 marker，任一查询异常均返回 blocked；factory 将其注入 Reminder module（[`powersync.ts:42`](../../packages/reminder/src/server/infrastructure/powersync.ts:42) 至 [`powersync.ts:77`](../../packages/reminder/src/server/infrastructure/powersync.ts:77)）。Notification 有同等逻辑并注入 module（[`powersync.ts:294`](../../packages/notification/src/server/infrastructure/powersync.ts:294) 至 [`powersync.ts:313`](../../packages/notification/src/server/infrastructure/powersync.ts:313)、[`powersync.ts:364`](../../packages/notification/src/server/infrastructure/powersync.ts:364) 至 [`powersync.ts:374`](../../packages/notification/src/server/infrastructure/powersync.ts:374)）。Desktop AI 的 reminder action 复用 Reminder PowerSync module，因此该 action 受同一 checker 保护。 | **module create use-case 局部关闭；schedule notification composition 仍断裂（P0）** |
| Account 缺失/查询失败 fail-closed | 两个 checker 实现都对 Account 缺失或非 Active 返回 `true`，并在 catch 中返回 `true`。Reminder 5 项测试覆盖 Active、marker、Deactivated、缺行、查询失败；Notification 4 项覆盖前四项，查询失败虽由实现保证但没有对应测试。 | **实现满足；Notification 测试覆盖有缺口（P1）** |
| 成功后安全清理 marker，避免永久锁死且不重新放行 | `afterCloudAccountClosed()` 先无条件删除全部 marker，再调用 `closeCurrentCloudConnection()`（[`main.ts:258`](../../apps/desktop/src/main/main.ts:258) 至 [`main.ts:261`](../../apps/desktop/src/main/main.ts:261)）。该 callback 实际只删除 token 并调用 `disableCloudSync()`（[`main.ts:345`](../../apps/desktop/src/main/main.ts:345) 至 [`main.ts:349`](../../apps/desktop/src/main/main.ts:349)）；`disableCloudSync()` 明确保留 active local Profile，且断连失败也被吞掉（[`desktop-profile-runtime-manager.ts:159`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:159) 至 [`desktop-profile-runtime-manager.ts:162`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:162)）。真正停止 scheduler、销毁 profile modules 和关闭 PowerSync 只发生在 `deactivateProfile()`（[`desktop-profile-runtime-manager.ts:324`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:324) 至 [`desktop-profile-runtime-manager.ts:330`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:330)），本流程没有调用它。 | **未满足（P0）：成功后重新打开本地旁路** |
| 无 no-op fallback、无契约破坏 | `markAccountClosing` 与 `afterCloudAccountClosed` 在 options contract 中仍为可选（[`desktop-account-profile-sync.ts:17`](../../packages/account/src/electron/desktop-account-profile-sync.ts:17) 至 [`desktop-account-profile-sync.ts:28`](../../packages/account/src/electron/desktop-account-profile-sync.ts:28)），handler 以可选链跳过两者；canonical writer 取不到 cloud identity 时直接 `return`，不会阻止随后的云端 POST。另一方面，Desktop 在组装 schedule orchestration 时调用 `createNotificationPowerSyncScheduleNotificationPort(db)`（[`main.ts:123`](../../apps/desktop/src/main/main.ts:123) 至 [`main.ts:128`](../../apps/desktop/src/main/main.ts:128)），该 port 无第四参数构造 `CreateNotificationUseCase`（[`electron/index.ts:71`](../../packages/notification/src/electron/index.ts:71) 至 [`electron/index.ts:87`](../../packages/notification/src/electron/index.ts:87)），而 use case 明确在 checker 缺失时抛错（[`create-notification.use-case.ts:45`](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:45) 至 [`create-notification.use-case.ts:54`](../../packages/notification/src/server/application/use-cases/commands/create-notification.use-case.ts:54)）。 | **未满足（P0）：既有静默旁路，也有 Profile 注册期运行时崩溃** |

### Findings

#### P0-1：marker 在不安全状态下被清除，云端成功后本地新工作会重新放行

marker 的安全清除条件不能只是“HTTP close 返回成功”，而必须是本地 Account 已可靠变为非 Active，或承载新工作的本地 runtime 已停止。当前顺序是 `DELETE marker -> remove token -> disable cloud sync`；既没有等待 PowerSync 把 Account 投影同步为 Deactivated，也没有 deactivate Profile。若 close receipt 返回时本地 Account 仍为 Active，删除 marker 后 Reminder/Notification checker 将同时得到“Active + no marker”并返回允许；随后云同步被关闭，这个 Active 投影还可能长期保持。marker 由临时窗口门禁变成了在最关键状态被提前撤销的门禁。

删除语句还没有按 identity 限定，会清空该 DB 中全部 marker；虽然当前 profile-per-DB 设计降低了误删概率，但它没有表达 checker 与清理目标必须是同一 identity 的契约。无论采用“保留 marker 直到本地 Account 非 Active”还是“先可靠停用 profile/runtime 再清理”，都必须补一项真实状态迁移测试，证明 cloud success 后不会出现 `Active + no marker + runtime active`。

#### P0-2：Desktop schedule-notification composition 缺 checker，Profile 注册会直接失败

`registerBusinessModules()` 在创建 schedule orchestration 时同步调用 `createNotificationPowerSyncScheduleNotificationPort(db)`。该 factory 直接 `new CreateNotificationUseCase(...)`，没有传入 closure checker；构造器则对缺失 checker 立即抛出 `[FAIL-CLOSED] CreateNotificationUseCase requires closureChecker`。因此这不是某个 schedule 到点后才暴露的边缘错误，而是 active Profile 业务模块注册阶段的确定性失败。

Notification 235 项绿测没有调用这个 factory；全仓也没有该 PowerSync schedule port 的测试。六审已要求 schedule notification 走同一 policy，本轮虽然为 `createNotificationPowerSyncModule()` 注入 checker，却漏掉了绕过 module factory、直接构造 use case 的 schedule port。

#### P0-3：closure marker 仍允许静默 no-op，云端请求前门禁不是强制契约

`closeCloudAccount` 缺失时 handler 会 fail-fast，但同等安全级别的 `markAccountClosing` 和 `afterCloudAccountClosed` 却仍为 optional 并通过可选链调用。已有“closes cloud first”测试甚至不提供 marker callback 仍期待成功，这固化了无 marker 也可发起云端关闭的旁路。canonical `markAccountClosing()` 又在取不到 identity 时静默返回；handler 在取得 token 前虽检查过一次 cloud identity，但经过异步 token 获取后再读取 descriptor，缺少把已认证 `identityId` 作为 marker key 传入并强制写成功的契约。

这违反六审已确认的 production checker 必填/fail-fast 原则。对云端 close 能力存在的 composition，marker writer 和安全完成 callback 应同样必填；写 marker 失败或 identity 不一致必须阻止云端 POST，而不是继续执行。

#### P1-1：新增测试只覆盖分离单元，未覆盖真实 Desktop closure 状态机

Account 顺序测试只验证三个 mock 的 invocation order，没有使用实际 profile DB、marker writer、HTTP promise 和 cleanup callback，也没有断言云端调用悬挂期间 create use case 被拒绝、云端失败时 marker 保留、云端成功后 runtime/Account/marker 的安全组合。Reminder/Notification checker 测试手工创建普通 SQLite 表；它们证明查询函数本身，但没有驱动 PowerSync schema 与 Desktop composition。Notification 版本还缺查询失败用例。

因此“mark before POST”和“checker sees marker”分别成立，不能推出同一真实 close 流程端到端成立；本轮两个 P0 正是分离测试之间的接线与生命周期缺口。

### 回归与本地验证（第七轮）

- Account unit：24 files / 169 tests **通过**；Account integration：3 files / 14 tests **通过**，真实 PostgreSQL/Prisma。
- Reminder：48 files / 378 tests **通过**；其中新增 PowerSync checker 1 file / 5 tests **通过**。
- Notification：35 files / 235 tests **通过**；其中新增 PowerSync checker 1 file / 4 tests **通过**。
- Account Electron 指定测试：1 file / 5 tests **通过**；新增顺序断言有效，但边界如 P1-1。
- Desktop direct `tsc --noEmit -p apps/desktop/tsconfig.json` **未通过**：当前工作区的 source/dist 双份私有类型导致 7 项既有路径类型冲突，未出现 closure 新增代码的类型报错。由于环境没有 `pnpm` 且本地 Nx 入口断裂，无法按标准 Nx 依赖构建链复现提交方宣称的 Desktop typecheck green；不能把本次 direct tsc 当作第七轮 closure 回归，但也不能据此确认 Desktop 标准 typecheck 已复核通过。
- inventory `--check` **通过**：972 files（unit 842 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。
- 按 `memoflow:governance-check` target 的完整 command 直接运行 21 项 docs/governance audit，**全部通过**。
- 上述单测数量未见 W0--W2 用例删除或断言回退；六审的双 Coordinator、双 Worker 与 consumer 代码未被本轮改动触及，Account integration 14/14 仍通过。绿测不覆盖本轮发现的 Desktop schedule composition 与 marker 清理后状态。

### 第七轮终审判定

| 级别 | 第七轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | close success 后在 local Account/runtime 未安全关闭时删除 marker；Desktop schedule-notification port 缺 checker并在 Profile 注册期抛错；marker writer/cleanup 仍为 optional no-op 契约 | **3 项发布阻塞** |
| P1 | 新测试缺少真实 Desktop close-window/cleanup/composition 覆盖，Notification 未测查询失败；六审保留的 failed retry/crash-window、Repository consumer 必填、failed receipt 语义未见本轮关闭证据 | **新增 1 项，既有 3 项保留** |
| P2 | 六审保留的数据库自由字符串与较宽 `./server` public surface 本轮未处理 | **2 项保留** |

**终审：W3 第七轮不可放行。当前不能提交 W3，也不能进入 W4。**

放行前至少必须：让 Desktop cloud-close composition 对 marker write/安全完成 callback 强制 fail-fast；让 marker 保留到本地 Account 已非 Active，或先可靠停止 scheduler/profile modules/PowerSync 后再清理，且不得吞掉导致门禁重开的断连失败；给 PowerSync schedule-notification port 注入同一个 checker；新增一项真实 Desktop composition 测试，覆盖 marker 先于悬挂中的 cloud call、Reminder/Notification/schedule/AI reminder 在窗口内拒绝、cloud failure 的 marker 策略，以及 cloud success 后不存在 `Active + no marker + runtime active`。现有 checker fail-closed 与六审并发/worker 修复应保留。

## 第八轮审查

本轮只复核七审 3 项 P0 及其直接回归面；只追加本文档，未修改业务代码，未 commit/push。

### 结论

**W3 第八轮仍不可放行，不能提交 W3，也不能进入 W4。**

P0-2 与 P0-3 已关闭：Desktop schedule-notification port 已注入真实 PowerSync closure checker，Profile 注册不再因 `CreateNotificationUseCase` 缺 checker 同步抛错；`markAccountClosing`、`afterCloudAccountClosed` 已成为必填契约，close handler 对运行时缺失显式 fail-fast，canonical writer 取不到 cloud identity 时会抛错。

但 P0-1 只移动了 marker 删除位置，没有建立“安全 teardown 成功后才允许删除”的条件。`deactivateProfile()` 对 scheduler 停止和 modules 销毁失败均记录后继续，随后无条件删除 marker，并且删除发生在 PowerSync shutdown 之前（[`desktop-profile-runtime-manager.ts:324`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:324) 至 [`desktop-profile-runtime-manager.ts:335`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:335)）。更关键的是，此方法既用于用户锁定/切换，也用于应用退出；退出路径传入 `preserveSelection: true`，下次启动会重开同一持久 Profile（[`desktop-main-runtime.ts:60`](../../apps/desktop/src/main/desktop-main-runtime.ts:60) 至 [`desktop-main-runtime.ts:71`](../../apps/desktop/src/main/desktop-main-runtime.ts:71)）。cloud-close success callback 只移除 session 并断开云同步，没有把本地 Account 改为非 `Active`、移除 Profile 或触发 `deactivateProfile()`（[`main.ts:249`](../../apps/desktop/src/main/main.ts:249) 至 [`main.ts:266`](../../apps/desktop/src/main/main.ts:266)、[`main.ts:351`](../../apps/desktop/src/main/main.ts:351) 至 [`main.ts:355`](../../apps/desktop/src/main/main.ts:355)）。因此用户在 cloud close 成功后退出/锁定再重开，即可回到 `local Account Active + marker absent + modules active`；若 stop/destroy 失败，还会在同一次 deactivate 中更早重开旁路。七审要求的 marker 生命周期不变量仍未成立。

### 七审 3 项 P0 逐条核验

| 七审阻塞 | 第八轮证据与判断 | 结论 |
| --- | --- | --- |
| marker 在 Profile 真正 teardown 前持续阻断 | `afterCloudAccountClosed()` 已不再删除 marker，全仓业务代码也只剩 `deactivateProfile()` 一处 `DELETE FROM account_closure_requested`，这部分修复有效。但该删除不检查 Account status，不要求 `stopScheduleRuntime()`/`bootstrapper.destroy()` 成功，且早于 `shutdownPowerSync()`；普通 lock、profile switch 和保留选择的 app quit 都会清除持久 marker。持久 Profile 再激活时 local Account 可仍为 `Active`，closure checker 随即放行。 | **未关闭，仍为 P0** |
| schedule-notification composition 注入 checker | `createNotificationPowerSyncScheduleNotificationPort(db)` 现在把 `createPowerSyncClosureChecker(db)` 作为第四个参数传给 `CreateNotificationUseCase`（[`index.ts:72`](../../packages/notification/src/electron/index.ts:72) 至 [`index.ts:80`](../../packages/notification/src/electron/index.ts:80)）；checker 查询 Account 状态和 marker，并在查询失败时 fail-closed（[`powersync.ts:293`](../../packages/notification/src/server/infrastructure/powersync.ts:293) 至 [`powersync.ts:318`](../../packages/notification/src/server/infrastructure/powersync.ts:318)）。Desktop composition 在 Profile module 注册时调用的正是该 factory（[`main.ts:109`](../../apps/desktop/src/main/main.ts:109) 至 [`main.ts:129`](../../apps/desktop/src/main/main.ts:129)）。 | **已关闭** |
| marker/cleanup 契约不得静默 no-op | `DesktopAccountProfileSyncOptions.markAccountClosing` 与 `afterCloudAccountClosed` 均无 `?`（[`desktop-account-profile-sync.ts:17`](../../packages/account/src/electron/desktop-account-profile-sync.ts:17) 至 [`desktop-account-profile-sync.ts:29`](../../packages/account/src/electron/desktop-account-profile-sync.ts:29)）；handler 不再使用可选链，并对运行时缺失返回 `CLOUD_ACCOUNT_CLOSE_UNAVAILABLE`，写 marker、cloud POST、success callback 均在同一 fail-closed `try` 中按序执行（[`index.ts:100`](../../packages/account/src/electron/index.ts:100) 至 [`index.ts:139`](../../packages/account/src/electron/index.ts:139)）。canonical writer 缺 cloud binding 时抛错，写成功后才允许 POST（[`main.ts:249`](../../apps/desktop/src/main/main.ts:249) 至 [`main.ts:258`](../../apps/desktop/src/main/main.ts:258)）。 | **已关闭** |

### 测试边界与回归

- Account：24 files / 169 tests **通过**；`account:typecheck` 等价直接命令 **通过**。现有 Electron 5 项测试验证 marker 先于 cloud call，但没有覆盖“有效 token + marker callback 运行时缺失”的 fail-fast，也没有真实 Profile DB 的 close/deactivate/reopen 生命周期。
- Notification：35 files / 235 tests **通过**；typecheck **通过**。代码装配已足以确认 schedule port 不再缺 checker，但仍没有直接调用 `createNotificationPowerSyncScheduleNotificationPort()` 的 composition 测试。
- Reminder：48 files / 378 tests **通过**。
- Desktop：32 files / 165 tests **通过**；`tsc --noEmit -p apps/desktop/tsconfig.typecheck.json` **通过**。`DesktopProfileRuntimeManager.spec.ts` 只断言 deactivate 后 active id 为空和 shutdown 被调用，没有 marker 删除时序、teardown 失败或重启重开断言。
- `memoflow:governance-check` 的 21 项直接命令 **全部通过**；inventory `--check` **通过**：972 files（unit 842 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。
- 第八轮业务改动集中在 Desktop marker lifecycle、Account Electron contract 和 Notification Desktop composition；六审的 Coordinator/Worker fencing、W0--W2 既有模块实现未被改动，上述全量模块测试及 inventory 未见测试删除或断言回退。绿测仍没有覆盖本轮 P0 的 deactivate/reopen 反例。

### 第八轮终审判定

| 级别 | 第八轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | marker 可在 scheduler/modules teardown 失败后、PowerSync shutdown 前以及可重开的普通 Profile deactivate/app quit 中被删除；本地 Account 仍可能为 `Active` | **1 项发布阻塞** |
| P1 | 七审要求的真实 Desktop close-window/deactivate/reopen composition 测试仍缺失；六审保留的 failed retry/crash-window、Repository consumer 必填、failed receipt 语义本轮无新增关闭证据 | **既有 4 项保留** |
| P2 | 数据库自由字符串与较宽 `./server` public surface 本轮未处理 | **2 项保留** |

**终审：W3 第八轮不可放行。当前不能提交 W3，也不能进入 W4。**

放行前必须让 marker 的持久状态与本地 Account/Profile 生命周期一致：至少不得在 scheduler/modules/PowerSync 任一 teardown 未确认成功时删除，也不得因 app quit、lock 或可重开的 Profile deactivate 丢失 closure 状态。可选择先把本地 Account 持久化为非 `Active` 后再清 marker，或让 marker 跨 deactivate/restart 保留到 Profile 被永久移除/完成明确的本地闭户；并补真实 Profile DB 的 cloud success → deactivate/app quit → reopen 测试，以及 stop/destroy/shutdown 失败测试，证明任何路径都不会出现 `Active + no marker + 可运行 modules`。P0-2、P0-3 的本轮修复应保留。

## 第九轮审查

本轮只复核第八轮遗留的 marker 生命周期 P0、失败恢复与契约强度，并检查 W0--W2 及 W3 前轮的直接回归面；只追加本文档，未修改业务代码，未 commit/push。

### 结论

**W3 第九轮仍不可放行，不能提交 W3，也不能进入 W4。**

`deactivateProfile()` 中的 marker DELETE 已移除，普通 Profile 切换、锁定和保留选择的应用退出不再清除持久 marker；成功执行 `afterCloudAccountClosed()` 后也不清 marker。`clearAccountClosingMarker()` 已成为 TypeScript 必填契约，Electron handler 对运行时缺失会显式返回 `CLOUD_ACCOUNT_CLOSE_UNAVAILABLE`。这些修复关闭了第八轮指出的直接 deactivate/reopen 旁路。

但“marker 只在 cloud close 失败时清除”尚未成为真实控制流不变量。CLOSE handler 用同一个 `try/catch` 包住 `markAccountClosing()`、`closeCloudAccount()` 和 `afterCloudAccountClosed()`；因此 cloud close 已成功、仅本地成功收尾 callback 失败时，也会进入 catch 并删除 marker（[`index.ts:130`](../../packages/account/src/electron/index.ts:130) 至 [`index.ts:142`](../../packages/account/src/electron/index.ts:142)）。canonical callback 会先删除本地 cloud session，再断开同步；其中 `cloudSessionStore.remove()` 的异常没有在 callback 内收敛（[`main.ts:263`](../../apps/desktop/src/main/main.ts:263) 至 [`main.ts:269`](../../apps/desktop/src/main/main.ts:269)、[`main.ts:350`](../../apps/desktop/src/main/main.ts:350) 至 [`main.ts:358`](../../apps/desktop/src/main/main.ts:358)）。一旦该删除失败，云端账号已经关闭，但本地 Account 仍可能为 `Active`、Profile modules 仍活跃，catch 又清除了唯一 marker，恰好重建 `Active + no marker + modules active`。同一宽 catch 还会在 marker 写入本身失败时执行无 identity 条件的 DELETE；若数据库中已有前次 marker，也存在误清风险。这仍是 P0。

### 审查要点逐条核验

| 审查项 | 第九轮证据与判断 | 结论 |
| --- | --- | --- |
| marker 只在 close 失败路径清除 | 全仓显式 SQL 删除只剩 canonical `clearAccountClosingMarker()`（[`main.ts:260`](../../apps/desktop/src/main/main.ts:260) 至 [`main.ts:262`](../../apps/desktop/src/main/main.ts:262)）；`deactivateProfile()` 已只保留 marker 并完成 runtime shutdown（[`desktop-profile-runtime-manager.ts:324`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:324) 至 [`desktop-profile-runtime-manager.ts:344`](../../apps/desktop/src/main/profile/desktop-profile-runtime-manager.ts:344)）。但调用清除 callback 的 catch 同时捕获 marker write、cloud close 和 success callback 三类失败，并非只捕获 cloud close 失败。 | **部分关闭；宽 catch 仍为 P0** |
| 切换、锁定、退出、重开不丢 marker | `prepareProfile()`/`prepareGuestProfile()` 的切换路径仍调用 `deactivateProfile()`，而该方法已不删除 marker；应用退出与锁定复用同一 deactivate 也不会再删除。永久 `removeProfile()` 会删除整个 Profile DB，但该 Profile 同时从 registry 移除，不属于可重开路径。 | **已关闭** |
| 成功后持续阻断直到账号非 Active | 正常 success callback 完成后没有清 marker；Reminder/Notification PowerSync checker 均先检查 Account 是否非 `Active`，再检查 marker，查询异常也 fail-closed（[`powersync.ts:42`](../../packages/reminder/src/server/infrastructure/powersync.ts:42) 至 [`powersync.ts:59`](../../packages/reminder/src/server/infrastructure/powersync.ts:59)、[`powersync.ts:294`](../../packages/notification/src/server/infrastructure/powersync.ts:294) 至 [`powersync.ts:311`](../../packages/notification/src/server/infrastructure/powersync.ts:311)）。因此本地 Closed 投影永不到达时，marker 仍持续阻断，不会安全性 fail-open；Account 已非 Active 时状态检查也独立阻断。例外仍是上述 success callback reject 后被 catch 误清。 | **正常路径满足；异常收尾路径仍为 P0** |
| close 失败恢复、避免永久锁死 | cloud POST reject 的新增测试验证返回 `CLOUD_ACCOUNT_CLOSE_FAILED`、调用 clear 且不调用 success callback（[`index.spec.ts:234`](../../packages/account/src/electron/index.spec.ts:234) 至 [`index.spec.ts:268`](../../packages/account/src/electron/index.spec.ts:268)）。但 handler 对 `clearAccountClosingMarker()` 的失败直接 `.catch(() => undefined)`，随后仍返回普通 close failure；marker 会永久保留且没有重试/恢复入口（[`index.ts:137`](../../packages/account/src/electron/index.ts:137) 至 [`index.ts:142`](../../packages/account/src/electron/index.ts:142)）。 | **主路径关闭；回滚失败为 P1 可用性缺口** |
| 必填契约与 fail-fast | `clearAccountClosingMarker()` 与既有 marker/success callbacks 一样没有 `?`（[`desktop-account-profile-sync.ts:21`](../../packages/account/src/electron/desktop-account-profile-sync.ts:21) 至 [`desktop-account-profile-sync.ts:31`](../../packages/account/src/electron/desktop-account-profile-sync.ts:31)）；handler 对运行时缺失逐项返回 `CLOUD_ACCOUNT_CLOSE_UNAVAILABLE`（[`index.ts:118`](../../packages/account/src/electron/index.ts:118) 至 [`index.ts:129`](../../packages/account/src/electron/index.ts:129)）。 | **代码契约已关闭；缺少 clear 缺失的直接运行时测试为 P1 测试缺口** |

### 测试边界与回归（第九轮）

- Account：24 files / 170 tests **通过**；typecheck **通过**。新增失败测试只令 `closeCloudAccount()` reject；没有覆盖 cloud close resolve 后 `afterCloudAccountClosed()` reject，也没有断言成功时 `clearAccountClosingMarker` 必须零调用。现有测试标题仍写“clears after success”，但测试体只断言 mark/close 顺序和 success callback 调用（[`index.spec.ts:185`](../../packages/account/src/electron/index.spec.ts:185) 至 [`index.spec.ts:231`](../../packages/account/src/electron/index.spec.ts:231)），未守住本轮核心不变量。
- Desktop：32 files / 165 tests **通过**；typecheck **通过**。仍没有真实 Profile DB 的 close success/failure → deactivate/app quit → reopen 生命周期测试，也没有 success callback failure 的 composition 覆盖。
- Reminder：48 files / 378 tests **通过**；Notification：35 files / 235 tests **通过**。PowerSync Account status + marker checker 与 schedule-notification 接线未见回退。
- `memoflow:governance-check` 的 21 项直接命令 **全部通过**；inventory `--check` **通过**：972 files（unit 842 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。环境无 `pnpm`，上述均使用各 Nx target 对应的直接 Node 命令。
- 第九轮业务差异集中在 Account Electron close handler、Desktop marker callback 与 deactivate lifecycle；W0--W2 和 W3 前轮既有模块测试集合未见删除或断言回退。绿测不足以覆盖本轮发现的异常收尾反例。

### 第九轮终审判定

| 级别 | 第九轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | cloud close 已成功但 `afterCloudAccountClosed()` 失败时，宽 catch 仍清 marker，可产生 `Active + no marker + modules active`；marker write 失败也会进入同一无条件清除路径 | **1 项发布阻塞** |
| P1 | close 失败后的 marker DELETE 失败被吞掉且无重试，可能永久锁住未关闭账号；缺少 success callback failure、成功零 clear、运行时 clear 缺失和真实 deactivate/reopen composition 测试；第八轮保留的 failed retry/crash-window、Repository consumer 必填、failed receipt 语义本轮无新增关闭证据 | **新增 2 项，既有 4 项保留** |
| P2 | 数据库自由字符串与较宽 `./server` public surface 本轮未处理 | **2 项保留** |

**终审：W3 第九轮不可放行。当前不能提交 W3，也不能进入 W4。**

放行前必须把 cloud close 与 success callback 分成可区分的异常边界：只有能够确定云端关闭失败时才允许清 marker；cloud close 已返回成功后，任何本地收尾失败都必须保留 marker 并 fail-closed。失败回滚不能静默吞掉 marker DELETE 失败，应提供可观测且可重试的恢复路径。至少补三项直接测试：cloud close reject → clear；cloud close resolve + success callback resolve → never clear；cloud close resolve + success callback reject → never clear，并保留第九轮已经正确移除的 deactivate DELETE。

## 第十轮审查

本轮只复核第九轮遗留的 team 7C P0（宽 catch 误清 marker）、失败恢复与直接回归面；只追加本文档，未修改业务代码，未 commit/push。

### 结论

**第九轮剩余的 1 项 P0 已关闭；W3 可放行，可提交 W3 并进入 W4。**

CLOSE handler 已把 marker 写入、cloud close 和成功后本地收尾拆成可区分的异常边界。`markAccountClosing()` 抛错会立即返回且不会调用 cloud close 或 clear；只有 `closeCloudAccount()` 抛错才进入 identity-scoped clear；cloud close 返回成功后，`afterCloudAccountClosed()` 即使抛错也只返回 `CLOUD_ACCOUNT_CLOSE_TEARDOWN_FAILED`，不会落入 cloud failure catch。canonical DELETE 同时以 `identity_id = ?` 和当前认证 identity 参数限定。由此，cloud success 后误清 marker、marker write failure 误删已有 marker以及无条件 DELETE 三个 P0 反例均不再成立。

cloud close 失败且 marker DELETE 成功时会恢复本地访问。DELETE 自身失败仍被吞掉：这选择了安全侧 fail-closed，且用户重试 close 会再次执行同一 mark/close/clear 流程，不会形成代码层面的不可重试终态；但 rollback failure 没有独立错误码、日志或后台重试，暂时性数据库故障期间可能持续锁住本地新工作。该问题不重新打开安全性 P0，但保留为 P1 可观测性/可用性缺口。

### 审查要点逐条核验

| 审查项 | 第十轮证据与判断 | 结论 |
| --- | --- | --- |
| marker 只在 cloud close 失败时清除 | marker write 有独立 `try/catch`，失败直接返回且不 clear（[`index.ts:130`](../../packages/account/src/electron/index.ts:130) 至 [`index.ts:139`](../../packages/account/src/electron/index.ts:139)）；外层 catch 的 `try` 从 `closeCloudAccount()` 开始，成功后的 callback 又由内层 catch 收敛（[`index.ts:140`](../../packages/account/src/electron/index.ts:140) 至 [`index.ts:159`](../../packages/account/src/electron/index.ts:159)）。因此 clear 的唯一调用点只对应 cloud call reject。 | **已关闭（P0）** |
| cloud success 后收尾失败保持 fail-closed | `afterCloudAccountClosed()` reject 返回专用 `CLOUD_ACCOUNT_CLOSE_TEARDOWN_FAILED`，不会重新抛给 cloud failure catch；新增测试明确断言 clear 零调用及错误码（[`index.spec.ts:271`](../../packages/account/src/electron/index.spec.ts:271) 至 [`index.spec.ts:317`](../../packages/account/src/electron/index.spec.ts:317)）。 | **已关闭（P0）** |
| marker write 失败不误清已有 marker | Phase 1 catch 中没有 clear，且 cloud close 不会开始；`INSERT OR IGNORE` 仍保留同 identity 的既有 marker（[`main.ts:249`](../../apps/desktop/src/main/main.ts:249) 至 [`main.ts:258`](../../apps/desktop/src/main/main.ts:258)）。行为由控制流保证，但尚缺 `markAccountClosing()` reject 的直接回归测试。 | **实现关闭；保留 P1 测试缺口** |
| DELETE identity 限定 | 契约要求 `clearAccountClosingMarker(identityId: string)`（[`desktop-account-profile-sync.ts:26`](../../packages/account/src/electron/desktop-account-profile-sync.ts:26) 至 [`desktop-account-profile-sync.ts:31`](../../packages/account/src/electron/desktop-account-profile-sync.ts:31)）；handler 传入认证 identity，canonical SQL 为 `DELETE ... WHERE identity_id = ?`（[`index.ts:153`](../../packages/account/src/electron/index.ts:153) 至 [`index.ts:158`](../../packages/account/src/electron/index.ts:158)、[`main.ts:260`](../../apps/desktop/src/main/main.ts:260) 至 [`main.ts:262`](../../apps/desktop/src/main/main.ts:262)）。全仓业务代码未发现其他 marker DELETE。 | **已关闭（P0）** |
| cloud 失败恢复与 clear 失败 fail-safe | cloud reject 测试断言 `clearAccountClosingMarker('cloud-1')` 且不运行 success callback（[`index.spec.ts:234`](../../packages/account/src/electron/index.spec.ts:234) 至 [`index.spec.ts:268`](../../packages/account/src/electron/index.spec.ts:268)）。clear reject 时 marker 保留，安全上 fail-closed，并可通过重新发起 close 重试整段流程；但 `.catch(() => undefined)` 隐藏 rollback failure，缺少独立观测和自动恢复。 | **主恢复路径关闭；P1 保留** |
| 必填契约与分阶段 fail-fast | marker writer、identity-scoped clear 和 success callback 在 TypeScript contract 中均为必填；handler 在任何副作用前逐项检查运行时能力（[`desktop-account-profile-sync.ts:26`](../../packages/account/src/electron/desktop-account-profile-sync.ts:26) 至 [`desktop-account-profile-sync.ts:31`](../../packages/account/src/electron/desktop-account-profile-sync.ts:31)、[`index.ts:118`](../../packages/account/src/electron/index.ts:118) 至 [`index.ts:129`](../../packages/account/src/electron/index.ts:129)）。 | **已关闭；clear 缺失的直接运行时测试仍缺** |

### 测试边界与回归（第十轮）

- 使用仓库本地 Nx CLI 复跑 Account、Reminder、Notification、Desktop 全量单测：Account **24 files / 171 tests**、Reminder **48 / 378**、Notification **35 / 235**、Desktop **32 / 165**，全部通过。
- Account 与 Desktop typecheck 均通过；该 Nx 调用同时成功完成其 28 个依赖任务。
- `memoflow:governance-check` 标准 Nx target 通过；inventory `--check` 通过：**972 files**（unit 842 / integration 17 / smoke 2 / boundary-ipc 9 / boundary-main 5 / e2e 63 / perf 4 / governance 30）。当前环境没有 `pnpm`，因此使用 `node node_modules/nx/dist/bin/nx.js` 调用相同 target。
- W0--W2 与 W3 前轮的 Account/Reminder/Notification/Desktop 既有测试数量未下降，marker checker、schedule-notification 接线、Profile deactivate 保留 marker、双 Coordinator/Worker 等已审查代码未见本轮回退。
- 本轮核心负向用例已补，但正常 success 用例仍没有显式断言 clear 零调用；另缺 marker write reject、clear reject 和运行时 clear 缺失的直接测试。第 185 行测试标题仍写“clears after success”，与当前 fail-closed 语义相反，属于 P2 测试可维护性问题。

### 第十轮终审判定

| 级别 | 第十轮状态 | 终审判定 |
| --- | --- | --- |
| P0 | 宽 catch 已拆分；cloud success/teardown failure 与 marker write failure 均不 clear；DELETE 已 identity-scoped | **0 项发布阻塞，team 7C P0 关闭** |
| P1 | clear failure 仍静默 fail-closed，缺少独立观测/自动重试；正常成功零 clear、marker write reject、clear reject、运行时 clear 缺失及真实 Desktop lifecycle composition 测试仍缺；前轮 failed retry/crash-window、Repository consumer 必填、failed receipt 语义没有新增关闭证据 | **本轮 2 项及既有项保留，不阻塞本次 P0 放行** |
| P2 | 数据库自由字符串、较宽 `./server` public surface及错误的测试标题本轮未处理 | **3 项保留** |

**终审：W3 第十轮可放行。可以提交 W3，并进入 W4。**

后续应优先让 marker rollback failure 可观测并具备明确重试/修复入口，同时补齐四条直接测试（normal success never-clear、marker write reject、clear reject、runtime clear missing）和真实 Desktop lifecycle composition 测试；这些保留项不应通过重新合并异常边界或在成功路径清 marker 来修复。
