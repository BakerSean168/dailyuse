# 全量检测报告（2026-05-01，重检版 v3）

## 概要

| 类别 | 状态 | 说明 |
|------|------|------|
| Typecheck | 通过 | 29 个目标全部通过（`utils:typecheck` 偶发 flaky） |
| Lint | 35/35 | 全部通过，0 errors |
| Test | 部分通过 | 单元测试通过，Prisma 集成测试需数据库环境 |

本轮修复：
- `ai-service:lint` 146 个 Ruff 错误全部清零（I001 自动修复 + E402/E501 手动修复）
- importance 枚举值回归已修复（`High`→`Important`、`Medium`→`Moderate`、`Low`→`Minor`）
- `goal`/`task` 循环依赖已拆除
- `goal:lint` 已通过
- ESLint `_` 前缀忽略规则已添加

---

## 1. Typecheck

全部 29 个目标通过。`utils:typecheck` 偶发 flaky。

---

## 2. Lint

全部 35 个目标通过，0 errors。

| Warnings 类型 | 数量 |
|------|------|
| `@typescript-eslint/no-explicit-any` | 1385 |
| `@typescript-eslint/no-unused-vars` | 458 |
| `vue/no-unused-vars` | 0 |

---

## 3. Test

### 3.1 单元测试

全部通过（`task:test` 623 passed，`goal:test` 273 passed，`ai-service:test` 77 passed 等）。

### 3.2 Prisma 集成测试（需数据库）

所有失败均因测试环境未启动 PostgreSQL 或外键约束问题：

| 项目 | 失败/总数 | 原因 |
|------|-----------|------|
| `schedule:test` | 2/257 | 外键约束（需 DB） |
| `goal:test` | 2/275 | 外键约束（需 DB） |
| `reminder:test` | 2/300 | 外键约束（需 DB） |
| `task:test` | 17/640 | 外键约束（需 DB） |

枚举值回归已修复，剩余失败均为数据库环境问题。

---

## 4. 剩余工作

有一个值得记录的事实：TaskInstance 的 Prisma 模型并不持久化完整 completionRecord/
  skipRecord，只持久化扁平字段，所以我把那条错误的 round-trip 断言收紧到了当前真实持久化
  边界。

### P2
2. 渐进清理 warnings（`no-unused-vars` 458、`no-explicit-any` 1385）
