# 全量检测报告（2026-05-01，重检版）

## 概要

旧版报告中的“全部通过”“0 errors”“最终版”结论已失效。

本次按标准测试环境重检后的当前结论：

| 类别 | 当前状态 | 说明 |
|------|----------|------|
| Lint | 失败 | `goal:lint`、`ai-service:lint` 失败；其余项目仍有大量 warnings |
| Typecheck | 失败 | `pnpm typecheck` 被 `goal -> task -> goal` 循环依赖阻断 |
| Test | 失败 | `pnpm test` 未通过；`goal`、`task` 的 package test 仍混入未正确准备的 Prisma repository integration 用例 |
| Specialized tests | 部分通过 | `api:test:smoke`、`desktop:test:ipc`、`desktop:test:main` 通过；4 个 integration target 全部失败 |

---

## 1. 检测前提

### 1.1 本次使用的标准测试环境

- 启动了 `docker compose --profile test up -d postgres-test`
- 测试库容器状态：`Memoflow-test-db` healthy
- 测试库端口：`127.0.0.1:5433`
- 测试库名：`memoflow_test`

### 1.2 额外发现

- 仅启动 `postgres-test` 容器并不足以让所有测试通过。
- `goal:test`、`task:test` 当前包含 Prisma repository integration 测试，但默认 `test` target 不会像专门的 `test:integration` target 那样先同步 Prisma schema。
- 专门的 `test:integration` target 会自动执行 test-utils 的 schema sync，因此它们暴露出了比“表不存在”更深一层的真实回归。

---

## 2. 本次实跑命令与结果

### 2.1 质量门禁

| 命令 | 结果 | 关键结论 |
|------|------|----------|
| `pnpm lint` | 失败 | 失败项目：`goal:lint`、`ai-service:lint` |
| `pnpm typecheck` | 失败 | 任务图循环依赖：`app-vue:typecheck -> goal:build -> task:build -> goal:build` |
| `pnpm test` | 失败 | 当前 workspace 快测入口未通过，主要失败集中在 `goal`、`task` |

### 2.2 已确认通过的测试 target

| 命令 | 结果 | 备注 |
|------|------|------|
| `pnpm nx run ai-service:test` | 通过 | `77 passed` |
| `pnpm nx run assets:test` | 通过 | `5 passed` |
| `pnpm nx run api:test:smoke` | 通过 | `58 passed` |
| `pnpm nx run desktop:test:ipc` | 通过 | `42 passed` |
| `pnpm nx run desktop:test:main` | 通过 | `42 passed` |

### 2.3 已确认失败的测试 target

| 命令 | 结果 | 关键失败原因 |
|------|------|--------------|
| `pnpm nx run goal:test` | 失败 | 内嵌 Prisma repository integration 测试失败；最初表现为缺表 |
| `pnpm nx run task:test` | 失败 | 内嵌 Prisma repository integration 测试失败；最初表现为缺表 |
| `pnpm nx run goal:test:integration` | 失败 | `goals_identity_id_fkey` 外键问题，且存在保存后查不到行的断言失败 |
| `pnpm nx run task:test:integration` | 失败 | `task-instance` 集成用例出现 `PriorityCalculationError`；`task-template` 集成用例出现 `accounts_id_fkey` 和 deadlock |
| `pnpm nx run schedule:test:integration` | 失败 | `schedule_tasks_identity_id_fkey` 外键问题 |
| `pnpm nx run reminder:test:integration` | 失败 | `reminder_templates_identity_id_fkey` 外键问题 |

---

## 3. 当前失败详情

### 3.1 Lint

#### `goal:lint`

- 阻塞错误：`packages/goal/src/application-server/use-cases/commands/__tests__/create-goal.test.ts`
- 具体错误：`@dailyuse/task/testing` 命中 `no-restricted-imports`
- 同时仍存在大量 warnings：本次命令输出为 `221 problems (1 error, 220 warnings)`

#### `ai-service:lint`

- 当前由 Ruff 阻塞
- 主要规则：
  - `E402`：module import not at top of file
  - `I001`：import block 未排序
  - `E501`：line too long
- 主要分布：
  - `apps/ai-service/src/ai_service/api/dependencies.py`
  - `apps/ai-service/src/ai_service/app.py`
  - `apps/ai-service/src/ai_service/evals/**`
  - `apps/ai-service/tests/**`
- 本次命令输出：`Found 146 errors`

### 3.2 Typecheck

- `pnpm typecheck` 当前并未进入实际 TS 检查阶段。
- 失败发生在 Nx task graph 构建阶段：

```text
app-vue:typecheck --> goal:build --> task:build --> goal:build
```

- 这说明旧报告中“已拆除 goal 与 task 循环依赖”的结论当前不成立。

### 3.3 `pnpm test`

- 当前 workspace `test` 总入口仍失败。
- 拆分后确认，失败核心在 `goal:test` 与 `task:test`：
  - 当测试库只启动容器、但未先同步 schema 时，这两个 target 中的 repository integration 测试会报缺表，例如 `auth_identities` 不存在。
  - 这意味着当前 package `test` target 混入了依赖数据库 schema 的用例，但默认快测入口并未负责准备这层环境。

### 3.4 Dedicated integration tests

#### `goal:test:integration`

- `2/2` 失败
- 关键问题：
  - `goals_identity_id_fkey` 外键约束失败
  - `persists and reloads...` 用例中保存后数据库行仍为 `null`

#### `task:test:integration`

- `17` 个用例中 `9` 个失败
- `task-instance` 集成测试主要失败原因：
  - `PriorityCalculationError`
  - 非法 importance 值：`High`、`Medium`、`Low`
  - 当前允许值为：`Vital`、`Important`、`Moderate`、`Minor`、`Trivial`
- `task-template` 集成测试附加问题：
  - `accounts_id_fkey` 外键失败
  - `deadlock detected`

#### `schedule:test:integration`

- `2` 个用例中 `1` 个失败
- 失败原因：
  - `schedule_tasks_identity_id_fkey` 外键约束失败

#### `reminder:test:integration`

- `2/2` 失败
- 失败原因：
  - `reminder_templates_identity_id_fkey` 外键约束失败

---

## 4. 当前可确认的结论

1. 旧报告中的“全部通过”结论不可继续引用。
2. 当前问题不只是环境缺失。
   - 环境层问题：默认 `test` target 未正确准备 Prisma integration 所需 schema。
   - 代码/配置层问题：`lint` 阻塞、Nx 循环依赖、integration 级别的外键与枚举值回归。
3. 当前较稳定通过的范围主要是：
   - `ai-service:test`
   - `assets:test`
   - `api:test:smoke`
   - `desktop:test:ipc`
   - `desktop:test:main`
4. 当前最需要优先处理的阻塞项是：
   - 恢复 `goal` / `task` 的 build 依赖图，解除 typecheck 环
   - 修复 `goal:lint` 与 `ai-service:lint`
   - 统一 package `test` 与 `test:integration` 的数据库准备策略
   - 修复 `goal` / `task` / `schedule` / `reminder` 的 integration 外键与枚举回归

---

## 5. 与旧版报告冲突的条目

以下旧结论已被本次重检直接推翻：

- “所有目标已全部通过（0 errors）”
- “Typecheck 全部通过”
- “Lint 全部通过”
- “Test 全部通过”
- “已拆除 `goal` 与 `task` 之间的循环依赖”

本文件现以 2026-05-01 的实跑结果为准，后续若继续修复，应再次重检并覆盖本报告。
