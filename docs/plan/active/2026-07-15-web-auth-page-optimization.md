---
tags:
  - plan
  - active
  - web
  - authentication
description: Web 登录与注册页面后续优化方案
created: 2026-07-15T00:00:00
updated: 2026-07-15T00:00:00
---

# Web 登录与注册页面后续优化方案

## 摘要

本方案记录 Web 端认证入口的后续优化方向，重点解决入口能力与运行表现不一致、注册反馈不足、错误提示不够精细、法律文本不可访问、主题选项冗余以及页面语义层级不足等问题。

本轮只形成方案，不修改代码。后续实施完成后，必须部署到远程开发服务器，并以 `http://localhost:58080/` 对应的远程运行环境进行浏览器验收；本地仓库中的修改或测试结果不能替代远程运行验证。

## 背景与事实边界

### 运行环境

- `58080` 是远程开发服务器上启动的项目，不是当前本地仓库直接提供的运行实例。
- 本方案中的界面问题来自对远程 `58080` 页面的浏览器走查。
- 本地源码仅用于辅助理解当前设计意图和历史状态。远程运行产物与本地源码不一致时，应分别记录，不能根据本地修改推断远程问题已经解决。
- 后续每项优化都要经过“代码完成、远程重新构建或部署、浏览器验收”三个阶段，才可以标记完成。

### 当前观察

- 远程 Web 登录页曾显示“访客模式”，点击后提示该能力仅在桌面端可用。
- 注册页在字段为空、邮箱格式错误、密码过短或两次密码不一致时，缺少明确、就近的返回信息。
- “忘记密码？”目前呈现为辅助入口，但没有形成可完成的找回流程。
- 服务条款和隐私政策目前只是说明文字，没有真实可访问的链接。
- 登录错误切换语言后可能继续保留原来的中文文本，说明错误状态与当前语言没有完整联动。
- 登录页提供系统、浅色和深色三个主题选项，但页面主体本身使用固定的品牌化深色视觉，切换能力没有带来足够价值。
- 页面主要依赖普通容器和段落组织内容，缺少清晰的主区域、标题、表单和错误区域语义。

## 目标

- 让 Web 登录页只展示当前宿主真实可用的能力。
- 让用户能够理解注册失败的具体原因，并知道下一步如何修正。
- 为后续找回密码能力明确产品流程、依赖和验收边界。
- 为服务条款和隐私政策提供真实、可访问、可维护的入口。
- 确认 Dashboard 独立页面的退役状态，并避免把仍被复用的统计能力误删。
- 让中文错误信息更具体、可行动，同时保证中英文界面的错误语言一致。
- 登录页固定使用一个品牌主题，移除无必要的主题选择。
- 建立正确的标题、表单、错误提示和法律信息层级。

## 非目标

- 本方案不实施任何代码改动。
- 本方案不修改 Desktop 或 Mobile 的访客模式能力。
- 本方案不删除 Dashboard 统计接口、统计数据或被 AI 工作区和胶囊预览复用的 Dashboard 能力。
- 本方案不同时重构登录后的整个工作区主题系统。
- 忘记密码能力作为后续阶段实施，不要求与第一批登录页整理同时上线。

## 优化项

### 1. Web 端移除并隐藏访客模式

#### 问题

远程 Web 页面向用户展示“访客模式”，但点击后才说明该能力只在桌面端可用。这会让用户先形成可直接体验产品的预期，再立即遇到拒绝，属于入口能力与实际能力不一致。

当前本地 Web 登录页源码中已经没有访客按钮，但仍保留 Web 专用的访客模式文案；远程运行页面又曾出现该入口。这说明本地源码、构建产物或远程部署状态之间可能存在差异，不能仅以本地页面代码作为完成依据。

#### 优化要求

- Web 端不渲染访客模式按钮、链接、分隔符或占位区域。
- 不使用“禁用按钮 + 仅桌面端可用”的表现替代移除，因为禁用入口仍会制造能力预期。
- 清理 Web 专用国际化资源中已经不再使用的访客模式文案，避免后续误接回页面。
- Desktop 和确实支持访客模式的宿主保持现状，不把本项扩大为跨端能力删除。
- 远程重新构建或部署后检查浏览器缓存和旧静态资源，避免旧入口继续出现。

#### 验收标准

- 未登录访问 `/`、`/auth` 和受保护路由跳转到认证页时，均看不到访客模式。
- Web 页面源码和无障碍树中均不存在可交互的访客入口。
- Web 认证流程不会再出现“访客模式仅在桌面端可用”的提示。
- Desktop 端访客模式回归不受影响。

### 2. 细化注册页面的返回信息

#### 问题

当前注册处理对必填项缺失和两次密码不一致采取静默返回。用户点击注册后没有明确反馈，也无法判断需要修改哪个字段。服务端错误则主要集中显示在页面级错误区域，字段问题与系统问题没有分层。

#### 反馈分层

注册反馈分为三层：

1. 字段级反馈：显示在对应输入框附近，说明具体问题和修正方式。
2. 页面级反馈：用于账号冲突、频率限制、网络异常和服务不可用等不能归属于单一字段的问题。
3. 成功反馈：说明注册已经完成以及接下来将进入的页面，避免无提示跳转。

#### 建议文案

| 场景 | 建议中文提示 | 展示位置 |
| --- | --- | --- |
| 邮箱为空 | 请输入邮箱地址 | 邮箱字段下方 |
| 邮箱格式错误 | 请输入有效的邮箱地址 | 邮箱字段下方 |
| 密码为空 | 请输入密码 | 密码字段下方 |
| 密码少于 8 位 | 密码至少需要 8 位 | 密码字段下方 |
| 密码复杂度不足 | 密码需包含大写字母、小写字母、数字或特殊字符中的至少两类 | 密码字段下方 |
| 确认密码为空 | 请再次输入密码 | 确认密码字段下方 |
| 两次密码不一致 | 两次输入的密码不一致，请重新确认 | 确认密码字段下方 |
| 邮箱已经注册 | 该邮箱已注册，请直接登录或使用找回密码 | 页面级提示 |
| 请求过于频繁 | 尝试次数过多，请稍后再试 | 页面级提示 |
| 网络不可用 | 网络连接失败，请检查网络后重试 | 页面级提示 |
| 服务暂不可用 | 注册服务暂时不可用，请稍后再试 | 页面级提示 |
| 注册成功 | 注册成功，正在进入工作区 | 成功状态或短暂提示 |

#### 交互要求

- 首次提交时统一校验全部字段，并把焦点移动到第一个错误字段。
- 用户修改字段后及时清除已经解决的错误，不保留过期提示。
- 本地可判断的问题不得等待服务端返回后才提示。
- 提交期间禁用重复提交，并保持明确的“注册中”状态。
- 页面级错误不能覆盖字段级错误；两类错误应同时保持可理解性。
- 服务端错误必须按稳定错误码映射，不直接把后端原始文本展示给用户。
- 注册区域使用真实表单语义，使 Enter 提交、浏览器辅助能力和自动填充行为一致。

#### 验收标准

- 空字段、非法邮箱、弱密码和密码不一致都有明确且就近的提示。
- 修正字段后，对应错误会被清除。
- 已注册邮箱、频率限制、网络失败和服务不可用可以被区分。
- 错误提示与当前页面语言一致。
- 键盘用户可以定位错误并完成注册流程。

### 3. 将忘记密码作为后续实施点

#### 当前处理

在完整能力上线前，不保留一个看似可点击但没有行为的“忘记密码？”入口。第一阶段可以暂时隐藏该入口；如果产品必须提前展示，则需要明确标记“暂未开放”，并确保视觉上不被理解为可以立即操作的链接。

#### 后续完整流程

1. 用户输入邮箱并请求重置验证码或重置链接。
2. 页面以不暴露账号是否存在的统一文案响应请求。
3. 用户完成验证码或链接校验。
4. 用户输入并确认新密码，密码规则与注册保持一致。
5. 重置成功后使相关旧会话失效，并引导用户返回登录页。

#### 产品与安全要求

- 请求结果不暴露某个邮箱是否已经注册，避免账号枚举。
- 明确验证码或重置链接的有效期、重新发送倒计时和最大尝试次数。
- 对发送、校验和重置三个阶段分别提供加载、成功、过期、频率限制和系统异常提示。
- 邮件内容、发件域名、品牌文案和多语言版本需要一并纳入实施范围。
- 重置成功后明确说明用户需要使用新密码重新登录。

#### 验收标准

- 忘记密码入口只有在完整流程可用时才表现为可操作链接。
- 用户可以从登录页完成请求、验证、设置新密码并返回登录的闭环。
- 页面不会泄露邮箱是否存在。
- 过期、错误、频率限制和服务异常均有可行动的提示。

### 4. 补充服务条款和隐私政策

#### 问题

当前“继续即表示您同意服务条款与隐私政策”是普通文本，服务条款和隐私政策没有真实链接。用户无法查看自己被要求同意的具体内容。

#### 优化要求

- 分别提供可访问的“服务条款”和“隐私政策”链接。
- 法律文档至少包含版本号、生效日期、更新日期和联系渠道。
- 中英文界面应进入对应语言版本；没有完整英文版本时不得用空白页或机器占位文本替代。
- 建议在新标签页打开法律文档，避免用户查看后丢失已经填写的登录或注册内容。
- 注册是否需要显式勾选同意，由产品和合规负责人确定；如果需要勾选，默认不得选中，并提供清晰的错误反馈。
- 法律文档内容必须由产品或合规负责人确认，不能由前端实现临时编写后直接上线。

#### 验收标准

- 两个法律链接都可通过鼠标、键盘和读屏访问。
- 链接目标不是占位页或空页面。
- 查看法律文档后，原认证页面的输入状态不会意外丢失。
- 中英文入口、标题和文档语言保持一致。

### 5. 确认 Dashboard 页面已经移除

#### 当前结论

本地仓库中的独立 Dashboard 页面已经退役：`/dashboard` 作为兼容路由跳转到 `/`，AI 工作区承接原来的首页入口，并已有针对 Dashboard 退役行为的 Web E2E 用例。

这不等于 Dashboard 相关能力已经全部删除。Dashboard 统计服务、聚合数据、统计组件以及被 AI 工作区和胶囊预览复用的能力仍然存在，属于当前产品能力的一部分，不在本方案的删除范围内。

由于远程 `58080` 环境在未登录状态下会先进入认证页，本次浏览器走查无法独立确认登录后的远程 `/dashboard` 最终落点。远程部署版本仍需在获得有效会话后再次验收。

#### 后续确认项

- 登录后的远程环境访问 `/dashboard`，最终必须落到 `/` 的 AI 工作区。
- 主导航、胶囊导航和设置入口中不再出现独立 Dashboard 页面入口。
- 不存在可直接渲染的 Dashboard 独立页面组件。
- 继续保留被其他界面消费的 Dashboard 数据与统计能力。
- 复查仍以 Dashboard 页面为目标的 Lighthouse 配置和旧性能测试，区分“退役路由兼容测试”和“已失效的页面性能测试”，再决定更新或归档。

#### 验收标准

- 远程环境登录后访问 `/dashboard` 会稳定跳转到 `/`。
- 页面中没有独立 Dashboard 导航入口。
- AI 工作区中的统计信息和相关预览仍可正常加载。
- 自动化测试不再把已退役的 Dashboard 独立页面当作现有产品页面。

### 6. 更细致地处理中文错误信息

#### 问题

错误提示不仅要“翻译成中文”，还需要说明发生了什么、用户能做什么，并避免将后端原始信息、内部术语或不适合当前场景的通用文案直接展示出来。当前错误文本在生成后被保存为字符串，切换语言时已有错误可能不会随当前语言重新翻译。

#### 处理原则

- 页面状态保存稳定错误码和必要上下文，不保存已经翻译完成的最终字符串。
- 渲染时根据当前语言将错误码映射为用户文案，切换语言后现有错误同步更新。
- 优先使用场景化错误文案，不使用笼统的“操作失败”覆盖所有情况。
- 中文文案保持简短、自然、可行动，不出现堆栈、接口路径、数据库约束名或内部错误对象。
- 登录失败统一使用“邮箱或密码错误”，不区分邮箱不存在和密码错误，避免账号枚举。
- 注册场景可以明确提示“该邮箱已注册”，并同时提供登录或找回密码方向。
- 校验错误显示在字段附近；认证失败和系统错误显示在页面级错误区域。
- 错误被用户修正、重新提交成功或切换场景后，应及时清除。

#### 建议错误分类

| 类型 | 示例 | 用户动作 |
| --- | --- | --- |
| 输入校验 | 请输入有效的邮箱地址 | 修改对应字段 |
| 认证失败 | 邮箱或密码错误 | 检查凭据或使用找回密码 |
| 账号冲突 | 该邮箱已注册 | 返回登录或找回密码 |
| 频率限制 | 尝试次数过多，请稍后再试 | 等待后重试 |
| 网络异常 | 网络连接失败，请检查网络后重试 | 检查网络并重试 |
| 服务异常 | 登录服务暂时不可用，请稍后再试 | 稍后重试 |
| 未知异常 | 登录失败，请稍后再试 | 重试并保留问题反馈入口 |

#### 验收标准

- 中英文切换后，当前错误信息会立即使用对应语言重新显示。
- 登录、注册、网络和服务错误可以被明确区分。
- 页面不直接显示后端原始中文或内部错误信息。
- 中文文案能够指导用户采取下一步动作。

### 7. 登录页移除主题切换，统一使用单一主题

#### 决策

Web 登录页固定使用当前品牌化深色紫色主题，移除“系统、浅色、深色”切换控件。登录页不再根据系统主题或本地主题偏好切换视觉模式。

本项只约束 Web 登录与注册入口。登录后的工作区、Desktop、Mobile 以及其他宿主的主题策略不在本方案内，除非后续形成单独的全局主题决策。

#### 优化要求

- 移除登录页右上角的主题切换控件，只保留语言切换。
- 删除登录页对系统主题、浅色主题和深色主题选择状态的依赖。
- 认证页加载时直接应用固定品牌主题，避免先显示一种主题再切换造成闪烁。
- 统一背景、卡片、输入框、错误提示、按钮和法律文本的对比度。
- 浏览器原生控件的 `color-scheme` 与固定主题保持一致。
- 不因为移除登录页主题选择而清除或覆盖登录后工作区已有的主题偏好。

#### 验收标准

- 登录页不再出现任何主题切换按钮。
- 系统切换深浅色后，登录页视觉保持一致。
- 页面首次加载没有明显的主题闪烁。
- 文字、输入框、错误提示和焦点状态满足可读性要求。

### 8. 优化标题层级与页面语义结构

#### 问题

当前登录和注册页面主要使用普通容器及段落表达内容，没有清晰的主标题、主区域和表单结构。视觉上可以理解，但浏览器、键盘用户和读屏工具难以准确识别页面层级。

#### 建议结构

- 页面根区域使用 `main`，认证卡片使用具有明确名称的 `section`。
- 每个场景只保留一个 `h1`：
  - 登录场景：`登录知行`
  - 注册场景：`创建知行账号`
  - 后续找回场景：`重置密码`
- 当前的说明文字作为标题后的普通段落，不替代标题。
- 登录和注册输入区域使用真实 `form`，主按钮使用提交语义。
- 字段继续使用可见 `label`，错误通过 `aria-describedby` 与对应字段关联。
- 页面级错误使用 `role="alert"` 或合适的 `aria-live` 区域，但避免重复播报。
- 法律信息放在 `footer`，语言选择使用有名称的控件组。
- 品牌徽标和 Logo 不作为页面主标题；Logo 的替代文本根据是否传递信息决定使用品牌名或空文本。

#### 信息层级

1. 品牌识别：说明用户正在使用哪个产品。
2. 场景标题：明确当前是在登录、注册还是重置密码。
3. 场景说明：解释完成操作后的结果。
4. 表单字段与字段错误：支持完成当前任务。
5. 主操作：登录、注册或重置密码。
6. 次级入口：注册、返回登录或后续找回密码。
7. 法律信息：服务条款和隐私政策。

#### 验收标准

- 页面存在唯一且与当前场景一致的一级标题。
- 登录与注册均可通过真实表单完成键盘提交。
- 字段错误与对应输入框建立语义关联。
- 键盘焦点顺序符合视觉顺序，所有真实入口都可以被聚焦。
- 页面通过基础标题、表单和可访问名称检查。

## 实施顺序

### 第一阶段：收敛当前登录页

1. Web 端彻底移除访客模式入口和残留 Web 文案。
2. 移除主题切换并固定品牌主题。
3. 补充登录和注册页面的标题、表单及错误区域语义。
4. 完成注册字段校验和细分返回信息。
5. 调整错误状态结构，使错误码可以随当前语言重新翻译。
6. 补充真实的服务条款和隐私政策链接。
7. 远程重新构建或部署，并在 `58080` 环境完成浏览器回归。

### 第二阶段：补齐账户恢复能力

1. 确认邮件发送服务、验证码或重置链接策略。
2. 实现忘记密码请求、验证和设置新密码流程。
3. 补充安全限制、错误反馈、多语言邮件和自动化测试。
4. 能力完整后再将“忘记密码？”作为真实入口重新加入登录页。

### 并行确认项：Dashboard 退役状态

1. 使用有效账号验证远程 `/dashboard` 的登录后落点。
2. 清点导航入口、旧 Lighthouse 目标和旧页面性能测试。
3. 保留仍被 AI 工作区和其他模块消费的 Dashboard 数据能力。

## 远程验收清单

- [ ] Web 登录页不显示访客模式。
- [ ] Web 登录页不显示主题切换控件。
- [ ] 登录和注册场景都有唯一、清晰的一级标题。
- [ ] 注册字段错误能够就近显示并在修正后清除。
- [ ] 页面级错误能够区分账号冲突、频率限制、网络失败和服务异常。
- [ ] 切换中英文后，当前错误信息同步切换语言。
- [ ] 服务条款和隐私政策可以访问，且不是占位页面。
- [ ] 忘记密码入口在能力未完成前不会表现为可操作链接。
- [ ] 登录后的 `/dashboard` 跳转到 `/`，页面中没有独立 Dashboard 入口。
- [ ] AI 工作区依赖的统计信息仍然可用。
- [ ] 重新构建或部署远程项目后，使用 `58080` 浏览器实测以上项目。

## 完成定义

只有同时满足以下条件，本方案才可以从 `active` 移入 `archive`：

- 第一阶段所有项目已经实现并通过相关单元、组件或 E2E 测试。
- 远程开发服务器已经重新构建或部署对应版本。
- `58080` 的浏览器验收清单全部通过。
- Dashboard 远程退役状态已经使用有效会话确认。
- 忘记密码若未在本轮实施，已经有独立的后续计划或任务承接，且当前页面不存在误导性入口。
