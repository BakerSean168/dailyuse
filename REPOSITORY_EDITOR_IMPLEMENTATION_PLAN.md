# Repository + Editor 实现方案

## 1. 目标

在现有统一 Resource 架构基础上，形成一套稳定、清晰、可扩展的仓库与编辑器协同方案。

目标不是推翻当前架构，而是在现有实现上收口业务规则、补齐关键流程、为未来资源库演进保留空间。

---

## 2. 核心设计原则

### 2.1 统一 Resource，不急于拆仓储

当前不建议立即拆成 `NoteRepository` 与 `AttachmentRepository` 两套独立仓储。

原因：

- 当前 contracts、repository、app-vue、editor 都已经围绕统一 `Resource` 形成闭环
- 强行拆分会引入大范围迁移成本
- 当前目标更适合做业务语义收口，而不是数据模型重建

### 2.2 Markdown 是主创作对象，资源是可复用素材

- Markdown 是编辑器主路径
- 图片/音视频/PDF 等是仓库资源
- 资源通过 Markdown 引用进入内容创作流程

### 2.3 固定类型分组优先于用户自定义文件夹

当前主导航继续采用固定类型分组：

- Notes
- Images
- Videos
- Audio
- Documents
- Other

用户侧不开放创建文件夹，避免产品复杂度失控。

### 2.4 分享能力与仓库存储解耦

- 仓库内默认保存路径引用
- 分享或导出时再转换为 base64 内嵌模式
- 不把“分享优化”反向污染日常存储格式

---

## 3. 当前能力与缺口

### 已具备

- 工作区可新建 Markdown 笔记
- 支持批量导入资源
- 支持真实搜索
- 支持书签管理
- 支持 Markdown 编辑与预览
- 支持 wiki-link suggestion、backlink、graph
- 支持媒体资源预览

### 未收口或未补齐

- 旧文件夹能力仍残留在遗留组件中
- 主编辑器不支持粘贴图片自动上传
- 主编辑器不支持从已有资源选择并插入图片
- 不支持图片路径引用与 base64 内嵌的切换
- 不支持自包含分享导出
- 不支持资源引用反查与失效检测

---

## 4. 最终业务形态

### 用户能做什么

- 新建笔记
- 导入任意资源
- 搜索资源
- 收藏资源
- 在 Markdown 中引用笔记和附件
- 直接粘贴图片到笔记
- 从已有资源中插入图片
- 选择图片的保存/分享方式

### 用户不能做什么

- 不能主动创建文件夹
- 不能把 repository 当成传统文件管理器来使用

---

## 5. 核心用户流程

### 5.1 创建笔记流程

1. 用户进入 `/repository`
2. 点击“新建笔记”
3. 系统创建默认 Markdown 文件
4. 工作区自动打开笔记并进入编辑

### 5.2 导入资源流程

1. 用户点击“导入资源”
2. 选择图片/视频/文件/Markdown
3. 系统上传并创建资源记录
4. 左侧固定分组立即刷新

### 5.3 粘贴图片流程

1. 用户在 Markdown 编辑器中粘贴截图
2. 编辑器捕获 Clipboard 图片文件
3. 调用 repository 上传接口保存为图片资源
4. 返回稳定路径
5. 在当前光标处插入 `![](path)`

### 5.4 插入已有图片流程

1. 用户点击“插入资源”
2. 打开资源选择器
3. 过滤并选择已有图片
4. 在光标位置插入 Markdown 图片引用

### 5.5 分享导出流程

1. 用户点击“分享/导出”
2. 系统扫描当前笔记中的图片引用
3. 选择导出模式：路径引用 / 自包含 base64
4. 生成分享内容或导出文件

---

## 6. 重点实现方案

### 6.1 文件夹业务收口

目标：让产品表现为“固定分类仓库”，不是文件管理器。

做法：

- 保留 folder 领域能力，不删底层代码
- 在主工作区隐藏和禁用所有创建文件夹入口
- 把 `TypedFileTree` 作为唯一正式主导航
- 将旧 `FileTree`、`FileTreePanel`、`CreateFolderDialog` 标记为 legacy 或内部能力

### 6.2 编辑器资源插入中台

目标：统一“插入图片 / 插入附件 / 插入笔记链接”的逻辑。

建议新增一个统一的前端编排层：

- `useResourceInsertion()`

职责：

- 插入已有图片
- 插入已有文件链接
- 粘贴图片上传后插入
- 拖拽图片上传后插入
- 统一决定插入文本模板

### 6.3 图片引用模式

建议定义两种模式：

- `path`
  - 默认模式
  - 引用仓库资源路径
- `base64`
  - 显式模式
  - 用于分享、自包含复制、离线发送

不建议把 base64 作为默认保存策略。

### 6.4 自包含导出

建议把“base64 内嵌”主要落在导出层，而不是日常存储层。

做法：

- 读取当前 Markdown
- 解析图片引用
- 读取仓库资源二进制
- 转换为 data URL
- 生成临时导出内容

这样既保留仓库整洁，也满足分享需求。

### 6.5 资源可维护性能力

建议在第二阶段补齐：

- 资源失效检测
- 资源被引用反查
- 删除资源前影响提示
- 最近插入资源

这些能力会让仓库真正可长期使用。

---

## 7. 建议的数据与接口策略

### 保持现状不大改

- 保留统一 `Resource` DTO
- 保留现有上传接口
- 保留 tags 能力在所有资源上存在的扩展空间

### 业务层新增约束

- 用户侧只在必要界面上暴露 tags 管理
- 主工作区不暴露文件夹创建
- 图片插入统一走资源路径引用

### 新增建议接口能力

- `insertExistingResource(resourceId, mode)`
- `pasteAttachmentAndInsert(file, mode)`
- `exportMarkdownAsSelfContained(resourceId)`
- `listResourceReferences(resourceId)`
- `validateMarkdownResourceReferences(resourceId)`

---

## 8. 分阶段实施

### 阶段 1：业务收口

目标：统一主用户路径。

任务：

- 隐藏创建文件夹入口
- 明确 TypedFileTree 为正式导航
- 梳理 legacy 组件状态

### 阶段 2：补齐编辑器资源插入闭环

目标：让 Markdown 真正能高效使用图片资源。

任务：

- 粘贴图片自动上传并插入
- 拖拽图片自动上传并插入
- 从已有资源中插入图片
- 插入资源面板

### 阶段 3：补齐分享能力

目标：让仓库内容可传播。

任务：

- 图片路径引用 / base64 内嵌切换
- 自包含 Markdown 导出
- 一键复制分享版本

### 阶段 4：补齐资源维护能力

目标：让仓库适合长期使用。

任务：

- 资源引用反查
- 失效检测
- 删除前影响提示
- 最近插入资源

---

## 9. 推荐优先级

### P0

- 不允许用户创建文件夹的主路径收口
- 粘贴图片自动上传并插入 Markdown
- 从已有资源插入图片

### P1

- 统一资源插入器
- 图片引用模式切换
- 自包含 Markdown 导出

### P2

- 引用反查
- 失效检测
- 最近插入资源
- 资源属性面板

---

## 10. 验收标准

### 产品层面

- 用户进入仓库后不会再看到“新建文件夹”主入口
- 用户可以只靠新建笔记、导入资源、搜索资源完成日常使用
- 用户可以在编辑器内完成图片插入，而无需手动复制路径

### 编辑体验层面

- 粘贴图片后能自动上传并插入引用
- 可以从已有资源中选择并插入图片
- 可以把当前笔记导出为可直接分享的单文件内容

### 资源管理层面

- 所有资源在固定类型分组中可见
- 搜索可覆盖笔记与附件
- 后续能平滑扩展为完整资源库

---

## 11. 最终建议

当前最优策略不是大重构，而是：

- 保留统一 Resource 架构
- 收口产品主路径
- 强化编辑器与资源库的协同能力
- 把“分享、自包含、引用维护”作为下一阶段重点能力

这条路线兼顾了：

- 当前实现成本
- 用户体验闭环
- 后续扩展空间
