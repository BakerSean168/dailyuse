/**
 * @deprecated Legacy DI layer — use `createAuthenticationModule()` factory instead.
 * 已废弃的 DI 层 — 请使用 `createAuthenticationModule()` 工厂函数代替。
 */

/** @deprecated Use `createAuthenticationModule()` instead. 请使用 `createAuthenticationModule()` 代替。 */
export { AuthenticationContainer } from './authentication-container';
/** @deprecated Pass repos directly to `createAuthenticationModule()`. 请直接将仓储传入 `createAuthenticationModule()`。 */
export { AuthenticationRepositoryFactory } from './authentication-repository.factory';
