/**
 * Sync Conflict Repository Port Interface
 *
 * Re-exports from domain-server for consistent access.
 */

export type {
  ISyncConflictRepository,
  SyncConflictQueryOptions,
} from '@dailyuse/domain-server/sync';
