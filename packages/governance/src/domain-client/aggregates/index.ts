/**
 * Governance Aggregates - Domain Client
 * 规则治理模块聚合根导出 - 领域客户端
 * 
 * domain-client 的聚合根特点：
 * - Anemic Domain Model + Rich View Model
 * - 专注于 UI 辅助和展示逻辑
 * - 使用 fromDTO() 从 API 响应创建
 */

export { Rule } from './rule';
