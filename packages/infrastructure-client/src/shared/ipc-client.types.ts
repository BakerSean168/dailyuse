/**
 * IPC Client Types  Re-exported from @dailyuse/ipc-client
 *
 * Kept for backward compatibility. All types are now defined in @dailyuse/ipc-client.
 */
export type { IpcClient, ElectronAPI } from '@dailyuse/ipc-client';
export { createIpcClient, getElectronAPI, IpcClientError } from '@dailyuse/ipc-client';
