export interface SubtaskClient {
  id: string;
  name: string;
  isCompleted: boolean;
  order: number;
}

export interface SubtaskClientDTO {
  id: string;
  name: string;
  isCompleted: boolean;
  order: number;
}

