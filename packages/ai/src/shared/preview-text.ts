/**
 * Residual 995: previewText dual retired for AI package consumers
 * (generate-ai-goal + automation/planning adapters + internal client).
 * Residual 1011: sole body elevated to @memoflow/utils/shared/preview-text;
 * this file re-exports so package-local import paths stay stable.
 */

export { previewText } from '@memoflow/utils/shared';
