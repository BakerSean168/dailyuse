/**
 * Schedule Aggregates
 * 调度模块聚合根统一导出
 * 
 * 【规范说明：聚合根（Aggregate Root）】
 * 聚合根是 DDD 中的核心概念：
 * - 聚合的入口点：外部只能通过聚合根访问聚合内的实体
 * - 事务边界：一次事务只能修改一个聚合根
 * - 不变量守护者：确保聚合内的业务规则始终满足
 * - 领域事件发布者：状态变更时发出领域事件
 * 
 * 【Schedule 聚合根】
 * - 日程事件管理：创建、修改、删除日程事件
 * - 时间冲突检测：检测事件时间冲突
 * - 重复规则管理：支持日/周/月/自定义重复
 * - 提醒配置：提前通知时间、多层级提醒
 * 
 * 【ScheduleTask 聚合根】
 * - 日程任务管理：日程内的子任务
 * - 任务完成状态：跟踪日程任务完成情况
 */

export * from './schedule-task';
export * from './calendar-entry';
