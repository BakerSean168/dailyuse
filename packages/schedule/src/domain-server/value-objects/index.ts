/**
 * Schedule Module Value Objects - Domain Server
 *
 * Domain-shared value objects are now exported only from
 * `@dailyuse/schedule/domain-shared`.
 * This subtree keeps server-only runtime errors.
 */

export * from './errors';
