/**
 * DI barrel — legacy container exports.
 * DI 桶导出 — 遗留容器导出。
 *
 * @deprecated Prefer using createScheduleModule() composition root.
 * @deprecated 请优先使用 createScheduleModule() 组合根。
 */
export { ScheduleContainer } from './schedule-container';
