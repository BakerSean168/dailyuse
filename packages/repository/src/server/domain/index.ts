/**
 * Repository server domain.
 *
 * Knowledge-repository contracts, policies, and application services own the
 * live business surface (`@dailyuse/contracts/repository` + application layer).
 * Legacy database Repository/Folder/Resource domain aggregates were removed
 * with the Obsidian vault migration; portable table reimport stays in
 * data-portability.
 */

export {};
