/**
 * Goal Module Ports - Re-exports from domain layer
 */

export type {
  IGoalRepository,
  IGoalFolderRepository,
  IGoalStatisticsRepository,
  IFocusModeRepository,
  IFocusSessionRepository,
  IWeightSnapshotRepository,
} from '@/domain-server';
