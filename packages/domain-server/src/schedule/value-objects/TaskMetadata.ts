/**
 * TaskMetadata - Re-export for backward compatibility
 * TaskMetadata 别名 - 向后兼容重新导出
 * 
 * Note: domain-shared 使用 ScheduleTaskMetadata 作为类名
 * 这里使用 TaskMetadata 作为别名以保持向后兼容
 */

export { ScheduleTaskMetadata as TaskMetadata } from '@dailyuse/domain-shared/schedule';
