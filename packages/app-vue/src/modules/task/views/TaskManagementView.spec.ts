import { describe, expect, it } from 'vitest'; import fs from 'node:fs'; import path from 'node:path'; describe('TaskManagementView vNext',()=>{it('renders flat task plans without graph mode',()=>{const source=fs.readFileSync(path.resolve(__dirname,'TaskManagementView.vue'),'utf8');expect(source).toContain('task-management-view');expect(source).not.toContain('TaskDAG');expect(source).not.toContain('Dependency');expect(source).not.toContain('graph');});});
