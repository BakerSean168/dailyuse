import { describe, expect, it } from 'vitest'; import fs from 'node:fs'; import path from 'node:path'; describe('TaskDetailView vNext',()=>{it('presents plan lifecycle and goal link without DAG state',()=>{const source=fs.readFileSync(path.resolve(__dirname,'TaskDetailView.vue'),'utf8');expect(source).toContain('task-detail-view');expect(source).toContain('Goal linked');for(const retired of ['TaskDependency','CriticalPath','parentTaskId','dependencyStatus']) expect(source).not.toContain(retired);});});
