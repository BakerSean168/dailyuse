import { describe, expect, it } from 'vitest'; import fs from 'node:fs'; import path from 'node:path'; describe('TaskTemplateDialog vNext',()=>{it('uses flat Task plan form without DAG controls',()=>{const source=fs.readFileSync(path.resolve(__dirname,'TaskTemplateDialog.vue'),'utf8');expect(source).toContain('TaskTemplateForm');for(const retired of ['DependencyManager','parentTaskId','folderId','TaskForDAG']) expect(source).not.toContain(retired);});});
