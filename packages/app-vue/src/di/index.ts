/**
 * DI Barrel — re-exports all injection keys, types, and navigation defaults.
 *
 * @module di
 */
export * from './keys';
export type * from './types';
export { defaultMainNavigation, defaultBottomNavigation } from './navigation';
