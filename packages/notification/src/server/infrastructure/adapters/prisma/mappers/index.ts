/**
 * Notification Prisma Mappers — Barrel Export.
 * 通知模块 Prisma 映射器 —— 统一导出。
 *
 * @internal Mapper classes are persistence implementation details.
 * @internal 映射器类为持久化实现细节。
 */

export { NotificationPreferencePrismaMapper } from './notification-preference-prisma.mapper';
export { NotificationPrismaMapper } from './notification-prisma.mapper';
