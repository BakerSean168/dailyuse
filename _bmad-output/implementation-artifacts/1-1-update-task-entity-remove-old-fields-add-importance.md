# Story 1.1: 更新 Task 实体 - 移除旧字段，添加 Importance

Status: ready-for-dev

> **This story was auto-generated by SM agent in YOLO mode using comprehensive artifact analysis.**

## Story

As a 系统架构师,
I want Task 实体移除 `priority` 和 `urgency` 持久化字段，新增 `importance` 字段,
So that 数据模型反映新的概念设计，并减少冗余性。

## Acceptance Criteria

1. **Given** Task 实体已存在于 `packages/contracts/src/modules/task/task-defs.ts`
   **When** 开发者修改实体定义
   **Then** 移除 `priority` 和 `urgency` 字段

2. **Given** Task 实体已定义
   **When** 新增 Importance 字段
   **Then** 新增 `importance: ImportanceLevel` 枚举字段（值：Vital, Important, Moderate, Minor, Trivial）

3. **Given** 新增了 importance 字段
   **When** 运行单元测试
   **Then** 单元测试验证新字段存在且类型正确

## Tasks / Subtasks

- [ ] Task 1: 修改 TaskEntity 定义 (AC: #1, #2)
  - [ ] 移除 `priority` 字段
  - [ ] 移除 `urgency` 字段
  - [ ] 添加 `importance: ImportanceLevel` 字段
  - [ ] 确保 Importance 枚举值完整（Vital, Important, Moderate, Minor, Trivial）

- [ ] Task 2: 更新相关类型定义 (AC: #2)
  - [ ] 更新 `packages/contracts/src/modules/task/task-defs.ts` 中的 Task 类型
  - [ ] 验证 ImportanceLevel 枚举定义正确

- [ ] Task 3: 创建/更新单元测试 (AC: #3)
  - [ ] 编写测试验证 importance 字段存在
  - [ ] 编写测试验证旧字段不存在
  - [ ] 测试覆盖 >= 90%

## Dev Notes

### Architecture Compliance

This story operates at the **Contracts Layer** (`packages/contracts`), which is the foundational layer of the Clean Architecture. Per ADR-010 (Standard Centralized Contracts):

- **Layer**: Contracts (No dependencies - everyone depends on this)
- **Clean Architecture Position**: Lowest layer, shared across all packages
- **Key Constraint**: This file should NEVER have dependencies on other packages

**Architecture Decision Reference:**
- ADR-010: Standard Centralized Contracts
- ADR-009: Standard Clean Architecture Layers
- ADR-011: Standard Naming Conventions

### Task Entity Evolution

**Context from Epic 1 Foundation:**
This story is the **foundation task** for the entire Task Priority Refactor initiative. Decisions made here cascade to:
- Story 1.2: PriorityCalculator domain service
- Story 1.4: TaskDTO updates for API contracts
- Story 1.5: Application layer integration
- Story 1.6: Database migration

**Key Design Decision:**
- ImportanceLevel enum serves as the **single source of truth** for task importance
- This is used by PriorityCalculator (Story 1.2) to compute priority scores
- It replaces the old priority/urgency model which was both persisted and calculated

### Source Tree Components to Touch

**Primary File (MUST modify):**
- `packages/contracts/src/modules/task/task-defs.ts` - Task entity definition

**Files that import from task-defs.ts (will need verification after change):**
- `packages/domain-server/src/task/**` - Domain services
- `packages/application-server/src/task/**` - Use cases
- `packages/infrastructure-server/src/task/**` - Repositories
- `apps/api/src/**` - API controllers
- `apps/web/src/**` - Web application
- `apps/desktop/src/**` - Desktop application

**Pattern to verify compilation:**
After modifying task-defs.ts, ensure all imports still resolve:
```bash
pnpm nx run-many --target=build --projects=contracts,domain-server,application-server,infrastructure-server,api
```

### ImportanceLevel Enum Definition

**Current state (verify in existing codebase):**
Check if ImportanceLevel enum already exists in:
- `packages/contracts/src/shared/importance.ts` (per Architecture Document)
- Or needs to be created

**Expected enum values (per Architecture & Epic Requirements):**
```typescript
enum ImportanceLevel {
  Vital = "Vital",           // Critical tasks
  Important = "Important",   // High priority
  Moderate = "Moderate",     // Default/Medium priority
  Minor = "Minor",           // Low priority
  Trivial = "Trivial"        // Minimal priority
}
```

**Technical Requirements:**
- Use TypeScript enum or type union (prefer enum for type safety)
- Ensure all 5 values are present
- Export as a named export for use across packages
- Add JSDoc comments explaining each level's meaning

### Testing Standards (from project-context.md)

**Test File Location:**
- Create/update: `packages/contracts/src/modules/task/task-defs.spec.ts`

**Testing Requirements (per ADR-013):**
- Minimum coverage: 90%
- Use Vitest framework (per vitest.config.ts)
- Test structure:
  - Verify Task entity has importance field
  - Verify priority and urgency fields are removed
  - Verify ImportanceLevel enum values
  - Verify type safety (TypeScript compilation)

**Test Pattern:**
```typescript
describe('TaskEntity', () => {
  describe('fields', () => {
    it('should have importance field of type ImportanceLevel', () => {
      // Test implementation
    });
    
    it('should NOT have priority field', () => {
      // Verify removal
    });
    
    it('should NOT have urgency field', () => {
      // Verify removal
    });
  });
});
```

### Naming Conventions (from project-context.md)

**File Naming:**
- Keep filename: `task-defs.ts` (kebab-case for files)

**Type Naming:**
- Entity: `Task` (PascalCase for types)
- Enum: `ImportanceLevel` (PascalCase)
- Values: `Vital`, `Important`, etc. (PascalCase for enum values)

**Export Style:**
- Prefer named exports: `export type Task = { ... }`
- NOT default exports

### Previous Story Intelligence

**This is Story 1.1 (First Story in Epic 1)**
- No previous stories to learn from in this epic
- However, review Story 2.x patterns from previous epics if they exist (for consistency)
- This story sets the pattern for all subsequent stories in Epic 1

### Git Pattern Reference

**Code changes pattern for this story:**
```
1. Modify packages/contracts/src/modules/task/task-defs.ts
2. Update or create test file task-defs.spec.ts
3. Run tests: pnpm nx test contracts
4. Verify no compilation errors: pnpm nx build contracts
5. Verify dependent packages still compile
```

**Commit message pattern (for developer reference):**
```
feat(contracts): refactor Task entity - remove priority/urgency, add importance

- Remove persisted priority and urgency fields from Task entity
- Add importance: ImportanceLevel field with enum values
- Update tests to verify field definitions
- Aligns with Epic 1: Dynamic Priority Foundation
```

### Latest Technical Information

**ImportanceLevel Enum Best Practices:**
- TypeScript enums with string values provide type safety
- Prefer string enums over numeric for better readability in logs/DB
- All enum values should be documented with meaning
- Consider adding utility functions: `getImportanceWeight()`, `isHighPriority()`

**Type Safety Consideration:**
- Ensure TypeScript strict mode is enabled (check tsconfig.json)
- Use `as const` assertion if using type unions instead of enums
- Run type check: `pnpm nx run contracts:typecheck`

### Project Context Reference

**From project-context.md:**
- Clean Architecture layers must be respected
- Contracts package is the **Holy Grail** - centralized types
- All DTOs and shared types must live here
- No dependencies should exist from this package
- Import using: `import type { Task } from '@dailyuse/contracts';`

**Key Rule:** Rule #1 - Type Centralization
> **NEVER** define shared types, DTOs, or Interfaces in `application-*` or `infrastructure-*` if they are used across boundaries.
> **ALWAYS** place them in `packages/contracts`.

## Dev Agent Record

### Agent Model Used

Claude Haiku 4.5 (Scrum Master Mode - YOLO)

### Completion Checklist

- [ ] Code changes complete and tested
- [ ] All 5 ImportanceLevel values present and documented
- [ ] Task entity properly updated in task-defs.ts
- [ ] Unit tests written and passing (>=90% coverage)
- [ ] No TypeScript compilation errors
- [ ] Dependent packages still compile successfully
- [ ] Code follows naming conventions from project-context.md
- [ ] Clean Architecture boundaries respected

### File List

**Created/Modified:**
- `packages/contracts/src/modules/task/task-defs.ts` (MODIFIED)
- `packages/contracts/src/modules/task/task-defs.spec.ts` (MODIFIED or CREATED)

**Verified (no changes needed):**
- `packages/contracts/src/shared/importance.ts` (Verify ImportanceLevel exists)
- `tsconfig.json` (Verify strict mode enabled)
- `vitest.config.ts` (Verify test configuration)

---

## Implementation Status

**Ready for Dev Agent:** This story is fully prepared for developer implementation. Developer has complete context on:
1. What to change (task-defs.ts entity definition)
2. Why (architecture decisions, epic goals)
3. How (based on Clean Architecture patterns)
4. Testing requirements (>=90% coverage)
5. Quality gates (no compilation errors, dependent packages compile)

**Estimated Effort:** 1-2 hours (includes testing and verification)

**Dependency Chain:** ✅ No blockers - This is the first story and foundation for all subsequent stories in Epic 1
