# Deprecated

This directory is no longer a runtime asset source for MemoFlow applications.

Shared static assets must live in `packages/assets/src` and be consumed via `@memoflow/assets`.

Do not add new application icons, logos, avatars, or audio files here.
