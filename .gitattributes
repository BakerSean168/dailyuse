# 1. 自动处理所有文本文件
# ---------------------------------------------------------
# Git 会根据文件内容判断是否为文本。
# 在 Windows 上检出时转为 CRLF，提交到仓库时转为 LF。
* text=auto eol=lf

# 2. 强制指定特定后缀为文本并使用 LF（推荐用于跨平台项目）
# ---------------------------------------------------------
# 无论在什么系统上，这些文件在 Git 仓库内都保持 LF
*.js      text eol=lf
*.jsx     text eol=lf
*.ts      text eol=lf
*.tsx     text eol=lf
*.vue     text eol=lf
*.json    text eol=lf
*.css     text eol=lf
*.scss    text eol=lf
*.html    text eol=lf
*.md      text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf

# 3. 强制指定 Windows 专有文件使用 CRLF
# ---------------------------------------------------------
*.bat     text eol=crlf
*.ps1     text eol=crlf

# 4. 显式标记二进制文件（避免 Git 尝试转换它们的换行符）
# ---------------------------------------------------------
# 这些文件不应被 Git 视为文本处理，防止损坏图片或压缩包
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.ico     binary
*.pdf     binary
*.woff    binary
*.woff2   binary