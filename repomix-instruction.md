# Repomix Usage For MemoFlow

Use this snapshot for resume writing, project summaries, architecture overviews, interview preparation, and README updates.

Do not treat the snapshot as live truth for implementation work. For day-to-day development, first query CodeGraph for symbols, call paths, and impact radius, then use nx-mcp for Nx project or target context, and only then read concrete files.

When extracting resume material, ground every claim in files present in the snapshot. Prefer evidence about product scope, technical stack, domain packages, testing, build/deploy workflow, and cross-platform architecture. Do not invent user counts, revenue, latency, or business metrics unless they appear in the repository.
